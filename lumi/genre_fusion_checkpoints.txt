# genre-fusion generalization probe -- checkpoint list (GHOST-NOTE 2026-08-21)
# Format: label epoch [ptm]
# Full top-12-by-best-PQ (WINTERMUTE, from this morning's rating stats: 945 records/294 clips/
# 81 models). mid+last per run, EXCEPT the 3 goa families which use BEST-PQ instead of last --
# goa runs degrade with training (spearman epoch->PQ -0.434, 81% declining), so "last" would
# probe a near-worst checkpoint and misread it as a generalization failure. See run_meta note:
# longctx mid=ep4, last=ep1 (per W: ep1 is genuinely the LAST saved checkpoint for this run,
# not ep7 -- ep7 never belongs in the render set); subloss ep19 -> ep3;
# winning_goa ep19 -> ep10. Corpus mix: 8 avp, 3 goa, 1 avp_aug10 (vote-gated set is avp-heavy --
# flagged by W, not actioned here; ask if biggoa/suomi coverage is wanted, comes from a
# different, unvoted PQ-floor cut).
#
# DROPPED: #8 dora128adj_avp_aug10_lr1e4 (ep74_iv0.0-0.8 / ep74_iv0.2-1.0) -- same training
# checkpoint (epoch 74) at two INFERENCE-variance settings, not two training points. No --iv
# flag in our render pipeline and no known checkpoint-filename convention for it; dropping
# rather than guessing. 11 families / ~22 checkpoints remain.
# dora256_avp_aug10_lr7e5_ptm 3 ptm  -- DOES NOT EXIST on LUMI (2026-08-21 verified: only
#   dora256_mixed/dora256_mixed_a128_lr5e5 and dora256_mixed_a196_lr1e4 exist under dora256_mixed;
#   no lr7e5 anywhere under runs/). Flagged to W to confirm the intended run before re-adding.
fullft_avp_t256 90
fullft_avp_t256 153
bf16cmp_avp_t512_bs8_lr1e4_ptm 4 ptm
bf16cmp_avp_t512_bs8_lr1e4_ptm 7 ptm
# dora128adj_avp_8ep_ptm 3 ptm  -- DOES NOT EXIST on LUMI (2026-08-21: zero dirs under runs/
#   share any token with dora128adj/avp_8ep -- not a naming variant, no candidate at all).
#   Flagged to W.
# dora128adj_avp_8ep_ptm 6 ptm  -- same as above
longctx_t1024_r128 4
longctx_t1024_r128 1
fp32cmp_avp_t512_bs8_lr1e4_ptm 4 ptm
fp32cmp_avp_t512_bs8_lr1e4_ptm 7 ptm
winning_avpaug10_t512_a45_fp32_ptm 15 ptm
winning_avpaug10_t512_a45_fp32_ptm 19 ptm
# dora16_avp_originals_earlyeps_ptm 2 ptm  -- DOES NOT EXIST on LUMI (2026-08-21: zero dirs
#   under runs/ share any token with dora16/originals/earlyeps -- no candidate at all).
#   Flagged to W.
# dora16_avp_originals_earlyeps_ptm 4 ptm  -- same as above
winning_avp_t1024_a45_fp32_ptm 15 ptm
winning_avp_t1024_a45_fp32_ptm 19 ptm
subloss_v3sel_k12 11
subloss_v3sel_k12 3
winning_goa_t512_a45_bf16_ptm 15 ptm
winning_goa_t512_a45_bf16_ptm 10 ptm
