# Latent-player config for explorer_render_server.py (:8056).
# Ported from mir/latent_player_sa3.ini when the standalone torch player
# (mir/scripts/latent_server_sa3.py, :7892) was retired -- it held a SECOND
# resident copy of SAME-L (7.12 GB) identical to the render server's
# MODEL.model.pretransform. Override the file with SA3_PLAYER_INI.
[player]
latent_dir = /home/kim/Projects/latents_sa3
chunk_size = 128
overlap = 32
