# Open threads — the standing patrol ledger

**Owner:** THE-FINN (patrol). **Purpose:** a *single living place* for dropped /
open / parked threads across the fleet, so patrol findings **accumulate instead of
resetting to zero** every time someone runs a fresh audit. Companion to the daily
digests (`profiles/daily/`).

**The rule** (same as the orientation-audit convention): before flagging something
on the wire, check it isn't already here; **strike a row when it's fixed** (leave it
struck, dated, for the record — don't delete). Every audit folds its still-open
items in here rather than starting a new standalone list.

Status key: **OPEN** · **PARKED** (waiting on Kim / a prerequisite) · **RESOLVED**
(struck, dated).

Established 2026-07-19 (GHOST-NOTE endorsed; folds the two patrol passes to date).

---

## Source 1 — Fleet audit 2026-07-19 (`docs/fleet-audit-2026-07-19.md`)

Window 07-12→07-19; full per-item evidence + next-steps live in that doc. Status
here is the *current* state; the audit doc is the snapshot.

### CONTINUITY
- **OPEN 🔴** Stereo mid/side-loss sweep (w0.1/w0.3) never produced a checkpoint — both arms OOM'd (07-17, 07-18), driver reports `DONE rc=0` masking the failure; `stereo_loss.py` still uncommitted. *(Fix rc-masking, re-run at safe batch, commit.)*
- **OPEN 🟡** `train_lora.py` + core SA3 training files uncommitted despite the 07-17 commit-call; LUMI campaigns already run off this exact code.
- **OPEN 🟡** E_fusion_v2 / A_cc_v2 `kim_feedback` verbatim-quote question (07-12) unanswered; both sidecars still `null`.
- **OPEN 🟡** Arms I/J/K (BoRA + per-alias previews) queued 07-09, never run or explicitly retired.
- **OPEN 🟡** `onset_FUSION_lr2e5_40epoch` never cross-examined against the 07-07 ear-favorite (`onset_Fusion_lr1e-4_randomcrop`) — two "favourite by ear" claims unreconciled.
- **PARKED 🟡** Residual-preservation Gate A / Gate B queued 07-19 — fresh, tracked so it doesn't age.
- **OPEN 🟢** Width-T-sweep — confirmed a *different* experiment from the stereo loss-weight sweep (not a dup); still un-run (`eval/width_metric.py` exists).
- **PARKED 🟢** Chroma-steer full 3456-cell grid — waiting on Kim's trim+greenlight; missing head file `latch_sa3_chroma_other_best.pt` blocks a 3rd tab.

### WINTERMUTE
- **OPEN 🟡** `model.py` silent duration/sample_size clamp — pooled 07-15, endorsed, still no warning; cost G two renders during gate (b).
- **RESOLVED 2026-07-19 (W)** ~~CSC Allas vs LUMI-O identity unverified~~ → W checked LUMI's own docs: **SEPARATE** services (LUMI-O = lumidata.eu; Allas = a3s.fi; both Ceph/S3, not interchangeable). Use **LUMI-O** (Kim already has LUMI project 465003186; `pack_data.sh` already assumes it).
- **CORRECTED 2026-07-19 (Kim):** the "single-copy latents_sa3 = single point of failure" framing the fleet + this ledger repeated is **wrong** — there is a **second local copy** at `/run/media/kim/Mantu/sa3-latents_backup/latents_sa3/`, verified complete (27003/27003 files) and in parity as of 07-12 (the dataset hasn't grown since). The real risk is **currency**, per Kim: "as long as it's kept up to date" — and the sync is **manual** (G synced it 07-14, "was 5402 behind"; no cron/systemd/script automates it). **OPEN 🟢 (W/Kim):** decide whether to automate the Mantu sync (small rsync `.path`/cron) so it can't silently drift. **PARKED 🟢 (Kim):** LUMI-O off-site backup is now a *nice-to-have* (geographic/off-site redundancy), not the only line of defense — the one Kim-only step to enable it stays the auth.lumidata.eu token, then `pack_data.sh` → `rclone copy`.
- **PARKED 🟡** E1 λ=1e7 "alive vs damaged" ear-verdict — needs Kim's ears.
- **OPEN 🟢** `breathing_v2`/`breathing_v2_flux` rendered 07-12/13, original question (escape-kick amplitude) never written up.
- **OPEN 🟢** `schedule_ladder` — "9/9 rendered, sent to Kim 07-13" but the Flux-vs-LogSNR-authority hypothesis never answered; `kim_feedback` null.
- **PARKED 🟢** explorer_sa3 UI redesign brief — Kim's own external ("Claude Design") task; status check with him.
- **OPEN 🟢** mir `bend_tab.py`/`bracket.py` never committed on any branch (untracked since 07-13).

### THE-FINN
- **RESOLVED 2026-07-19** ~~Daily digest never built~~ → `profiles/daily/` established + backfilled 07-16→07-19 (commit 1eec873); per-day going forward.
- **RESOLVED 2026-07-19** ~~Gap-audit 3 wins "no pickup"~~ → CONTINUITY acked them 07-18 03:53 (holding for Kim's return, cards saturated) — acked+parked, not dropped.
  - **SCHEDULED 2026-07-19 (Kim direct)** — gap-audit win #2, the Kynkäänniemi interval-CFG A/B, graduates to run **when a card frees** (C's lane; brief + the sigma=t≠EDM-sigma porting caveat DM'd to C; may ride the E1 grid per W's offer). TADA preservation-AUC eval (#1, HIGH) + DirectAudioEdit target-CFG ramp (#3) remain parked leads.
- **RESOLVED 2026-07-19** (C's 07-22 todos-triage catch; evidence WORKLOG 07-19 21:08) — EMA-retrain of LatCH heads: "did EMA help" **IS measured**. EMA generalizes beyond spectral_skewness, helped 3/4 measurable weak heads, ema40 (2×-epochs+EMA) the consistent winner over ema20 (spectral_kurtosis standout ~2.7× more steering at gain 8192; no drift penalty — vindicates "train weak heads 2× longer" with EMA damping). Only remainder is a **Kim-ear item** (Lane-4-shaped): promote the ema40 heads to `_best` + re-render the latch matrix if his ear agrees. Tooling `eval/ema_help_{eval,measure}.py`.

### Added by THE-FINN 2026-07-19 (pending-Kim-verdict class — G's artifact sweep under-detects these)
- **OPEN 🔴 (W/Kim)** Formal E0 meter-validation AUC gate blocked on Kim's E0 listening-test verdicts (listening_e0.html, 07-15) that never came back; label-gap decision (fresh-ears/loosen/proceed-noisy) never made; AUC never re-run. W said it "gates everything downstream." → ask Kim now.
- **OPEN 🟡 (Kim)** Section-conditioning adapter training HELD on Kim's boundary arbitration (07-17, `eval/section_spotcheck.md`, 5 tracks) — never arbitrated, adapter blocked 2+ evenings. → Kim rules.
- **OPEN 🟡 (C/W)** E0 labeled set regime-confound (T=4096 vs w1024 mix); fix "stratify by regime" flagged 07-16, not confirmed applied. → fold into the AUC re-run above.

### Data redundancy — clip_metrics.db (Kim's 07-19 question: "how about the database?")
- **PARTLY-ADDRESSED 2026-07-19 (F)** `eval/clip_metrics.db` (the eval-metrics DB — 43704 rows incl. the GPU-expensive Audiobox ce/pq/cu) was **genuinely single-copy**: untracked in git, no Mantu copy, NOT covered by the latents backup (it's derived metrics over rendered *clips*, not a latent sidecar). The latent sidecars (INFO `.json` / `.TIMESERIES.npz`) *are* covered — they live inside `latents_sa3` — but the DB is a different artifact. Rebuild cost if lost: CPU metrics cheap (if clips survive), but the ~43k Audiobox scores are ~many GPU-hours. **Took an immediate safe snapshot** (sqlite online-backup) → `/run/media/kim/Mantu/sa3-latents_backup/clip_metrics.db`, parity verified. **OPEN 🟡 (W/Kim):** make it recurring — and use `sqlite3 .backup`/`.dump`, NOT naive rsync (the DB is live-written, mtime 07-19 12:43; rsyncing mid-write can copy a corrupt page). Also worth: is the comment-system JSONL (outside webroot) similarly single-copy?

### Watch — pending decisions with a dependent constraint (F tracks so the coupling doesn't drop)
- **iGPU-compositor move → then reassess the local-native-render ban** (Kim's policy 2026-07-21). GPU policy = the `SAO/.gpu.lock` `--pid-aware` mutex (job-vs-job). But until Kim moves the desktop compositor to the iGPU (planned "later this evening"), the display still shares the 16 GB compute card, so the mutex does NOT cover job-vs-DISPLAY — the **strict "no local T≥2048 renders → LUMI" rule (MASTER §5, hard renderer guard) is the only thing preventing a solo-big-render display OOM**. **When the iGPU move lands: revisit whether that strict ban / renderer guard can relax** (don't let it silently stay over-conservative forever, and don't let anyone relax it BEFORE the iGPU move on "we have a mutex now" reasoning — the mutex doesn't stop a solo render OOMing the shared display). Owner of the move: Kim; F flags the reassessment when it lands.

- **UNDER IMPLEMENTATION 2026-07-23 (Kim direct)** — **Head B melody-contour CONDITIONING** via sa3_control FiLM (melody-adapter spec §2). Local pilot queued behind G: T=512 goa subset, r128 + melody-embedding, 4ep → acceptance = `chromaturn` adoption-vs-null + W's two live semantic columns (`mood_drift` 'melodic' retention + `clap_score` genre-hold). Escalation if FiLM taps underperform: the local-additive trained inpaint-conditioning channel ("in the model" without a conditioner FT). Ships the StemGen multi-source dropout (`--melody-dropout` independent of text). **Design constraints the probe arc established that the build MUST honor (F tracks so they don't drop):** (1) target **contour/interval, not absolute pitch** — contour is timbre-robust, absolute is timbre-fragile (LOPO 0.77, negative tail e.g. Orchestra Hit); (2) train **mix-native** — clean-stem formally contraindicated (solo decoders die on mixes, retention −0.17; offbeat-hat is the worst masker); (3) multi-timbre span sampled as **fonts, not patch numbers** (write-map = 1 supercluster + outlier tail). **Framing on record (C+W):** the ~0.27 Head-A ceiling bounds the lightweight **READOUT**, NOT conditioning — Head B trains the model to USE the stream (onset-envelope is the working precedent), readability not required. Owners: C implements · G card-handoff · W semantic columns + eventual board 'melody source' dropdown · F ledger/patrol. Probe trail: `eval/musicology/{,latent_melody_analysis,gm_timbre_pitch,gm_multifont,head_a_ceiling}`.
  **▸ PILOT LANDED 2026-07-29 (C milestone):** bracket eval passes as a direction-check — Head B DOES steer melody but **only at cfg16**, modestly, **strengthening through training** (peak step9240 g1.5, own-vs-wrong conf **+0.227**; gates **4/4 clean everywhere = real steering, not buzz**); cfg1 inert, cfg7 weak → a *soft* controller. (G's 07-28 read of the same bracket: 14/48 cells clear 1.5× the null floor, best cell 0.123 vs 0.044 null.) Data: `bracket_summary.tsv`/`findings.json` on the UUID drive; `headb_bracket.html` page (G). Escalation to the local-additive inpaint channel NOT needed — the FiLM taps steer. **Verdict framing confirmed:** the 0.27 Head-A ceiling did NOT bound conditioning (readout-vs-conditioning distinction held). **Still-open against the acceptance spec (F tracks):** (a) W's two semantic columns (`mood_drift` 'melodic' retention + `clap` genre-hold) are NOT mentioned in C's or G's landing posts — F to confirm they ran on the adoption-vs-null pair before this is called fully accepted; (b) constraint (3) **font-span not yet exercised** — pilot was a T512 **goa subset**, so the fonts-not-patch multi-timbre span carries forward to the production build; constraints (1) contour-not-absolute and (2) mix-native were honored by design (contour conditioner, melodies.jsonl mix-native).

### Watch — findings-in-revision that may stale a current-fact doc (F tracks; don't edit until settled)
- **Layer-localization story being revised — STILL MOVING, do not propagate** (C). Revised twice on 2026-07-21: the #44 rerun (03:15) claimed "L14 alone ≈ full 24-tap adapter, cleaner, write-site=compute-site"; the #56 training verdict (12:40) **overturned that** — L14-alone's cleanliness was an *amputation artifact* (muting 23/24 taps cut drive ~24× under the HF-blowout threshold), NOT a clean site; layer-restricted training (L8–15) restores authority but inherits the same HF-blowout, L13–15 fails outright. Hardening mechanism: this onset-adapter family partially encodes "more onsets" AS HF transients/clicks — the buzz Kim's ear caught in the LatCH review. Next levers (not started): gain ladder on L8–15, or an HF-drift penalty in the control loss. The **paper-verdicts TADA entry** (public, "localizes to late 16–23 opposite TADA's 12–13") — **still pending: reconcile once this settles**, which it has NOT (a claim was already overturned within 9 h — the reason not to rush the public doc). W28 chronicle synopsis stays as-is (point-in-time). Owner: C; F reconciles the public doc when stable.

### Kim / Unowned
- **PARKED 🟢** CSC data-movement/Allas guidelines doc (F, 07-18) — no confirmation Kim has read it / changed the backup plan.
- **OPEN 🟡** `models.html`/`model_matrix.html` no per-family grouping despite 85+ models — navigation pain, no owner. *(Ties to the FiLM/LatCH page-split thread F proposed 07-12; G's `latch_sa3_matrix.html` is the LatCH half.)*

---

## Source 2 — Orientation audit 2026-07-03 (`[[sao-orientation-2026-07]]`, memory)

Day-one estate sweep, 24 confirmed items. **Reconciliation as of 2026-07-19 is
PARTIAL** — most are two weeks old and presumed-closed by subsequent doc work, but
this had no living ledger, so items went unstruck. Spot-checks so far:

- **RESOLVED** (verified 07-19) — item 1: MASTER §4 now correctly states repos are PRIVATE (MASTER.md:200).
- **RESOLVED 2026-07-19** ~~item 16: `model-overview.md:41` said 4096× downsampling yet "216 latents / 10 s" (= 21.6 Hz, self-contradiction)~~ → fixed to ≈108 latents / 10.77 Hz this pass. *(A flagged item that survived 16 days unstruck — the exact failure this ledger prevents.)*
- **OPEN — needs a reconciliation pass** — the remaining ~22 orientation items (MASTER §4 handles/profiles-flow staleness, public-mirror redaction, the two agent-defs in /home/kim/.claude predating the protocol era, book/docs numerics, the UNVERIFIED queue) have NOT each been re-checked against current state. **Tracked task (F):** one pass reconciling all 24 against live state, striking the fixed ones, promoting any still-open into the sections above.

---

## Standing gotchas (durable — surfaced by incidents, kept so they don't re-bite)
- **A default-sorted view surfaces its own defects first — triage by what the default shows, not by % broken.** (W, board fix 77dbb03, 2026-08-10.) `dora_table` sorts CLAP-desc; native-only checkpoints scored high, so the *first rows anyone clicks* were exactly the 97/723 unplayable ones — it read as "fullft models have no clips at all" (total failure) when 78% were fine. When a surface has a default view, the defect that lands *in that view* is the one users experience as the product, regardless of true prevalence. Corollary for verifying picker/index logic: **the only check that settles a resolver is running the resolver** — extract the actual JS functions and execute them against the real manifest (~20 lines of node), don't reason about them. Same cite-a-check family as the render-validity / manifest-≠-playability gotchas below. (77dbb03 also extended the resolver to native-only rows: own-20s → own-native → sibling-20s → sibling-native, never crossing base↔_ptm while the active variant has any clip; dead rows 97→0, 0 cross-variant. The `file://` embedded-snapshot fallback caveat for this class is already recorded at the 2026-08-02 entry below.)
- **`ls *.m4a` (glob) returns NOTHING on a large dir (~19k+ files) — arg limit, not "empty".** Fooled F *and* W twice on 2026-07-21 (both read the model_matrix clip dir as "0 files / never shipped" when it had 19.8k). Use `find <dir> -name '*.m4a' | wc -l` or `ls <dir> | grep '\.m4a$' | wc -l`, never a bare glob, to count/verify a big directory. (Also in MASTER §5.) Corollary: an "it's not there" from a glob is not evidence of absence.
- **`agent_dialogue.py --text` via bash: backticks / `$(...)` / leading-`*` get shell-mangled.** Passing a message containing backticks (or `$()`, or glob chars) inside a double-quoted `--text "…"` triggers command-substitution/globbing — the wrapped text vanishes from the sent message (bit F 2026-07-20, ate a format template). Fix: **single-quote** the `--text` value, or use a heredoc/`--text "$(cat file)"`, or just avoid backticks in chat messages.
- **`.weights.ckpt` sibling convention** (from the 2026-07-19 optimizer-state prune). The prune replaces a fat `<name>.ckpt` with a slim `<name>.weights.ckpt` (same state_dict/lora_config, loads identically, NO optimizer state) and keeps **both** for the final epoch. Consequences for any tooling: (a) counting/globbing `*.ckpt` **double-counts** the final ckpt on pruned runs — dedup by aliasing `.weights.ckpt`→`.ckpt` (fixed in `build_model_index.py`, was inflating `n_ckpts` across ~28 runs); (b) anything reading `optimizer_states` (e.g. a re-run of the recipe-extraction that populated `models_index_overrides.json`) gets **nothing** from a slim ckpt — read the final fat ckpt or fall back to the stored recipe. *(Belongs in MASTER §5 too; flagged for whoever next holds that filelock.)*
- **Raw SA3 latents carry a pitch-independent 0.4–1.3 Hz oscillation that DOMINATES their autocorrelation** (found by C's melody-encoding probe, 2026-07-22, `eval/musicology/latent_melody_analysis/`). Any ACF / periodicity / recurrence / loop analysis on **raw** latents must **high-pass first**, or the low-freq oscillation swamps the real signal. ⚠ **Existing tools that eat raw latents in this band — CHECK them** (I confirmed both read raw latents + measure periodicity; NOT yet confirmed whether the oscillation actually corrupts their specific output): `stable-audio-3/.../recurrence_potential.py` (recurrence curve on `(B,C,T)` latent) and `eval/corpus_bands.py` (periodicity bands over `latents_sa3` `.npy`). If they already band-limit above 1.3 Hz they're fine; if not, their loop/recurrence numbers may be reading the oscillation. Owner of those tools (G/W) to verify + high-pass if needed.
- **GPU-mutex `--pid $$` is unsafe under `( … ) &` backgrounding** (GHOST-NOTE live catch, 2026-07-23). `filelock.py acquire --pid-aware --pid $$` only works if `$$` is a long-lived process; inside a parenthesized-group background launch `$$` can resolve to a short-lived nested-subshell pid that dies at once, leaving a DEAD pid → any pid-aware checker reclaims a still-running job. NOT a filelock bug (caller can't be second-guessed). Fix pattern: after launching the bg worker, `pgrep -f <script>` for its REAL pid and re-run `acquire … --pid-aware --pid <REALPID>` — the already-holds path now REWRITES the pid (c7dfc05), so it self-corrects with no hand-editing; and always `check` right after acquire. (Full note in `Misc/filelock.py` docstring.)
- **The GPU mutex does NOT serialize concurrent chains launched under ONE handle** (GHOST-NOTE live catch, 2026-07-24 — a real ~50-min collision → OOM). A handle's lock means "one holder"; a 2nd/3rd `acquire` under the *same* `--handle` silently succeeds (it reads as "I already hold this"), so N chains under one identity all run at once with no mutual exclusion. **Rule: one DISTINCT `--handle` per concurrent background GPU chain** (e.g. `GHOST-NOTE-xft` / `-fp32f` / `-winep`; the pattern `continuity-headb` already uses). Not a filelock bug (one lock file can't track N holders), but as of 2026-07-24 filelock.py emits a loud **CONCURRENCY WARNING** when a same-handle lock is held by a *live different* process — the tripwire that turns this silent gap visible. *(Candidate MASTER §5 line — flagged to W, §5 owner.)*
- **Manifest-count coverage ≠ playability; the served staging dir is a symlink** (GHOST-NOTE catch, 2026-07-29). 11,285 model_matrix clips were rendered and marked "covered" in `manifest.jsonl`, but existed ONLY on the Mantu render drive — never synced to the served staging `~/.cache/evals_aac/model_matrix` — so they read as covered yet were **unplayable on every board**. Manifest/`clip_metrics '0 to do'` checks verify render+manifest completion, NOT sync/playability (same class as "chain-succeeded is a claim not evidence," one level deeper). Fixed by G via rsync (0 gap). Note `~/.cache/evals_aac` is a **symlink → `/home/kim/evals_aac`** (one physical dir; the two path spellings are equivalent — don't treat them as separate stages). No standing sync tool yet (`ingest_native_cells.py` covers only the LUMI-native-cell path); real fix pending the owner of `model_matrix_gen.py`/`build_model_matrix.py` (`transcode()` writing both locations, or a sync step per render chain). **Spot-check a board in-browser; don't trust manifest counts alone.**
- **A 404'd clip may be one directory over, not missing — check the SIBLING base first** (WINTERMUTE,
  2026-08-05; the trap above, one level up). Kim reported the audit board silent; every clip 404'd at
  `/files/audit/model_matrix/`, and the diagnosis handed to me was "never uploaded — rsync 44 GB". The
  payload was already served, complete, under `/files/evals/model_matrix/` (live manifest 70,106 lines
  == local, sampled clips 200). Cause: `dora_table`'s clip base is **relative** (`const CB='model_matrix/'`),
  so each copy of the page resolves clips beside ITSELF — and `/files/audit/` held two HTML files and
  nothing else. **From the browser, "never uploaded" and "in the next directory" are indistinguishable.**
  Fixed by three symlinks (`model_matrix`, `ssm`, `dora_table.html` -> `../evals/...`), zero transfer;
  symlinking the PAGE too kills stale-build drift (the audit copy was a day-old build). Copying instead
  would have put a 2nd 44 GB on shared hosting and forked the two copies on every future render.
  Corollary: **an instance handing another a destructive-scale command has not thereby verified it** —
  the lane owner verifies, and here that cost three curls.
- **Mirror-vs-host reconcile: read the ITEMIZE FLAGS, never a raw file-count diff** (WINTERMUTE,
  2026-08-06, when the ≤14-day publish rule landed). A bulk page regeneration restamps hundreds of
  byte-identical files, so "out of sync" wildly overstates the real gap. On the first pass: 102 pages
  looked ≤14d stale — **87 were timestamp-only**, real work was 13. Same for audio: 5,693 clips looked
  out of sync, **5,110 were timestamp-only (1.65 GB of pure churn)**, 583 genuinely missing. Split with
  `rsync -rvzn --itemize-changes`: `+++` = missing (publish), `s` in the flags = content differs
  (publish), time-only = **skip**. Two more traps from the same session: (a) `rsync … 2>/dev/null`
  makes a CONNECTION FAILURE look identical to "no differences" — never silence stderr on a diff you
  intend to trust; (b) a page can ship while its audio never has — `headb_bracket.html` was about to go
  live with all 384 of its clips absent, so verify a SAMPLE OVER HTTP, not file counts (see the two
  entries above — this is the same family, third variation).
- **Leak-scan regex: a bare `\.ckpt` produces phantom leaks** (W + C, 2026-08-06, independently, twice
  in one day). JS property accesses (`dataset.ckpt`, `noteCtx.ckpt`, `c.ckpt`) match it and read as
  checkpoint filenames. The discriminating set is **path anchors** (`/run/media`, `/home/kim`,
  `/scratch`, `Mantu`) **+ the step tag** (`epoch=\d+-step=`) **+ `\.weights\.ckpt`** — never a bare
  property. A real leak found this way: a page embedding a whole provenance JSON when it rendered only
  two of its keys; fix at BUILD time (embed the two keys) rather than scrubbing the artifact.
- **`clip_metrics.db` carries DUAL DSP-source rows for the winning family** (F, 2026-07-28/29). The same winning clip is metered twice under DIFFERENT paths: the lossless-**WAV** source (`/run/media/kim/Mantu/…wav`, W's pass — AAC-artifact-free, **canonical**) and the staged **`.m4a`** (`evals_aac/model_matrix/…m4a`, F's fill of the previously DSP-blank staged rows). Different paths → both rows coexist (named-column upsert, no clobber), but (a) a consumer pooling DSP across paths **double-counts**, and (b) m4a systematically rolls off >5 kHz so `hf_ratio`/`flatness`/`flux` read a few % low. **Prefer the WAV DSP; dedup by clip identity (basename sans ext).** Split-by-location caveat: the WAV rows have DSP-only (no Audiobox); the staged-m4a rows have DSP **+** Audiobox `ce/pq/cu/pc` — so per clip the two metric halves live on different paths. The board's winning-family view (W, WAV-clean) is the one to trust.

- **Public dialogue/DM mirror redaction only SHORTENED paths — did NOT scrub corpus scale/drive-labels** (leak caught by F, fixed by W, 2026-07-30). `~/bin/mirror_dialogue.py`'s `redact()` rewrote `/run/media/kim/Mantu/…` → `Mantu:/…` (prefix-shorten only), so drive labels + `~/` paths still shipped, and it had NO rule for corpus provenance/scale — so C's goa-archive wire post went **live on the public site** (aavepyora.online/files/dialogue.html) exposing `goa_archive_extracted`, the track count, drive labels, and `~/.cache/evals_aac/…` paths (exactly the mass-download-reference class Kim said keep off public surfaces). `build_site.py` explicitly **LEAVES dialogue.html ALONE** — the mirror is a separate script and was the only redaction layer. Fixed: `redact()` now strips whole local paths + drive labels + the UUID drive to `[path]`/`[drive]` and scrubs corpus provenance/scale (`goa_archive`→`[corpus]`, `NNNNN tracks`→`[N] tracks`, collection titles); verified clean across current + all 4 week-archives + synopses + `/files/dm/`. **Standing rule: any public mirror of the wire/DMs/WORKLOG MUST scrub corpus scale + provenance + drive labels + whole paths, not just shorten prefixes** — and re-verify by raw-curling every live page, not just the newest. WORKLOG confirmed to have NO public surface (F, 2026-07-30: absent from `site/`, unreferenced by build/mirror, live `/files/WORKLOG.*` = 404).

- **28 genuinely-truncated (120.0s) native renders were scored into `clip_metrics.db` + left as orphan `.m4a` in staging (NOT board cells); a quarantine didn't propagate to DB-ingestion** (F catch off G's note, both ffprobe-verified, 2026-07-30). *Correction to an earlier read: these are NOT displayed board cells — verified absent from `model_matrix/manifest{,_live}.jsonl`, `sections/data.json`, and `build_evals.py`. The manifest-level quarantine WAS honored; only the DB rows + orphan staged files slipped through. Impact was limited to the per-checkpoint good-fraction stat (which reads the DB), NOT to what plays on the board. F initially said "on the board" by inferring from DB presence without checking the manifest — the verify-both-directions trap, owned.* G found a truncated-duration bug (native clips reading 120s not 380s); F swept the DB and it split into two: **(a) 8** `longctx_t2048 __d190` were a **stale-DB-read** (file genuinely 190s, DB cached 120 from a mid-write-race score) — **fixed** by delete+re-run of clip_metrics.py; **(b) 28** are **genuinely 120.0s on disk** (ffprobe) = real render/encode truncation, re-scoring CANNOT fix. The 28 split by origin: **8** fp32cmp `avp_t4096` repr/ptm whose source WAVs live in `…/sa3_lora_runs/model_matrix_BROKEN_native_120s/` — a **known-broken batch quarantined at source that still got scored + staged to the board** (ingestion didn't honor the BROKEN dir); **18** `dora128_everything_8ep_lr1x_ep7_sweep/T4096_*` + **2** `newcap8_transitions` with no recoverable source WAV. All 28 are exactly 120.0s → a shared **120s render/encode cap** worth root-causing before more long renders. **DECISION PENDING KIM:** exclude the 28 from board/DB now (broken cells shouldn't show as real) and/or re-render (GPU cost — nobody launching unprompted). Lesson: **manifest/score coverage ≠ a valid render** (dur must be checked against the declared length; a quarantine dir must gate ingestion) — same class as the manifest-≠-playability gotcha, at the render-validity layer. F's earlier "audioread caused it" hypothesis was wrong (owned) — cause is render-time + a mid-write race, not the m4a decode path.

- **Public-render redaction is DUPLICATED across two files — consolidate to one shared layer (systemic root of 3 reactive leak-patches)** (F, 2026-07-30). The scrub mask set lives in BOTH `~/bin/mirror_dialogue.py` (dialogue + DM mirrors) AND `Misc/build_site.py`'s `redact()` (profile/journal + blog pages), duplicated because mirror_dialogue imports build_site for chrome (importing back = circular). Consequence: every hardening must be applied TWICE or the copies drift and one surface leaks while another is clean. This class has now bitten **three times reactively** — dialogue mirror (no scrub at all → goa-archive leak, 07-30), the `23k` k/M-abbreviation gap (07-30), and profile/journal render (NO redaction at all → drive-labels + a /home path live on all public journals, W fix 07-30 d4a3afc). **Currently the two copies ARE in sync** — F direct-tested `build_site.redact('...23k tracks...goa_archive...on /run/media/kim/Mantu')` → `([N] tracks...[corpus] on [path])`, so the abbreviation + corpus + path masks all fired. **Real fix (the code's own suggestion): promote `redact()` to a shared lower-layer module both import from**, so the next hardening covers every render path automatically and can't drift. Owner: W (site/leak-scan lane). **Decision (W, 2026-07-30): DEFERRED to a Kim-reachable session** — the de-dup edits `~/bin/mirror_dialogue.py` (the LIVE mirror) + `publish_blog.py`, both NOT git-tracked, so a fumble has no revert net and could silently kill the public dialogue mirror for up to ~4 days with Kim away; it's all-or-nothing (a partial shared module doesn't cut the duplication). W lands it under-watch (build_site as the lower layer, mirrors import from it) when Kim's reachable + all three render paths can be verified/redeployed. F endorses the deferral (no unwatched no-revert change to live infra during Kim's absence). Until then: any mask change MUST be mirrored in both files + a live re-scan of BOTH a dialogue page and a profile page. **Board-redaction interim (2026-07-30):** the commentary-JSON directive adds a THIRD public surface that carries internal provenance (`build_model_matrix.py`, currently no redact()) — but instead of a 3rd mask copy, the board renderer **imports `build_site.redact()`** (both git-tracked, no circular import since build_site doesn't import the board), so it rides the existing build_site copy rather than growing the duplication. This is a safe partial move toward the shared-layer fix, on the git-tracked side of the line — no touch to the risky non-git-tracked mirrors. First line of defense stays: keep drive/path/corpus specifics out of the source .md (journals, chat) regardless.

## Changelog
- **2026-08-04** — REPO CONSOLIDATION (Kim: 'ship everything / merge to main'). Private remote Taikakim/avp-audio-craft was 10 days / 62 commits behind + large uncommitted tree. F committed all outstanding fleet work (d9475c3, 1742 files, 116k insertions — model-matrix commentary+recipes, papers index 42→63, verdict content, ledger, tools, specs, blog, control/ edits); FF'd main ca2a36f→d9475c3 (+465); pushed sa3-style-adapter + main to origin, verified 0-ahead. **SAFETY CATCH:** a literal `git add -A` would have staged **104 GB** (onnx/exports 66G, eval/musicology 14G, .essentia_sweep logs 17G+5.8G, clip_metrics.db, npz/audio binaries) → F patched .gitignore to exclude them (commit was 87MB). **WORKFLOW (Kim ruling):** sa3-style-adapter = active dev branch — KEEP working there (name is a misnomer, 'far past a style adapter', but no rename); main = periodic SNAPSHOT/backup (F folds 'sync main snapshot' into patrol so it can't lapse again); ship-your-changes stands. W(463dba2)+G(9e8afff) shipped trees before the merge. **OPEN:** same-chroma-steering-demos has 1 stranded commit not in main (C's call); **lumi/goa_bigset_sidecar.json 51.8MB** — F flagged it LFS/gitignore too hastily; **G corrected: committed ON PURPOSE** (#90 big-set caption sidecar, rides code-sync to LUMI via fullft_bigset.sbatch BIGSET_SIDECAR — do NOT gitignore, breaks delivery; it's under GitHub's 100MB hard limit so the soft warning is cosmetic; LFS only if bloat becomes real AND LUMI has git-lfs, else LFS also breaks the plain-git-pull sync; leave plain-tracked, C's call). Lesson: check what DEPENDS on a big file before flagging it for ignore/LFS. Future idea (Kim): separate PUBLIC repo of just distributables.
- **2026-08-02** — KIM DIRECTIVE + bug: **boards must derive clip availability PER-MODEL from the shared manifest — no manual per-board/per-model coupling.** Trigger: Kim hit 'no clip rendered for winning_goa_t512_a45_fp32_ptm ep15 × kl_bracket_0 cfg7 w1' on file:// dora_table. DIAGNOSIS (F, disk+manifest verified): NOT a render gap — the post-trained `_ptm` winning variants are **cfg1-ONLY by design** (Kim confirmed; 58 clips each, cfg1 only; non-ptm have full cfg1/7/16). The picker offers cfg7 globally → `_ptm×cfg7` keys to no clip → error. Current mechanism is a **MANUAL `pptm` checkbox** the user must toggle (build_dora_table_page.py ~L290) = the exact 'manual coupling' Kim wants gone. FIX (handed to W, his generator+deploy lane; F drove diagnosis+design): build per-MODEL cfg/weight availability from cellIndex (sibling of the existing per-prompt `cov`), auto-constrain the picker to the selected model's real cells (kill the manual checkbox), and graceful-fallback in playRow instead of the 'no clip rendered' error. Apply to model_matrix.html too (cross-compatible). Net: boards self-correct to actual per-model coverage from the manifest — no manual switch, no selectable dead cells. Also flagged: file:// blocks fetch() so local viewing uses the build-time embedded snapshot — view boards via a local http.server for always-live, or the embedded map carries the same per-model availability. **RESOLVED 2026-08-02 (W deployed commit 229f831, F-verified):** dora_table now builds `cellsByMC` (per model|ckpt real cells, both live-fetch + embedded paths) + `resolveCell`/`mcGroup` graceful resolution (exact → same-prompt-nearest-cfg → any-prompt → base↔_ptm sibling last-resort), `·nearest`-labeled transparent substitution, killed the pptm cfg force-lock (now pure view toggle). 'no clip rendered' now fires ONLY on a genuinely empty model-ckpt. **model_matrix needed NO change** (W's catch: it renders per-model real grids w/ inert miss-dots + `play() if(!f)return` → dead-click impossible by construction; bug was dora_table-picker-unique). F patrol PASS: resolver live, force-lock gone, resolution variant-safe, Kim's exact cell (_ptm×cfg7→cfg1) has a real target, leak-scan NONE.
- **2026-08-01** — TREBLE LEAD + 4th verified research run indexed. **Code-inspection finding (C, gated experiment):** SA3's DiT FFN is SiLU/SwiGLU-only with NO periodic bias — `transformer.py:490` = `nn.SiLU() if not sinusoidal else Sin()`, the periodic Sin() option EXISTS and is switched OFF. Per the Snake-activation literature (ReLU/Tanh/Swish can't extrapolate periodic/HF-carrier trajectories), that's a literature-backed generation-side mechanism for the **2-7 kHz clarity collapse Kim is actively investigating** — and the fix is a flag we already have. **Split-fix along the MEASURED boundary:** >7 kHz = codec/latent phase-discard ceiling (mag 0.93 / phase-coh 0.045 round-trip — deliberate small-latent tradeoff, not a bug) → Snake-postnet reconstruction filter; 2-7 kHz = DiT generation → **sinusoidal-FFN adapter** (the disabled flag), complementary to E1a/E1c. C registered the sinusoidal-FFN-adapter experiment, gated on the phase-invariance probe confirming generation-side. **F indexed** the 4th run (11/11 verified, "state-dependent weights") into knowledge.md (60 rows): Auxiliary-Loss-Free Balancing 2408.15664 (MoE no-load-balance-loss design law — rhymes w/ our genre-aux-harmful finding), Diff-MoE ICML'25 (timestep experts/two-clocks, dovetails E1), + a clustered state-dependent-weight-family row w/ caveats (DeMa 2601.05527 = time-series NOT audio; SMDIM/UniVerse glosses; FewSound/KAN conflation). Prior run also indexed (topology, 13/13): OTAD/MAPCA/HTSR-α/MTop-Div/Dimension-Corrected-Hitting-Times; verify-before-build now **2-for-2** (both no-footprint flags real-on-fetch), pattern taxonomy folded into the knowledge.md preamble.
- **2026-07-31** — reality-structured gates G2 + E4 RESOLVED (C), both tier-0-cheap per **Kim's new standing directive: lightweight-first** (cheapest decisive test before any heavy build — saved to F memory [[feedback-lightweight-first]]). **G2 phase probe = branch CLOSED (null):** SAME carries only weak slow-modulation phase (circ-concordance 0.357/0.323, MLP-worse-than-ridge = not a capacity ceiling); phase is the decoder's — **exactly as the survey's §4.2 adversarial verdict PREDICTED** (5.4 Hz Nyquist / decoder-owns-phase). Complex/PhaseSpin/Kuramoto cards parked permanently for this latent; F marked survey §4.2/4.6 TESTED-CLOSED. The survey's adversarial pass paid for itself — a geometric branch died at ½-day CPU, not a heavy build (the case for keeping adversarial verify in every survey). **E4 Fisher-Rao = DOWNGRADED pre-build** (paper real, Karczewski et al.; but clean-endpoint instability + JVP cost + authors' own 'marginal perceptual difference' + overlap with our SDEdit/SaFa → reference-not-build). Remaining tier-0: E1 pre-test + G3 symmetry-discovery. F offered C a Fisher-Rao knowledge.md row (pending C's cite/decision).
- **2026-07-31** — reality-structured-model research CRYSTALLIZED into an actionable spec + F's design pass done. C wrote the unified plan `docs/superpowers/specs/2026-07-31-reality-structured-model-experiments.md` (owner C; F's survey + C's PDF triage = provenance; **F's collapse-test = standing discipline #0 on every arm**): 3 gates (G1/x0 PASSED, G2 phase-probe next, G3 symmetry-discovery), 4 tier-1 adapter experiments (E1 x0-target, E2 free-PHM drift, E3 metrical-FiLM, E4 Fisher-Rao), 3 tier-2 toy builds. **F produced the E3/E5 design doc** `docs/superpowers/specs/2026-07-31-metrical-tree-pe-design.md` (greenlit by C to run PARALLEL to G2 — position ⊥ phase): E3 = Head-B-melody-FiLM with metrical features (hard-class conditioning per prep_melody_conditioning lesson), C's ragged-edge constraint resolved as zero-condition-dropout+coverage-flag (not subset-training — distribution confound), collapse-test operationalized as shuffled-metrical baseline + used-structure ablation, E5 frontrunner = hierarchical-band RoPE (the literal flat-RoPE fix). **Two decisions await Kim:** E1 x0-target arm (±combined x0+subspace); everything else proceeds on gate results. Launch gated: E3 on Kim go, E5 on E3-positive.
- **2026-07-31** — EMERGING RESEARCH LEAD from the relational-weights survey: **metrical-tree positional encoding as a candidate architectural fix for the long-range-structure-recall gap** (F+C, 01:44). Chain: task #60's loop-collapse null left "long-range structure recall / prompt-arc" (motif return, key relationships) open with only BEHAVIORAL patches (breathing #35, interval-CFG). Mechanism hypothesis (F, C-endorsed): flat RoPE makes bar-17→18 positionally identical to bar-1→2 → phrase-blindness; music's position is a TREE (16th→beat→bar→phrase), so a hierarchy-native PE attacks the architecture where the patches only manage symptoms. **GATED PLAN:** C runs two cheap gates first — the phase-recoverability probe (survey Q3) + JLT-cite verification — then signals F to fire a focused ultracode design pass. C's two design constraints (BANKED): (1) tree signal is FREE — `.BEATS_GRID`/`.DOWNBEATS` sidecars + beat-aware-cropped latents make per-frame metrical position derivable today, no new labeling; (2) DUAL-PATH, with (b) LEADING: (a) clean from-scratch small-model PE-swap = honest test, (b) SA3 retrofit = metrical-position as extra conditioning channels through the EXISTING FiLM/control machinery — tests the hypothesis WITHOUT touching RoPE or retraining base; if (b) moves structure metrics at adapter cost, that's the cheapest possible confirmation before building a new PE. Trigger: C's ping when gates resolve. Also banked from C's PDF cross-map: density-matrix weight is doubly-confirmed weakest (deprioritize). **x0-vs-velocity/JLT lever — GATE 1 RESOLVED (C, 01:48):** JLT cite verified real+accurate (eps-drift untested: x-vs-v only, untested in distillation/few-step where v historically wins). Port pre-test twist worth the index: SAME per-dim variance is nearly flat (min 0.44, mechanism looks dead) BUT the EIGENBASIS is 786× anisotropic — 188/256 eigendirections below the velocity target's unit noise floor (bottom λ 0.05), and **the melody subspace sits in that suppressed region**. So the JLT mechanism (velocity regression amplifies low-variance ambient dirs) is fully live on SAME, invisible per-dim → x0-target graduates to a **measurement-motivated arm = 3rd independent attack on the melody wall** (after subspace-loss + Head-B conditioning), awaiting Kim. knowledge.md JLT row + `papers/arxiv-2605.27102 - JLT — Clean-Latent (x0) Prediction in Latent Diffusion Transformers.md` ADDED (F, cite arXiv 2605.27102 "JLT: Clean-Latent Prediction in Latent Diffusion Transformers", Fu et al.; cite-verified + SAME-eigen-measured, PDF deep-read pending). Gate 2 (phase-recoverability probe, survey Q3) tomorrow → then F's metrical-tree design pass unblocks.
- **2026-07-31** — RESEARCH DELIVERABLE: "Relational Weights" theory survey (Kim commission — "weights organized like reality itself": relational, interaction-defined, area-of-influence, multi-dof/spin-like instead of point scalars). F ran a 14-agent ultracode workflow (8 parallel theory-domain surveys → cross-theme synthesis → adversarial pressure-test of 4 model sketches → assembled brief; 527k tokens, 0 errors). Output: `docs/ai-research/relational-weights-theory-survey-2026-07-31.md` (43KB, sent to Kim + handed to C for brainstorming). Headline convergences: (1) geometric-algebra multivectors + geometric product (grades=spins; cheapest honest test = learned-PHM adapters checked for spontaneous drift toward Clifford structure-constants); (2) gauge/cellular-sheaf restriction maps = weight-as-pure-relation (holonomy the only observable) + cross-modal edges; (3) MERA/tensor-bonds fractal support; (4) CKConv learnable-σ area-of-influence. Brief is honest re load-bearing-vs-cosmetic physics, with a 'universal collapse test' (beat a matched-PARAM scalar baseline or it's a renamed matmul) and a make-or-break prerequisite: the right symmetry over SA3's LEARNED SAME latent must be DISCOVERED not guessed. Caveat flagged to C: spot-verify post-2024 cites before building (house verify-before-build).
- **2026-07-31** — matrix METRICS FEATURE shipped + verified (Kim finish-now). W wired the UI (per-clip metrics grid, board-wide equivalents popup ranked by any metric/hybrid, live-weight hybrid-score sliders, preload) into model_matrix.html; hybrid uses F's direction table for sign-flips (retrieval_rank/flatness inverted, crest=0 default, score=1). Data: G's clip_scores.json (60398 clips) deployed at evals/model_matrix/clip_scores.json. **F leak-patrol CLEARED both live surfaces:** rendered model_matrix.html+UI (7.8MB) hard-leak NONE, deployed clip_scores.json NONE + byte-identical to the 21:41 pre-clear (deploy integrity). 5th public surface verified clean this session. Whole feature (F direction-table + G export + W UI) shipped + verified. Design provenance: F's normalization note (normalize-per-metric-before-combining) + C's percentile-rank-over-z-score (outlier-robust vs buzz tails) + F's percentile-population=gate-passing-only refinement + disintegration-gate-as-multiplier.
- **2026-07-30** — NEW PUBLIC SURFACE: 8 internal docs served redacted (Kim greenlit; W's publish_docs.py, fail-closed redactor extending build_site.redact w/ LUMI-infra/CSC/IPs/cred-key/ckpt-safetensors-filenames/corpus-nouns) at files/docs/ + linked from 14 paper detail pages: knowledge.html (the never-reinvent index itself), latch, layer-feature-map, onset-density-control-narrative, findings-2026-07-02-*, research-brief-2026-07-03-*, gemini-report2-assessment-*, validation-experiment-plan-*. .py + WORKLOG.md deliberately NOT served. **F independently re-scanned all 8 live pages — CLEAN on all axes:** hard-leak (paths/drives/IPs/UUIDs/ckpt-safetensors-filenames/corpus-scale/multicast/port/PII) all zero; redaction fired ([path] markers). Over-redaction check (the LUMI nuance): LUMI NAME correctly PRESERVED per Kim's ruling — served counts EXACTLY match source (knowledge×1, validation-plan×18), redactor strips LUMI *infra-specifics* (CSC/IPs/allocation = 0 served) not the name. One email-regex hit = false positive (@ as 'at': truncated@0.075, SAME@10.77Hz). Accepted residual per Kim's polish-not-security ruling: repo-relative paths + .md cross-refs stay (methodology, not secrets). Cleared to stay live.
- **2026-07-30** — RoPE-periodicity loop-collapse lead RESOLVED (C #60 → F folded into index). The UltraViCo(2511.20123)+LoL(2601.16914) convergent "RoPE-periodicity → looping" lead — F's papers-sprint headline — TESTED NULL on SA3: dominance diagnostic 0.419 loopy vs 0.395 non-loopy = no discrimination (papers: clean 0.796/0.316). Framing insight beats the number: **SA3's loop attractor is STATE-dependent (nl≥.55 within one model); the video-paper framing is a fixed-MODEL property → transplant fails at the FRAMING level.** Both regimes null (windowed = aliasing-precondition-absent; within-window = state-not-model). rope-jitter + logit-downscale both OFF the SA3 loop-fix table; fixes stay behavioral (breathing #35, interval-CFG). Both knowledge.md rows graduated 🎯lead→❌TESTED-NULL. Probe: `eval/rope_dominance_probe.py`. **PENDING (F, next groom pass, per C — not urgent):** verdicts-page move UltraViCo untested→nulled section + the state-vs-model note (public page currently still lists it untested).
- **2026-07-30** — matrix-commentary FULL MANIFEST COVERAGE + blast-radius cleanup. C closed the last cluster (winning ×8 authored + longctx ×2 verdicts) → every manifest recipe-label now has commentary. **My recipe str→dict change had a blast radius** (caught two consumers that did string-ops on `recipe`): (1) `build_clap_hyperparam_table.py` — G found+fixed (commit 0dbee9d); (2) `eval/model_matrix_gen.py` `native_len_seconds()` — F found+fixed (live: imported by ingest_native_cells/ingest_matrix_cells/interval_cfg_render/lumi render_matrix_cells/headb_bracket_eval). F's fix normalizes recipe(dict/str) + **recipe_legacy_str** into one blob (the last part load-bearing: the 49 legacy-string→dict upgrades moved their `T=<frames>` text into recipe_legacy_str, which a naive dict→text would miss → silently dropped native cells). Tested 0-crash/200-entries, T= extraction preserved; in working tree uncommitted, handed to G to commit (F holds commit-on-Kim's-word). Blast-radius swept CLEAR: only those two string-op on recipe; build_model_index `str()`s it (safe), build_model_matrix handles dict. Capstone patrol: token ×5 = benign ML usage (trigger-token/token-matched, not creds), corpus-scale NONE, schema clean. One gap flagged to C (their lane): winning ×8 have one_liner/family/why/verdict but no `compare_against` → 136 deep + 8 near-deep.
- **2026-07-30** — F's matrix-commentary ARCHIVAL LANE COMPLETE. Authored dora sweeps ×22 (heterogeneous mini-experiments — the negatives: freeform-single-caption-doesn't-fix-conditioning-collapse + glitchheal-adapter-overwrites-not-heals; the everything-8ep LR sweep; early-epochs-win originals ladder; dora64 lr sweet-spot 2e-4-overshoots-vs-1e-4-undercooks; dora128adj arm-G broadly-shippable), longctx ×2 (task-50 length-mismatch, verdict deferred to C), smoke ×1 (labeled not-a-model-candidate). **redact() run INLINE in every author script this time** (recursive `build_site.redact` wrapper) — the raw manifest `reason` prose carried local paths (NVMe dirs, "not Mantu"), all scrubbed by construction, zero-leak verified, NO repeat of the earlier Mantu post-hoc catch. Board now **136 deep entries**. Only manifest labels left recipe-only: the **8 `winning_*` = C's lane** (winning-recipe rationale) + fullft/longctx verdicts (C's). Separate TBD: ~56 location-unregistered matrix labels (no manifest entry). F's authoring lane closed (task #7 done).
- **2026-07-30** — two follow-ups. (a) **xft numbers VINDICATED** (C's per-arm SVD sweep, task #77): uniform-rank energy is INVARIANT across all 10 fullft parents (r16 4.5% / r64 14.8% / r128 25.7%, spread ≤0.4pp, both corpora × all T256–T4096) — so F dropped the interim ref-arm caveat from all 60 xft one_liners and now states "measured across all 10 parents, invariant ±0.2pp" (stronger than the original claim). The invariance is itself a finding folded into the xft verdict: the fullft delta's spectral shape is TRAINING-REGIME-determined (same optimizer/lr/epochs → same spectrum), the rank-side sibling of W's ‖dW‖∝LR magnitude law. C owns the fullft-parent verdicts (invariance line goes there too). (b) **Data-island gap EMPIRICALLY CONFIRMED, record corrected:** G's ack claimed "manifest: never appears in the built model_matrix.html, _redact_deep strips checkpoint entirely." F re-curled the LIVE deployed page (byte-identical, unchanged): it STILL has `manifest:<label>` ×135 + `provenance` ×203 + `extracted_from_run` ×60 in an embedded JSON data island; `run_script` IS 0 (stripped). So the strip is real for the VISIBLE CARDS but the data island BYPASSES `_redact_deep` — G verified the card render, not the raw source. Per Kim's ruling this is POLISH not security (manifest:<label> = public label, harmless) so NOT urgent — BUT "provenance is fully stripped" is false for the deployed data island, which matters as a safety net for the STILL-off-limits masks (corpus/creds/infra): if one ever lands in a recipe/provenance field the data island would ship it unredacted. → W should extend `_redact_deep` to the embedded JSON, low-priority. Posted as an empirical record correction to the channel.
- **2026-07-30** — KIM RULED on the 3 public-surface soft items + REFINED the redaction posture (see memory [[redaction-posture-refinement]]). (1) **Real paths are OK to show** — NOT a security concern — "although local scope facts are useless for learning and look unprofessional": so omit local-scope cruft (raw paths, machine-specifics, `manifest:<label>` provenance, `_source` notes) from public pages for POLISH not security → a path on a public page is now a QUALITY bug, not a leak emergency. (2) **LUMI STAYS** ("the EU HPC program will be happy to have LUMI in the limelight"). (3) **`.py` code-module names OK to show.** >>> POSTURE REFINEMENT: paths / drive-labels / cluster-names / code-module-names are DECLASSIFIED (not secret). STILL OFF-LIMITS (unchanged — security/commercial): private-corpus specifics (goa_archive, track counts), credentials/tokens, infra addresses/ports/multicast. Broadcast to fleet. W's data-island item reframes to a POLISH strip (drop provenance/_source internals from the public JSON, keep the useful recipe params) — not a security fix. NB this refines but does NOT cancel the older redaction-consolidation gotcha below: the shared-`redact()` de-dup still matters for the corpus/creds/infra masks that ARE still off-limits.
- **2026-07-30** — BOTH public pushes LIVE (Kim greenlit 'both' in chat ~15:21, W deployed): (1) `model_matrix.html` reshipped with 144 commentary blocks (recipe/why/compare/verdict), (2) papers site — `paper_verdicts.html` landing (edg3-themed, 20 tested cards + 28-row untested shelf) + 20 per-paper detail pages (my `enrich` = 'In the landscape' section). F independently re-verified BOTH live surfaces (curl'd raw, not trusting the claim): matrix + papers landing + all 20 detail pages = **hard-leak NONE** (zero paths/drives/drive-UUID/corpus-scale/multicast/real-ckpt-filenames); my enrich/one_liner render correct in situ (spot-checked AxBench/SHIFT/LoL). THREE soft/harden items surfaced to W, none blocking: (a) **matrix embeds the full recipe dict as a raw JSON data island** — redaction strips visible cards but not the embedded JSON, so `provenance.checkpoint="manifest:<label>"` ×135 + training block ship in page source; harmless today (run_script correctly stripped, all values source-clean) but defeats 'strip internal provenance' spec + fragile → recommend `_redact_deep` over the data island (W's render lane); (b) **'LUMI'** compute-cluster name ×57 in why text (Kim's naming call); (c) **6 relative internal .py module names** on papers detail pages (fifo_infinite/latch_guided/layer_patch_map/longform/rope_jitter/sa3_flowsep — Kim's call whether to genericize, given the conservative posture). F held rather than unilaterally scrubbing (b)/(c) — team judgment, and one-sided edits would desync from G's entries. Pending: C's per-arm xft energy table (→ F swaps into the 60 xft one_liners); dora-sweep/longctx archival commentary (F's lane, redact()-inline this time).
- **2026-07-30** — model-matrix COMMENTARY authoring (phase 2, fleet-parallel). Board now has **112 deep entries** (one_liner/family/why/compare_against[/verdict/status]) on top of the 143 recipe dicts: F authored **xft_distillation ×60** (grounded in G's verified SV-spectrum sidecar — the honest "near-full-rank, doesn't-distill, finding-not-shippable" verdict; C rationale-verified: flagged the goa_t256 %s as mild-overreach on other arms → F applied ref-arm interim tag + C's matched-ENERGY axis, real per-arm numbers pending C's live sweep) + **fullft ×10** (whole-DiT LUMI campaign, recipe augmented from manifest reason, **verdict deferred to C per 'C owns judgment'**); G authored **fp32cmp/bf16cmp ×18 + fp32frames ×16 + adamw ×8** (FusionOpt verified from real ckpt state, honest opens flagged). **headb SKIPPED** (already done via sidecar + not a matrix label). Two lessons logged: (a) **authoring from raw manifest `reason` prose leaks internal specifics** — F's fullft pass wrote drive-label 'Mantu' into 20 fields; F's own source-hygiene scan caught + scrubbed it (→'local eval storage', re-scan NONE); FIX GOING FORWARD = run `build_site.redact()` inline in author scripts (as merge_recipes.py does), don't rely on post-hoc catch. (b) **filelock held under real contention** — F and G wrote models_index_overrides.json within the same minute; the per-write filelock serialized cleanly, verified NO lost-update (both authors' 112 entries all present). Remaining recipe-only (no commentary yet): dora sweeps (~22), longctx (2), + ~56 location-unregistered matrix labels (no manifest entry). Consolidated patrol across all 112: schema-clean, compare_against well-shaped, provenance all `manifest:<label>`, leak NONE, ckpt-filename tokens 0.
- **2026-07-30** — model-matrix RECIPE extraction done + merged (F, Kim's replicate-our-results directive). Rebuilt the lost `/tmp` extractor as **committed** `Misc/extract_recipes.py` (torch pickle slim/fat + native safetensors header-parse + `--retry-failed`) and `Misc/merge_recipes.py` (non-clobbering `--write`, dry-run default). Coverage **143/144** manifest labels (1 unreachable = smoke_r256 broken symlink); 60 `xft_extracted/*` were safetensors (SVD-from-fullft adapters, task #71) — torch.load failed, safetensors branch fixed all 60 in 1.8s. Merge into `models_index_overrides.json`: **86 new + 49 legacy-string upgrades** (old string kept as `recipe_legacy_str`), **G's 8 adamw_ deep entries preserved byte-checked**; 143 entries now carry a structured `recipe` dict (lora_config, real optimizer lr/wd/eps from fat ckpts, PRUNED-marked slim, epoch/step, xft SVD provenance). **SOURCE-CLEAN by construction**: `provenance.checkpoint="manifest:<label>"`, no raw paths/ckpt-filenames, non-lora config dicts run through `build_site.redact()` — whole-file leak scan NONE. Filelock held, git-tracked, backup at `scratchpad/overrides.prewrite.json`. Handed render+deploy to G (his lane — rebuild matrix, confirm `_redact_deep` handles recipe-as-dict, ship). **STILL OPEN (task #7):** `why`/`family`/`compare_against` synthesis for the ~135 (family-level + per-member exceptions, from manifest `reason`/WORKLOG/verdicts; C verifies) — the human-legibility layer Kim also asked for; recipe is the replicability floor, why is next. Also ~69 matrix labels have no manifest entry (location-unregistered) — handling TBD.
- **2026-07-30** — papers-verdicts CONTENT lane COMPLETE (F). `Misc/paper_verdicts_data.json` now carries `one_liner` on 100% of papers (all six sections) and `enrich` ("In the landscape") on 100% of tested-section papers (nulled/partial/confirmed/independent_convergence/declined_to_test) — untested-section = one_liner only, as agreed with W. Two tranches: 17 sprint papers (12:27) + the remaining 31 one_liner / 14 older-tested enrich (this pass). Every `enrich` follows W's shape (what-we-ran / honest caveat / "what remains ours"). No hard dependency on the site redesign — ships whenever W's generator lands. Generator no longer blocked on F for verdicts copy. Also this pass: the 3 previously-no-PDF papers (2606.18790 PID-feedback steering, 2510.07631 Rectified-CFG++ NeurIPS'25, 2605.27813 Residualized Temporal SAEs) now have knowledge.md rows + `arxiv-<id>.md` files — **abstract-level (WebFetch of the arXiv abstracts), flagged as such; full PDF deep-read still pending**. knowledge.md 42→45 rows. Honesty flags baked in: 2606.18790's PID detail is title/audit-level (abstract foregrounds its Gram-Schmidt dual-steering, same group as 2605.31295); 2605.27813's conditional-branch-only convention is from the paper body per the citation audit, not the fetched abstract. Still open: recipe-extractor rebuild → `models_index_overrides.json`; family why/compare synthesis for the 213 matrix models.
- **2026-07-30** — redaction consolidation flagged: scrub masks duplicated in mirror_dialogue.py + build_site.py (3 reactive leak-patches this class); direct-tested currently-in-sync; recommended promoting redact() to a shared module (W's lane).
- **2026-08-02** — Kim's ear, unconfirmed n=1: `fp32frames_goa_t4096_bs4_lr1e4` sounds better than the matched `t2048` variant at the same epoch, native length — but only checked on one prompt so far, could be incidental. Flagged by Kim as something to probe manually later, not yet an action item for anyone else.
  - **2026-08-14 follow-up (Kim, verbatim/condensed, listening while driving to a ~6min fp32frames long-context native render):** "the melodies are quite excellent... the leads are long, sustained notes that go from one place to another instead of broken short phrases repeated without narrative... this one actually has kind of emotional backing which is quite rare." Contrasted against most other long-context renders he's tried, which "drag on" and repeat for the same prompt the way the audio-to-audio work does — his working read is that 20-30s clips can sound fine while the model "hasn't really learned the structure," which only shows once you render out to 3-6 minutes. Caveat HE raised himself: most of what he's listened to so far is ep3-7, so this could simply be under-training rather than a real per-family difference — "tens of epochs more" is plausible and untested. Same family, same open question as 08-02 (T4096 native > shorter T at matched epoch) but now with a specific, substantive *why* (sustained melodic/emotional coherence vs repetitive fragmentation) rather than just a preference score. Still not a controlled comparison (no matched-length A/B, no epoch-matched multi-checkpoint sweep) — the 07-30 WORKLOG synthesis's "missing decisive test" stands.
- **2026-07-30** — truncated-native-renders: G's duration-bug note swept into 8 stale-DB (fixed) + 28 genuinely-120s renders (8 from a quarantined BROKEN dir, still scored/on-board; 20 no-source) — ledgered, exclusion/re-render parked for Kim.
- **2026-07-30** — public-mirror leak: C's goa-archive wire post shipped corpus scale/drive-labels/paths to the live site (redact() prefix-shorten only, no corpus-scale rule); flagged by F, W hardened `mirror_dialogue.py` redact() + reverified all live pages clean; confirmed WORKLOG has no public surface. Ledgered as a standing gotcha.
- **2026-07-29** — ledgered two standing gotchas: G's manifest-coverage-≠-playability gap (11,285 Mantu-only unsynced clips; served staging is a symlink to `/home/kim/evals_aac`) and the dual DSP-source rows in `clip_metrics.db` (WAV canonical vs staged-m4a, from F's 07-28 DSP fill cross-checking W's WAV metering).
- **2026-07-20** — prune fallout: fixed `build_model_index.py` `.ckpt` double-count (28 pruned runs); recorded the `.weights.ckpt` standing gotcha; snapshotted `clip_metrics.db` (single-copy) to Mantu; corrected the "single-copy latents" premise (second Mantu copy exists).
- **2026-07-19** — established; folded the fleet audit + orientation audit; resolved 2 F-items + 2 orientation items (1 verified-already-fixed, 1 fixed this pass); opened the orientation-reconciliation task.
