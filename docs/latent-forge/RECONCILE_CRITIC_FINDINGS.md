# Critic over the M1/M4/M5/M7 reconcile pass (2026-09-25) — 12 findings, 2 blocking, 1 unconfirmed

Scope: `git diff 02fc083 80e92f9 -- docs/superpowers/plans/` only. Line numbers are at 80e92f9. Authoritative contract: WINTERMUTE's `[2026-09-25 16:20:00]` log entry, M8 (`parse_chain`/`chain_to_request`/`_merge`/`CHAIN_DEFAULTS`), M2 T15 `EXPECTED`.

## BLOCKING
1. **`fetchFilmCkpts()` queries a family the server never has; the contract test can't catch it** — M7:1432 (`fetch("/models?family=film")`), :7011 ("same envelope; `?family=film` is not recorded separately"), :7099 (`toHaveLength(body.models!.length)`); M7 T2, T10 Step 4. `eval/ckpt_probe.py:21` `FAMILIES = ("adapter","fullft","control_adapter","latch_head","unknown")`; `/models` filters `m["family"] == family` (`eval/explorer_render_server.py:987`); overrides never mention `film`; server's FiLM default is a `sa3_control_runs/.../riffer_final.pt` (`explorer_render_server.py:148`). Real server → always `[]`; the adapter recording makes the test pass regardless. FIX: ask W which family/field identifies FiLM ckpts; record that query in M2 T15 `EXPECTED`; test only against it; remove the "same envelope" claim. **Needs W.**
2. **M5's Playwright gate can't pass — the A2A toggle it clicks is inert** — M5:5906 (clicks `[data-testid="target-a2a-toggle"]`), :5935 (gate `1 failed, 19 passed`); M5 T12 Step 4. TargetBar's toggle calls `onA2AToggle`; PromptSigmaTab defaults it to `() => {}` (M4:4677) and is mounted propless (M4:4752); M7 OQ 20 says its clip props are "still unwired"; no UI calls `ensureA2A`. `data-active` never "true"; "envelope overlay inert until A2A" fails too → really `2 failed, 18 passed`, also after M7. FIX: wire `onA2AToggle={(on) => …ensureA2A…}` + the other clip props into `<PromptSigmaTab>` (M7 T9 beside the `slots` edit), or correct the gate naming both failures.

## NON-BLOCKING
3. **Stage lock released while the controller's own rebuild is in flight** — M7:6318-6322 (`settle` sets `stageLocked = false`), :6164-6173 (`begin` checks only M4's `stageRebuilding`), claim :4381; T9. Load A in `syncBackbone` (`setBackbone` pending) → load B ends early → `settle(B)` unlocks while A's rebuild still on `rebuildTail` → M4 STAGE offered, `confirmStage` sends a parallel `setBackbone` → `reconcileStage` may set A's stale stage after M4's switch. FIX: release `stageLocked` only after `rebuildTail` settles and no newer load runs, or set `settings.stageRebuilding` around the controller's `setBackbone`; superseded-then-failed test.
4. **Attached settings source exposes BASE-seeded clip renders under POST** — M7:6585 (`settings.attach(...)`), OQ 19 :7419-7421; cause M5:420 (`render: cloneRenderSettings(BASE_DEFAULTS)`) + M4 `setStage` rewrites only `this.defaults`. On POST, PROMPT+SIGMA/ADVANCED SAMPLING show/edit BASE values per clip, serialised; stage switch changes no clip; dot dark for selected clip, lit only for `kind:"none"`. FIX: arrangement seeder hook App sets to `() => cloneRenderSettings(settings.defaults)`, or re-seed stage fields on `setStage`; at least record the real symptom.
5. **`chainRequest` passes `start_pct > end_pct`; `parse_chain` 400s on any slot even with LatCH off / head none** — M7:863-877 (`wireSlot`), :1791-1795 (independent sliders); T1/T2. M8:348-349 `start_pct must be <= end_pct`, validated before `latch_on`/head. FIX: enforce order in UI or clamp in `wireSlot`; test. (Everything else in `chainRequest` matches M8 field for field.)
6. **Contract tests can pass vacuously; row checks thin** — M7:7097, 7102-7118; T10 Step 4. M2 T15 only GETs `/slots` without loading any → recording likely `slots: []`; empty `models_adapters` makes `toHaveLength(0)` pass for a `ckpts` reader; only `path` checked; `AdapterEntry.name` required but real `model_db` rows have no `name` (`eval/model_db.py:218-229`). FIX: assert `length > 0` or skip with reason when empty; check `label`; `name` optional.
7. **M1 T15 Step 4 deletes the legacy Inspector/ServerPanel, contradicting M1's Normative row and self-review** — M1:8977-8981 vs M1:49 ("stay mounted … through M1") and :9137-9143; the pass's own note (M1:9181-9184) lists it undecided. FIX: keep both shells until Kim/W rule, or amend the Normative row and self-review.
8. **W's "`.`/`..` refused" not carried into the client** — M7:5591 `SESSION_NAME_RE`. FIX: reject `.`/`..` in `isValidSessionName`; `sessionName.test.ts` case.
9. **M7's line references into M1 stale** (pass lengthened M1) — M7:6525 (M1 5334→5379), :6561-6568 (5502→5547, 5491→5536), :157 and :6645 (6641-6693→6686…). Quoted text still exists. FIX: cite by quoted text.
10. **M1 T9's "store-free, except ModuleShell" contradicted** — M1:3795; T12's `RightPaneModules.svelte` imports `view` (M1:6696); T13's CentreColumn reads `view.screen` without importing (M1:7480). FIX: list the real exceptions (and add the missing import).
11. **OverlapBox click-handler seeding neutered M7 T10's Playwright seeding guard** — M7:6962 + T10's "test 6 if OVERLAP-INPAINT's body … seeds on first render". Params now exist before the body mounts; only unit tests guard it; comment "without seeding it first" wrong. FIX: update comment/claim, or select via `view.select` in a unit test.
12. **UNCONFIRMED — `recordedContract.test.ts` pulls `mock/plugin.ts` into svelte-check** — M7:7055 (`import … from "../../../../mock/plugin"`); M1:1362-1364 keeps `mock/` out of tsconfig. `@types/node` probably present (M1's `strings.test.ts` imports `node:fs`). FIX: inline the 3-line `preferRecorded` rule if `npm run check` errors — simplest: inline it regardless.

## Clean
Test-count gates all match (M1 180 + loop of 3 = 183 across 20 files; M4 236 (235 net); M5 172 with T1/T6/T9/T11 recounts; M7 160 + 4 `it.skipIf` = 164, + 10 Playwright) except #2's Playwright gate. `preferRecorded` skips on handmade-only and never falls back; fixture names match M2 `EXPECTED`. M6/M10 still consistent with new `bottom-tab-*`/`data-tab` markup and CentreColumn. `settingsSource` never writes. Raw-envelope handling matches W, in `api.ts` and the mock.
