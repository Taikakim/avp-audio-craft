# M7 critic pass 3 — Task 9 / SessionController only (2026-09-25), 12 findings, 1 blocking, 2 unconfirmed

Target: `docs/superpowers/plans/2026-09-23-latent-forge-m7-chains-mix-sessions.md` at commit b2915a8. Narrow scope: Task 9's `SessionController`, load/import/save/autosave paths, TopBar/App wiring, and the tests that claim to prove them. Line numbers refer to that plan at b2915a8 unless another plan is named.

## BLOCKING

1. **Any session body that isn't `version: 2` goes through the never-failing converter and becomes the session's saved state** — `sessionController.svelte.ts` `toLoaded`, plan 5429-5435. Step 3 says "A malformed file or session stops here — nothing is PUT" (4244-4245), but `toLoaded` sends everything not version 2 to `convertProjectV1`, which "never throws" (4098-4128). Missing/mistyped `version`, `"2"` as a string, or a future `version: 3` → the converter reads v1 field names (`source`, `startSec`), a v2-shaped body loses every clip; step 9 names and arms it; first edit autosaves it over the real session. IMPORT too: wrong JSON silently replaces the timeline and reports "imported". FIX: only `version === 1` goes to the converter; otherwise throw "unsupported project version". Test: load a `{version: 3, …}` session → no PUT, old name still armed.

## NON-BLOCKING

2. **Master-preset recall refusal checked only before the fetch** — App `loadMasterPreset`, 5775-5788. `sessionCtl.loading` tested before `await forgeApi.preset(...)`, never after. Preset picked on take1, then take2 picked; if the response lands during take2's steps 5-8 (rebuild wait can be long), `applyMasterPreset` writes take1's chains/mix/master/prompt into take2's stores; after step 9 they count as edits and autosave. Contradicts line 427. Also `masterPreset = name` set before refusal/failure. FIX: expose a load generation `seq` from SessionController; re-check `seq` + `loading` after the await; set `masterPreset` only after success.
3. **Unsaved work replaced without asking** — `loadSession`/`importProjectFile` 5475-5512; wiring 5811-5813. When `session === ""` (fresh tab, or after IMPORT) picking a session or importing goes straight to step 5; nothing was ever autosaved (by design, 4282-4284) → everything built since is gone. FIX: `confirm` dependency; before step 4, if `session === ""` and the current snapshot differs from the last baseline or the blank project, ask.
4. **SAVE silently overwrites an existing session when a new name is typed** — `saveSession` 5594-5607; App 5768-5773. Prompt answer checked against the regex only, not `sessions`; `saveMasterPreset` same. FIX: `exists(name)` check / confirm before overwrite.
5. **Failed autosave never retried; saves unordered** — `createSnapshotAutosave` 5143-5149; controller save callback 5456-5459. `baseline = snapshot` set before the PUT resolves; rejection only logs; nothing re-queues. Loading another session afterwards (step-4 flush finds nothing pending) loses the edit. PUTs fire-and-forget: two autosave PUTs, or the step-4 flush and a quick re-load's GET of the same name, can reach the server in either order. FIX: `save` returns a promise; on rejection restore the last successfully saved baseline so the next `observe` re-queues; chain PUTs per name.
6. **Superseded load + failed rebuild leaves client claiming a model the server didn't load** — `syncBackbone` 5557-5572; `settle` 5576-5584; Known incomplete 6 at 6192-6195. Load A (POST) applies and starts rebuild; load B fails at fetch/validation; B's `settle` clears `loading`, sets session `unsaved`; A's rebuild fails and `if (mySeq !== this.seq) return` skips revert and log → `settings.stage` POST while server is BASE. "The newer one decides" is false here — the newer load applied nothing. Related: M4's own `confirmStage` running during a load can later `setStage` over the loaded session's defaults, then autosaved. FIX: track last confirmed server backbone in the controller; in `settle` when stores are orphaned, revert `settings.stage` to it (or re-query `forgeApi.backbone()`).
7. **v1 IMPORT on POST resets session defaults to BASE values** — `applyProject` 5320-5325; converter 4115; test 4848-4861. `restoreModel: false` keeps the stage, but `settings.defaults = cloneRenderSettings(project.defaults)` writes `BASE_DEFAULTS` (24 steps, euler, model schedule) under POST; M4's `setStage` exists to keep those matched. The v1-import test checks only stage and backbone. FIX: for v1 input overlay `STAGE_FIELDS` from the current stage's defaults; assert `defaults.steps`.
8. **`validateProjectV2` checks less than its docstring claims** — 5251-5288. Docstring: "an object that passes cannot throw half-way through the apply", but `ui` only `isObj`-checked: `ui: {modules: "files"}` passes, then M1's `restoreUi` calls `.filter` on a string (M1 plan 3301) on the last line of `applyProject`, after every store is written. The apply-throws fallback prevents a bad PUT but the previous (possibly unsaved) timeline is already destroyed. Also unchecked: clips' `offset_sec`, `downbeats_sec`, `history`, `latentState`, `loop`; lanes' `gain`, `muted`, `solo`; each overlap entry; `mix.nodes.*`; `master.*`; whether `snap` is a valid mode. FIX: extend checks; add a `ui.modules`-not-an-array test.
9. **Branches the last fix round added have no tests** — `sessionController.test.ts` 4745-4862; self-review 6160. Untested: `settle`'s orphan branch (stores left `unsaved`), superseded SAVE (`mySeq !== this.seq`, 5613), apply throws (session `""`, 5531-5535), two racing loads. Self-review still marks §9.2 "no load or import can … SAVE a half-applied or mismatched project" done. FIX: three deferred-promise tests — A applies then B fails; SAVE's PUT pending when a load commits; `applyProject` throws.
10. **UNCONFIRMED — App.svelte anchors may not exist** — Task 9 Step 4, 5728-5742; M1 plan 8854-8886. M1 Task 15 says App's `<script>` "becomes" a block with no `forgeApi`, `sessions`, `session`, `masterPresets`, `masterPreset`, `loadTopBar` (and no `view`/`viewStore` import). Task 9 deletes M1 lines 5491/5502 "inside `loadTopBar()`", says "`forgeApi` is already imported by M1 T10", and reads `sessions`/`masterPresets`. FIX: state the required starting state, including restoring Task 10's top-bar block and `forgeApi` import if Task 15 removed them.
11. **During a load the SESSION select names a session the stores don't hold** — GC #10 (99-103) vs step 4 (4246-4248), `commit` 5525. Previous name stays through steps 5-8, the whole rebuild ("roughly a minute", M1 plan 7773); timeline shows the new project, no loading indicator, SAVE refused with only a TERMINAL line. FIX: transient `loading <name>…` option + SAVE disabled while `loading`, or reword GC #10.
12. **UNCONFIRMED — `applyMasterPreset` can apply half a preset, then autosave it** — 5371-5391; App 5783-5787. Payload unvalidated; missing `defaults`/`clips` throws after lanes (maybe mix/master) are written; `catch` only logs; armed session autosaves the half recall. Same class as critic-2 #1. FIX: validate the payload shape before the first write.

## Clean
All names/signatures Task 9 consumes exist (forgeApi methods, view-store methods, `scheduleStretch`, M4 settings, `peekOverlapParams`, `clearOverlapParams`, `durableChain`, `convertProjectV1`). TopBar anchors match M1 exactly (5299, 5341, 8942-8949). Select `value` + `init_select` snap-back compiles and behaves as the test expects on Svelte 5.57.0. Fake timers vs 2 s debounce, deferred promises, `vi.waitFor` under fake timers all work. Task 9's 35 `it()` (3+7+8+6+2+6+3), Task 10's 6 Playwright, and "1 failed, 16 passed" match the task bodies.
