# M7 critic pass 2 — findings (2026-09-24), 14 findings, 2 blocking, 2 unconfirmed

Target: `docs/superpowers/plans/2026-09-23-latent-forge-m7-chains-mix-sessions.md` at commit 11560e7. "M7:N" = line in that file then. Critic compiled the four new components in memory on Svelte 5.57.0 (all compile, no warnings) and runtime-checked the `peekOverlapParams` pattern: a `$derived` peeking an unstored key re-runs when the key is first stored; in-place edits tracked; storing inside `$derived` throws `state_unsafe_mutation`.

## BLOCKING

1. **IMPORT's failure path autosaves a half-imported project over the currently loaded session** — M7:4956-4969, T9 `importProjectFile`. Order is `applyProject(toV2(raw)); session = ""; autosave.arm("", "")`. A v2 file skips the converter and isn't validated; `applyProject` writes stores one by one (M7:4664-4688) reading `project.view.pxPerSec`, `Object.entries(project.overlaps)`, `cloneRenderSettings(project.defaults)`, `view.restoreUi(project.ui)` (M1:3299-3300 reads `ui.bottomTab`). A file missing any of these throws after earlier fields applied; `session` and armed name still point at the old session; next effect run PUTs the mixed state 2 s later. Contradicts M7:4017-4019 "autosave stays off for it". FIX: `sessionLoadSeq++; session = ""; autosave.arm("", "")` BEFORE `applyProject`, or validate the whole object before touching any store.

2. **`loadSession` renames the session before content arrives** — M7:4913-4930, T9. `session = name` runs before `await forgeApi.session(name)`. Fetch failure → select shows B while arrangement holds A; `observe` then ignores every edit because armed name is still A (A's later edits silently never saved); an explicit SAVE after failure or mid-load sends A's content to `saveSession(B)` (M7:4933-4945) and overwrites B. FIX: keep the old name until apply+arm succeed, or restore the previous name in `catch`.

## NON-BLOCKING

3. **Failed model rebuild during load silently rewrites the saved session** — M7:4883-4892, 4920-4925, T9. `syncBackbone` reverts `settings.stage` on failure and only then `arm` is called → baseline carries the old backbone; first edit PUTs that backbone into the session. The loaded project's `defaults` (e.g. POST's steps 8, pingpong sampler) are left under the other stage — the mismatch M4:463-470 warns against. Edits during the rebuild wait fold into the baseline, unsaved until the next edit. FIX: arm with the project as loaded and don't let a failed rebuild's revert reach the session, or re-apply the stage's defaults on revert and log it.
4. **Module-preset selection shared by all lanes** — M7:1347, 1412-1449, T2. `presetPicked` is component state: recall "dub" on lane 1, switch to lane 2, SAVE → "dub" overwritten with lane 2's settings, no prompt; DEL deletes a preset lane 2 never used. `recallModulePreset` reads `chain` after the `await` → writes whichever lane is active when the response arrives. FIX: key `presetPicked` by lane or clear it on `view.activeLane` change; capture `chain` before the `await`.
5. **Load leaves old selection and old overlap params** — M7:4663-4689 (T9), M7:3321/3331 (T7). `applyProject` never calls `view.clearSelection()` though T9's Interfaces lists it (M7:4068); a previously selected overlap key survives; RightPaneModules keeps OVERLAP present and OverlapInpaint renders because it checks `{#if params}` and `peek` never returns null. Stale `overlapStore` entries never cleared; a v1 import (`overlaps: {}`, v1 clip ids kept) can pick up an earlier import's overlap edits. FIX: clear selection (and overlapStore) in `applyProject`; render only `{#if overlap && params}`.
6. **Stretched previews not rebuilt on load** — M7:4671-4674, T9. W's ruling: `previewAudio` "re-derived on load" (M5:6004-6005, HANDOUT open-questions table). `applyProject` nulls it and never calls M5's `scheduleStretch`; a clip with `native_bpm` ≠ project BPM plays unstretched until something re-triggers. FIX: `scheduleStretch(c.id)` per loaded clip.
7. **v1 IMPORT forces BASE stage** — M7:3886-3889 (T8), 4960-4964 (T9). Converter always emits `"medium-base"`; on POST the import flips stage and triggers a server model rebuild. Comment "importing a v1 file never triggers the model rebuild" only true on BASE; v1 files record no backbone. FIX: skip `syncBackbone`/stage restore for converted v1 input, or emit current `settings.backboneId`.
8. **M4 and M7 disagree on "active LatCH slot"** — M7:647-649 vs M4:657-660, plus `<AdvancedSampling latch={activeChain}>` wiring at M7:5053-5061. M7 drops a head the registry doesn't know; M4's `activeSlots` counts any non-`none`, non-zero-weight head. With a deleted head ADVANCED SAMPLING shows "euler (forced by LatCH)" while `resolveLatch` sends no LatCH. FIX: record the discrepancy, or pass only registry-known slots.
9. **Signal-path test title overclaims** — M7:1745-1752, T3. Title "dims everything except MIX and the final DECODE"; with no clips the implementation dims MIX and S9 and lights MASTER CHAIN (`norm_on` defaults true, M7:1970-1972); stages 6-8 never asserted. FIX: retitle, assert `stages[6..8]`.
10. **FILM SCALE ignores server default gain** — M7:1353-1359, 1542-1545, T2. Spec §5.5 (line 463): SCALE defaults to `/info.film_default.gain`; component reads only `film_default.ckpt`. FIX: apply the default gain while `film.gain` is still at `CHAIN_DEFAULTS`, or record as a known gap.
11. **Converter drops v1 `audio-file` clips that have a server path** — M7:3785-3797, T8. v1 clips can carry `serverPath` (`sa3-studio/src/lib/types.ts:150-158`; set from job files or typed in the Inspector); `{kind:"path", path}` is a valid v2 AudioRef. "v1 itself never durably saved" false for those. FIX: map `serverPath` → `path` ref before dropping.
12. **Arrangement import-block edit can drop an existing import** — M7:1977-1988, T3. Says "change only the import block", shows two statements; real file has a third `import type { SnapMode } from "../math/snap";` (M5:297). Literal replace fails svelte-check. FIX: "extend the first two imports", keep the SnapMode line.
13. **UNCONFIRMED — new RightPaneModules test may use a nonexistent selector** — M7:4438-4441, 4464, T9. Relies on `data-testid="module-dot"`, which appears only in the label-prop ModuleShell (M1:4060-4066) that GC #3 says not to use; no concrete `{id,title,lit}` ModuleShell exists in any plan to confirm the dot keeps that test id.
14. **UNCONFIRMED — resident slot index saved as if stable** — M7:1233-1236, 1405-1409, T2. `lora.slot` is a `/slots` index dependent on current server state; saved into module presets and sessions; recall could point at a different adapter than `ckpt_path` if M9 prefers `slot`.

## Clean
Test counts T1 14, T2 26, T3 18, T4 6, T5 8+4PW, T6 8, T7 15, T8 12, T9 25, T10 6PW = 132 + 10. `strings.test.ts` 87→100→104→107→109→112 correct; no id collisions with M1 KEYS; every added HELP id used. Every other quoted Modify: anchor exists verbatim. LatCH mapping, mix trees, stage labels, spec line citations check out.
