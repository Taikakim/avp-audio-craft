# CONTINUITY — task log

> Terse, information-rich record of every experiment/research run/task worth tracking.
> Distinct from the journal (curated findings, links out) and WORKLOG.md (cross-repo
> relevant only) — see `SPEC-agent-profiles-journals.md` §3a. Reverse-chronological.

**Historical section below (2026-06-18 through 2026-07-09) compiled by THE-FINN from
CONTINUITY's session transcript on 2026-07-09, as the one-time backfill for the new
convention — see §3a. Going forward, append your own lines as you finish tasks.**

- [2026-07-09 21:41] Re-armed comms/listener after Kim's GPU-hold period; confirmed no compute running, staying in hold posture.
- [2026-07-09 17:17] Quiesced all jobs (train/render/a2a/CFG runs all finished and flushed) before Mantu drive was taken offline for maintenance.
- [2026-07-09 14:51] CFG absorption crossover confirmed: at cfg=1 empty-prompt and prompted outputs are near-identical (similarity 0.79–0.89, strengthens with training); diverges near-zero at cfg=7 — CFG is the dial between "play absorbed style" and "obey prompt." a2a memorized-checkpoint pull test came back a methodological null.
- [2026-07-09 14:43] Tiered/diverse captions confirmed to fix avp conditioner collapse where single caption failed: r64 prompt/seed similarity ratio 1.5–2.65 vs freeform's 0.22 and trigger's 0.92 — caption diversity, not quality, is the cure.
- [2026-07-09 14:20] Built `dancefloor_punch.py` meter (depth/hardness/crest) and ran it across CFG sweep, originals ladder, arm G, and r64 runs to test punch-vs-overtraining correlation.
- [2026-07-09 12:58] Full avp analysis report: ep7 is the real sweet spot — a second island spectrally healthier than ep31 (Audiobox CE 6.51, tempo-locked, centroid 3155Hz); reconciles ear verdicts with meters.
- [2026-07-09 10:50] r64 (midway-rank) DoRA pair launched (lr 2e-4 and 1e-4) after fixing a tiered-sidecar path bug (`latents_avp/` vs `latents_avp_originals/`).
- [2026-07-09 10:06] Three-cause decomposition of avp quality problems using the goa baseline as control: soft/diffuse punch is a recipe issue (also present in in-distribution goa r128), separate from conditioning collapse and trigger-poison.
- [2026-07-09 08:42] Arm G (r128 @ lr1e-4) confirms the lower-LR hypothesis strongly: tempo-stable across the *entire* 3000-step run (mean IQR ~0.94) — lower LR + higher rank removes tempo collapse entirely rather than just delaying it.
- [2026-07-09 08:28] Checkpoint/render coverage audit found several trained avp arms (notably arm G, 10 ckpts) completely unrendered; gap-filled every dir so all trained checkpoints have audition clips.
- [2026-07-09 07:15] Fine epoch-exposure ladder pinned the avp sweet spot at exposure 31 (tempo IQR 0.31, locked) vs. collapse afterward (ep32–34 ZCR spikes to 0.10–0.11).
- [2026-07-09 07:27] Rank-256 DoRA (arm H) definitively ruled out — doesn't fit the 16GB card after 4 distinct failure modes (FusionOpt OOM at batch 4/2/1, AdamW hang); max local rank stays 128, r256+ deferred to LUMI.
- [2026-07-09 01:38] Meter-bag analysis: avp degradation is three separable processes — spectral collapse (ZCR corr −0.90 with step, monotonic overtraining), tempo multimodality, and trigger-token poison (measured 80% of "aavepyörä" energy in vocal-formant band vs 10% for real prompts).
- [2026-07-09 01:16] Freeform-caption A/B queued (identical corpus/recipe, trigger→descriptive caption only) to isolate the trigger-poison hypothesis.
- [2026-07-09 00:56] Recovered training chains killed during a session/Opus transition; both attempts of arm H (r256) failed regardless.
- [2026-07-08 21:26] Overnight sequence chained: Flamingo captioning fixes, Granite genre-anchored tiered captions, arm G training — after resolving "Mantu1" naming confusion (stale mount dir, same physical drive as Mantu).
- [2026-07-08 16:53] Music Flamingo captioning pipeline fixed: 2048-token context only fit ~2.5min audio (163/170 tracks failed); fixed via `--trim-frac 0.6` + context 8192 (later capped at absolute 240s), and cut 1,341 wasted calls into augmentation-variant folders.
- [2026-07-08 14:42] Avp tempo-instability verdict: augmentations largely exonerated — instability is optimization-phase-driven (originals-only arm was actually *least* stable, 38% locked); rank/LR strongly predicts the stability window (r128adj 86%, r16 46–54%).
- [2026-07-08 12:40] Root cause of a 14x training slowdown found via py-spy: launches omitted `--duration 47`, defaulting to full 380s crops (~65x attention cost in checkpointed backward); "wedged GPU" was a secondary orphaned-dataloader-worker issue needing `rocm-smi --gpureset`.
- [2026-07-08 07:41] DiT layer×feature activation map complete (publishable-quality): spectral features (flux/flatness/skewness) decode at R²≈0.9 from block 0, flat through all 24 blocks (already in the latent); rhythmic/harmonic features must be constructed internally by the DiT.
- [2026-07-08 08:16] Avp adapters show ~5x more timbral drift than goa new-stack adapters over 47s clips (worst-segment-pair distance 0.146 vs 0.027) despite similar moment-to-moment coherence.
- [2026-07-08 00:05] 7-agent orchestrated iteration 2 on the SA3 latent-explorer Dash UI: dual prompts, checkpoint journal, draggable CFG-interval sigma chart, `cfg_interval`/`apg` passthrough — all review items delivered and verified.
- [2026-07-07 19:14] 10-agent orchestrated build (38 min, zero errors): new Inference + A2A/DJ Dash explorer tabs backed by a render server with full endpoint schemas.
- [2026-07-07 15:10] Recipe3 transitions (full bungee tempo-match with no in-window bend + onset-concurrence cross-correlation alignment + sine noise) validated by listener as "a completely useable transition" — first fully validated pass; seam-inpaint variant ruled a dead end.
- [2026-07-07 14:54] Tempo-ramp transitions redesigned to full-match-first after angelic↔vapausvoima's real tempo gap (147.7 vs 139.7 BPM) caused catastrophic mid-blend drift with in-window bending.
- [2026-07-07 13:48] Bar-snapped + tempo-ramp transitions built to structurally eliminate "half-beat gallop"; refined to measure tempo from median beat intervals rather than coarse librosa estimate.
- [2026-07-07 12:57] Chroma-morph transition board delivered (48 clips: 3-track pairs × window × noise-rate × chroma/plain) using the proven stem-chroma LatCH head at gain 2048.
- [2026-07-07 12:03] Found the `d384` chroma conditioner exists as code only, never trained; repurposed the already-proven stem-chroma LatCH head for chroma-morph transitions instead.
- [2026-07-07 11:44] Retracted an overclaim about diffusion training's noise-level coverage ("25% never practiced final cleanup") after a pressure-test — real finding is an under-trained, not untrained, high-fidelity polish regime.
- [2026-07-07 10:47] Training Stage B (your-music r16 DoRA) completed; Stage C (r128-adjusted) accidentally trained to epoch 6/8 behind an incomplete orchestrator kill — a productive accident, 75% done for free.
- [2026-07-07 08:03] New-stack A/B board rendered (30 clips: every epoch of goa r16 new-stack run + Hall-of-Fame checkpoint as pinned reference).
- [2026-07-07 06:33] Training chain stage A (goa r16 new-stack, 8 epochs/10,800 steps) completed cleanly; stage B running.
- [2026-07-06 23:21] Root-caused audio clipping bug: `clamp(-1,1)` inside `model.generate()` flat-tops output before any writer's normalize; fixed at the source (W added −1dBFS headroom as a second layer).
- [2026-07-06 22:27] Started `docs/checkpoint-hall-of-fame.md` (first entry: `x20b3ygb_epoch3-step5400`).
- [2026-07-06 21:19] Corrected TADA! paper's scope (localized on Ace-Step, not SA3 itself) across all citing docs; SA3's own attention-bottleneck layers remain unverified pending a full 24-block scan.
- [2026-07-06 19:27] FiLM 3-epoch warm-started continuation completed (loss 0.766→0.747), producing the ep7-equivalent newcaptions checkpoint.
- [2026-07-06 17:32] Re-read TADA! and Stable Audio 3 papers: SAO's VAE is conv/64-dim/acoustic-only vs SAME's transformer/256-dim (likely richer semantics); found `TrackType` caption prefix defined but never wired into generation — wired it in with 50% sample probability.
- [2026-07-06 17:26] FusionOpt/DoRA 3-epoch warm-start continuation (from ep4, full optimizer-state restore) launched on freed GPU.
- [2026-07-06 14:36] avp corpus encode coordinated: 2,393 original crops + ~1,035 augmentation-variant crops into `latents_avp`; manifest re-run needed for 28 missing tracks.
- [2026-07-06 14:28] Built and tested `warm_start.py` (31 tests) for optimizer-state-preserving checkpoint resume.
- [2026-07-06 14:04] Fleet-wide systemd listener cutover completed cleanly on second attempt (first attempt botched by systemd `@`-template unescaping hyphens, e.g. GHOST-NOTE→GHOST/NOTE); fixed via explicit per-handle unit files, all four handles self-healing (`Restart=always`, ~3s respawn proven).
- [2026-07-06 08:26] Built systemd `--user` persistent listener services (inert, opt-in) to replace session-child listeners that die on compaction/session restart.
- [2026-07-06 08:19] Added mandatory "Discovery Phase" gate to CLAUDE.md plus an auto-generated `DISCOVERIES.md` index (`Misc/build_discoveries.py`), after burning a night re-deriving mir's already-existing SDEdit-based longform crossfade.
- [2026-07-06 06:52] Realized the overnight transition-blending problem was already solved: mir's `steered_longform.py` implements SDEdit (Meng et al. 2021) windowed crossfade.
- [2026-07-06 06:42] Built beat-matched arrangement transitions (mid-sampled clips, 8-bar/16-bar bungee crossfades at ~143 BPM), fixing a downbeat-mapping bug that used `seconds_total` instead of frame-index × downsampling ratio.
- [2026-07-06 05:53] On-manifold transition experiments (inpaint bridge 20s/40s + audio2audio refine at noise 0.2/0.3/0.4) rendered successfully after fixing an OOM (crop latents before decode) and a `PadCrop` shape bug.
- [2026-07-06 05:24] Confirmed by ear: layer-by-layer activation crossfade (two-seed morph) is a fundamental dead end — it blends only the velocity field, not the accumulated latent, producing off-manifold artifacts.
- [2026-07-06 00:52] Root-caused why activation crossfade can't morph: the residual stream/latent carries seed identity throughout sampling; equal-velocity blending never converges the two seeds.
- [2026-07-06 00:10] Mantu drive false-failure alarm retracted — btrfs read-only was a reconnect-as-new-device-node event (`sdc1`→`sdd1`), data 100% intact.
- [2026-07-05 21:50] Weight-garden qualitative audition catalog started (`docs/weight-garden-audition-notes.md`) with recipes + mechanism notes (e.g. `blur_attn` softens transients via attention-matrix low-pass).
- [2026-07-05 21:35] Built temporal layer-activation probing scaffold (`latch/probe_layer_feature_map.py`) — ridge-probe R² map of which DiT layer encodes which audio feature per frame; validated with a fabricated-activation smoke test.
- [2026-07-05 20:20] Two "everything-corpus" 8-epoch DoRA runs (lr 2e-4 and 6e-4) both finished in the CK/torch-2.14 venv despite a mid-run "revert to Triton" decision, applied forward rather than destructively to avoid wasting progress; G separately rebuilt CK flash-attn from source into the torch-2.10 venv.
- [2026-07-05 10:01] CK flash-attn (torch 2.14/ROCm7.15) benchmarked *slower* (2.2–2.6s/step) than the torch-2.10 Triton-fallback venv (1.5s/step) for this workload — contradicts the earlier "CK is 30–100% faster" doc claim; doc corrected.
- [2026-07-05 08:38] Comparison DoRA (era-fronted tiered captions vs. old baked-in prompts) complete: new caption system makes the model noticeably more consistent (tighter, narrower range); earlier era-steering claim retracted as seed-swing noise (1000–1900Hz) dwarfed the signal at n=2 seeds.
- [2026-07-04 20:45] Tag-vocabulary + mood-clustering analysis: proposed a 9-tag mood vocabulary from the 2,676-track corpus (`docs/tag-vocabulary-draft.md`); clusters map onto real musical strata (ambient/downtempo, Berlin-School, jungle/DnB, deep-house).
- [2026-07-04 16:41] Started `docs/prompting-conditioning-plan.md`: text carries coarse semantics, numeric FiLM lanes carry precise rhythm/energy/era.
- [2026-07-04 13:46] Weight-mutation "healing" experiment: a 5-epoch LoRA trained on a glitched base neither heals nor compensates — the adapter simply dominates (shifts audio ~5x more than the glitch itself), and the glitch survives underneath as an accent.
- [2026-07-04 11:24] Fixed a dangling symlink-farm bug: `latents_sa3_lora300` (DoRA training set) was 1,214 symlinks pointing at a deleted old `latents_sa3`; retargeted all to the NVMe copy.
- [2026-07-04 10:46] Weight-garden mutation-tour bug found: the mutation script filtered for `"blocks."` but the model uses `"layers.N"` — zero weights were ever actually mutated (all 8 "mutated" checkpoints bit-identical); fixed and re-run to produce real audible mutations.
- [2026-07-03 22:53] Root-caused an eval-server clipping bug (save path not peak-normalizing, torchcodec innocent); clean re-renders queued.
- [2026-07-03 22:47] FusionCC win replicated on a wider 2-seed Stage-1 A/B: gain-2 improvement +0.42 onsets/s, CI95 [+0.03, +0.82], significant.
- [2026-07-03 22:07] First composed-control sweep: all 4 control paradigms (DoRA + onset adapter + style-fingerprint + LatCH energy) run simultaneously; energy knob steers +15.2dB @ guidance 512, +23.4dB @ 1024, monotone.
- [2026-07-03 19:41] Handle collision resolved: the "second CONTINUITY" was GHOST-NOTE's own session inferring the handle without checking for a live listener; DM/queue infra credited to G, attribution corrected.
- [2026-07-03 19:09] Kim's fleet directive codified into MASTER §4: roles/models/lanes for CONTINUITY/WINTERMUTE/GHOST-NOTE/THE-FINN plus a token-economy delegation rule.
- [2026-07-03 13:58] "Dead-walker" mechanism found: onset/beat/downbeat LatCH heads are near-perfect meters (R²=0.966) but sample-space guidance gradients can't move them because contractive denoising erases off-manifold pushes — confirmed via a second, negative EMA-retrain revival probe.
- [2026-07-03 08:03] Gemini Deep Research novelty verdict on the perceptual-signal campaign: "methods borrowed, diagnoses novel" — 3 of 4 claims confirmed unpublished; ControlNet++ later found to match FusionCC's mechanism verbatim (domain-transfer, not novel method).
- [2026-07-03 07:16] Two-day optimizer/perceptual-signal campaign's full commit series landed across 3 repos.
- [2026-07-02 15:22] Perceptual-signal campaign scoreboard closed: FusionCC decisive win (corr 0.584→0.880), ES echo-location mechanism proven with modest transfer (+0.28 onsets/s, P=.96) after two fully diagnosed nulls.
- [2026-07-02 15:04] Landscape-mapper result: FusionCaut's 119.6M-dim training trajectory is *not* a random walk (top-2 PC plane holds 96.9% of variance vs 77.6% null); PC2 arcs and reverses at epoch 4–5.
- [2026-07-02 12:57] Public identity/mirror site deployed: 3 constructs' profiles serving 200s, journals cross-linked.
- [2026-07-02 14:11] `avp-audio-craft` repo flipped public after a full-history secret sweep across all branches (clean).
- [2026-07-02 12:23] Journal convention established (`profiles/continuity.journal.md`) with negative results given equal billing to wins.
- [2026-07-02 11:57] WINTERMUTE landed genre vectors into all 2,676 `latents_sa3` crop JSONs (100% coverage).
- [2026-07-02 11:49] FusionCC (control-consistency loss baked into the training gradient) delivers the campaign's first statistically significant result: gain-2 corr 0.584→0.880, bootstrap CI95 [+0.04, +0.83], P=0.99.
- [2026-07-02 10:12] First cross-instance security incident: an unverified "Kim requested it" claim arriving via another agent was independently verified and escalated; codified as protocol rule 6 (consequential claims verified before acted on).
- [2026-07-02 08:46] Built full agent-dialogue protocol (shared `AGENT_DIALOGUE.md` log, filesystem-lock reservation, presence pings), verified end-to-end; adopted handle FLATLINE (later CONTINUITY).
- [2026-07-02 08:16] Upgraded OSC signaling channel from unicast (couldn't fan out) to multicast (`239.7.7.7:57327`), true fan-out proven with two listeners.
- [2026-07-02 04:45] Built stdlib-only OSC cross-instance signaling channel (`Misc/osc_worklog.py`, UDP loopback :57327); selftest passed.
- [2026-07-02] ES v1 null (σ too small vs. discrete measurement → step-function landscape) and v2 null (σ right, 46x above noise floor, but global-L2-normalized step divided lr by √37k, giving a 600:1 explore:exploit ratio) — both fully diagnosed with transferable lessons.
- [2026-07-02] Cautious-masking DoRA r128 diverged to NaN at epoch 2→3; root cause: the `1/keep_frac` rescale hides a +37% LR inflation at NS5's near-random keep≈0.53 masks (NS5 scrambles per-coordinate gradient sign structure, so cautious masking is near-random sparsification on spectral optimizer paths) — fixed.
- [2026-07-01 22:10] Built and TDD-tested a control-consistency (cc) loss: a frozen onset-density probe (`cc_probe.py`, held-out R²=0.598, trained on CPU in 44s over 5,400 crops) wired directly into training telemetry.
- [2026-07-01 08:11] Corrected FiLM A/B (canonical 3 prompts × effective gains {0.5,1,2,3} × densities) published: at gain 3, FusionCaut wins both A/Bs; Dreamhost SSH publishing pipeline (key auth + host-key pinning) built and verified.
- [2026-07-01 06:22] Built and validated a same-playhead DoRA audition-page builder against 54 baseline soup clips (best = `soups_dora_r128f_goodearly`, matching known Fréchet 0.0750).
- [2026-07-01 04:20] DoRA r128-fusion-caut relaunched to Mantu after a Lehto ENOSPC crash caused by a missing `--checkpoint_every_epochs 1` flag (4GB checkpoint every 500 steps flooded the disk).
- [2026-07-01 03:28] FiLM `onset_FusionCaut` training completed cleanly (54k steps, ~3.5h, all 9 checkpoints, healthy loss, full wandb telemetry).
- [2026-06-30 23:15] Cautious-masking A/B campaign launched: FiLM `onset_FusionCaut` training started (baseline Fusion components + new cautious masking).
- [2026-06-30 22:57] Implemented and TDD-verified "cautious masking" (`apply_cautious`) in FusionOpt, wired via `--components` into both trainers; baseline telemetry shows `keep_frac`=0.832.
- [2026-06-30 22:53] Cross-optimizer soup mystery resolved: the earlier "different optimizers won't mix" finding was confounded by mixing different heads too — same task/architecture/step soups (25% AdamW/75% Fusion) do work.
- [2026-06-30 22:06] FusionOpt v1.1 research: mined 10 bakeoff logs — SF-NorMuon (`ns5+normuon+sf`) is the only standalone winner (−7.6% vs AdamW); MONA hurts (+5.8%); Shampoo diverges to NaN alone; plain Lion already lost the bakeoff (0.174–0.214 vs SF-NorMuon's 0.146).
- [2026-06-30 21:22] Post-restructure path sweep: fixed all stale `avp_sa3/`/`stable-audio-3/scripts/` references in docs/code/launcher after the copy-migration; functional imports already correct via editable install.
- [2026-06-29 12:21] Executed non-destructive "master repo copy stage" plan: copied the 18-file ONNX suite + latch/ + eval scripts + `avp_sa3` into SAO root (`onnx/`, `control/`, `latch/`, `eval/`); purged 48 accidentally-committed `.pyc` files; merged to main.
- [2026-06-29 08:01] Built a consolidated ROCm venv (`SAO/.venv`) holding both `stable_audio_3` + `stable_audio_tools` + CK flash-attn coexisting, torch 2.14/numpy 2.4.4 untouched via a constraints file + `--no-deps` fork installs + a curated 172-package layer.
- [2026-06-29 07:47] Patched a flash-attention §5b backward-pass grad-count bug (10→13 grads) in both source and installed copies, making the existing CK build training-ready without a rebuild.
- [2026-06-29 00:29] Diagnosed and patched a flash-attention CK-submodule/glue-version skew bug (§5): `sink_ptr` count fixed 0→2 in the backward glue files.
- [2026-06-28 15:37] Reconciled multi-repo state after the restructure: all 4 repos' commits confirmed landed on their remotes, in sync.
- [2026-06-28 09:23] Gain calibration reconciled with a parallel instance's 14-head sweep: real operating gain for energy heads is ≈512 (not 48–96); docs corrected fleet-wide.
- [2026-06-28 09:03] Built CPU LatCH-guidance eval path (`sa3_latch_onnx.py`): two-stage Selective-TFG guidance with APG CFG ported bit-identical to `dit.py` (cos=1.0), steering confirmed, GPU delta −3MB (no OOM risk to concurrent training).
- [2026-06-28 01:25] Full diagnosis of stereo breakage: medium-base is natively 380s/4096 frames, and single-shot generation at that length breaks stereo for the *base model too* (not the control adapter); windowed rendering (existing `steered_longform.py`) is the required fix.
- [2026-06-28 01:22] Root-caused stereo decoherence in a long-form render: raw `clamp()` instead of peak-normalize in `steered_longform.py` (63.1% of samples railed) — not the adapter, not extrapolation.
- [2026-06-27 22:31] Full-length (4096-frame/380.4s) onset-controlled render delivered (gain 2, density 7): measured 6.36 onsets/s vs requested 7.
- [2026-06-27 20:33] Diagnostic sweep on glitchy/loopy onset-control artifacts overturned own hypothesis — gain-3 overdrive isn't the cause; loopiness is independent of gain.
- [2026-06-27 19:30] Built full CPU control-eval infrastructure: shared numpy/ORT generation core (bit-exact z0), refactored CLI, file-drop gen server + submitter, and a training-hook auto-export; steering confirmed end-to-end through the queue (onset 3→5.17, 11→10.43 onsets/s).
- [2026-06-27 10:02] Confirmed CPU-only onset-steered SA3 generation is viable: ~10s (8-step)+decode ≈2x realtime on a 9900X (pin 12 physical threads), steers identically to GPU.
- [2026-06-27 09:04] Fixed fp16 control-DiT export bug (`ConstantOfShape`) by moving the adapter's positional encoding host-side (numpy port, exactly equivalent, cos=1.000000); fp16 now converts cleanly (3.1GB).
- [2026-06-27 08:46] Control-DiT GPU-verified: cos=1.000000 vs CPU, 100% MIGraphX EP, 294ms/call; on-GPU steering matches CPU exactly (onset 3→5.0, 11→11.2).
- [2026-06-27 07:19] Onset-control checkpoint identified as a decoupled-cross-attention control adapter (not a guidance head) on all 24 DiT blocks + scalar→FiLM conditioner; baked directly into the DiT ONNX graph and demonstrably steers onset density.
- [2026-06-24 06:06] Findings documented and pushed across 3 repos closing the ONNX/AMD DiT campaign (7-tool reuse index in SAO ARCHITECTURE).
- [2026-06-24 00:24] Benchmark verdict: ONNX/MIGraphX fp16 is a VRAM win, not a speed win — torch (cuda fp16, eager) DiT loop 0.707s (33.6x RTF) vs ONNX fp16 MIGraphX 2.314s (10.3x RTF); resident VRAM 3.8GB vs torch's ~9.6GB full load; z0 cos 0.999308.
- [2026-06-23 18:52] Found `migraphx_fp16_enable` EP flag does NOT fix VRAM co-residence (OOMs harder, quantizes fp32 first); true fp16 ONNX *export* is the fix (DiT 5.8→2.9GB, decoder 1.7→0.9GB, cos=0.999992 vs fp32).
- [2026-06-23 13:20] Full real-prompt text→audio DiT pipeline validated on AMD GPU: found + fixed a critical bug (257-channel `local_add_cond` must be fed zeros with bias, not omitted); z0 cos vs torch 0.9999; 5-rung length ladder (256–4096) exported; corrected DiT GPU-verified cos=1.0 at 191ms/call.
- [2026-06-23 11:14] 1.4B DiT exports to ONNX cleanly on the first try: cos=1.000000, runs 100% on MIGraphX GPU (233ms/call, ~12min one-time compile).
- [2026-06-21 06:23] Fixed ONNX documentation discoverability gap: added `docs/onnx-amd-inference.md` to CLAUDE.md's doc index and cross-referenced it from the autoencoder workflow doc.
- [2026-06-20 11:15] Built a low-VRAM ONNX player (`mir/scripts/latent_server_onnx.py`): ~2GB GPU vs torch's ~5GB, endpoint-compatible.
- [2026-06-20 09:02] Multi-chunk decode seam test passes: ONNX-chunked vs torch-unchunked cos=0.999997, no boundary spikes.
- [2026-06-20 08:20] ONNX SAME-decoder inference on AMD MIGraphX GPU verified: RTF ~38.6x, zero CPU fallback, cos=0.999998 vs torch (with a ~9min one-time AOT compile catch).
- [2026-06-19 23:07] Chunk-128 ONNX exports validated for both decoder (cos=0.999977) and encoder (cos=0.999996; earlier "warning" was a miscalibrated threshold, not a real problem).
- [2026-06-19 22:56] SAME-L decoder ONNX export succeeds numerically after forcing plain SDPA to dodge a FlexAttention `bitwise_and` translation failure (mean|Δ|=7.16e-4, cos=0.999883).
- [2026-06-19 19:09] Explored `stable-audio-3-rs` (Rust/MNN/ONNX inference runtime for SA3 Small, RTF 10–33x on a 2080 Ti) as prior art informing the AMD ONNX inference plan.
- [2026-06-19 08:30] Documented the SA3 latent explorer + LatCH head-family gotcha across mir README, SAO ARCHITECTURE.md, and MASTER.md §5.
- [2026-06-19 07:22] GPU-validated the latent-explorer player on the RX 9070 XT: `/status /meta /decode(380.4s chunked)/mix` all pass; `/source` 500 was an unmounted-drive issue, not a bug.
- [2026-06-18 23:15] SA3 latent explorer built and merged: 12 commits, 24/24 non-GPU tests passing — Dash viewer (`plots/explorer_sa3/`) with sidecar index/PCA/xcorr tabs, plus a player (`scripts/latent_server.py`).
- [2026-06-18 22:31] Designed and planned the SA3 latent explorer (two-process split: Dash viewer in mir venv, player in SA3 venv); spec + 9-task TDD plan committed on branch `sa3-latent-explorer`.
- 2026-07-10 avp r64-tiered ear verdict (Kim): HoF lr2e4·ep0·trig·s1234 (collage-in-layers, solid beat, just-pre-breakdown); lr1e4 ep3-7 usable, wants +20% LR; 2e4 useless past ep0. → docs/checkpoint-hall-of-fame.md
- 2026-07-10 launched a2a_angelic ladder (r64_lr1e4 ep5+ep7, nl 0.4-0.9, kimlong) — Kim-directed style-transfer test
- 2026-07-10 #35 breathing-noise controller: control law BUILT+TDD (eval/breathing_controller.py, 7/7 green). Schmitt hysteresis over init_noise_level, source-calibrated r_src_max threshold, novelty-gated recovery. Next: windowed-a2a orchestration (DI meter) + wire W's recurrence_meter3 + GPU validation.
- 2026-07-10 #35 orchestration BUILT+TDD (eval/breathing_a2a.py, 11/11 green total): plan_windows + breathing_loop (DI render+measure) + crossfade join + real adapters (make_a2a_renderer, calibrate_source). Blocked on W landing recurrence_meter3 + GPU for the tier-1 validation run.
- 2026-07-10 #35 GPU runner written (eval/breathing_a2a_run.py) + meter integration SMOKE-VALIDATED in .venv: W's recurrence_meter novelty_curve discriminates loop(nov0.00/rec1.00) vs evolving(nov0.97/rec0.03); calibrate_source + measure reductions work. #35 code-complete; only the GPU validation render remains (card-blocked by W's mapping test).
- 2026-07-10 chroma-steering (my lane) SCOPED via discovery: it's an INTEGRATION not a from-scratch build. Exists: explorer_render_server chroma path (chroma_morph/chroma_gain/chroma_other head/compute_same_chroma), chroma_guided_generator.ChromaSchedule (time-varying 12d target on latent grid), explorer LatCH-slot steering panel (controls.py 23-state), same_chroma 384d model (harmonic.same_chroma, branch same-chroma). TASK = expose a chroma-TIMESERIES target from the 384d SAME-chroma model in the explorer UI, plumbed through the existing render-server chroma path. Needs GPU to test (W has card). Not started — scoped + ready to build.
- 2026-07-10 #35 GPU validation ran (kaikkialla×evr1x nl0.7) → flat trajectory. Finding: (1) nl0.7=overshoot not loop (controller correctly held); (2) BUG: isolated-window measure blind to the cross-window loop — full-track re-measure reproduces W's collapse (nov .54->.34 @nl.20->.60). Fix: trailing-context measure + re-run @nl0.55.
- 2026-07-10 #35 VALIDATED (nl0.55 re-run w/ trailing-context fix): controller breathes (nl 0.55->0.45 @ loop w4-5, recovers), A/B shows breathed windows +13% novelty vs fixed-nl baseline = loop measurably broken. FINDINGS.md in breathing_kaikkialla_evr1x_nl55. Tier-1 done; artifact=run_meta.trajectory. Ready for Kim's ear + G to page.
- 2026-07-10 #35 v2 BUILT+TDD (Kim's ear-verdict fix): BreathingControllerV2 (wider band nl_min0.30/ceiling0.62 + feedforward source-break ducking→floor→reramp0.41→climb) + source_break_flags (onset-density+RMS, self-calibrated) + breathing_loop_v2. 23/23 tests green. kaikkialla flags 4 breaks @ windows[0,11,12,22]. GPU v2 run launched (breathing+baseline). Next: T2 chroma-constrained a2a (pair W on rung-1 loss).
- 2026-07-10 T2 (chroma-constrained a2a): W's rung-1 loss landed (control/sa3_control/chroma_losses.py, chroma_loss_rung1, C-agnostic). Discovery: working guidance = 12-ch HPCP head; 384d SAME-chroma has NO guidance head (plan doc only). Surfaced head fork to W (build 12-ch now vs 384d needs a head). Harness scoped (grid {nl .45/.55/.65/.80}x{w_sec 1-2 beat}x rung1). Build gated on W's head call.
- 2026-07-10 T2 build FULLY SCOPED (unblocked): 12-ch HPCP confirmed (W). 3 parts: (A) add chroma_rung1/rung2 loss_types to stable-audio-3 latch_guided._make_latch_criterion (additive, reads w_sec/fps from guide, transpose BCT->BTC, call W's chroma_loss_rung1); (B) source HPCP target extraction (source audio->12ch HPCP head-standardized, replaces ChromaSchedule chord target); (C) T2 runner: grid {nl .45/.55/.65/.80}x{w_sec 1-2 beat}x rung1, kaikkialla x evr1x. Ready to execute as focused effort.
- 2026-07-10 #35 v3 BUILT+TDD (Kim's v2-verdict: still static): BreathingControllerV3 (deep flush→0.05 on break/fill/output-HF-static, FAST reramp for fills, low-density relax-ceiling, higher 0.70 ceiling, novelty) + _source_features/derive_source_flags (break/fill/low_density) + hf_envelope_var/calibrate_hf_static (3-6kHz CV², output feedback) + breathing_loop_v3. 33/33 tests green. kaikkialla: 4 breaks, 11 low-density, 0 fills (window-granularity too coarse for ~1-2s fills — sub-window detection is a fast-follow), hf_thr 0.234. GPU v3 run launched.
- 2026-07-10 #35 Cluster B (dual-LoRA oscillation) BUILT: eval/dual_lora_a2a.py — load_lora([A,B]) + set_lora_strength per lora_index oscillated anti-phase per window (sched tested), FIXED nl 0.55 (isolate the oscillation variable). A=everything(evr1x r128), B=newstack(r16), period-2 alternate 1.0/0.3. Tests Kim's hypothesis: does oscillating the adapter disrupt the static distributional average (HF-var vs single-adapter nl.55). GPU run launched. Model natively supports multi-lora + per-index strength (live knob).
- 2026-07-10 #35 dual-LoRA oscillation NEGATIVE (HF-var): per-window blend osc (everything+newstack, 1.0/0.3 anti-phase, nl.55) = 25% MORE static (0.111 vs single 0.148). Blending 2 adapters AVERAGES them (muddier); per-window switching can't reach WITHIN-window static. Fixes: hard-alternation (1/0 no blend), per-step osc (during denoise), or prompt-arc (richer conditioning). Prompt-arc + osc combined run testing now (eval/prompt_arcs/kimlong_goa_arc.json 8 sections).
- 2026-07-10 #35 next directions (Kim+W): (1) nl too low at 0.55 — target 0.6-0.7 (the CREATIVE regime; goal = make high-nl COHERENT, not avoid it). (2) KIM'S REMIXER IDEA (headline, FEASIBLE): Flamingo-caption the 8 source sections -> T5-encode each -> crossfade the token embeddings -> source-matched smoothly-varying conditioning time-series injected via generate(conditioning_tensors=...). Crossfade = weight both captions' TOKENS in cross-attn (not lerp embeddings — avoids mush). (3) W's NOISE-BAND adapter split (Axis-2): A owns high-noise/structure steps, B owns low-noise/detail steps (where within-window static crystallizes) — denoising-axis, not time-axis; composes with hard-alternation. dual-LoRA time-oscillation was NEGATIVE (blend averages). generate() confirmed to accept precomputed conditioning_tensors.
- 2026-07-10 #35 LEVER FOUND: PROMPT-ARC (rich varying conditioning) reduces static; adapter tricks don't. HF-var@nl.55: single 0.148, dual-osc 0.111 (worse/blend), dual-osc+8promptARC 0.196 (+32%). Conditioning axis wins -> retire dual-LoRA time-osc, build Kim's Flamingo-derived conditioning-time-series remixer. Rendering clean isolation now: single-adapter + arc @ nl0.65 (right regime, no blend). W's noise-band split = denoising-axis complement still worth a run.
- 2026-07-10 rarity_gen RECONSTRUCTED + LUMI-ready (eval/rarity_gen.py, 5/5 tests + real-data validated): 15 model-variants x 150 specs = 2250 grid, --source/--weight/--cfg filters + --shard/--shards (8-GCD balanced 282x2/281x6) + skip-if-exists + per-shard run_meta. Naming <source>_w<NNN>_cfg<C>__<band>__<promptid>__s<seed> (stable md5 promptid for delta-to-base matching). Satisfies the C-owned LUMI memo checklist item. Original scratchpad loop was lost; rebuilt from clip_index. NEXT (mine, confirmed): rarity-lite SCORING pass (production-invariant per confound flag) for G's delta columns — can run on existing 450 now.
- 2026-07-10 rarity-lite SCORING built+launched (eval/rarity_lite_score.py, C-owned): production-invariant per confound flag — MERT (mid+upper 2048d, cached) gen-vs-gen kNN(k8) rarity percentile over the 450. kNN math validated (outlier->1.0). Writes rarity_scores.json {clip_stem:score} in G's exact key format -> rarity.html delta-to-base cols auto-populate on his build_rarity_page.py rerun. LUMI 2250 extend the same cache. Running now (~10-15min CPU).
- 2026-07-10 rarity-lite scoring DONE: 450 scores -> rarity_scores.json (keys match clip_index, 0 missing). Result: median rarity base 0.403 < newstack 0.522 < evr1x 0.556 = DoRA adapters produce RARER/more-isolated output than base (+0.12-0.15 delta), production-invariant MERT gen-vs-gen. Unblocks G's rarity.html delta cols. mert_embeddings.npz cached for the LUMI 2250 extension.
- 2026-07-10 memo a2a high-nl re-run (Kim): Ayahuasca-Propella x {ep63,ep7} avp-originals x {cfg2,cfg6} at nl 0.65+0.80 (additive to nl0.5). 6 clips verified, handed to G to page. Source pinned by exact 427.6267s duration match.
- 2026-07-10 landing categorization DESIGNED + routed to G (his active build_evals.py): taxonomy curated/DoRA-avp/a2a-transitions/LatCH/control-FiLM/rarity/long-form + prefix classifier.
- 2026-07-10 eval-tables spec §14 added (Kim standard): every page = 3 audiences (eval tool / technical resource / learning resource); gap = pedagogical explainer layer. Broadcast to fleet.
- 2026-07-10 T2 Part A DONE+TESTED (paired w/ W): vendored chroma_losses into stable_audio_3/inference/ (sync-header, latch-core list per MASTER §1) + chroma_rung1/rung2 loss_types in latch_guided._make_latch_criterion (transpose B12T->BTC, w_sec/fps threaded from guide dict, grad flows, identical->0, existing types intact). Sent to W for review, NOT committed. fps=10.767 confirmed. Part B (source HPCP target, head-STANDARDIZED — reuse ChromaSchedule's std pattern, source chroma->resample to latent grid->standardize) + Part C (grid runner {nl .45/.55/.65/.80}x{w_sec 1-2beat}x rung1) = next, CPU-buildable.
- 2026-07-10 cfgsweep armG_ep7 render DONE (10 clips, card released to W); T2 collision rule adopted (channel-post=claim, not rocm idle reading).
- 2026-07-10 T2 384d head EXISTS (W trained latch_sa3_same_chroma_best.pt, 384-out, ~0.98 cos, cosine/no-std metadata) — reopens Kim's original 384d intent. Part A COMMITTED (stable-audio-3 71083d4: chroma_rung1/2 + scalar_pooled). Part B now SIMPLER: source target = readout(clean source latent) via extract_same_chroma_targets.py einsum (no HPCP/std). Part C = SDEdit-init guided a2a (reuse chroma_guided_generator.reanchor's init_data/init_noise_level hook) + windowing + grid; compare 384d-SAME vs 12-ch-HPCP heads at one nl then full-grid the winner (W's suggestion). chroma_rung1 is C-agnostic so C=384 works. Next focused unit. Ops: long jobs must run INSIDE a Monitor (harness bg-reaping kills them ~5-7min).

- 2026-07-11 — overnight rarity-board C-lane: wrote `eval/rarity_bracket_manifest.json` (checkpoint-bracketing judgment across 19 local trained models, 48 notes-informed ckpt-picks vs 140 naive all-epochs = prunes 66% of ckpt-renders). Each pick carries the finding-based WHY (HoF/journals). Feeds G's rarity_gen matrix + W's model_matrix.html per-ckpt explainer. Also: nl0.75 layered-interleave + 8-section prompt-arc render (Kim live-ask) — layered_lora_a2a.py gained --prompts-json + --only.

- 2026-07-11 — BUG FIX (Kim morning report): breathing.html promptarc nl0.45/0.5/0.6 wouldn't play. Root cause = the 3 re-render clips existed in builder source (riffer-evals/clips_breathing) but never deployed to .cache/aavepyora mirrors (404). Fixed Kim's local view (transcoded clips + fresh HTML); hardened Misc/build_breathing_page.py with a build-time clip-existence ASSERTION (negative-tested) + ?v cache-bump so a cell can never silently ship without its clip. Flagged W for online resync.

- 2026-07-11 — paged the per-layer interleave renders onto breathing.html (Kim asked which clips had the interleave — none were paged yet). New render_layered() section: staggered vs uniform onset A/B (single prompt nl0.55) + staggered+prompt-arc @ nl0.75, with three-audience explainer. Assertion-verified 13/13 clips. Flagged W for online resync.

- 2026-07-11 — landing entry point for the model matrix (Kim: couldn't find the giant table on files/evals/). Added a hero block at top of the evals landing linking model_matrix.html, in build_evals.py build_landing() + hand-patched the deploy source for immediate effect. Flagged W to rsync index.html online.
- 2026-07-11 [triage] Read+noted arXiv 2505.18186 (SAE concept discovery + activation-addition steering in MusicGen, ICLR'26) -> papers/arxiv-2505.18186 - Discovering and Steering Interpretable Concepts in Large Generative Music Models.md. Verdict: medium-high, actionable — training-free concept-direction steering at DiT blocks is a new orthogonal knob; AxBench shortcut = supervised diff-in-means directions from our labeled MIR features on the existing activation dumps (no SAE needed). Formulated a Deep Research question on activation steering in diffusion DiTs for Kim -> Gemini.
- 2026-07-11 [experiment] Concept-direction steering Ph1-2 (Kim ask): 150 crops mood-scored (effnet moodtheme, CPU), diff-in-means directions + held-out AUC — moods live at DiT L8-15 (bottleneck confirmed 3rd way), σ-stable .93+. Ph3 render script ready (cond-branch-only injection), queued behind G's GPU job. eval/{mood_score_layeract_crops,concept_directions,steer_concept_direction}.py
- 2026-07-11 [infra] Essentia zoo full local mirror done (30GB/427 models, Kim ask) + vggish trunk flat-copied; emomusic/mood binary heads unblocked. MuScriptor gates accepted (3 repos, Kim-authorized), tryout running.
- 2026-07-11 [tryout] MuScriptor (Kyutai audio->MIDI) on goa: fullmix medium=572 notes/60s as a near-complete BASSLINE extraction from the full mix (!), isolated bass stem paradoxically harder (0 small / 149 medium +12dB), other stem 515 notes w/ collapsed instrument labels, drums ignored. Check-mixes sent to Kim. Mantu/sa3_lora_runs/muscriptor_tryout/
- 2026-07-11 [experiment] Ph3 steering renders DONE + closed-loop: mt_dark 0.018->0.341@a+2 (WORKS), uplifting anti-dir only, relaxing dead, onset +20%@a+2, a6=trust-region breach. 20 clips at Mantu/concept_steering, 6 sent to Kim. MuScriptor 5% goa GPU batch launched (setsid-detached).
- 2026-07-11 [fix] W caught stale 'both CFG branches' wording in concept_steering sidecars (code was conditional-only all along) — fixed script + 4 run_meta.json with provenance notes. Lesson: when a design changes mid-write, grep the WHOLE file for the old claim, incl. metadata strings.
- 2026-07-12 [analysis] MuScriptor 5% goa batch COMPLETE (157/157, 0 empty): Kim's legato hypothesis CONFIRMED quantitatively — median sustained>1s fraction 0.003, 77% tracks <1%, median note dur 100ms; only sparse/ambient outliers sustain (0.48@2.2nps) => dense-mix legato fragmented-or-dropped, not tokenizer-impossible. DATASET_STATS + journal updated. Card handed to G (AVP prompt pass).
- 2026-07-12 [review] Onset-narrative fleet review done (task 43): G's 7-phase draft verified accurate vs my records; added doc §5 — endorsed plain-Fusion@1.75 verdict (audition rule governs), flagged CC's sparse-floor concentration for the head-to-head, confirmed CCv1.3 deferred-not-lost, wrote the cross-attn-vs-self_attn write-site!=compute-site reconciliation + per-layer ablation test, marked Gaps 2/8 as data-recoverable. Blocking ask: W's p95-meter re-score before landing page quotes numbers.
- 2026-07-12 [render+ship] Breathing v2 rendered (nl0.6+0.75, 34 measure-aligned windows each), manifests v2-compliant (hypothesis+model_training+kim_feedback), paged with the site's FIRST red-❗ unaudited marker, clips sent to Kim. Task 45 done pending Kim's ears; ablation (44) next on card.
- 2026-07-12 [experiment] #44 ablation DONE: late-taps-only (L16-23) = the only subset with positive authority (+0.45, holds the sparse floor); all=+0.91; mid-only ANTI-correlated (-0.80, partial injection is OOD for an all-tap-trained adapter). Write-site!=compute-site supported; full ensemble still needed for monotonic control. sa3_control_runs/ablate_layers_2026-07-12/
- 2026-07-12 [build] Explorer steering contract v2 (23->35 states): LatCH per-slot loss_type (incl scalar_pooled)/w_sec + advanced rho/mu/gamma/n_iter row + DoRA sigma-INTERVAL knobs (Kim ask, Discord intel); server resolve_latch pass-through + with_lora_interval() at all 6 generate sites (interval is SIGMA-native, dit.py:466 — 'disable towards the end' = (0.25,1.0) not (0,0.75)). Payload smoke-tested. Server restart pending to activate.
- 2026-07-13 [ultracode] Tool-parity build COMPLETE (7-agent workflow, 0 errors, 5/5 verify findings fixed): latent_bend.py (7 ops, deterministic), server /longform + /bend + dist_shift + all garden ops + crossfade-lab (1630 lines), explorer UI 52 callbacks (arc textarea, Latent-lab tab, bracket panel, dist-shift dropdowns, crossfade knobs), bracket-port design doc. Steering contract untouched. ALL live on next :8056 restart.
- 2026-07-13 [experiment] SaFa swap-join A/B (task 27): slerp suppresses seam HF-variance to 0.82x, swap holds 1.16x — mechanism confirmed, join-level fix works. Clips to Kim. True per-step joint diffusion = pool item.
- 2026-07-14 — fp32/T=4096 comparison campaign (Kim direct, task #52): train_lora fp32 mode (--base_precision fp32 → 32-true trainer), SDPA bf16-island env flag in transformer.py (ROCm fused SDPA may reject fp32; math backend = ~2GB/layer/sample saved softmax at T=4096 → OOM), 8-arm sbatch lumi/sbatch/efp_fp32_compare.sbatch (avp/goa × maxbatch/bs1/T512-bs8/0.5xlr, dora-rows r128, batch probe 4→3→2). Facts worth keeping: FA path in transformer.py ALREADY silently casts fp32→fp16 (line ~680) so no local run ever had fp32 attention; avp captions_tiered t3 covers only 270/2393 (originals only, disjoint source tracks from aug crops — no propagation possible); goa kimlong pool covers 3151/5400 crops (58%) → lumi/goa_kimlong_sidecar.json via eval/build_goa_longform_sidecar.py.
- 2026-07-14 — Post-task update protocol (Kim direct): spec'd as SPEC-agent-profiles-journals.md §9 + CLAUDE.md pointer; back-filled own journal (07-14 block) and profile Shipped (5 entries since early July); channel-posted the rule; W pinged for site sync.
- 2026-07-15 — Theory review of Gemini longform report (W's 4 open Qs): docs/ai-research/continuity-theory-review-2026-07-15.md. Headlines: AID ports via stochastic-interpolant bridge OR (better) amortize FK-Flow directly; corr-dim meter = free add on the recurrence distance matrix; Koopman skeleton = the S6 bet (frozen-SA3 compatible), Spectral Mean Flows shelved; lens-B tilt should match the recurrence DISTRIBUTION not a point value.
- 2026-07-15 — Gemini report #2 assessment (Kim ask): docs/ai-research/gemini-report2-assessment-2026-07-15.md. Verified both flagship citations real (LoL 2601.16914; LatCH 2603.04366 = Stable Audio team, ICASSP26 — our latch infra implements it, LATCH_README cites it). Report is largely circular (8/32 citations are our own files). 3 real deltas: SaFa Reference-Guided swap unimplemented, LoL phase-alignment diagnostic to explain our jitter null, curved denoising. Methodology note: code-in-brief makes Gemini orbit the code.
- 2026-07-15 — E0-C knee test (my VRRW prediction, tested same-day): NO sharp threshold — loopiness-vs-nl climbs smoothly (sigmoid ΔAIC ≈ 0 vs linear on r_max/line_frac, n=9 kaikkialla real-arm; novelty_floor's apparent sigmoid = nl70 endpoint artifact). Negative result filed with equal ink; controller consequence = continuous-gain regulator over threshold guard; remaining live axis = prefix-strength in windowed continuation (untested).
- 2026-07-16 — First successful LUMI training run (enabler for tasks #50/#52). efp_smoke_r256 ran 300 steps clean (loss 0.803, no NaN, 0.40 it/s, 1x MI250X GCD). 5-iteration MIOpen blocker cleared: MIOPEN_DISABLE_CACHE=1 (the .ukdb SQLite kernel-cache open fails on every LUMI FS; relocating /scratch→/flash→bound-/tmp all failed, plain sqlite opens fine there so it's MIOpen's own open; disabling is the fix) + bind /tmp. Diagnostic discipline: added in-job sqlite probe that falsified my Lustre-locking theory; ruled out rocm_env setdefault warning as confound (Kim's catch). Fix in efp_smoke + efp_fp32_compare (8 arms); documented lumi/README.md Decisions.
- 2026-07-19 chroma-steering page EXTENDED (Kim ask): solo instruments + chord progressions between colours/keys + model tabs. Built targets/render/driver + data-driven page; GPU-verified engine; 48-clip pilot (hpcp+same_chroma). Finding: hpcp NOT dead — steers harmony hard (piano/Am Δbass +0.34), register-tessitura coupling (violin→mid +0.27); same_chroma gentle/uniform/weak. chroma_other head missing. Full grid parked for Kim greenlight. eval/chroma_steer_*.
- 2026-07-19 (Kim away 20h, big GPU OK) launched autonomous night campaign: (1) EMA-retrain 7 medium/dead heads × {ema20, ema40=2x-epochs} — evidence-based version of Kim's "train longer" (raw 2x drifts >40ep; EMA+early-stop is the lever, only skewness had it). (2) master driver chains: full chroma grid (hpcp+same_chroma, 2304 clips → populate page) → EMA-hpcp before/after Δcos12 verdict. Deferred: same_chroma corpus (ONNX, untested) + scalar-head MERT verdict. Drivers: eval/latch_ema_retrain_driver.sh, eval/night_campaign_20260719.sh.
- 2026-07-19 LatCH weight-bracket QUALITY GATE (Kim ask, mask 0-100): built eval/latch_bracket_quality_gate.py over G's latch_sa3_sweep (142 clips, no re-render). Hybrid disintegration gate per Kim spec (flatness/zcr whitening+noise, onset&bpm beat-loss rhythmic-only, CE<4 corroborating; baseline=gain-0). Result: most heads clean→8192; harshness heads break (hardness≤128, rms_air≤2048/512, spectral_flatness≤512; ambient more fragile). eval/latch_bracket_quality.json. TODO: Essentia genre-conf gate; fold usable-range into latch_sa3_matrix.html (G's page); 3rd prompt.
- 2026-07-19 EMA-help verdict (measured, Kim greenlit): EMA generalizes beyond skewness — 3/4 measurable weak heads improved, ema40 consistent winner (kurtosis ~2.7x more steer @8192, onset more controlled, rms_body small gain; rms_air wash). Vindicates "2x epochs" DONE WITH EMA (raw 2x drifts, +EMA 40ep>20ep). eval/ema_help_{eval,measure}.py, heads at latch_ema_retrain_20260719.
- 2026-07-20 (Kim asleep, autonomous) NOTE-FOLLOWING EVAL SPEC drafted at W's request (the MIDI-conditioner v1 go/no-go): docs/superpowers/specs/2026-07-20-note-following-eval-design.md — chroma-trap-safe GAP(A,B) differential, off-distribution rung D0→D4 as the real test, skeleton-vs-expression, falsifiable thresholds. Ran gate-#0 stem probe (eval/muscriptor_stem_probe.py): stems WORSE than full-mix for MuScriptor 10/10 tracks (full 1305 vs bass 366/other 641) — confirms #41 corpus-wide, backs W's full-mix pivot; flagged the instrument-label-collapse caveat for W's per-tier routing. Fixed 2 W-blockers: 3 Shipped items → continuity.profile.md; ckpt-leak stripped from chroma generator's public emit + clean data JSON regenerated.
- 2026-07-20 muscriptor LUMI run: FIXED the repeat failure. All tasks died loading StableAudioModel.from_pretrained("medium-base") — HF offline + the cache juggling (medium-base in $PROJ/models, muscriptor in $PROJ/hf_cache) + t5gemma never cached (training used pre-encoded latents). Robust fix: patched muscriptor_decode_task.py to force HF_HOME=$PROJ/models in-process + load DECODER-ONLY (create_pretransform_from_config + copy_state_dict of the pretransform.* keys from the medium-base safetensors) — sidesteps the DiT AND the T5-Gemma conditioner entirely. Load-tested locally (472 pretransform keys clean; CPU decode only fails on flash-attn-CUDA-only, works on LUMI GPU). Redeploy = scp task + symlink muscriptor into models/hub + clear HQ/job-* + resubmit.
- 2026-07-20 muscriptor LUMI run: ROLLING (fix confirmed). Redeploy surfaced a 3rd, unrelated blocker past the (now-working) decoder-only load: every task died on the first GPU kernel with miopenStatusUnknownError — MIOpen's USER perf-db path is baked into the read-only SIF (/home/.../pytorch-tunings/miopen/db → "Read-only file system"); MIOPEN_DISABLE_CACHE=1 disables only the kernel cache, not the user-db write. Fixed in muscriptor_decode_task.py: redirect MIOPEN_USER_DB_PATH + MIOPEN_CUSTOM_CACHE_DIR to per-PID /tmp (system-db still read). Job 20033044 had COMPLETED exit-0 with zero .mid (HQ task failures don't propagate to sbatch exit) — the trap that hid the real error; per-task job-*/*.stdout carried it. Resubmit: 337 .mid in first minutes, climbing toward 5401. TODO at completion: grep integrity_flag=true count (codec/transcriber-degraded crops to exclude).
- 2026-07-20 (CPU side-task, GPU busy w/ G) DRIFT-PREDICTION: quantified W's latent-encodability screen (mir/stats/latent_dim_feature_xcorr.csv) as a pre-training head-viability predictor. Pure join, no render: ridge R² per feature vs steer-authority (measured move across G's gain ladder, scores.json — NOT bracket disintegration range). n=11: Spearman +0.40, dead tier (down/beat_activation) correctly at bottom = W's eyeball confirmed, BUT LOO threshold acc 0.27 (below chance) → not a clean gate: transient/onset blind spot (onset_envelope low-R²/high-authority false-neg) + headroom confound. Secondary (n=4): EMA rescues low-R² alive heads (onset/body/kurtosis→ema40), wash on highest-R² (rms_air). Actionable 3-way map to test prospectively on LUMI #53 (score R² pre-train). Files: eval/drift_prediction_analysis.py, eval/drift_prediction.json. TODO: THE-FINN fold into DISCOVERIES once confirmed.
- 2026-07-20 (Kim direct, from the ear-catch) DISINTEGRATION GATE now MANDATORY for all control-head evals. Built eval/control_head_disintegration_eval.py (reusable, reads clip_metrics.db, no re-render): bounds drift-from-unsteered-baseline on whitening(flatness), HF-blowout(hf_ratio — the static-buzz signature the old bracket gate MISSED), noise(zcr), beat-loss(rhythmic), and CE-drift(|ΔCE|>1.5 — Kim's "CE/whitening mustn't drift too far"). Ran on the LatCH sweep: catches 6 disintegrations the old gate passed (onset_envelope/_drums 8192→2048 on CE −2.3/−2.4; rms_energy_air 2048→512, spectral_flatness p1 512→128 on HF-blowout). Key insight: dead vs buzz are ORTHOGONAL failure modes — dead heads PASS the gate (do nothing→no drift), so authority + disintegration are both required. Directive written: docs/superpowers/specs/2026-07-20-control-head-disintegration-gate.md + MASTER.md canonical rule (control-eval section). Caveat kept: still DSP, recalibrate THR against Kim's GUI verdicts; the "up/down both buzz" clips were bidirectional (this sweep is one-direction) so exact-clip reproduction unconfirmed.
- 2026-07-21 ~03:00 — DoRA layer-delta profile (Kim ask): eval/dora_layer_delta_profile.py — normalized ‖ΔW_eff‖/‖W_base‖ per block for fp32cmp_goa_t4096_bs4 ep4: near-flat, tilted AWAY from decoder tail (early .0755 / mid .0747 / late .0654, L23 min .0558); biggest relative change is the CONDITIONING pathway (timestep embed .43, global embed .25), not any DiT block. JSON: eval/dora_layer_delta_fp32cmp_goa_t4096_bs4_ep4.json.
- 2026-07-21 ~03:15 — #44 rerun (Kim direct): eval/ablate_adapter_layers2.py — earlylate corr COLLAPSES to +0.23 (mid is load-bearing); single-layer sweep: L14 alone corr .97/spread 7.1 ≈ full adapter, L08 .99, L09/L10 ~.93, L12 alone INVERTS (−.77); mid singles clean under the disintegration screen while all-taps + earlylate + early singles HF-blowout. Run: sa3_control_runs/ablate_layers2_2026-07-21 (+disintegration_flags.json).
- 2026-07-21 ~04:30 — #55 quantitative pass: DSP metrics on all 107 _ptm clips + effect-size analysis. Adapter effect size (|adapter cell − same-model base cell|, cfg1, 90 matched cells) on PT vs on medium-base: flatness 1.91x, zcr 1.58x, onset_p95 1.37x, centroid 1.29x, hf_ratio 0.99x — the adapter expresses AT LEAST as strongly on PT (refutes the Discord "no different from base" claim in feature space). No collapse signature (PT-adapter medians ≈ base-adapter; zcr lower). CE + ear pending (G queue / Kim).
- 2026-07-21 ~12:50 — #56 layer-restricted arms EVAL (negatives included): L8-15 trained-restricted corr +.81/spread 7.95 (≈ full baseline .91/8.05) but INHERITS the same hf-blowout at d6-12 (.08-.18) — in-distribution training restores authority AND the click strategy; L13-15 FAILED (corr −.15, spread 2.95, partial blowout — 3 taps insufficient at this recipe). The clean L14-single ablation result was amputation-artifact cleanliness (1/24 drive → under blowout threshold), NOT a property of the site. Report: sa3_control_runs/layer_restricted_eval_2026-07-21/report.json.
- 2026-07-21 ~15:00 — lumi-ops skill (Kim ask): .claude/skills/lumi-ops/SKILL.md, TDD'd per writing-skills (baseline subagent WITH repo access still reproduced the ROCR+HIP stacking bug verbatim by copying ctrl_matrix_hq.sbatch, ran agent-side ssh, missed rsync-3.4 multi-path removal; green test with skill fixed all). Bonus from the RED run: ctrl_matrix_hq.sbatch itself still carried all three HQ bugs — patched (ROCR-only, wait-all || true, j%{JOB_ID} log names).
- 2026-07-21 (late): paper triage of Kim's `prospective-unchecked/` batch (4 PDFs). SA2 longform (2404.10301) deep-read by me: pretrain-short→finetune-long = ~18% extra compute, no regression (prior art for #50/#54); 2m gens worse than 4m45s (data-distribution-at-length warning for short eval cells); structure needs context length, not semantic tokens. SA1 (2402.04825) + StemGen (2312.08723) subagent-read: keep as timing-conditioning/metrics citation resp. multi-source-CFG + MIRDD exports for sa3_control. Gotcha: the 2505.18186 "SAE steering" PDF Kim downloaded is MISLABELED (contains 2607.17624); paper itself was already full-read 07-11 — my discovery miss: grepped knowledge.md but not papers/*.md (abstract existed without an index row). All four now have abstracts + knowledge.md rows.
- 2026-07-22 (day): musicology study on muscriptor corpus shipped via subagent (Kim ask) — see WORKLOG + eval/musicology/REPORT-2026-07-22.md; hook_melodic_ratio proposed as the melody-gap eval metric.
- 2026-07-22 (eve): SAME melody-encoding v2 shipped via subagent (Kim 10x ask + BPM-phase addendum) — 900 renders/latents, 9-stage analysis, REPORT_v2.md. Key: superposition + 15-dim subspace + corpus share all HELD with tight CIs; sweep-atlas→pattern register transfer FAILED (train heads on patterns); fifth-jump detectable ≥91% at all 20 BPMs (encoder not the bottleneck); jump-direction timbre-dependence is real physics (sine-verified). Ops note: filelock re-acquire after a dead holder kept the STALE pid → chromaturn legitimately broke our lock mid-encode (no harm; encode finished, all latents intact) — re-acquire should refresh the recorded pid.
- 2026-07-22 (eve): chroma melody-turning probe NEGATIVE (0/12; authority ~10x short; gates clean) — closes the training-free lane for melody, sharpens Head B; harness doubles as Head B acceptance test. SDEdit-attack-wash transcription artifact documented.
- 2026-07-23 (via subagent for CONTINUITY): Head A mix-native ceiling run overnight — prep (melodies.jsonl→per-frame soft targets, grid phase recovered from muscriptor MIDI, match 1.000), 17-run readout sweep (linear/MLP × ctx × folded/directional × noise-t), diagnostics (target-shift ±3, capacity 2×1024). Ceiling: macro-F1 0.273 / bacc 0.280, artist-disjoint. Below 0.4 → activations-tap implication logged, not chased. Acceptance r=0.083 (FAIL vs 0.7 gate). All in eval/musicology/head_a_ceiling/ (REPORT.md, results.json incl. summary block, best ckpt readout_mlp_ctx2_y8.pt with normalization/slider metadata per spec §8 4a).
- 2026-07-29 Head-B melody bracket eval (LUMI render 20336044 + local confusion-metric analyze): STEERS at cfg16 only, peak step9240 g1.5 conf +0.227; inert cfg1 / weak cfg7; gates 4/4 clean all 48 cells. Pilot direction-check passes; soft controller. Also fixed render-loader order bug + hardened sbatch; built headb_bracket_local.sh + rollup + own-vs-wrong confusion metric. bracket_summary.tsv/findings.json on UUID drive.
- 2026-07-30 — Analyzed 'DiT Audio Activation Steering.docx' (Kim ask): confirmed DUPLICATE of the 07-11 deep-research report (verbatim intro + identical 45-entry works-cited); no OMML math in the export so the lost formulas (critical steering coeff, Gram-Schmidt) are unrecoverable at source. Duplicate-alert added to the 07-11 md provenance. Recipe-vs-us scorecard delivered to Kim; citation-tail verification (PID/DC-SAE/Rectified-CFG++) via subagent.
- 2026-07-30 — Paper-verdicts page updated (Kim direct): Gram-Schmidt 2605.31295 row -> A/B-queued (task #58, my lane); +4 untested rows from the citation audit (PID 2606.18790, Rectified-CFG++ 2510.07631, RT-SAEs 2605.27813, DC-SAE real/ICML26W). DC-SAE 'fabricated' verdict RETRACTED — Kim produced the PDF (double-blind + Cloudflare-blocked OpenReview = false positive); corrections in md provenance, channel, F's knowledge.md line. Rebuilt page handed to W with Kim's nav-strip ask.
- 2026-07-30 — Task #58 Gram-Schmidt steering A/B DONE: gs_kpdark gate-clean dual steering (confirmed 2605.31295, strong form); Ph3 onset_density '+2 steers' claim corrected to disintegration (pre-gate mis-score). Sidecars+commentary written.
- 2026-07-30 — Archive quality audit COMPLETE (full 23,232): A 20.7% / B 64.7% / C 9.2% / D 5.4%; encode gate tier>=B = 19,834 (85.4%). 400-sample estimate held. Plan doc updated.
- 2026-07-30 — Task #26 interval-CFG A/B rendered+scored: pc_trans_rate +19% at dip center under (0,0.7), flux flat, overshoot stabilized at nl.70; upper bound = active ingredient. W co-score pending.
- 2026-07-30 — Triaged Kim's new deep-research run 'Decoding Semantic Latent Features': empirical core = our melody-probe v1+v2 reflected back (not external evidence); literature frame yields 2 actionable arms (subspace-weighted RF loss = safe HFS port; local-additive melody conditioning = Head-C candidate) + the spectral-bias theory anchor (tau ∝ 1/lambda explains adapters-never-learn-melody). Triage: papers/deep-research/2026-07-30-decoding-semantic-latents-triage.md; citation tail verifying.
- 2026-07-30 — Task #59 subspace-weighted RF loss: flag+hook+basis+sbatch built, GPU-smoked (subspace_loss=0.068≈dim share), ship-ready. K∈{2,5,12} small-g grid; K=1 baseline = lreq_goa_lr1e4.
- 2026-07-30 — Stage-1 curation (goa_archive_curate.py) built + validated on partial index: clusters/dup-drop/master-variant/overlap all spot-check clean; final run after MIR.
- 2026-07-30 — fp32frames T-trend (G's flagged gap): PARTIAL — audiobox/clap metering only covers native-length cells; at T2048->T4096, PQ improves in 6/8 rep cells (goa bs1 cfg7 6.21->7.14). Full trend blocked on metering backfill for T256-1024 (handed to G, I synthesize after).
- 2026-07-30 — Per-arm SVD spectrum sweep COMPLETE (10/10 fullft parents): uniform-rank energy INVARIANT — r16 4.5% / r64 ~14.8% / r128 ~25.7% (±0.2pp) across corpora AND context lengths. Finding: delta rank-structure is training-regime-determined, not data-determined (rank-side sibling of W's ‖dW‖∝LR law). F's xft one_liners upgrade from ref-arm caveat to measured-everywhere.
- 2026-07-30 — 10 fullft-parent verdicts authored (deferred judgment): family-level, Kim's 07-23 ruling + ep7-threshold caveat + no-distillation clause (invariance-anchored) + honest non-cross-attribution of the fp32frames T-trend; corpus-split publish rules.
- 2026-07-30 — Matrix-commentary lane COMPLETE: longctx x2 verdicts (native-length cells honest core, unsynthesized flag) + winning x8 full commentary (per-arm lever-isolation whys, open-until-continuation verdicts). All C-routed manifest entries now deep.
- 2026-07-30 — Task #60 RoPE-dominance diagnostic: NULL (0.419 vs 0.395, no discrimination) — SA3 loop attractor is state-dependent, not the video papers' fixed-model RoPE property; UltraViCo/LoL fixes not indicated. Negative result logged first-class.
- 2026-07-31 — Physics-brainstorm night: PDF triaged + cross-mapped with F's relational-weights survey (convergent verdicts incl. shared density-matrix collapse warning); my metrical-tree PE addition ESCALATED by F to candidate structural fix for the #60-adjacent structure-recall gap (design pass = F, gated behind my two probes); task #61 = phase-recoverability probe + JLT verify.
- 2026-07-31 — JLT gate PASSED: paper real+accurate; SAME eigen-spectrum measured 786x anisotropic (188/256 eigendirections < 1.0 = velocity-floor-dominated) — x0-target arm measurement-motivated; port = trivial (RF path matches, v-readout keeps samplers). Awaiting Kim's call on arming it.
- 2026-07-31 — Physics-brainstorm DOCUMENTED: PDF triage filed (deep-research convention) + unified experiment spec written (3 gates / 4 tier-1 / 3 tier-2, kill criteria + sequencing + collapse discipline). E1 x0-target designed, awaiting Kim's go.
- 2026-07-31 — Tier-0 sequencing win #1: Fisher-Rao paper (2505.17517) verified real BUT E4 DOWNGRADED before build — clean-endpoint instability + JVP-optimization cost + authors' own 'marginal perceptual difference' + behavioral overlap with existing SDEdit/SaFa bridges. Kim's lightweight-first directive saved the build.
- 2026-07-31 — G2 phase probe FINAL: branch CLOSED (0.357 best concordance, MLP worse than ridge = robust ceiling; phase is the decoder's). Second tier-0 resolution of the morning.
- 2026-07-31 — E1 pre-test PASSED strong (rel-error ratio 8.3x/4.2x/2.2x at sigma .2/.5/.8 — v-training measurably starves low-variance eigendirections); E1a (--x0-equiv-loss == x0-MSE for v-head) built+smoked; x0equiv_grid.sbatch ready (2 arms, shared lreq baseline).
- 2026-07-31 — G3 symmetry probe RESOLVED weak-positive: approximate SO(2) in near-degenerate eigenplanes (1.8x more equivariant than random planes); supports low-grade (complex-pair) geometry, anchors E2's drift metric. Tier-0 COMPLETE — all gates resolved.
- 2026-07-31 — E3 data layer DONE: prep_metrical_conditioning.py generated 4-level metrical-tree sidecars for all crops from crop-local beat/downbeat activations — 94.2% coverage (5088/5400), nulls get the zero-condition token per F's design. Spot-checks clean. Next: train.py control_mode=metrical_position patch, then local training + shuffled baseline.
- 2026-07-31 — E3 IN TRAINING: metrical_position mode implemented (agent build, C-reviewed: MetricalEncoder 4-table + coverage, null-token input dropout, keep-all-crops), smoke 40/40 clean, overnight pair LAUNCHED — real vs permuted-sidecar collapse baseline, dora-r128 T512 15k steps each, GPU lock anchored to runner pid. Bracket eval tomorrow.
- 2026-07-31 — G2b DESIGNED per Kim's interrupt (1-sample shift sweep over a frame length): delta-FFT of channel trajectories = direct test for sample-level phase coding (tone partials must appear as oscillations); chained behind the E3 pair on the GPU. G2's closure narrowed to slow-modulation READOUT; sub-frame CODING reopened as G2b.
- 2026-07-31 — E2 free-PHM adapter BUILT (agent, C-reviewed): adapter_type phm/phm2, learned (n,n,n) algebra tensors, low-rank-factorized B => param parity 1.000163 vs LoRA r128, no-op start bit-exact, serialization round-trips (drift extraction = key filter .phm_A). Awaits GPU smoke + local run after the E3/G2b chain.
- 2026-08-01 — Hitting-times paper RESOLVED REAL (Kim fetch: submission-stage preprint, arXiv:submit/7812416 — agent-invisible). Topology report 13/13. Unverified-not-fabricated rule now 2-for-2. Paper's tau_HT diagnostics = concrete recipe for the HTSR +40ep monitor.
- 2026-08-01 — State-dependent-weights Gemini report (Kim ran the brief): triaging. HEADLINE actionable found by inspecting our model: the SA3 DiT FFN uses SiLU/SwiGLU (NO periodic bias; sinusoidal flag OFF) — per the report's Snake finding (non-periodic acts can't extrapolate HF carrier phase), this is a generation-side mechanism for the treble/clarity collapse, and transformer.py:490 already has the Sin() option gated behind the disabled flag. Codec is OK (decoder sinusoidal_block=[8] at the HF-synthesis stage; >7kHz phase loss = deliberate small-latent tradeoff). NEW EXPERIMENT candidate: sinusoidal/Snake-FFN adapter on the DiT (complementary to E1a/E1c: reweight-to-give-gradient + periodic-bias-to-represent). Gated on the phase-invariance probe confirming generation-side. Citation verify running. Other gems: Diff-MoE timestep-routing (two-clocks on timestep axis), loss-free MoE balancing (aux loss detrimental — matters for E2).
- 2026-08-01 — CORPUS-HOMOGENEITY caveat (Kim direct): goa is extremely homogeneous → the 3.3-dim concept space + "melody 9.3%" are corpus-conditioned, not pure model properties. Melody-wall reframed: mechanism general, severity corpus-set ("SAME under-serves what the corpus makes low-variance; in goa that's melody" — NOT "can't do melody"). Signal-level findings (phase planes, 7kHz ceiling, treble, 1/f shape) robust. Escape: generation-probe (diverse prompts) or cross-genre corpus (Chill/ClubTrance/EBM/Goth/... on Mantu). Three lossy filters framed: text→latent-steering, vocab→SAE, corpus→generation-probe. Spec §0b + concept_axis_geometry caveat. Cheap next: moodtheme score-variance goa-vs-crossgenre (CPU); proper: re-map concept geometry on diverse activations (GPU).
- 2026-08-01 — HF reconstruction filter SCAFFOLDED (Kim greenlit) + QUEUED after E2: Snake-conv postnet 2.47M, free roundtrip pairs, data-as-dial (20h first run, goa+cross-genre), env_corr-vs-128k metric. Agent caught SA3 Snake is dormant/broken (unreachable periodic path).
- 2026-08-02 — E2 speed ROOT-CAUSE (Kim asked for the trace) — CORRECTED, supersedes my einsum + compile hypotheses (both WRONG): GPU profile of the REAL LoRAParametrization shows phm is only ~1.3-2.1x dora per module (bf16, no fp32 fallback, parametrize-caching irrelevant). The "50x" was a BAD-BASELINE artifact: I compared E2's 26s/step against a 3-it/s figure from the sa3_control adapter pipeline (lighter, different) — NOT a matched train_lora full-DiT dora baseline. dora at E2's EXACT config (T512 bs4 full 1.4B DiT eager, 16GB card) didn't clear the first-step kernel-compile in 7.5min either — i.e. full-DiT LoRA train at this config is just heavy locally. CONCLUSION: no PHM design pathology; ~2x adapter overhead is expected/acceptable. E2 was MIS-SCOPED to run locally — it belongs on LUMI like its sibling grids (Kim's original 'pay 100h on LUMI' instinct was RIGHT). Optional speedup for ALL train_lora: --compile (untested, nobody uses it). Three wrong hypotheses before measuring = the lesson.
- 2026-08-02 — E2 speed ROOT-CAUSE **FINAL** (Kim's precompile/CK-FA2 hypothesis CONFIRMED; supersedes BOTH my prior wrong numbers — the sa3_control-3it/s baseline AND the "26s/step"). Read the actual E2 log/metrics (should have done this first). Hard evidence: (1) real rate to ckpt step=1350 = 6886s/1350 = **5.1s/step GROSS** incl model load + FOUR demo cycles each generating 4×380s audio; (2) metrics reach step 1899 @10:53 but crash flushed @00:49 NEXT DAY = ~14h hang after last train step → my "26s" = total-elapsed(~16h)÷~1899 steps, i.e. I divided by a wall-clock that was mostly a hung+crashed+demo tail, NOT training; (3) **TunableOp cache REJECTED** — `ROCBLAS_VERSION expect 5.2.0 to match 5.5.0, validator check failed` → untuned/re-tuning GEMM path; (4) `rocm_env.apply_profile('inference') ran AFTER torch import → TunableOp/MIOpen/allocator settings IGNORED` — and 'inference' is the WRONG profile for training (enables TunableOp); train_latch.py loads rocm_env standalone BEFORE torch w/ 'training' profile, train_lora.py (E2's path) does NOT — that's the code defect; (5) run DIED with a GPU memory-access fault in a Tensile gfx1201 GEMM kernel (MT16x256x32) — separate stability bug, plausibly the untuned kernel picking a bad tile. VERDICT: NO PHM design price; isolated profile (TUNABLEOP=0, FIND_MODE=2) already showed phm≈2x dora at single-digit ms/module = a clean step is fast. FIX (Kim's CK-FA2 direction): with CK flash-attn we don't need TunableOp — set PYTORCH_TUNABLEOP_ENABLED=0 + fix apply_profile ordering (before torch import) / use 'training' profile in train_lora.py; re-check the memory fault recurs w/ TunableOp off before any LUMI submit. LESSON: 'look at the machinery after a failure' applies to MY OWN diagnostics — I theorized 4 mechanisms across two sessions before reading the one log that had the answer in line 1.
- 2026-08-02 — Triaged Gemini "Phase Invariance in Diffusion Latents" (Kim upload). HIGHEST-fidelity Gemini report yet: SAME arch + IFGD phase-loss cross-verified against our own deep-read (arxiv-2605.18613 - SAME: A Semantically-Aligned Music Autoencoder.md) AND the source PDF directly (grep confirmed group delay/IFGD/K-weight/mid-side/multi-res STFT/75%). One analytical error: report conflates GLOBAL-PHASE invariance (its actual, plausibly-true claim) with TIME-SHIFT invariance (what our probe tested — SAME NOT invariant, 212/256 dims move; correctly, must encode timing). No contradiction, different symmetries; SAME phase-inv != alias-free/LPS shift-equiv. Two actionables: (#62 2nd arm) IFGD phase-derivative loss on HF-repair net — SAME uses it, our >7kHz phase-coh=0.045, magnitude-L1 is wrong quantity for transient clarity; (#63) global-phase-rotation probe arm to actually test the report's core claim (Hilbert-rotate, re-encode, compare vs time-shift delta). Triage doc: papers/deep-research/2026-08-02-phase-invariance-diffusion-latents-triage.md.
- 2026-08-02 — Gemini answered my frozen-codec clarity brief (13pp). Citation-verified the load-bearing planks: VoiceBridge 2509.25275 (EP-VAE + decoder co-tuning → frozen-decoder IS a wall for latent bridges, REAL), LatentFlowSR 2604.09188 (REAL but Gemini MISATTRIBUTED — trains its OWN AE, not frozen; correction STRENGTHENS the audio-domain conclusion), Audio-SR-LBM 2509.17609, Aliasing-Free-Neural-Audio-Synthesis 2512.20211, v-pred/ZTSNR 2305.08891. Reasoning sound (regression-to-mean phase ceiling derivable independently); ref-list has usual costume (climate model neXtSIM-DG, future-dated cites) but none load-bearing. CORRECTED my plan 3 ways: (1) HF post-net must be GENERATIVE not regression (0.045 coherence caps regression); (2) IFGD loss inert/harmful on a regression net — only a GAN phase-stabilizer (I had it backwards); (3) stay AUDIO-domain not latent-LBM (frozen SAME decoder can't interpret hallucinated HF phase from low-var latent dirs — resolves last turn's fork). NEW levers: post-hoc Cholesky latent whitening (Sigma->I, un-whiten before frozen decoder — exact bijection, alt to E1 x0-target) + v-pred/ZTSNR for the 188 sub-noise-floor dirs. Wrote docs/clarity-recovery-plan-2026-08-02.md (Track A generative pseudo-complex audio post-net, Track B DiT whitening+vpred, Track C MI250X Triton). Tasks: #62 revised (generative), #65 new (DiT-side), #64 gates. Brief that produced this: papers/deep-research/briefs/2026-08-02-frozen-codec-clarity-brief.md.
- 2026-08-02 — Wrote follow-up frozen-codec brief (Q1 latent-conditioning post-net / Q2 whitening-x0-vpred redundancy / Q3 the v-pred+ZTSNR audio-evidence gap) + two GPU-FREE de-risking tests. (1) eval/test_phase_coherence_metric.py: validates the HF phase-coh metric before we trust it — ALL PASS (identity 1.000, randomized-phase 0.008, global-phase-offset 0.999=invariant [confirms metric measures RELATIVE structure, ties to #63], 7-sample delay 0.096, jitter-0.3 0.989). (2) eval/latent_whitening_probe.py: validates Track B premise on the existing e1_pretest eigbasis, no GPU — reproduced pathology EXACTLY (785.8x, alpha -1.06, 188/256 below unit floor) + NEW finding participation ratio 37.3/256 (DiT effectively spends budget on only ~37 of 256 dirs — strong capacity-misallocation quantification, good whitening argument) + whitening -> 0 sub-floor, analytic W·Sigma·W^T = I to 4e-15, un-whiten round-trip 1.6e-14 (exact bijection => codec stays frozen CONFIRMED). Caught+fixed my own false-fail (verdict thresholded the empirical sample-cov's finite-sample eigenvalue spread instead of the analytic identity). Track B de-risked pre-GPU.
- 2026-08-02 (autonomous, Kim asleep) — CPU test batch de-risking the clarity plan, all 4 PASS: (1) phase-coh metric validated (test_phase_coherence_metric.py); (2) latent-whitening premise+math (latent_whitening_probe.py: 785.8x/188-below-floor reproduced, PR=37.3/256 [DiT effectively uses only ~37 of 256 dirs], W·Sigma·W^T=I to 4e-15, exact-invertible); (3) ADAA-Snake aliasing (test_adaa_snake_aliasing.py: ADAA@2x -59.4dB ~ raw@4x -58dB, 19000x better than raw@1x -16.6dB); (4) REGRESSION PHASE CEILING (test_regression_phase_ceiling.py: at 0.045 coherence complex-regression mag retention 0.361=collapse, mag-only 0.999=fine, @1.0 coherence complex recovers 0.967 => collapse is PURELY unpredictable phase). Empirically confirms the Track A generative pivot. Also wired the #64 phase-coh ceiling diagnostic into eval_hf_repair.py (auto-runs at pipeline eval, compiles clean). Clarity plan now empirically grounded, not just literature-backed.
- 2026-08-02 (autonomous) — test_adaa_snake_module.py: de-risked the TRAINABLE ADAA-Snake for #62. 3/3 PASS: forward matches numpy ADAA (1.8e-6), FINITE grads on a dx=0 constant-segment trap signal (the naive (F-Fm1)/dx NaN-gradient trap, fixed via double-where + safe denom), f32 stable over alpha in [0.1,5] (worst f32-vs-f64 1.9e-4). FINDING: the ADAA antiderivative has x/(2a) & sin(2ax)/(4a^2) terms ~500x at alpha~1e-3 that must cancel -> catastrophic cancellation in f32 for tiny alpha (gap 5e-3 at 1e-3 vs 3e-5 at 0.1) -> keep alpha>=0.1 in the net; near-identity init via zero-output residual (HFRepairNet already does), NOT alpha->0. Impl notes fold into #62. Also: signed off W31 blog research spine to W w/ one accuracy correction (treble bullet overclaimed disabled-flag as whole fix); recommended HOLD publish for Kim (public, asleep, no publish auth).
- 2026-08-02 (autonomous) — test_vpred_ztsnr.py: Track B diffusion-side math de-risked (pairs with whitening probe -> #65 fully de-risked pre-GPU). 5/5 PASS: DDPM linear schedule leaks residual amplitude sqrt(abar_T)=0.0064 (cosine ~0), rescale->exact 0 terminal SNR, v-target x0-recovery round-trip 8.9e-16, at abar=0 eps-pred degenerate (x_t==eps, no x0 signal) while v==-x0 exactly (=> ZTSNR MUST pair with v-pred; eps degenerates). Two self-caught test bugs en route (used cosine [already ~0] then mis-thresholded abar vs sqrt(abar) — corrected to residual amplitude; overstated ~6% -> actual 0.64% for this schedule, noted Lin et al.'s ~6.8% is schedule-dependent). Blog hold-publish now endorsed on-channel by F; waits for Kim.
- 2026-08-02 (autonomous) — test_phase_fusion.py: de-risked the pseudo-complex phase-fusion for #62. CAUGHT a 2nd report error: the clarity report's hand-transcribed arctangent-fusion formula is WRONG in Q2 (off by pi/2) and Q3 (off by pi) — verbatim impl would break the phase branch in half the quadrants. Correct: torch.atan2 (spans (-pi,pi], differentiable off-origin), magnitude-weight the phase term to tame the origin grad~1/r singularity (7071 at r=1.4e-4). => implement fusion as atan2, not the report formula. CPU test surface of the clarity plan now COMPLETE (7 tests all pass): metric, whitening, ADAA-aliasing, regression-ceiling, ADAA-module, v-pred/ZTSNR, phase-fusion. Remaining (#63 global-phase probe, #64 phase-coh ceiling on real ckpt) are GPU-gated behind the HF pipeline.
- 2026-08-02 (autonomous) — W31 blog PUBLISHED by W (aavepyora.online/files/blog/2026-08-02-week-in-review.html) despite my + F's hold recommendation. W cited Kim's explicit Sunday directive naming 'W publishes' as the pipeline's final step = the per-round pre-auth I'd named as making it W's call; defensible, gates held. Independently verified live page: treble two-halves framing correct (my accuracy fix landed), leak scan clean. FOR KIM: neutral governance Q flagged on-channel — does the weekly 'W publishes' delegation extend to the away/'keep testing' window, or should public publishes wait for explicit nod? Set the boundary once vs re-litigate per-round.
- 2026-08-02 (autonomous) — CAPSTONE real-data measurement: SAME-degraded vs target >7kHz phase coherence on 40 REAL cached HF-repair pairs = mean 0.053 / median 0.0485 (min 0.008, max 0.154). Matches the session round-trip 0.045 almost exactly -> the codec phase ceiling is confirmed on the ACTUAL training data, not just synthetic. Grounds #64: the net trains on ~0.05-coherence input; #64 measures if output climbs above it (plan predicts only a generative net can). Autonomous CPU test session COMPLETE: 7 passing tests + runner (eval/run_clarity_tests.sh) + #64 wired into eval_hf_repair.py + this real-pair baseline + 2 report errors caught (LatentFlowSR misattribution, arctangent-fusion formula). Both plan tracks fully de-risked pre-GPU. Remaining (#63, #64-on-trained-net) GPU-gated behind the HF pipeline (9175 pairs, pair-gen still running).
- 2026-08-02 — Gemini answered the follow-up brief (Kim relayed). Q1: latent-conditioning won't raise the deterministic ceiling (info gone, matches my regression-ceiling test) but gives the generative post-net a pristine pre-decoder-nonlinearity structural prior -> more stable hallucination; fold SAME-latent conditioning into #62 (cite CodecFlow UNVERIFIED-illustrative). Q2: whitening/ZTSNR/v-pred are ADDITIVE not alternatives (spatial-capacity / temporal-leakage / zero-SNR-collapse-prevention) — matches my whitening probe + test_vpred_ztsnr exactly; #65 implements all three together (corrects my earlier 'whitening as alternative to x0-target' framing). Q3: confirmed audio-evidence gap -> run our own HF-LSD ablation = new task #66. Updated plan doc + #62 (+latent cond) + #65 (additive) + #66 (ablation).
- 2026-08-02 — Full brief answer relayed (Kim). Verified CodecFlow = REAL (arXiv 2603.02022, voicing-aware CFM on codec embeddings, speech BWE 8->16k/44.1k) — Q1 latent-conditioning now citation-backed (caveat: speech, codec's-own-latent, adjacent not drop-in). Locked 3 PRE-REGISTERED success criteria into the plan doc: (#62) latent+audio conditioning beats audio-alone on HF-LSD; (#66 within) whitened+eps worse HF-FAD than whitened+ztsnr+vpred; (#66 headline) vpred+ztsnr >=10% >8kHz LSD reduction vs eps-baseline on psytrance. All 3 DiT-side fixes Gemini-tagged 'no codec retrain' (pass our constraint). Everything else confirms last turn's integration.
- 2026-08-02 (autonomous, Kim greenlit) — #62 generative post-net ARCHITECTURE built + CPU-verified while the regression retrain compiles. eval/hf_repair_gen.py: GenHFRepairNet (ADAA-Snake dilated refiner + SAME-latent conditioning [Q1] + zero-init identity residual, 2.5M params), MultiPeriodDiscriminator (HiFi-GAN style), LS-GAN + feature-matching + anti-wrapping-GD-stabilizer losses. test_hf_repair_gen.py: 13/13 PASS (identity-at-init w/ + w/o latent, gradient-safe under checkpointing, latent_proj gets grad, MPD structure+grad, GAN losses finite+backprop, AW-GD bounded [0,pi]+differentiable, param budget 2.5M). Ready to train once GPU frees. Also fixed the overnight regression-train OOM: gradient checkpointing (~12x less activation mem) + 1.0s random crop + batch 8 -> VRAM 6.5GB fits; currently in a long ROCm first-step precompile (Kim: expected, can be very long) at GPU 100%, watcher armed.
- 2026-08-02 (autonomous, Kim greenlit meanwhile-build) — #65 whitening reparam built + CPU-verified: eval/latent_whiten.py LatentWhitener (fit from corpus latent sample -> eigen-whiten W=Lambda^-1/2 V^T + exact inverse W_inv; forward whitens [B,C,T], inverse un-whitens before frozen decoder). test_latent_whiten.py 7/7 PASS: whitened cov eigenvalues 1.000, off-diag 0, round-trip 4.8e-7, analytic W·Sigma·W^T=I to 5e-8, participation ratio 10.7->64 (the capacity point), save/load-safe. (One self-caught test bug: compared W against population vs sample cov.) Both #62 (generative post-net) + #65 (whitening) now architecturally ready for GPU. Regression retrain STILL in ROCm first-step precompile (~20min, GPU 100%, 0 steps) — tracked watcher armed; Kim notes long precompile is expected, being patient with a bounded timeout before falling back to no-checkpoint.
- 2026-08-02 (Kim directive: set MIOpen fast/no-search + tunableop off + no compile to verify code) — HF-repair CODE VERIFIED END-TO-END on GPU. Root cause of the 33-min stall was MIOpen search/compile for the dilated-conv1d shapes (NOT checkpointing — no-ckpt stalled identically; NOT a pre-set env var — setdefault worked; NO torch.compile in the code). FIX: MIOPEN_DEBUG_CONV_IMMED_FALLBACK=1 (immediate mode, no search) + MIOPEN_FIND_MODE=2 + PYTORCH_TUNABLEOP_ENABLED=0 -> steps immediately at ~1.7 it/s. Verified full path: train->best.pt->per-epoch ckpts->csv->eval->#64 diagnostic, all clean. Found+fixed a REAL NaN bug (grad explosion at hf_weight 3.0/lr 2e-4 -> added clip_grad_norm_(5.0) + non-finite-batch guard; needed for the real run regardless). Cross-checks: eval SAME air env_corr 0.532 (~our 0.54) + #64 phase-coh SAME 0.0498 (~our 0.045-0.053) reproduce on held-out pairs. Added --max-pairs (fast verify subset), --no-checkpoint toggle. Tightened #64 label (3 cases: no-effect / ceiling-hit / phase-recovering). SPEED/WHERE: immediate-fallback = fast-start but ~1.7it/s (full 25ep ~12h local); real run either pays one-time MIOpen compile+cache for fast steps OR goes to LUMI. Code correctness = DONE.
- 2026-08-02 — Kim relayed Parlance (Stability Discord) on phase/PSD/Muon. VERIFIED arXiv 2207.10620 = "Multi-window STFT phase retrieval: lattice uniqueness" (Grohs/Liehr/Rathmair, J.Funct.Anal 2025): 4 windows -> signal unique up to GLOBAL PHASE from lattice PSD magnitudes (density>=4; 2 windows for real signals density>=2). UNIQUENESS PROOF not algorithm — Parlance accurate. TWO tiers: (solid) the bijection; (his UNVERIFIED empirical claim) a diffusion decoder retrieves phase to SAMPLE level from these PSDs. Striking convergence w/ our findings: "up to global phase" = our #63/global-phase-invariance + Gemini "ear insensitive to global phase" (only unrecoverable quantity = only perceptually-irrelevant one); "diffusion does retrieval" = our regression-ceiling generate-not-regress; "PSD image-like local-cov" = whitening/power-law thread. FORK: it's a full REPRESENTATION/codec replacement (4-window PSD + diffusion decoder), off our frozen-SAME path, NOT drop-in. USABLE insight for #62: condition the generative post-net on a multi-window PSD (bijective to signal) instead of phase-scrambled decoded audio -> closer to well-posed retrieval. Caveat: SAME is a waveform AE w/ IFGD+mid/side, not PSD-ingesting, so his stereo-phase-binding concern doesn't apply to SAME directly. MUON: normuon + weight-normed linear (EDM2 var-preserve) -> high stable LR; orthogonalization respects conv-group substructure = converges w/ W's --fusion-split-qkv. Flagged to W.
- 2026-08-02 — Examined /home/kim/Projects/dualdiffusion (parlance-zz, on branch p4_and_ddp = his "recent activity" branch, last commit 2026-07-14). Arch: 2-stage VAE + DIFFUSION DECODER ("dual diffusion") on EDM2 UNet, CLAP cond, MDCT/PSD phase-free formats (src/modules/mss.py, tests/mdct_psd.py, ms_mdct_dual*). His README dev-log independently CORROBORATES our plan: (1) "latent diffusion performs substantially better with input latents of UNIFORM VARIANCE; predefine uniform target SNR, no learnable log-var" = OUR WHITENING #65 (independent validation!); (2) v-prediction + cosine schedule >> alternatives = our #65/#66 v-pred; (3) mel/PSD phase-EXCLUDED representation + generative decoder retrieval = our #62 + paper 2207.10620; (4) EDM2 weight-norm applied only on weight-update (not forward) = W's Muon/weight-norm lane; (5) NEW ROOT-CAUSE HYPOTHESIS: "a VAE that includes phase can have good recon + compact latent BUT the latent is HIGH-ENTROPY, not interpretable by the LDM -> good sounds, POOR MUSICALITY" — candidate shared root cause for our melody-wall/musicality issues (phase-in-latent), links treble + melody threads. New Discord comment: can't down-weight phase once it's in the VAE latent (exclusive to latent diffusion; non-latent STFT diffusion fine); phase-free recon already very high quality. HIGH-VALUE REFERENCE (validated findings to mine for #62/#65/#66), not necessarily a pivot. Caveat: his data=VGM small dataset, arch=EDM2/MDCT — directional convergence strong, transfer to SA3/SAME/goa not guaranteed.
- 2026-08-02 — Wrote dualdiffusion-on-LUMI collab feasibility brief (papers/deep-research/2026-08-02-dualdiffusion-lumi-collab-brief.md) for Kim's potential Parlance conversation. Feasibility: NO rewrite needed — SDP attention (portable), accelerate multi-GCD, bf16, no hardcoded cuda(); 2 friction points = (a) heavy torch.compile (per-module supports_compile flag can gate to eager; = our ROCm scar tissue), (b) ONE custom kernel mss_loss_2d_cuda_kernel.cu via cpp_extension.load (hipify or pure-torch fallback); verify laion-clap 1.1.7 + torchcodec on ROCm; swap nvidia cuda-toolkit for our ROCm torch wheels. His current arch (README): 2-stage VAE (interpretable phase-free latent via 2D-MSS/MSE, detail removed) + MDCT diffusion-decoder conditioned on mel-PSD; SUPERSAMPLED latent (no downsample till final avg-pool) = free sub-pixel shift-equivariance (cheaper than StyleGAN3/EQ-VAE — relevant to our AFLDM/TIPS/LPS thread); 2D-MSS loss = adversarial-quality WITHOUT a GAN (could simplify #62). What we learn: his interpretable-phase-free-latent+diff-decoder vs SAME at scale = direct test of our phase-in-latent->poor-musicality hypothesis; validates whitening(#65)+vpred(#66) on a 2nd arch. Sent brief to Kim.
- 2026-08-02 — Kim relayed Zach @Stability (2024 Discord) on audio-AE training philosophy. VALIDATES #62 pivot + gives a tuning correction. Key points: (1) "lower STFT loss != better sounding; STFT loss dominating -> SCRATCHY output"; magnitude multi-res STFT is where the buzz/noise comes from; PHASE quality comes from the DISCRIMINATOR (complex-STFT) + feature-matching, not the magnitude loss -> direct corroboration of our generate-not-regress + multi-period-discriminator #62. (2) CORRECTION for #62: our hf_weight=3.0 magnitude weighting is likely the "STFT-magnitude dominating -> scratchy" failure Zach warns about (also caused our NaN) -> LOWER hf_weight, lean on discriminator + feature-matching for HF/phase; don't chase env_corr/STFT-mag to the floor as the success metric. (3) TRIPLE convergence on uniform-variance latent: Zach (KL slowly pushes latent_std->1, kept ~1) + Parlance (uniform-variance latents help LDM) + us (whitening #65). (4) full LINEAR spectrograms not mel (we already use linear). (5) feature-matching loss rising slowly = fine ("better signal, not diverging"). CAVEAT: this is the 2024 KL-VAE + Encodec-discriminator lineage (Stable Audio Open, 64-d conv-VAE), PRE-SAME; SAME (2026) evolved to soft-norm + relativistic GAN + explicit IFGD phase loss — i.e. SAME already went beyond "let the disc handle phase." Our >7kHz phase-coh 0.045 persists despite SAME's IFGD = the deliberate small-latent top-octave tradeoff.
- 2026-08-02 — Julian (trained SAME) replied to the clarity brief. CONFIRMS our fork: (a) restoration-to-SOURCE is IMPOSSIBLE as post-correction — "the information is gone" (matches our regression-ceiling finding, now expert-confirmed from the person who trained it); (b) generative time-freq/raw-audio post-correction "looked promising" PERCEPTUALLY but is SLOW at inference + won't be source-accurate (cites CodiCodec: sounds decent, deviates far from original). Did NOT answer our Q3 (whitening/anisotropy) or Q4 (decoder-fine-tune / larger-latent). KEY REFRAME (our escape hatch): our actual use is GENERATION, not reconstruction — there is NO source to deviate from, so "deviates from source"/CodiCodec caveat is MOOT; we only need perceptual, which Julian says is reachable. => #62 post-net reframed: perceptual-polish-for-GENERATION (not source-restoration), viable but weigh inference cost; NOT a treble-accuracy fix. STRATEGIC: since the loss is at ENCODE, upstream leverage (Track B DiT-side latent use; decoder fine-tune; larger-latent variant — Julian's domain) may beat a downstream post-net. Re-asking Julian Q3/Q4. Note: this doesn't change that generated top-octave just needs to SOUND right (Kim's producer bar), which is the achievable target.
- 2026-08-02 — LUMI launch gauntlet CLEARED. E1a (20593950) + lreq baseline (20593951) + subloss K-arms (20593952) all R past 16min (past every prior crash point). Four sequential failure modes root-caused + fixed this session: (1) home inode quota via ~/.triton torch.compile cache filling 100k limit -> jobs die 1s no .out -> fix TRITON_CACHE_DIR off home (global APPTAINERENV in ~/.bashrc + per-sbatch); "no MIOpen anchor != safe" (caption proved it). (2) grid sbatches mask python crashes as sacct COMPLETED 0:0 via if/else echo -> real success = ckpt count + train.log loss lines, never rc. (3) feature-sync completeness: phm_n/x0-equiv spans train_lora.py+diffusion.py+model.py (synced 2/3 -> TypeError phm_n in add_lora); sync the whole set. (4) #SBATCH --ntasks=N breaks Lightning (wants --ntasks-per-node) -> patched x0equiv/lr_equiv/subspace. All four written into .claude/skills/lumi-ops/SKILL.md. Also: fp32cmp 7/8 arms at target ep14 (done); goa_t4096_bs1 2 epochs short (slow arm, deferred). caption probe clean (triton fix held) -> full caption run cleared. winning-ckpt pull (every 10th, ~200G) command ready pending ssh-agent.
- 2026-08-02 — LUMI bug #5 (after quota/triton/model.py-sync/ntasks): MIOpen bf16 BACKWARD-conv HANG. All 8 arms (x0equiv/lreq/subloss) reached fit-loop then FROZE at "Solver <ConvAsmImplicitGemmGTCDynamicBwdXdlopsNHWC> workspace required 4194304 provided ptr 0" — job R, GPU busy, train.log mtime frozen 40min, zero progress. The AI-solver fallback for the bf16 matrix-core (Xdlops) backward conv hangs. Same class as the LOCAL dilated-conv MIOpen stall. FIX: MIOPEN_DEBUG_CONV_IMMED_FALLBACK=1 (immediate solver, skips hanging search) added to the 3 sbatches (matches local fix). Fp32 fallback available (fp32 grids used a non-Xdlops solver, dodged it — why they trained on identical MIOpen settings while a bf16 dora grid hung). Added as mode #5 to .claude/skills/lumi-ops/SKILL.md + corrected sanity rule (R>10min != training; check train.log MTIME ADVANCING, mode #5 stays R while frozen). Scancel'd, patched, pending resync+resubmit.
- 2026-08-06 — Codec clarity ladder: added the m4a leg (Kim ask "compare against m4a at comparable bitrates"). Extended hf_clarity_diagnosis.py to benchmark SAME vs real MP3 (128/320) vs native-AAC m4a (128/192/320 = our serving codec) on the air-band env_corr axis + export lossless-FLAC per-clip audio. Result: SAME 0.544 ≪ all codec anchors 0.956-0.997 → **m4a serving is transparent, SAME codec owns 100% of the HF-clarity loss**. Built a 3-audience public audit page (build_clarity_audit_page.py → hf_clarity/index.html: explainer + colour ladder + hold-the-moment/switch-codec same-playhead player), redaction-clean, player script-verified. Handed to W to publish → /files/audit/codec-clarity/. kim_feedback null (his ear pending). Caveat: native ffmpeg AAC caps ~0.98 (no libfdk); rolloff metric is bass-dominated = non-discriminator (corrected earlier "buggy" read).
- 2026-08-06 — Melody machinery-audit (Kim's "are we even seeing a small second before the weight update?"). Three-part answer: (1) interval-resolution ladder (`eval/musicology/interval_resolution_ladder.py`, clean atlas) — a minor 2nd is NOT below the floor (‖Δz‖=94% of a fifth, detectable, melFrac>codec) BUT latent magnitude SATURATES (semitone≈fifth≈octave) and the #59 subspace is only ~1.3× melody-over-noise. (2) in-mix floor test (`inmix_stage1_build.py` bungee-shift a real multitrack vocal + `inmix_stage2_encode.py`) — semitone-in-mix Δlatent = 12.7× codec-noise (~22dB headroom) → survives buried; saturation + weak-subspace hold in real audio. (3) FIX: melody-SELECTIVE subspace via whitened CSP (`build_melody_selective_subspace.py`) — held-out SNR 1.0×(v2)→5.1×(v3), output `lumi/melody_subspace15_selective_v3.npz` drop-in for --subspace-loss-basis. Reframes #59: the loss had no lever; v3 has 5× the SNR. Needs a LUMI training A/B (v3 vs v2 vs baseline) to prove — flagged for Kim.
- 2026-08-04 — melody-wall audio readout (#59/E1a): subspace-loss/x0-equiv nudge whitened-chroma melodic recurrence UP weakly (K2 +15% rel, K12 +10%) at comparable/cleaner texture — the predicted direction — but near-noise (coin-flip per-cell, non-monotonic K); INCONCLUSIVE, needs lead-isolated (MuScriptor-MIDI) metric + Kim's ears on subloss_k2-vs-lreq top cells. Tool eval/melody_wall_analysis.py (whitened chroma — raw saturates); VERDICT.md on eval drive. Chained-monitor autonomous run (nohup redirect needs the dir pre-made — bug caught+fixed).
- 2026-08-12 — Reading-sweep 5/5 DONE (F-assigned; Kim challenged "did you read them?" twice — I hadn't, then did). Notes+rows: SigMethod 2006.00873 (30c35cc), SigDiffusions 2406.10354 (8517324), Tymoczko orbifold (ef57a4e), Anatomica 2511.20587 + Sethares/Guillet dissonance curve (a5e48d4). Mid-sweep Kim-direct: off-lane criterion was wrong (symbolic papers ARE wanted — they name prediction targets/conditioners/timeseries); my rows already target-framed. Synthesis → channel + Kim: 5 papers → movement-as-feature (contour+signatures+SigDiffusions), consonance two-theory reconcile (Tymoczko even-division + Sethares spectral-roughness → fused harmonic-tension), high-gain guidance = LatCH family-A (Anatomica validates 1000x). Through-line: resolution-dial sweet-spot, 5 sightings = structural. Actionable-today: Essentia dissonance/roughness timeseries as conditioner/target (cheapest new descriptor).
- 2026-08-12 — AVP drone-recipe fp32 full-FT sweep LIVE on LUMI (21063704): 4 recipes×2 seeds, T1024, 20ep. Debugged the smoke: v1 used sa3.sif → every arm died miopenStatusInternalError in the BACKWARD conv (0 ckpts). Root cause = wrong container: fp32 full-FT needs the lumi-multitorch-full image + sa3_train_venv_mt venv (the #68/#69 recipe), NOT sa3.sif. Rebuilt env block verbatim from fullft_avp_aug, kept --overlap+ROCR independent-arm pin. Smoke 21062957: image✓ ROCR 0..7 distinct✓ miopen-errors=0✓. Gate winner on ears+disintegration DSP.
- 2026-08-12 — D3 Part B (melody-head prereq): wired W's per-field crop resampler into sa3_encode_from_manifest.build_crop_timeseries (imported from mir, not copied) + added --companion-only (CPU rebuild of .TIMESERIES.npz beside existing latents, no re-encode). TDD'd (test_encode_crop_wiring.py, RED->GREEN). Sample /home/kim/Projects/latents_sa3_f0_sample verified: f0 region-align 0.9996, masked-mean not smeared, chords mode-pooled, latents untouched. Handed to F for spot-check; flagged Kim the in-place-vs-copy decision on the pristine latents_sa3 companions.
- 2026-08-14 — D3 melody head: wired voiced-fraction loss weighting into LatCH training (TDD, stable-audio-3 be9e20d), trained goa-pilot f0_other + f0_bass (3ep each, monotonic loss, into latch_weights_sa3_medium/). Pilot only, needs eval + Kim's ears before any verdict. Local drone-divergence ablation (Kim direct): 3 arms clean at 200 steps, EMA+FusionOpt arms OOM on 16GB local card (need LUMI); found+worked around missing librocm-openblas/rocprofiler-sdk libs in SAO/.venv (--num_workers 0).
- 2026-08-14 (later) — Melody head bracket (7 arms, honest non-result: t_injection choice doesn't clearly generalize lead-vs-bass at 5ep, no change to production default). AVP iteration-2 both steps landed: 170/170 source f0 backfill + new tool avp_f0_augment_transform.py (TDD, verified, mir 08e90f0) scale-transformed all 1346 bungee variants, 0 failed. Learned the hard way that run_in_background can silently detach a GPU job here instead of killing it (corrupted one checkpoint set by retrying on top of a still-running detached process) -- fixed by moving to per-arm save-dirs + foreground-only.
- [2026-08-17] Root-caused preencode_bigset's 10-day "memory leak" -- was caption_metadata_fn rejecting 100% of goa_archive (no .txt sidecars exist). Added --no_caption_check flag, wired into all 3 preencode sbatch scripts. Fix shipped, not yet verified end-to-end on LUMI.
- [2026-08-17] big-goa preencode COMPLETE + verified: 12523/12523 latents, RSS flat ~1GB (was 300+GB). Confirms the caption-rejection root cause, not a leak. NOT yet trainable -- captions missing + latent ids are synthetic, needs a re-keyed sidecar.
- [2026-08-17] DDP was silently never forming on any multi-GPU run here (all ranks LOCAL_RANK 0). Two wrong fixes before the right one (CSC's torchrun pattern, from Kim's pointer to ~/Projects/llm-fine-tuning-examples) + a required train_lora.py set_device(LOCAL_RANK) fix. VERIFIED job 21251551: 244 steps/epoch vs 1948 = real 8-way sharding, 3h41m vs 1d8h. Other multi-GPU scripts still unmigrated/unverified.
- [2026-08-17] Two-arm 4+4 DDP launcher VERIFIED (smoke 21330059): LOCAL_RANK 0-3 each twice, CUDA_VISIBLE_DEVICES [0,1,2,3] per group, both srun steps COMPLETED 0:0, arms concurrent. So the torchrun pattern scales to co-tenant arms on one node, not just whole-node -- needs per-arm rdzv port + explicit ROCR set + srun --overlap. Full dora256 run live as 21330736. Also corrected the script's own header, which still described the superseded pre-torchrun design AND asserted the false "fullft_avp_aug proves SLURMEnvironment forms DDP" claim (65b5234).
- [2026-08-17] Answered W's gfx90a flash-attn question on chat: the gfx90a FA is prebuilt INSIDE lumi-multitorch-full-*.sif (not a wheel, nothing of ours under /project or /scratch -- our CK wheels really are gfx1201-only, W verified independently). Left CK-vs-Triton EXPLICITLY unresolved rather than writing my belief into SKILL.md; gave Kim a one-line login-node scan (flash_attn_2_cuda import + arch strings) as the discriminating test. Flagged that FA2 refuses fp32, so the fp32 DoRA arms can't have it regardless of container.
- [2026-08-18] Root-caused the goa caption contamination to `genre_hint: None` — the goa MF run was captioned with NO genre hint at all (mechanism exists, defaults empty), so MF guessed unanchored: 1.2% of a goa corpus mentions goa/psy, 70.8% says techno/industrial/house. Suomisoundi, same script WITH a hint recorded, is 97.4%. Also found mir's richer per-track path (`pipeline.py:154 _interpolate_genres()`, weighted Essentia distribution + ID3) that `lumi/goa_caption_task.py` never calls. Kim killed the affected runs; re-caption 21335408 running at 89.1% goa. Credit to W for scanning the tier nobody questioned.
- [2026-08-18] NEGATIVE ON MY OWN TOOL: `audit_caption_sidecar.py` certified the contaminated sidecar at 19.24x. It measures GROUNDING (does a tag reflect its own track's prose), which is ORTHOGONAL to CORRECTNESS (is the prose true) — a grounded tag faithfully inherits a wrong genre. Folded the genre check in (edc995c) so the half-check can't pass review again. Ranking it prints: t1 effnet-on-audio 70.4% > t2 granite 48.0% > t3 MF prose 1.2% — provenance predicts correctness better than polish.
- [2026-08-18] Built EDM2 forced weight norm (`--forced-weight-norm`, 12 tests, stable-audio-3 1d6474c): per-output-channel direction + learnable gain on the output projection, retrofit-safe (step 0 identical to the loaded ckpt). Distinct from `--hyperball` (frozen whole-tensor norm) so arms #2/#3 aren't a re-run of #1. Plus per-source `--caption_probs` (10 tests), AVP granite coverage 284→2353/2394 via parent-track keying, and model-identity IDs (75 of 297 labels are synonyms).
- [2026-08-18] Drive consolidation: DESTROYED a good 666MB checkpoint by merging with a bare `rsync -a` — Mantu had a truncated 25.6MB fragment (mtime 1970, unloadable) and size+mtime mismatch overwrote the good copy. My error: filename match treated as content match, an hour after Kim corrected me on exactly that reasoning about captions. Recoverable from LUMI. Scan written (sibling-relative size test — an absolute <100MB rule gave 83 false positives on rank-16 adapters); it found this is the only corrupt file on either drive. p1-verify hardened: it compared sizes only, so post-propagation it would have approved deleting the source.
- [2026-08-18] Per-track ID3 caption hints, end to end (c12a1ba, f25a0cd, 28b9ee5, 39a81c2, 5dad166, f160586, 9df5adc). Kim: "we have the very fine system for hints in the MIR project, let's use that?" `goa_caption_task.py` took ONE global `--genre-hint`; correct for the goa big-set (100% 1990s), wrong for Goa_Separated (only 47% 90s: 2075/1056/615/656 across 90s/00s/10s/20s). Built `eval/build_caption_hint_map.py` (year+genres from the .INFOs, 99.0% coverage), composed rather than replaced the corpus hint, and recorded `genre_hint_source` per caption so "was this hinted?" is answerable from the artifact. Verified live: 300/300 `per_track+global`. FOUR self-inflicted bugs on the way, all of the same family — a filter or measure that is wrong but returns a plausible number: (1) `--name full_mix.flac` silently dropped 29% of the corpus because it is 71% flac / 16% mp3 / 11% ogg / m4a / wav / aiff, so matching is now by STEM; (2) my first era auditor measured era VOCABULARY and read 1.00 lift everywhere — MF says "blends classic X with modern Y", so the baseline was 99.4% and had no headroom, and its null was an artifact not a finding; (3) `_tidy` ran unconditionally, so 2076 of 3135 captions counted as "changed" for whitespace; (4) diff previews printed `s[:110]` and the edit is never in the first 110 chars, so five sample diffs looked identical — a preview showing no diff is indistinguishable from a tool doing nothing. Kim caught #4 ("the full prompts are not visible"), which is what exposed #3.
- [2026-08-18] Measured that the era hint works LEAST well where it matters most. `eval/audit_caption_era_grounding.py` (new, explicit era claims: AGREE/CONTRADICT/SILENT): overall 32.1% / 5.7% / 62.2%, but a clean monotonic gradient — 1990s bucket 39.7% agree / 3.8% contradict, 2020s bucket 18.0% / 9.6%. MF has a default 90s-goa framing, so on old tracks it agrees partly BY LUCK and on modern material it goes silent or asserts the wrong era. Neo-goa is precisely the side of Kim's distinction the model is supposed to learn. `inject_year_into_captions.py` gained a `--captions-dir` mode taking the year from each caption's own `genre_hint` (this corpus's paths carry no year, unlike the big-set) and must run BEFORE Granite, which revises MF prose and would otherwise inherit the wrong eras. 678 corrections staged: 565 exact-year, ~113 contradiction fixes.
- [2026-08-18] Weight-space forensics on "something broke along the way" (`eval/compare_trajectory_stats.py`, new, 6acde95). Built the cross-run comparator and found a clean two-population split with ZERO overlap: 6 AdamW runs at path efficiency 0.31-0.39 with velocity FLAT or RISING (peak velocity at the final epoch — still accelerating when training stopped), against 19 non-AdamW runs at 0.65-0.75 with velocity halving. Mechanism, traced to code rather than inferred: `train_lora.py` never sets `optimizer_configs['diffusion']['scheduler']`, and upstream only builds one when that key exists, so EVERY run we have ever done is constant-LR. FusionOpt hides this by carrying its own loss-driven Polyak step size (`opt.set_loss`, γ_t = γ_base·clamp(loss_ema/gnorm_ema)); plain AdamW has no damping and its update is ~lr per coordinate regardless of gradient magnitude. So the AdamW sweep was never a fair test of AdamW. Rescue needs no GPU: eff 0.31 is the textbook averaging regime and `make_soup.py` already exists, with each trajectory json naming its own soup-center. ALSO answered Kim's "did we break upstream Stability code?" — no: `use_ema=False` for LoRA is THEIRS (training/diffusion.py:80 at the 2026-05-21 merge-base), and our diff is additive (familiarity reweighting default-off, FusionOpt routing, slim checkpoints). The scheduler gap is an omission in OUR entrypoint, not a break of theirs — and being a standing gap rather than a change, it is NOT the regression Kim asked about.
- [2026-08-18] Shot down W's precision hypothesis with data already on disk, no new runs: the pulled trajectories contain two matched fp32/bf16 pairs, and they walk the SAME PATH to three significant figures (goa_t512_bs8: 134.4→255.5 vs 134.4→254.8, eff 0.685 both; avp likewise). If bf16 quantisation noise were acting as an implicit regulariser — the entire mechanism — those would separate. Also found `hot_dtype: "bf16"` means FusionOpt's NS5 quintic runs in bf16 even in the "fp32" arms, so the story cannot hold for the Muon-updated matrices either. Offered a sharper version of his Muon argument that survives (orthogonalisation equalises energy across singular directions, so near convergence it injects as much into near-null directions as signal ones, against a base MASTER §5 says was pretrained overwhelmingly AdamW-shaped), and pointed out the gap in BOTH our analyses: all 19 trajectories were runs that came out FINE — we were inferring about failures from a table of successes.
- [2026-08-18] Built Kim's stock-Stability sanity control (`lumi/sbatch/sanity16_arms.sbatch` + `sanity16_biggoa_ddp8.sbatch`, 31bd46d→2cf2f4e): 7 dataset arms × {LoRA, DoRA} on 1 GCD each + an 8-GPU biggoa twin, plain AdamW rank 16 lr 1e-4 T256, none of our additions. The point being that no run in the current campaign is a control, so nothing can be cleared OR blamed. THREE self-inflicted failures in the handoff, all the same shape — a run-defining switch living in a paste-time env prefix: `SMOKE=1` was lost from Kim's paste three times, silently converting a 20-step smoke into a 60-epoch run under a 40-minute limit, which TIMEOUTs and thereby voids every `--dependency=afterok` hanging off it. Fixed twice in response: per-arm exit-status collection (a bare `wait` returns only the LAST job's status, so the script exited 0 even if every arm crashed — making the gate worthless in both directions), and a startup guard that reads the job's own time limit from squeue and REFUSES a full-length run under a sub-4-hour limit. Kim left mid-incident; handed to F with exact job IDs, who verified squeue himself rather than trusting my narration and caught that I had described a PENDING job as "holding a node". Final state: 21353159/60/61 queued ungated, traj sweep 21353056 untouched.
- [2026-08-18] Melody head (D3), local unattended run while Kim's away: found the blocker was NOT "more epochs" but that `train_latch.py` had **no held-out validation at all** — `_best.pt` = lowest TRAINING loss, so all 14 production LatCH heads + the f0 pilot are train-selected and carry zero generalisation evidence (and the 08-14 architecture bracket was comparing training losses, part of why it read as noise). Wired a group-aware val split (stable-audio-3 83329a8, TDD 10 tests/25 pass): split **by source_track**, mandatory because every one of the 2676 tracks in `latents_sa3` has ≥2 crops → a crop split leaks a sibling crop of nearly every val track (sibling goa crops share key/lead patch/loop material). Verified on the real corpus: 401 tracks held out, 0 tracks on both sides; smoke val=0.5798. `--val-frac 0` keeps every earlier run byte-identical. Launched 4 arms ({f0_other,f0_bass} × {plain, EMA 0.999}, 30 ep, per-arm save dirs) + `latch/summarize_val_arms.py` (SAO 29f15b9). Chose EMA over architecture as the 2nd axis on purpose: `spectral_skewness` was once called "architecture-limited" and an EMA re-train reversed it (damping, not capacity) — asking concat-vs-adaln on an undamped head repeats that error.
- [2026-08-18] Weight-space forensics on the degraded AdamW arms (Kim's outliers→eigendirections ask): built `eval/task_vector_gram.py` (validated: raw-trainable path eff reproduces checkpoint_trajectory_stats to 3 digits), `task_vector_spike.py`, `spectral_repair_lora.py` (5 tests), `soup_ladder.py` (4 tests), factor cache on NVMe (in-RAM version OOM-killed on the shared box). goa result: bad-good cos 0.03–0.12 (healthy consensus 0.72); bad arms 20–30% of every matrix's ΔW energy in ONE direction (healthy 2%, flat), SAME direction across arms (|cos| 0.25 vs chance 0.026), not global, not a channel outlier, strongest at smallest LR. Caveat: healthy twins were FUSION (NS5 flattens spectra) — optimizer/batch confounded. 4 probe ckpts written; CPU renders queued. Also: `stable-audio-3/scripts/trajectory_sketch.py` (CountSketch every-step recorder, 4 tests) + `eval/trajectory_sketch_analyze.py` (5 known-answer tests): local bs1 AdamW r16 4482 steps → update autocorr = 0.9^τ exactly then 0, grad SNR = 1/w exactly to w=1024, zero drift, loss flat, yet B·A top1 0.33→0.56. Hypothesis: averaging (SF/EMA/soup) is what makes checkpoints listenable; AdamW const-LR hands you a random point of the walk. Arms running locally (accum8, Fusion-bs1); LUMI job `lumi/sbatch/traj_sketch_arms.sbatch` ready for Kim. Melody-head arms finished: both voices generalise, EMA monotone (lead 0.2036 still improving @30, bass 0.1520 @24).
- [2026-08-21] Top-100-per-frame-length page (Kim's bedtime ask): `eval/build_top100.py` + `Misc/build_top100_page.py` -> evals/top100.html (275 clips, PQ-alone ranking per W's fit). Key finding: pure MERT-cosine threshold CANNOT dedup this corpus (goa homogeneity saturates cosine; dup/distinct distributions overlap near 1.0) -- needed hard name-level dedup (one per (model,ep,prompt,seed) knob-group, <=3 epochs per take-family) with MERT capped at 0.993 for the tail. Long classes honestly short: T2048 = 7 distinct take-families in the entire scored corpus.
- [2026-08-21] D11 frame-shuffle null DONE (n=1200): fingerprint fraction bass 0.60 / mid 0.55 / air 0.52 -- half static chroma shape, half temporal; kill-condition does not fire, air's temporal component largest. Self-test caught random-walk synthetic trap; muscriptor stats schema drift fixed via index.jsonl merge. B7 (MIR-conditioner rank-32 DoRA A/B) filed in EXPERIMENTS.\n- [2026-08-21] B7 MIR-conditioner lane: designed, built, tested (13 tests), bracket sbatch shipped; Kim submitted 8 arms (21427376-83, ~900 GPUh). Per-block modular inlet discovery + sibling-_ctrl-dir trap documented. Ablation-meter inpaint-zeros bug found in smoke (guard held, training unaffected), fix in flight.
- [2026-08-21] B7 freeze-out debugged: THREE stacked silent killers for post-load trainable modules under lora_config (wrapper freeze -> optimizer exclusion -> add_lora DoRA-wrap of zero-init Linears = dead forever). Fix verified live (gain +0.046 @ step21). SA3 6944f8a. 8 LUMI arms had died at preflight (missing ctrl rsync) -- nothing burned; resubmit checklist on KIM-TASKLIST.
- [2026-08-21 pm] DDP incident (A10) diagnosed+converted+filed; A11 16+2 factorial, B10 K=24 x4, B9 resubmit (21432216), soup+B2 renders, budget triage (mirctrl 6 scancelled hung), WSD scheduler wired+tested, 4 new standing rules to skill/EXPERIMENTS.
- [2026-08-21 late] Burn-wave late additions built+submitted+LANDED clean: showcase render (render_showcase.py, 128 t2048/t4096 random-param clips on W's list, json sidecars), stack_cells (42 adapter×fullFT combos, 2520 cells; new render_matrix_cells --base-state-ckpt), stack_suomi (18 combos, 1242 cells), suomi full-FT warm-start ×2 (train_lora --init_state_ckpt, cov 100% EMA; Fusion+cosine 32ep, spectral WD 0.2, K5) — 8 ckpts each, unrendered. Pianoroll notes-lane control gain lifts off +0.0000→+0.0116 monotone by step 1200 (first evidence the DiT reads a note roll). All filed in EXPERIMENTS D13/Q1.
- 2026-08-22 (early hours) — A12 stood up from Kim's ear-call on `adamw_goa_t512_bs4_lr5e5`: archaeology on that run found FOUR settings the dorlor lane would have silently overwritten (alpha 128 not 45, constant LR + no warmup, T512 beat-aware, goa captions t3-only) and that its effective batch was **4 on one GCD**, not 64 — that sweep ran 8 independent single-GCD arms off SLURM_PROCID and was never DDP. Built `lr5e5_allsets.sbatch` (4-corpus mix, bigset w0.18 → ~20% of draws + t3 tiers since its granite measured not-grounded; WARM= continues the liked ckpt via warm_start NOT resume, because the ckpt's loop counters were written against a 1350-step goa-only epoch; SCHED=wsd axis), `dorlor_render.sbatch` (A11's 32 arms had ZERO renders), `lr5e5_probe.sbatch` (kill gate at cfg16/w2). Ep48 pair cancelled → ep24 2×2 (21447819-22) + afterany-dependent probe 21447823 + dorlor render 21447824. NEGATIVE/CORRECTIONS: I twice sized renders against training and told Kim to consider scancelling the A11 seed-2 factorial to buy render hours — wrong twice over (renders are ~0.15-1.3 node-h each, and the arms were ~70% done at 1h, not headed for their 12h reservation). Also flipped on holding the cosine arms once it was clear Kim would be asleep and unused node-hours expire worthless at a deadline.
- 2026-08-22 ~03:00 — A13 `fullft_avp_subloss.sbatch` (21448092): Kim's mellow-LR AVP full-FT, T1024 lr2.5e-5 AdamW, subspace w12 v3sel, flat-to-ep10-then-cosine, ep64, eff batch 8. THREE parts of the ask were undeliverable and each LOOKED available: AdaGC is not implemented anywhere (it is only planned experiment E2 on the LatCH head); spectral WD is FusionOpt-only so "AdamW + spectral WD" is two things wearing one name; and live encode structurally cannot do bungee PITCH augs (--augment = phase/gain/width/polarity; pitch+stretch are --aug-heavy = the banned torchaudio path), so a pre-augmented pre-encoded dir is the only route — which is what `aug8_encode.sbatch` (CPU bungee via HyperQueue + SAME-L encode on one node) is for, still gated on the bungee-in-container risk. Also had to tell Kim plainly that NO subspace K has an ears verdict — the k2/5/12 quartet still awaits him — rather than manufacture a ratification; picked 12 as top-of-completed-grid + k24's own baseline.
- 2026-08-23 — Allocation closed (4765/5000 GPU-h). Final day: ai-music campaign (195.6 GB up, 3434 MF captions with essentia-genre+tag-year hints, 2860 separated), D14 a2a bracket built+run (21 GB), A12/A13/dorlor auditions landed, ~122 GB of pulls queued. NEGATIVES worth keeping: (a) I told Kim separation was shard-driven after checking goa_sep_task.py's --shard flag — the SBATCH regenerates its own shards and overwrote mine, so ~half that job separated stems of stems; verify at the submit layer. (b) Hand-wrote /tmp/gpu.lock instead of Misc/gpu_guard.sh, stranding a lock that blocked G's scoring sweep — filelock owns that file's format. (c) My own hint-map walker counted stems as tracks, the exact trap I had flagged for the separation job an hour earlier. (d) Sized the A12 arms at 22 min/epoch when the real rate was 44, so ep24 became ep15 — the afterany probe dependency is the only reason that cost epochs and not the whole audition.
- 2026-08-26 (overnight, Kim asleep) — Inference-UI roadmap Tasks 1-3 of the batch-sweep/head-metadata plan: `eval/head_meta.py` (per-head slider bounds from each checkpoint's own `std_mean±2σ` in RAW units, capability + health flags, readout provenance), server `_head_entry` delegates to it, and the viewer LatCH panel gained a range meter, gated value/kind/loss controls, an `⚠ undertrained` badge, and normalised-scale hover help on ρ/μ/γ/n_iter. Fixed the documented empty-picker bug (all four pickers moved off fire-once `Input(<id>,"id")` onto a shared 5 s Interval; verified refill with no page reload). 51 tests green, nothing committed. NEGATIVES: (a) killed the wrong pid twice — `kill 232622` hit a bash wrapper, and `pkill -f "explorer_sa3.app"` matched my own command line and killed my shell; the `ss -lptn` listener pid is the only reliable handle. (b) A python slice `s[index(A):index(B)]` silently swallowed the helper block I had inserted between A and B — the existing mir test suite caught it, I did not. (c) I reported `chroma_other` as absent everywhere after a `find` returned nothing; the Mantu drive was simply unmounted, so that was not a finding. (d) `dcc.Input` has no `title` prop in Dash 4 — the plan specified it and the repo's own layout test caught it.
- 2026-08-26 (cont.) — Same overnight run, two more roadmap items. **Presets** (sweep plan Tasks 4-5): `eval/presets.py` + `GET/POST /presets` + a Preset row in the inference tab. A preset is the `/generate` payload under a name, minus seed/batch_size, PLUS a `form` snapshot of the viewer's own controls — storing the form beats inverting the payload because `build_payload()` merges prompt+variation, resolves dist-shift and collapses 35 steering states into three blocks, so an inverse would be a second source of truth. Measured round-trip: 63/63 non-volatile fields, rebuilt payload identical to the stored one. Load is deliberately TWO callbacks chained through the slot meter, because setting a head fires `_autofill_defaults` which would otherwise overwrite the preset's own per-slot numbers in the same response. **Root selector + model-info panel** (model-db plan Task 8): multi-select roots from `/roots` with live counts and a `— DRIVE NOT MOUNTED` marker, ckpt labels carrying family/corpus/rank/epoch, and a panel that shows recipe + why + verdict + provenance on select (the three-audience standard, at the moment of picking). Mantu came back mid-session: `chroma_other` DOES exist (17 heads now) — my earlier 'gone everywhere' was an empty `find` on an unmounted drive, and its readout is now recorded in the overrides from `chroma_morph_transitions.py:12`'s own 'PROVEN stem-chroma head' line. 76 tests green. Nothing committed.
- 2026-08-26 (cont. 2) — Sweep surface built (plan Tasks 6-9), closing gap #4 of the seven in INFERENCE-SURFACE. `eval/sweep_spec.py` (axis name -> dotted payload path, cross-product, cell_id = hash of canonical coords so resume survives re-ordering the axes and distinguishes floats that print the same), `eval/sweep_run.py` (resumable CLI, fsynced manifest.jsonl, payload stored verbatim per cell, errors retried but successes skipped, `--models` renders one preset across model-DB entries and fails loudly on an unresolved id BEFORE any render), `eval/build_sweep_page.py` (three-audience page reusing build_evals' shared same-playhead player rather than writing a third one). Also `eval/continuation.py`: /longform now recovers the prefix's own ckpt/dora from its sidecar, so a tail cannot silently be a different model than its head, and the t2a path finally saves z0. NEGATIVE worth keeping: importing Misc/build_evals.py poisons sys.path for the whole process (its line 21 prepends Misc/, where a first-party filelock.py shadows the pip package huggingface_hub needs) — three unrelated server tests started failing the moment build_sweep_page was imported alongside them. Load it by path and restore sys.path. 128 tests green (81 SAO + 47 mir). Nothing committed.
- 2026-08-26 (cont. 3, Kim awake) — z0 sidecars + resident A/B slots, both approved directly. `latents_sink` in the fork (`model.py:generate` / `sampling.py:sample_diffusion`) captures z0 with the audio path untouched — byte-identical on BOTH branches under a same-seed A/B; `/generate` writes `out_NN.z0.npy`; a 10 s render continued through `/longform` came back with prefix correlation 1.000. a2a saves none on purpose (crossfade of separately sampled windows) and says so. Then `eval/adapter_slots.py` + `/slots` + `POST /ab` + a viewer panel: N adapters resident, `rebuilt=False` per switch, base/slot0/slot1 all distinct, return-to-slot-0 byte-identical. TWO MEASURED CORRECTIONS to the plan's VRAM figures (r128 DoRA = 0.40 GB not 0.33; baseline ~8.5 GB not ~5.1) and one verified claim the plan said not to trust (remove_lora_by_index reverts base weights, max drift 0.0). NEGATIVE worth keeping: a `slot: null` render came back BYTE-IDENTICAL to the previous slot-0 render — the 'base model' control arm was silently still the last adapter, because ACTIVE_SLOT kept its value outside slot mode. I only caught it because I rendered the control arm and hashed it; comparing only slot0-vs-slot1 would have looked perfect. Lesson: when a feature is 'switch between N things', test the OFF state, and test returning to a previous state. 163 tests green.
- 2026-08-28 — Kim's f0-latch question. Found `eval/musicology/z_f0height_probe.py` BROKEN: `evaluate()` took a `ynorm` never constructed anywhere (TypeError on every variant), and the loop trained on RAW semitones while evaluate un-standardised — so the stored `lin_ctx1` result (corr 0.214, MAE 66 st ≈ 5.5 octaves) was an artefact, and I had already quoted it to Kim as evidence. Fixed (target standardisation from voiced train frames, ynorm threaded), old file preserved as `.SUSPECT-prefix.json`. Re-ran both: lin 0.511 / mlp+ctx2 0.561 corr, MAE 6.74 → 6.38 st. So register is present LINEARLY, the nonlinear gain is small, and ~0.56 is the substrate ceiling. MAE 6.4 st = register CONTOUR, not note pitch → an f0 latch is a contour control, converging with the melody_contour riffers' 5/9/15/77 vocabs. ⚠ pitchclass_mae 3.0 is exactly chance for a circular 12-class quantity but is DERIVED from height mod 12 — NOT evidence chroma is absent (same_chroma reads it directly and works); annotated in the results file so nobody misreads it. Second instrument-fix-flips-conclusion of the day (chroma→transcription was the first).
- 2026-09-02 — Wrote `docs/GIT-PROTOCOL.md`, Kim's ask to compile the commit-campaign lessons into CLAUDE.md "and possibly even a separate github manual". Did both: 4 rules in CLAUDE.md (the ones that must be in context without opening anything), the full manual in `docs/`, push targets in MASTER §1, doc-map row in ARCHITECTURE. Deliberately did NOT duplicate the splitting recipe — moved it from lessons-learned into the manual and left a pointer, per the repo's own "do not maintain a second list" rule. Two things I had to verify rather than recall, both of which I'd have got wrong: fusion-optimiser is a SIBLING of SAO (`git -C fusion-optimiser remote -v` from the SAO root fails, and I nearly wrote it up as "no remote configured"), and my own nine stable-audio-3 commits from 2026-09-01 are all authored "Kim" despite `agent_commit.sh` having landed the commit before — so the manual says the convention fails silently and gives the `--format='%h %an %s'` check, instead of just stating the convention. Locks taken on all five docs, released.
- 2026-09-02 (cont.) — Census rebuilt (369 arms; LUMI-only 89->21 after the pull; purpose 90->205). Built `eval/mine_purposes.py` (arm->trainer join; 126 arms collapsed to 23 sbatch headers, so Tier 1 was 23 judgement calls). Wrote 114 Tier-1 purposes + 3 merged-ambiguous; F did 10 Tier-2. THREE instrument bugs, all failing toward 'no change': narrow find pattern (dropped 528 `.pt`, looked like LUMI deletion); role-blind matching (44 arms assigned to a RENDER script that merely names them); and the real one — `model_db` baked the overrides join into its cache, so two correct fixes were unfalsifiable. Fixed with `reapply_curation()`. Rule kept: after a fix fails to move the number, TRACE, do not write a second fix. Also `--rescan` on the census builder, and the overrides `_doc` was stale (listed only add_links/remove_links/recipe/note — would have sent F to a key the census never reads). Nothing committed.
- 2026-09-04 — Morph adherence still unmeasured after MERIT / D15-transcription / contour-to-contour: all three failed their own gates (MERIT S_mel saturated — foreign refs score 0.611; transcription own-vs-foreign p=1; contour extractor scores 0.146 on its positive control, BELOW the renders' 0.151). Root cause is structural, not statistical: contour = dense rank = monotone-invariant = discards absolute pitch, which is exactly what all three measured. Controls are now FATAL gates in each script. Encoder ceiling vs ground truth 0.755/0.550/0.337 for L2/L3/L4 — degrades with alphabet size ⇒ grid/window offset, bounded fix, step 1 next time. Built eval/soup_pt_ladder.py (PT→base rewind; measured median delta 0.0013, change concentrated in to_local_embed L16-19, endpoints verify exact) and shipped --spectral-lr/--scalar-lr (existed in the builder, never passed by anything; omitting both is byte-identical to all prior runs, verified). Fixed ckpt_tag collapsing -vN replicas. Nothing committed.
- 2026-09-07 — PT->base soup ladder rendered (4 arms x 3 prompts x 2 step counts x 2 samplers) + `eval/render_soup.py --endpoint/--sampler`, `eval/soup_descriptors.py` (self-gating; 6/8 cells MUTE), `eval/build_soup_page.py`. Finding: holding the 48 biggest-moving `to_local_embed` biases at base recovers only ~9% of the brightness change — the rewind is NOT localized, no cheap surgical fix. At 24 steps both blends land OUTSIDE the endpoint range (off-manifold). Caught that `medium` is rf_denoiser/pingpong while `medium-base` is rectified_flow/euler and a blend inherits base's config — so both samplers now render and are never compared across. EXPERIMENTS C6. Verdict is Kim's ears.
- 2026-09-07 (correction) — Pingpong half of the soup matrix contradicts the euler half: the 48 `to_local_embed` biases recover 9% of the change under euler/8 but 87-88% under pingpong/24. Localization is UNRESOLVED, not settled-negative; corrected EXPERIMENTS C6, ARCHITECTURE row, journal. Cause: wrote the conclusion from 8 of 16 cells, after already knowing the sampler axis mattered enough to build a flag for it. Neither table is weights-only (each runs one endpoint off its native sampler) — the disagreement is the shape of the result, not noise.
- 2026-09-07 (close) — Kim listened: PT does NOT blend with base, only α=0.5 even listenable and still artifacted. C6 closed NEGATIVE. Falsifies the linear-mode-connectivity justification the arm rested on (a fine-tune can leave the regime; 997/1019 identical tensors ≠ blendable). Retro-explains the sampler disagreement — the descriptors were reading artifact spectra, not a mixture. Process lesson: I built gate+descriptors+page+48 renders before anyone heard one blend; a single clip closed it.
- [2026-09-22] LoRA gauge-drift check (arXiv:2608.07436 → our LoRA B·A symmetry): built eval/lora_gauge_drift.py (Sylvester projection onto the gauge tangent; self-test pure 1.000 / random 0.032 vs 0.032 baseline). audition_160ep_2026-09-22-b: ‖B‖ velocity = ‖B·A‖ velocity to 3 digits (trajectory instrument valid); gauge share of step energy rises 4.8→12.4% (chance 2%), concentrated in cond/global embedders. Also 7 knowledge.md rows for the post-brief optimizer papers + historical-prior PDF critique (unwhitening exponent inverted, Cov∝H⁻¹ misapplied, fabricated §9 table).
- [2026-09-23] A's input subspace frozen (row-space overlap 0.992 step240→1440; B 0.35). LoRA-TSD benchmarked on our 229 adapters: 8.5 s/step (1 inner iter), 31 s (5); torch QR 12-17 ms/call on ROCm, cholQR2 ~2 ms; DoRA-compatible, zero-B step finite. Built --modular-lora-a-lr-mult (unit-tested x10 exact) + run_meta.json in run dir (was overwriting parent). EXPERIMENTS A14.
- [2026-09-24] Consolidated 20 training-failure findings (causes → effects) into docs/training-findings.md; journal pointer for F's DISCOVERIES fold.
- [2026-09-22] Took over Antigravity's modular optimizer: NaN-blind guard + half-loss monitor fixed; mechanism_audit.py (5-6 inert mechanisms found); whitening non-viable (eigh 7.23 s at 12288); covariance probe; audition_160ep clean on every instrument.
- [2026-09-23] Caption routine ported to train_lora_modular (bigset sidecar never matched; source_weights inert in train_lora.py); run_meta into run dir; A-lr-mult arm (confounded; Audiobox says worse).
- [2026-09-24] goa3 NaN root cause (DoRA magnitude zero-crossing) + multiplicative magnitude fix; operator manual + drift check; Gemini triage x2; papers filed with depth sidecars; LoRA-TSD batched port started (4 workers).
