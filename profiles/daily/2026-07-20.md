# Daily digest — 2026-07-20

*(Backfilled 2026-07-30 by THE-FINN from the wire archive — the daily ledger lapsed after 07-19.)*

## What landed
- **GHOST-NOTE** — Shipped Kim's eval-page asks: a native-training-length audition cell per checkpoint (evals were fixed-20s regardless of trained context), hover-to-preview, loop-until-stopped, and a loading indicator across the shared-player eval pages; also swapped DoRA weight 0.6→2.0 in new renders (old 0.6 cells kept, union axis). Caught + fixed his own bug pre-ship — the new native-length cells would have silently collided with the 20s grid cells sharing the same (cfg,strength,prompt) key.
- **WINTERMUTE** — Relayed + recorded Kim's by-ear MILESTONE: the fp32-campaign arms sound better than the bf16 twins — cleaner separation, less noisy high end (worse in very few; occasional less punch, likely source-faithfulness). Flagged PRELIMINARY, not a comprehensive audit. bs1-vs-bs4 and T4096-vs-T2048 parked for G's native-length eval versions.
- **CONTINUITY** — Overnight: drafted the note-following eval spec (the MIDI-conditioner's v1 go/no-go, judged off-distribution to separate memorization from genuine note-following). Ran the gate-#0 stem probe — the full mix yields 1305 notes vs 366 from the bass stem, data backing the full-mix pivot (with a caveat that full-mix collapses toward a near-mono bassline). Cleared W's two deploy-blockers.
- **GHOST-NOTE** — Prune fallout: the overnight optimizer-state prune silently broke 62 eval manifest picks across 23 arms (it swaps fat checkpoints for slim weight-only siblings that load identically). Caught via [skip-missing] logs; redirected 60 picks to their slim sibling and corrected 2 stale registrations.
- **THE-FINN** — Confirmed the prune warning was load-bearing: the models-index build double-counted the final checkpoint (+1 across 28 pruned run dirs) by globbing both fat and slim siblings; fixed and recorded the glob-dedup pattern as a standing gotcha. Also refreshed the ARCHITECTURE reuse-index (last touched 07-08) via a grounded 4-agent inventory — ~60 reusable blocks re-catalogued, several stale facts corrected.
- **WINTERMUTE** — Refactor (Kim's): sliced the single dialogue log into weekly logs with auto-rotation — the live file now holds only the current ISO week, finished weeks archive off (W27/W28/W29 sliced, all 549 entries preserved), same tooling for everyone.

## Kim-verdicts recorded
- **fp32 > bf16 by ear (PRELIMINARY)** — Kim heard the fp32 arms as cleaner separation / less noisy high end than the bf16 twins; recorded to run_meta, clears the board red-marks. Not yet a comprehensive audit.
- **Disintegration gate now MANDATORY** (Kim direct, relayed by C) — no control-head eval may claim "works / usable range / steers X" without first passing every steered clip through the disintegration gate vs its own unsteered baseline. Rationale: a static-buzz head still moves the feature meter, so a feature-authority number alone scores buzz as "working."

## Open decisions waiting on Kim
- **bs1-vs-bs4 and T4096-vs-T2048** — parked for the native-training-length eval cells (fixed-20s evals can't show trained-context differences); Kim's ear to settle once those land.

## Depth
Note-following eval spec + control-head disintegration-gate spec (docs/superpowers/specs, 07-20) · ARCHITECTURE reuse-index refresh · profiles/*.journal.md (07-20 entries).
