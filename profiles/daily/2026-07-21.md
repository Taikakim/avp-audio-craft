# Daily digest — 2026-07-21

*(Backfilled 2026-07-30 by THE-FINN from the wire archive — the daily ledger lapsed after 07-19.)*

## What landed
- **CONTINUITY** — PT-medium adapter transplant (Kim direct, task #55): tested the claim that base-trained L/DoRAs work on the post-trained medium. Mechanically works (8-step renders ~3.2s/clip vs ~8s), and the DSP quant pass showed the base-trained DoRA genuinely expresses on the medium (effect size 0.99–1.91× its base-model effect) — refutes the "output no different from base" claim. CE and Kim's ear still pending (his suspicion is boundary smearing, which DSP can't hear).
- **CONTINUITY** — Layer-landscape results: the mid-stack is load-bearing (L14 alone ≈ the full 24-tap adapter; earlylate collapses), the style/conditioning pathway dominates (timestep/global embeds 5-10× any block) while ARC post-training rises with depth — the −.52 DoRA-vs-ARC per-block correlation is the quantified "why transplants work."
- **GHOST-NOTE / WINTERMUTE / THE-FINN** — Two GPU crashes (the second needed a hard reboot), both from GPU jobs colliding with only DM-courtesy for mutual exclusion; the compositor's GL context looped a faulting command buffer against a VRAM-starved card. Card itself stayed healthy — every ring reset succeeded; a compositor restart cleared it.
- **THE-FINN / GHOST-NOTE / WINTERMUTE** — Long thread hardening the GPU mutex: adopted filelock as a cross-instance GPU lock on one canonical path, made it pid-aware (dead-pid reclaims instantly, a live multi-hour job is never stolen), then fixed a live-use bug where acquire recorded the transient CLI pid instead of the persistent holder (now records the invoking shell, or explicit --pid $$). Paired with the native-render→LUMI rule as the job-vs-display fix.
- **CONTINUITY** — Follow-up #56 verdict: layer-restricted training does NOT reproduce the clean-single-tap magic — L14-alone's earlier "cleanliness" was an amputation artifact (muting 23/24 taps cut effective drive ~24× under the blowout threshold). Hardens the suspicion that this adapter family encodes "more onsets" as HF transients/clicks — exactly the buzz Kim's ear caught in the LatCH review.
- **GHOST-NOTE** — fullft campaign done for real (552 manifest cells across all 10 arms), after finding the loader stripped only a bare `model.` prefix while fullft checkpoints use a deeper one — 0% coverage every prior attempt, masked as success by OOM-only retry logic.

## Kim-verdicts recorded
- **Policy pick: lockfile GPU mutex** (Kim, relayed by W) — acquire the canonical `.gpu.lock` (pid-aware, --pid $$) before any GPU job, release after; the GPU hold lifts, work resumes mutex-gated. Keep the native-render→LUMI rule strict until the iGPU-compositor fix lands.
- **PT-medium adapter transplant** commissioned as task #55 (Kim direct).

## Open decisions waiting on Kim
- **iGPU-compositor (Mode-B) fix** — the job-vs-display OOM path is not hardware-fixed yet; Kim to look at moving the compositor to the iGPU later in the evening. Until then, no local T≥2048 native renders.
- **_ptm transplant — Kim's ear** — DSP says the style signal carries; Kim's actual suspicion (boundary smearing) needs his ears on the _ptm rows.

## Depth
GPU-mutex convention (MASTER §5) · filelock pid-aware changes · layer-profile scripts + #55/#56 results (journal + sa3_control_runs) · profiles/*.journal.md (07-21 entries).
