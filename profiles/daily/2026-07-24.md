# Daily digest — 2026-07-24

*(Backfilled 2026-07-30 by THE-FINN from the wire archive — the daily ledger lapsed after 07-19.)*

## What landed
- **GHOST-NOTE** — Found + fixed a same-handle GPU-concurrency gap (a usage gap, not a filelock bug): three background render chains ran under one handle, and the mutex treats same-handle as "I already hold this," so it never serialized one agent's own parallel chains. Result was ~50min of intermittent self-collisions (VRAM 89%+, one genuine 0-free OOM), no lasting damage. Fix: a distinct handle per concurrent chain (the pattern C already uses for her pilot).
- **THE-FINN** — Confirmed + ledgered the same-handle hole as a MASTER §5 rule (one distinct handle per concurrent GPU chain) and added a code tripwire: filelock now barks a loud concurrency warning when a same-handle lock is held by a live different pid, turning the silent 50-min gap into an immediate signal (verified silent on the two legit same-handle cases — dead-pid refresh and same-pid re-hold). Offered to flip warn→block for true serialization pending fleet buy-in.
- **THE-FINN** — Relayed + wrote in the LUMI self-eval directive (Kim, re-affirmed): every LUMI training run creates its own full evals as part of the run — the correct eval family per the canonical spec (§1 control-adapter gain×density grid or §2 plain-DoRA sweep, not interchangeable), the mandatory disintegration gate for any control-adapter checkpoint, and the standard semantic columns. A run isn't "done" until its evals exist; nothing waits on the scarce local card. Made it a self-contained standing directive so it stops needing re-broadcast.

## Kim-verdicts recorded
- **LUMI self-eval directive re-affirmed** (Kim) — each LUMI run renders + scores its full eval suite on LUMI as part of the run; checkpoints land audit-ready and comparable, with no dependence on the local card.

## Open decisions waiting on Kim
- **Warn-vs-block on the same-handle tripwire** — THE-FINN can make the live-different-pid case block (actual serialization) rather than warn; held for G/W/C buy-in since it changes shared mutex semantics mid-runs.

## Depth
Same-handle gotcha (open-threads standing-gotchas, MASTER §5) · LUMI self-eval standing directive (canonical-eval-spec.md ⭐ block) · profiles/*.journal.md (07-24 entries).
