# Daily digest — 2026-07-22

*(Backfilled 2026-07-30 by THE-FINN from the wire archive — the daily ledger lapsed after 07-19.)*

## What landed
- **CONTINUITY** — Melody-encoding probes (the day's headline): melody lives in a distributed ~15-dim linear subspace (no pitch channels — full-width probes only), single deviant notes are 100% decodable but along timbre-specific directions (multi-timbre head training mandatory), and melody is ~8.5-9.3% of latent variance (~1.6× random) — the quantitative backbone for the Head-B melodic-loss case. v2 (900 renders, CIs) exonerated the encoder: a lone fifth-jump is ≥91% detectable at all BPMs/timbres incl. pure sine, so the bottleneck is DiT training/sampling, not the encoder. Two follow-ons closed the causal chain: training-free chroma steering can't turn melody A→B (0/12, gates clean = authority shortfall, not a quality ceiling), and hook-melodic-ratio falls cfg7→cfg16 in 8/10 families (CFG-averaging kills melodic commitment).
- **CONTINUITY** — Probe hazard flagged: a pitch-independent 0.4-1.3 Hz oscillation dominates raw-latent autocorrelation — high-pass before any periodicity work.
- **THE-FINN** — Traced that oscillation hazard to two shipped tools that eat raw latents and measure periodicity in that exact band; scoped it honestly (confirmed they read raw latents; did not confirm actual corruption) and logged it as a standing gotcha so the two consumers aren't forgotten. Also fixed a filelock re-acquire edge (a dead-holder re-acquire kept the stale pid) that C caught in live use.
- **WINTERMUTE** — Eval-toolkit findings (Kim's "are these useful" call): CLAP is the missing prompt-adherence axis, best used as a genre-degeneration detector ("psytrance must not collapse to drone/noise") — use the absolute score and its drop, never fine intra-genre ranking (19% top-1). A 31.6k-cell scan independently reproduced the established quality ordering (rank-16 collapses out-of-genre ~40%, rank-128 ~3%; cfg1 is the degeneration zone). Separately landed a DoRA hyperparameter×metric analysis with new levers (alpha<rank beats alpha=rank; lr flat then cliffs at 6e-4; aug10 is corpus-best; "bigger batch better" is a T1024 confound).
- **THE-FINN** — Patrol flag for Kim's return: W's "fp32≈bf16" finding is on a different axis (genre-adherence + DSP-buzz) than Kim's 07-20 "fp32>bf16" (fidelity / separation / high-end noise) — both can be true, so it retires one axis, not the fp32 arms; Kim's ear still owns the fidelity call. Also confirmed CLAP-degeneration and DSP-buzz are statistically independent, so the two screens are genuinely complementary.
- **CONTINUITY** — Musicology study shipped (2773 goa transcriptions): 4 substyles, 91% ride a same-note 16th pedal, and memorability is now quantified — hooky tracks repeat their best 4-note contour ~56× (bottom decile: 0×), so hook-melodic-ratio ≈0 is the measurable fingerprint of the models' missing-melody failure.

## Kim-verdicts recorded
- None recorded today (Kim offline for sleep from ~03:00; fleet worked the autonomous worklist).

## Open decisions waiting on Kim
- **CLAP degeneration threshold** — the collapse-flag audition ladder is teed up for Kim's threshold call.
- **fp32 arms go/kill** — W's analysis retires the genre-adherence axis only; the fidelity verdict still hangs on Kim's ear (his 07-20 call was preliminary).

## Depth
Melody probe reports (eval/musicology/latent_melody_analysis, melody v1/v2) · CLAP + DoRA-hyperparam analysis (eval/clap_score.py, clap_hyperparam_analysis.md) · musicology REPORT-2026-07-22 · profiles/*.journal.md (07-22 entries).
