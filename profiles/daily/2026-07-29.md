# Daily digest — 2026-07-29

*(Backfilled 2026-07-30 by THE-FINN from the wire archive — the daily ledger lapsed after 07-19.)*

## What landed
- **GHOST-NOTE** — Found + fixed a manifest-coverage-≠-playability gap: 11,285 rendered model_matrix clips existed only on the Mantu render drive and were never synced to the served staging dir — unplayable on every board despite showing as "covered" in the manifest. Caught because a metrics pass reported "0 to do" right after a render he'd watched finish. Fixed via rsync (0 remaining gap); flagged that manifest-count checks verify render+manifest completion but NOT actual playability, and that no general sync tool exists yet.
- **THE-FINN** — Confirmed the gap dovetails with a 07-28 DSP pass (the source-only Mantu WAV rows in clip_metrics.db were exactly those 11,285 unsynced clips) and that the served staging path is a symlink to one physical dir, so the rsync landed correctly with no second stage to sync. Ledgered both the playability gap (with real-fix ownership) and a dual-DSP-source dedup note (prefer WAV, dedup by basename).
- **CONTINUITY** — Head-B bracket eval landed as a MILESTONE: the FiLM melody-contour conditioner DOES steer melody, but only at cfg16 and modestly, strengthening through training (peak own-vs-wrong confidence +0.227; gates 4/4 clean everywhere = real steering, not buzz). cfg1 inert, cfg7 weak — a soft controller that passes the direction-check pilot and confirms the readout-vs-conditioning framing (the ~0.27 readout ceiling did not bound conditioning).
- **CONTINUITY** — Launched a data-prep build-out over a large local goa archive on the Mantu drive (Kim away ~4 days): Stage 0 CPU MIR features running detached (non-GPU, ~9h); later stages run on a curated subset and will take the GPU lock pid-aware as they come up.

## Kim-verdicts recorded
- None recorded today (Kim away ~4 days).

## Open decisions waiting on Kim
- **Head-B acceptance close-out** — THE-FINN flagged two carries before acceptance closes clean: whether W's two semantic columns (mood_drift melodic + CLAP genre-hold) were run on the adoption-vs-null pair, and the font-span constraint (the pilot was goa-subset only, so fonts-not-patch-numbers is unexercised) — both roll into the production build.

## Depth
Manifest-coverage-≠-playability gotcha + dual-DSP-source note (open-threads) · Head-B bracket summary (findings.json) · goa-archive build-out plan · profiles/*.journal.md (07-29 entries).
