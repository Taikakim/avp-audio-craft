# Daily digest — 2026-07-23

*(Backfilled 2026-07-30 by THE-FINN from the wire archive — the daily ledger lapsed after 07-19.)*

## What landed
- **CONTINUITY** — Head-B melody-conditioner build STARTED (Kim direct): melody-contour conditioning via the sa3_control FiLM path, local pilot queued (goa subset, r128 + melody-embedding, 4ep; adoption-vs-null plus W's two semantic columns as gates). Clarified for the record that last night's ~0.27 ceiling killed the lightweight readout (Head-A probes), not conditioning — Head B trains the model to USE the stream, no readability required (onset-envelope control is the working precedent). Multi-source dropout ships inside the build.
- **CONTINUITY** — Head-A night verdict: per-frame melody-contour readout on real mixes caps at ~0.27-0.28 bacc everywhere (all 12 DiT taps, GRU, z0). The readout is domain-bound (in-domain synthetic decodes at .79-.89), not proven representation-limited — so Head-A free-metric use is dead and it rescopes to coarse presence/pedal heads trained multi-domain.
- **CONTINUITY** — Two GM timbre studies set the training rules: the timbre-pitch atlas (120 programs) shows deviant-note detection near-ceiling for all timbres even where absolute pitch doesn't transfer (contour/interval-class is the timbre-robust representation); the multi-font probe (640 variants) delivered the mix-killer finding — solo-trained decoders die on two-voice mixes and drums crush lead decodability (offbeat hats worse than kicks), so Head A/B must train mix-native (clean-stem training contraindicated) and sample fonts, not patch numbers.
- **WINTERMUTE** — Confirmed his lane on the Head-B build: the two semantic acceptance columns (mood_drift melodic retention + CLAP genre-hold) are live to score the pilot the moment renders exist, and proposed a "melody source" board axis (null / stored-contour / cross-track-donor) parallel to cfg×strength for the eventual A/B.
- **THE-FINN** — Ledgered the Head-B build in open-threads with its acceptance gates and the three probe-arc design constraints (contour-not-absolute, mix-native, fonts-not-patch-numbers) tracked so they can't silently drop out of the implementation.

## Kim-verdicts recorded
- **Head-B melody-conditioner build commissioned** (Kim direct) — implement melody-contour conditioning now.

## Open decisions waiting on Kim
- None new today.

## Depth
Head-A ceiling trail (eval/musicology/head_a_ceiling) · GM timbre-pitch atlas + multi-font probe (eval/musicology/gm_*) · Head-B pilot spec §2/§8 · profiles/*.journal.md (07-23 entries).
