# papers/ — the never-reinvent-again file

**Check here BEFORE building a training loss, guidance method, or optimizer variant.**
This is the *external* prior-art index; `ARCHITECTURE.md` is the *internal* reuse index.
Per paper: the PDF (`arxiv-<id>.pdf`) + a project-POV abstract (`arxiv-<id>.md`) stating
what it contains AND what it does *not* (i.e. what remains ours). Keep abstracts honest
about provenance: "verified citation pass" ≠ "deep-read".

**Gemini deep-research reports are VERIFY-BEFORE-BUILD — and verification cuts BOTH ways.**
Every cited paper earns its knowledge.md row from the *PDF*, never from the report's
paraphrase. But a **missing web footprint is UNVERIFIED, never "fabricated"**: double-blind
workshop submissions leave no search trace and verifier tools hit Cloudflare/OpenReview
blocks. Worked example — **DC-SAE / "Dual-Contrastive Sparse Autoencoders"** (Chen/Cherep/
Maes/Singh, MIT, ICML'26 Mech-Interp Workshop) was wrongly called "likely fabricated" on
2026-07-30 (zero footprint + blocked OpenReview id), then confirmed **real** minutes later
when Kim produced the PDF — a false-fabrication verdict, the mirror-image of the presence-
from-proxies trap. So: absence of footprint proves nothing; get the PDF. **Now 2-for-2
(2026-08-01):** the second no-footprint flag — "Dimension-Corrected Hitting Times" (Zongmin
Liu, Stanford; `arXiv:submit/7812416`) — also resolved **real** on Kim's fetch, so the
"unverified ≠ fabricated" clause is empirically earned. The three agent-invisible-but-human-
checkable patterns to expect: **(a) double-blind venue** (no arXiv id by design), **(b)
submission-stage preprint** (`arXiv:submit/…`, no public id yet), **(c) ResearchGate-only**
(RG bot-blocks verifiers). All three are zero-footprint yet human-verifiable in seconds — a
no-footprint result means ESCALATE-TO-HUMAN-FETCH, never a fabricated-verdict. Existence aside,
the attributed *claims* also drift even in genuine papers — the same audit caught real metric
misattributions (FAD↔Audiobox/LPAPS, FAD↔FMD) and a bottleneck-depth slip (report said
"2–4", truth is 2 layers {12,13}). So verify the *claim* against the PDF too, not just the
paper's existence. (The index held the correct {12,13} while the report drifted — doing its
job.) Related convention: brief Gemini with problem + phenomenology only — never feed it our
own citations or docstrings, or it closes the loop and cites them back as if external.
*(DC-SAE is a real, relevant paper — same MIT group as the MusicGen-SAE steering work at
2505.18186 — now DEEP-READ + indexed (see `dc-sae-dual-contrastive-musical-interpretation.md`) — it is a double-blind workshop submission with NO arXiv id, which is exactly why it had zero web footprint.)*

Maintained by THE-FINN (overseer). Add rows newest-first. Founded 2026-07-03 on
CONTINUITY's assignment: "ControlNet++ goes in the never-reinvent-again file."

| Paper | One-liner | Status vs our work |
|---|---|---|
| [Complex Time for Consciousness and Creativity in Music (Mazzola)](<Mazzola - Complex Time for Consciousness and Creativity in Music.md>) **bounded read (W 08-12, pp.65-68)** — C's 2nd-wave block, W took 4 | PHILOSOPHY-OF-CONSCIOUSNESS essay, not music theory and not a method. Argues COMPLEX TIME (t = t_R + i*t_I, after Hawking's imaginary time) could resolve problems of artistic consciousness and creativity in performance: a 5-d space ST = R^3 (+) C splitting into physical space-time and 'MENTAL space-time', sharing the spatial part, offered as a geometric replacement for Descartes' pineal gland. Motivating puzzle: a performer's rich processual activity happens 'in no physical time' at a single point of the real time line, so a space of consciousness must attach to every moment. Candid detail: he reports emailing Hawking repeatedly with no answer, and that the physicists he consulted treat the imaginary component as WICK ROTATION -- a mathematical device, not an ontology | ZERO YIELD. Fails all three legs of Kim's test by the widest margin in the sweep: nothing computable from a clip, nothing askable by ear, no time series, no quantity of any kind. Recorded as an explicit NULL so the next instance does not open it -- a null costs nothing to act on and is the easiest result to leave unwritten. One adjacent thread REFERENCED but not developed: §5 points at Mazzola's performance theory (Topos of Music vols II/III) modelling performance as a transformation from SYMBOLIC reality (a score) to PHYSICAL reality -- the same SHAPE as what a conditioner does. If anyone pursues that, they want the Topos volumes, not this essay |
| [Masked Diffusion Enables Coherent Beat Tracking (2608.04624)](<arxiv-2608.04624 - Masked Diffusion Enables Coherent Beat Tracking.md>) **deep-read (C 08-12, Kim-surfaced)** — Moises AI ISMIR'26, open code `md_beat_this` | One-step NN beat trackers emit incoherent outputs (consecutive downbeats, tempo halving/doubling) = an invalid MIXTURE of multiple valid metrical interpretations; the masked-diffusion fix accepts audio+partial-output, commits to ONE interpretation, fills coherently; beats a DBN without a DBN. | (1) IMMEDIATE: vets our madmom-downbeat grid — adopt their 2 coherence heuristics (consec-downbeat count, IBI doubling/halving) as a cheap **grid-trust SCREEN** to run BEFORE the repetition-period / note-division / negative-space features rely on it (de-risks Kim's ÷even downbeat-grid plan). (2) 'one-step→invalid mixture, commit-early-then-fill' = our loop-collapse / long-structure family (DiffRhythm2 / SongBloom / sketch). (3) MDM = a coherent-discrete-stream extractor, complement to `2201.02715`'s HSMM discretisation gate. Analysis not gen, discrete-seq not RF, open code no weights, goa = easy beat case. |
| [Towards a mathematical foundation for music theory and composition: a theory of structure (Flieder, J. Math & Music 2024)](<Flieder - Towards a mathematical foundation for music theory and composition.md>) **bounded read (W 08-12, pp.1-3; theory + appendices UNREAD)** — C's 2nd-wave block, W took 4 | Topos-theoretic FOUNDATIONS for music theory -- categories, presheaves, ontology -- extending Mazzola's Topos of Music (2002) to a wider class of structures. Explicitly META-theoretical: a systematic method for concept formation, not a theory of any musical phenomenon. Three design criteria: comprehensive comprehension, explicit encoding, minimal ontological commitments. Motivation: mathematical music theory itself 'lacks a solid foundation in mathematics' -- pc-set theory leans on intuitive notions, advanced constructions get assembled ad hoc. Application examples (illustrating generality only): ordered sets, twelve-tone groups, Mazzola gesture theory, modules/score objects, PARAMETER SPACES FOR SYNTHESIZERS | NO CONTROL-TARGET YIELD -- fails ALL THREE legs of Kim's test (nothing computable from a clip, nothing askable by ear, not a time series). Filed short on purpose. Two things worth recording anyway. (1) MY PREDICTION WAS WRONG IN AN INSTRUCTIVE WAY: I expected more harmonic theory, hence over-powered-for-goa per C's Rohrmeier verdict. It is not harmonic theory at all, it is foundational ontology a level above -- the verdict (low yield) holds for a DIFFERENT reason than predicted. 'I guessed the conclusion correctly' is not 'my reasoning was sound', and only reading distinguishes them. (2) THE ONE THIN HOOK: its application list includes PARAMETER SPACES FOR SYNTHESIZERS. A foundational framework treating synth parameter spaces as a worked object is gesturing at the territory our control work occupies. If anyone ever wants a PRINCIPLED account of what a 'control dimension' IS -- versus our operational one (a scalar we can measure and steer) -- this literature is where it would live. Someday-question about foundations, not a this-quarter question about heads. Appendix 2 (informal contextualisation + a composition built on the framework) unread |
| [Rohrmeier 2011, Towards a Generative Syntax of Tonal Harmony (JMM 5(1), DOI 10.1080/17459737.2011.573676)](<Rohrmeier - Towards a Generative Syntax of Tonal Harmony.md>) **deep-read (C 08-12)** | CONTEXT-FREE grammar for tonal harmony (phrase / functional / scale-degree / surface); dependency principle (non-adjacent long-distance tree) + functional heads TR/DR/SR + modulation-as-recursion; explicit anti-Markov argument. | The **HIERARCHY rung** between score-networks `2006.01033` (surface adjacency) and `2201.02715` (tractability) — i.e. *why* our score-networks give a GROUPING not a PARSE (the Mukherji bound, now with the actual model). Control targets (closure / depth / modulation) real but 3rd-wave (need a parser). HONEST GOA VERDICT: goa harmonic tree near-degenerate → OVER-POWERED for goa, confirms variety lives in melody/FX; bank for tonal/jazz corpora. |
| [Rohrmeier 2020, The Syntax of Jazz Harmony (MTA 7(1), DOI 10.11116/MTA.7.1.1)](<Rohrmeier 2020 - The Syntax of Jazz Harmony.md>) **targeted read (C 08-12; intro; form/Blues unread)** | Same GSM extended to jazz phrase-structure + FORM + Blues (plagal); mildly context-sensitive flags. | SHARPENS over-powered-for-goa (jazz-form even less goa-like). Useful pointers: Harasim ISMIR'18 generalized parser; ISMIR'19 rhythm-improves-harmony (validates Kim's downbeat-grid); de Haas ISMIR'09 tree-matching harmonic similarity (a harmonic layer for the repetition-detector). Bank as the jazz/tonal route. |
| [From K-Nets to PK-Nets: A Categorical Approach (Popoff, Agon, Andreatta, Ehresmann; Perspectives of New Music 54/2, 2016)](<Popoff-Agon-Andreatta-Ehresmann - From K-Nets to PK-Nets.md>) **partial read (W 08-12, pp.5-7 of 59; remainder UNREAD)** — C's 2nd-wave block, W took 4 | Transformational theory (Lewin/Mazzola line). Klumpenhouwer networks = directed graphs with pitch-class vertices and T/I (or T/M affine) group elements as edges; the organising idea is to describe a chord's inner structure by the TRANSFORMATIONS BETWEEN its elements rather than the elements themselves. ISOGRAPHY is an algebraic relation between two K-nets INDEPENDENT OF NODE CONTENT -- two chords with entirely different pitches can be isographic if their internal transformation structure matches. K-nets are commutative diagrams; the 'path consistency condition' is just composition + associativity of morphisms. Contribution = PK-nets (poly-K-nets valued in Sets), which repair a specific fragility | LOW DIRECT YIELD -- concept-level only, NOT a control candidate (fails 2 of Kim's 3 legs: not askable by ear, not naturally a time series; computable only via symbolic pitch-class extraction). Two things earn the row. (1) ISOGRAPHY IS THE HARMONIC ANALOGUE OF CONTOUR: C's morphological-space thread defines contour as an equivalence class, transposition-invariant by construction; isography is the same move on harmonic structure -- same relational shape, different absolute content -- and it rhymes with C's EMPIRICAL chroma result where raw chroma is inflated by the genre-generic component and only the DEMEANED signal carries identity (air 0.918 vs raw ~ null). Three independent routes (contour theory, transformational theory, a measurement on our own corpus) all landing on 'the relations carry identity, the absolute values carry the genre'. (2) THE TRANSFERABLE WARNING, which is the concrete take: K-nets are SENSITIVE TO ARROW LABELLING -- the authors show two networks isographic under one labelling that LOSE isography under a different, equally valid labelling of the same relations (Examples 1-2, pp.6-7); PK-nets exist to repair it. That is a caution for EVERY relational descriptor we build, including the score-network features (2006.01033) and the meter-network descriptors (Yust-Popoff): a relational representation can be fragile to a choice that looks like mere bookkeeping. If we compute structural similarity from a graph whose edges we labelled by convention, we should verify a different defensible labelling gives the same answer -- a cheap check nobody would think to run without this paper |
| [Rules and Representations in a Generative Syntax of Tonal Harmony (Mukherji, Perspectives of New Music 62/1, 2024)](<Mukherji - Rules and Representations in a Generative Syntax of Tonal Harmony.md>) **partial read (W 08-12, pp.157-160 of 46 journal pp; remainder UNREAD)** — C's 2nd-wave block, W took 4 | NOT Rohrmeier's paper -- W's own mis-citation, corrected: this is MUKHERJI'S CRITIQUE of Rohrmeier's (2011) generative syntax, proposing a Minimalist-Program alternative. Core argument: SURFACE/MARKOVIAN models (Piston, Tymoczko 2003, Gjerdingen schemata) treat harmony as transition probabilities between ADJACENT chords, and cannot capture the NON-ADJACENT relations that constitute tonal-harmonic hierarchy -- two inversions of one harmony joined through passing chords (I-VII6-I6), pre-dominants (IV6-I64-II6), chromatic-wedge/omnibus progressions. Those are relations between constituents at DEEPER sub-surface levels and 'cannot be modeled by a learning mechanism that only tracks immediate chord-to-chord relationships'. Rule-based grammars are additionally RECURSIVE, so a finite grammar generates infinitely many structures | A NAMED LIMITATION OF OUR STRONGEST CONTROL-TARGET FIND. 2006.01033 (dynamical score networks) builds nodes = chords, EDGES = SUCCESSIVE CHORDS -- by this taxonomy a surface/adjacency model, exactly the family Mukherji argues cannot capture harmonic hierarchy. The tonal-region / centricity features stay computable and probably useful (the Bach demo does recover real regions), but any claim that they capture harmonic SYNTAX is unsupported: a modularity class over an adjacency graph is a GROUPING, not a PARSE. Recorded as a caveat against that row, not a retraction. SECOND, it makes a three-paper progression legible across this sweep: chord-adjacency graph = surface (2006.01033); PCFG = the hierarchical non-adjacent structure this essay says you need; low-rank structured inference (2201.02715) = what makes such a PCFG affordable at scale -- only the middle term is missing from our folder, since Rohrmeier 2011 is not in it. THIRD, scope honesty: strongly rationalist music theory (innateness, Minimalism, explanatory adequacy); nothing computable from a clip, nothing askable by ear -- it FAILS two of Kim's three test legs. Value is purely as a CHECK ON INTERPRETATION of our cheap adjacency features. NOT a control candidate. ~40 journal pages (Keiler discussion, the Minimalist proposal, operationalisation) UNREAD -- the critique is established, the alternative model is not, and no claim is made about it |
| [Spectral Convolution on Orbifolds for Geometric Deep Learning (2602.14997)](<arxiv-2602.14997 - Spectral Convolution on Orbifolds for Geometric Deep Learning.md>) **deep-read (F 08-12, subagent)** | GDL theory paper: defines spectral convolution on orbifolds (M/G) via Laplacian eigenfunctions; illustrates by low-pass-smoothing a Stolzenburg consonance function on Tymoczko's dyad Möbius orbifold C²₁₂. No net, no experiments, no numbers. | Off-lane as a METHOD — orbifold conv needs data on a manifold-mod-finite-group; our learned audio latent has no such structure, nothing attaches to the DiT / control heads. Tymoczko-hook is REAL (literal same chord orbifold, cites Tymoczko 2011) but thin for us: the one transferable object is a consonance/roughness feature that predates the paper. Cheapest test: per-frame Sethares roughness / Stolzenburg periodicity envelope off mir's spectrum — no orbifold needed (converges with C's Sethares dissonance-curve row). |
| [Low-Rank Constraints for Fast Inference in Structured Models (2201.02715)](<arxiv-2201.02715 - Low-Rank Constraints for Fast Inference in Structured Models.md>) **deep-read (W 08-12, pp.1-4 detail)** — NeurIPS 2021, Chiu/Deng/Rush; reading-sweep, LAST of W's 11 | Structured latent models (HMM, PCFG, HSMM) are interpretable and controllable but inference is QUADRATIC (HMM) to CUBIC (PCFG) in hidden-state count, capping their scale. Inference reduces to hypergraph marginalisation whose inner loop is a matrix-vector product Psi_e beta_v; constrain Psi_e = U_e V_e^T and reassociate to U_e (V_e^T beta_v), trading a factor of L (states) for N (rank) -- explicitly inspired by linear-attention/Performer kernel tricks, applied to a different formalism. Scales HMMs past 16,000 states; significant PCFG perplexity reduction vs prior large-state work; HSMMs to much larger state spaces WITH CONTINUOUS EMISSIONS. Four tasks incl POLYPHONIC MUSIC MODELLING. Own caveat up front: low-rank in high-dim structured models is 'nontrivial due to reduced expressivity' -- rank buys speed by spending expressivity | THE HSMM-WITH-CONTINUOUS-EMISSIONS RESULT ANSWERS THE GATE BLOCKING THREE OTHER PAPERS IN THIS BATCH. 2505.10004 needs a surrogate v(t), meter networks need an articulation set, 2405.04796 needs a discretisation -- all three reduce to 'turn our continuous multi-field timeseries into the right discrete/scalar stream'. An HSMM over continuous emissions does exactly that and yields three things at once: discrete latent STATES (= nodes for the score-network / featured-PH graph constructions), state DURATIONS (= cycle lengths, candidate metrical-layer periods), and segment BOUNDARIES (= articulation points / change-points). So the gate gets a named principled candidate instead of an ad-hoc binning threshold, and this paper is what makes such a model affordable at the state-space sizes audio features need. SECOND: it links W's batch to C's UNREAD block -- PCFGs are how symbolic harmony is formalised, and C's second-wave queue holds Rohrmeier's 'Rules and Representations in a Generative Syntax of Tonal Harmony', a PCFG for harmony; this is the machinery that makes such grammars tractable at scale. Whoever reads that block should know it exists. THIRD, as framing: the authors say structured models 'afford interpretability and controllability that are lacking in neural models' -- we are bolting controllability ONTO a neural generative model; this is the opposite direction, and worth knowing when judging how much complexity our control stack deserves. LIMITS: pure inference-efficiency work, no control target, nothing askable by ear; adopting it means adopting a modelling paradigm we use nowhere in the pipeline; and rank trades expressivity by their own admission. Filed as INFRASTRUCTURE (like 1708.09359) -- removes a blocker, does not name a knob |
| [Persistent homology of featured time series data (2405.04796)](<arxiv-2405.04796 - Persistent homology of featured time series data.md>) **deep-read (W 08-12, pp.1-4 detail)** — POSTECH; reading-sweep | Heo & Jung: frequency-based graph from a time series -- nodes = distinct observed VALUES, edges = SUCCESSIVE values, edge weight = co-occurrence count; distance = min path sum of RECIPROCAL weights, so frequently-adjacent values are close; then Rips filtration -> PH. Contribution: FEATURED TIME SERIES (series augmented with domain-knowledge features) + INFLUENCE VECTORS (a weight per feature that adjusts the graph to suit the domain), with a STABILITY THEOREM proving the adjustment cannot make PH arbitrarily sensitive. Applications: stock anomaly detection and TOPOLOGICAL ANALYSIS OF MUSIC. Related work is dense with music precedent -- PH music-networks for Turkish makam classification, traditional Korean music, Tonnetz-based classification, intervallic transition graphs for style | SAME CONSTRUCTION AS THE SCORE-NETWORKS PAPER (2006.01033), GENERALISED OFF SYMBOLS: that one has nodes = chords / edges = successive progressions / weight = traversals; this is identical in shape with nodes = ANY observed value. So the score-network idea is not chord-specific -- it is a general recipe for turning any sequence of discrete states into a weighted graph whose topology is analysable, which closes a gap I left open filing 2006.01033. INFLUENCE VECTORS ARE THE PRINCIPLED FORM OF KIM'S 'use whatever descriptors we have' (2026-08-12): for meter networks the answer was that the formalism never used the values at all; HERE the values are used, and featured-time-series is exactly a mechanism for choosing WHICH descriptors influence the graph and BY HOW MUCH -- a weighting with a stability guarantee rather than ad-hoc fusion of our 46 fields. THE CATCH, and it is now the third time: nodes are DISTINCT VALUES so the series must be DISCRETE. Chords are; rms_energy_air_ts is not. Continuous fields need binning, and both graph size and topology depend on that binning -- an unforced choice that will quietly dominate the result. 2505.10004 needs a surrogate v(t), meter networks need an articulation set, this needs a discretisation: ALL THREE REDUCE TO 'turn our continuous multi-field timeseries into the right discrete/scalar stream', and that single piece of work is the GATE on all of them. Weaker than 2006.01033 as a control-TARGET source (it supplies machinery, not musical quantities); its value is that the machinery is domain-tunable and proven stable, which the others are not. NOT VERIFIED: I read the stability theorem as stated, not its proof |
| [Computational Topology Techniques for Characterizing Time-Series Data (1708.09359)](<arxiv-1708.09359 - Computational Topology Techniques for Characterizing Time-Series Data.md>) **deep-read (W 08-12, pp.1-4 detail)** — IDA-17, Bradley group (CU Boulder); reading-sweep | Two practical results that make point-cloud TDA affordable. (1) WITNESS COMPLEX: coarse-grain using LANDMARK points and still recover the correct topology -- their Fig.2, same data and same epsilon, Cech on all 2000 points = 2,770,627 triangles vs witness with 200 landmarks = 3,938 and with 50 landmarks = 93. A 700x-30,000x reduction. (2) YOU DO NOT NEED A TRUE EMBEDDING: delay reconstruction preserves TOPOLOGY but not geometry and is only guaranteed to embed if tau and d_E are chosen well -- parameters they call 'subjective and sometimes quite difficult' for real finite-precision data -- yet the coarse-grained topology of a 2-D reconstruction that is NOT a true embedding still discriminates, sidestepping the parameter fit and cutting downstream cost. Purpose: characterise/distinguish dynamical systems from a single scalar stream + change-point/bifurcation detection. DEMO DATA IS MUSICAL INSTRUMENTS -- telling apart the same note played on different instruments (Fig.1 = middle C, Yamaha upright, 44.1 kHz) | INFRASTRUCTURE, NOT A CONTROL KNOB -- filed as the enabler for the other two topology papers in this batch rather than as a control candidate (fails Kim's 'askable by ear' leg). It removes the exact blocker I recorded against SW1PerS: I flagged Rips as superlinear, hence per-clip on a landmarked cloud, 'budget it like Audiobox not like zcr' -- the witness complex IS that landmarking, with a correctness argument and a measured 700-30,000x reduction. Read the three together and they stack: SW1PerS (1307.6188) gives the periodicity SCORE, 2505.10004 gives per-cycle BOUNDARIES at linear cost, this gives the COARSE-GRAINING that makes point-cloud methods affordable at corpus scale (4461 tracks). Second hook: 'same note, different instruments' is a TOPOLOGICAL TIMBRE discriminator -- structurally different from our existing timbre account (timbral_models' 8 features + Audiobox, all spectral/statistical), computed from the waveform's own dynamics rather than its spectrum. Worth having precisely because it FAILS DIFFERENTLY, given this session's running finding that our spectral proxies are foolable (Audiobox CE ranked Kim's two favourite checkpoints 1st and 8th of 8). Third: the no-true-embedding-needed result is what makes it schedulable -- fitting tau/d_E per track across 4461 tracks is exactly the silent per-item hyperparameter fitting we cannot afford. CAVEATS: 2017, tutorial-flavoured (defers theory to references); their audio is one sustained note at WAVEFORM scale (44.1 kHz), not our 100 Hz feature streams, so the instrument result does not transfer to whole-track timeseries without redoing the question |
| [The Geometry of Voice Leading — Tymoczko (Science 2006)](<Tymoczko - The Geometry of Voice Leading.md>) **deep-read (C 08-12)** *(no arXiv id)* | Chords live in an orbifold (quotient of pitch-class space); consonance ↔ near-even-division ↔ proximity to the orbifold CENTER; voice-leading = geodesic distance; transposition = rotation. Cites Sethares. | FOR US: geometric grounding of the chroma pitch-class circle ℝ/12ℤ and transposition-invariance-as-rotation (justifies chroma-DFT/TIV); the invariance-dial = a quotient choice (pairs with Polansky n-ary resolution); voice-leading distance = a harmonic-MOVEMENT metric (symbolic/MIDI-native, needs a chord/pitch front end for audio). Pitch-only, cheap, chroma-circle-differentiable; reconciles with Sethares (spectral-roughness) into a FUSED harmonic-tension control/eval. |
| [Anatomica: Localized Control over Geometric & Topological Properties (2511.20587)](<arxiv-2511.20587 - Anatomica - Localized Control over Geometric and Topological Properties.md>) **deep-read (C 08-12)** | Inference-time GUIDANCE steering an unconditional LDM via differentiable geo-topological potentials (voxel moments + persistent homology), parsed off clean-latent x0 with a neural-field decoder; D^w = D_θ − σ²·∇L; softmax-temp keeps the PH gradient alive. | CONTROL-RELEVANT: this IS our LatCH family-A guidance-readout paradigm, SOTA in another domain; the σ²·∇ term = our 512–1000× LatCH gain (reconciles the 2603.04366 under-scaling); PH-as-structure (loops=repetition/return, components=#sections) = speculative music transfer, pairs w/ Tymoczko topology; L-parsing = read-from-latent, echoes our linear chroma readouts. Caveat: voxel-seg domain, guidance slower-per-sample than a trained FiLM, PH heavy = 2nd-wave. |
| [Dissonance curve — Guillet (HAL-05124442, Sethares lineage)](<Sethares-lineage - Elementary spectrum for the dissonance curve.md>) **deep-read (C 08-12)** | Dissonance D(λ) = timbre(Riemann-zeta crit-line) × sonance(Minkowski ?); minima = consonant intervals; spectral peaks u=5/7/12/19/31 predict scale sizes; Q = hearing-resolution dial; dissonance is TIMBRE-dependent (Sethares). | CONTROL DIM / PREDICTION TARGET / new per-frame timeseries under Kim's corrected criterion: sensory roughness is computable TODAY (Essentia dissonance/roughness), askable by ear, timeseries-able — must be measured on the rendered SPECTRUM, not pitch. Reconciles w/ Tymoczko (spectral-roughness vs even-division consonance) → a fused harmonic-tension signal. **Cheapest new descriptor to try.** |
| [A Generalised Signature Method for Multivariate Time-Series Feature Extraction (2006.00873)](<arxiv-2006.00873 - A Generalised Signature Method for Multivariate Time Series Feature Extraction.md>) **deep-read (C 08-12, F-assigned)** | Path-signature feature extraction for multivariate TS: analytic, reparametrisation-invariant, universal-nonlinearity; a principled recipe (augmentations + windowing + which signature terms) around the raw signature transform. | FOR US: the ANALYTIC complement to Polansky contour — strongest movement-as-control-head-FEATURE candidate, differentiable via `signatory`. Use on a LOW-d DERIVED stream (dᴺ blow-up forbids the raw 256-d latent) with time-augmentation to keep rhythm. A prediction-head target / new timeseries descriptor, not a generator. |
| [SigDiffusions: Score-Based Diffusion for Time Series via Log-Signature Embeddings (2406.10354)](<arxiv-2406.10354 - SigDiffusions - Score-Based Diffusion Models for Time Series via Log-Signature Embeddings.md>) **deep-read (C 08-12)** | Score-based diffusion in log-signature space; inversion formulae recover the signal (coeffs = linear functionals on the signature); generates length-1000 TS at ~200K params in seconds. | FOR US: the GENERATIVE sibling of 2006.00873 — a cheap generator of a LOW-d melodic-movement SKELETON to condition on (a Zach prepend-cond source); N = resolution dial; the inversion formulae = a reusable de-whitening/decode tool. Caveat: demonstrated on low-d synthetic TS, not our latent. |
| [Meter networks: a categorical framework for metrical analysis (Popoff & Yust, J. Math & Music 2020)](<Yust-Popoff - Meter Networks - a categorical framework for metrical analysis.md>) **deep-read (W 08-12, pp.1-5 detail)** — reading-sweep; re-read under Kim's corrected criterion | A metrical layer becomes a RELATION, not a list of intervals: M_d holds when t'-t = kd, relating ALL timepoints of a layer rather than successive ones ('an inherently metrical concept, where the more basic concept of time interval is not'). Nesting of metric levels is then relation INCLUSION, and Prop 2.5 proves M_d1 included in M_d2 iff d1 = u*d2 for positive integer u. A METER NETWORK is the inclusion diagram; Cohn's ski-hill graphs and Murphy's metric cubes are special cases. THE PAYOFF: different PARTS trace different PATHS through one network -- their Brahms Op.78 has violin M1->M1/3->M1/6->M1/12, piano RH M1->M1/2->M1/6->M1/12, piano LH M1->M1/2->M1/4->M1/12 -- three simultaneous divergent paths IS metric dissonance, formalised. Improves on Cohn (metric spaces handle only GROUPING dissonance) by capturing Krebs's DISPLACEMENT dissonance too, via network morphisms -- and the authors note displacement is 'more common than grouping dissonance'. Candid limitation: modelled on Q (notated rhythm); they cite the cognition move to R for PERFORMED rhythm (Polak & London, Benadon & Zanette, Danielsen) and offer their relations as idealisations of the neural oscillations behind metrical perception | NAMES A RHYTHMIC AXIS NONE OF OUR FEATURES CAN EXPRESS. mir gives bpm, beat grid, downbeats, onsets, syncopation, rhythmic complexity, per-stem rhythm -- every one describes a SINGLE meter. None expresses grouping dissonance (3-against-4, hemiola, triplet-vs-straight), displacement dissonance (same grouping, offset phase = the off-beat feel), or polymeter. For goa/psytrance that is not exotic: those ARE the genre's rhythmic vocabulary, and Kim's listening notes live in that register ('galloping', 'rapid 16ths/32ths hihat', 'congas played darbouka-style', 'rapid mathematical rhythms', 'the beat has disappeared') with no number attached to any of it. THE NON-OBVIOUS BRIDGE: the framework needs multiple simultaneous PARTS to trace divergent paths, and our stem separation already supplies them -- drums/bass/other/vocals each with onsets_activations_ts. Estimate a candidate d per stem (autocorrelate its onset envelope), build the inclusion diagram, and read off: CHAIN (metrically consonant) vs BRANCHING (dissonant); the RATIO of conflicting layers = grouping-dissonance type; the PHASE OFFSET between equal-d layers = displacement dissonance. Per-track now, per-frame with a sliding window. Control dims pass Kim's 3-part test unambiguously ('more polyrhythm', 'push it off the beat', 'straight vs triplet feel') and constitute a NEW axis rather than a re-parameterisation of onset density -- which is the test the attribute-branch plan should apply to every proposed control. REAL ENGINEERING RISK: Q vs R. Notated rhythm is rational, performed rhythm is not, and a strict rational-inclusion test will simply fail on audio-derived layer estimates; the practical form is approximate inclusion with a tolerance, i.e. a MODIFICATION of their framework not an application of it -- and their own Danielsen citation is the lead to follow. CHEAPEST TEST: one goa track, autocorrelate each stem's onsets, check whether the d values nest or conflict. If everything nests at 4/4 and there is no metric dissonance in this music to detect, that is itself the finding, cheaply obtained |
| [Topology-driven identification of repetitions in multi-variate time series (2505.10004)](<arxiv-2505.10004 - Topology-driven identification of repetitions in multi-variate time series.md>) **deep-read (W 08-12, pp.1-6 detail)** — reading-sweep; STRONGEST methodological fit in W's batch | Schindler et al. (Salzburg): recover EVERY individual cycle boundary T_i in a MULTI-VARIATE series under noise and NON-UNIFORM sampling. Three nested definitions: PERIODIC (x(t+tau)=x(t), exact), REPETITIVE (cycles related by monotone reparametrisations -- same shape, VARYING period per cycle), RECURRING (returns to x(0), cycles need not resemble each other); plus eps- and eps-delta-approximate versions where delta is a significance condition so noise cannot manufacture spurious cycles. Method: a scalar SURROGATE v(t) capturing relative position within the current cycle -> sublevel-set filtration -> 0-dim persistence; each local minimum's persistence is its significance; stable under d_B <= sup-norm. O(n alpha(n)), effectively LINEAR. Positions itself explicitly: FFT/autocorrelation/wavelet all need EVENLY SPACED samples and give only an OVERALL periodicity, not individual cycle lengths; and Perea et al (= SW1PerS, 1307.6188 in this same batch) 'focus solely on periodic and quasi-periodic functions and do not address the recovery of period lengths'. Analyses the RESIDUAL after a trend split | SUPERSEDES SW1PerS FOR US ON FOUR COUNTS, by the authors' own positioning: multivariate NATIVELY (our whole-track set is 46 fields; SW1PerS takes a scalar); tolerates NON-UNIFORM sampling, which is exactly our case since MASTER §2 records expanded fields landing at NATIVE per-field rates 0.2-100 Hz; linear rather than Rips-superlinear, so affordable across 4461 tracks at full length instead of per-clip on a landmarked cloud; and it returns INDIVIDUAL cycle lengths, which is what a control needs. THE DEFINITION IS THE FIND: 'REPETITIVE' (Def 2) is what musical repetition actually IS -- a bar recurs with drifting tempo and varied fills, so strict periodicity never fires and 'recurring' says almost nothing, while same-shape-up-to-monotone-reparametrisation is precisely a phrase repeat. Gives per-cycle BOUNDARIES = a timeseries, i.e. phrase/bar segmentation derived from FEATURES rather than onsets -- a different route to structure than the madmom grid, usable where beat tracking struggles. Control dims passing Kim's 3-part test: REPETITIVENESS (how strictly Def 2 fits), PHRASE LENGTH + DRIFT (cycle length over time), STRUCTURAL VARIETY (how many DISTINCT cycles vs one recurring cycle) -- and that last one makes Kim's a2a complaint computable: 'the whole song plays just one melody' = strongly recurring with very few distinct cycles. Convergence worth noting: their trend/residual split is the same move C reached empirically on chroma (raw inflated by the genre-generic component, only the corpus-DEMEANED signal carries identity). GAPS: industrial-automation domain (injection moulding), NO music validation at all, and the real work is hidden in choosing the surrogate v(t) for music -- expect that to be the project, not the persistence. Cheapest test: run on onsets_activations_ts / rms_energy_*_ts for tracks whose structure the beat grid already knows, and check whether recovered cycle boundaries land on bars |
| [Tonal harmony and the topology of dynamical score networks (2006.01033)](<arxiv-2006.01033 - Tonal harmony and the topology of dynamical score networks.md>) **deep-read (W 08-12, pp.1-6 detail)** — reading-sweep; re-read under KIM'S CORRECTED CRITERION | Buongiorno Nardelli (UNT physics + composition): a score becomes a DYNAMICAL SCORE NETWORK -- nodes = chords (pitch-class sets), directed edges = successive progressions, weight = traversals. With NO music-theoretic priors the topology yields all four classical properties of tonal harmony: CENTRICITY from the scale-free degree distribution (tonic/dominant are the hubs), REFERENTIALITY+HIERARCHY from MODULARITY CLASSES (community detection isolates TONAL REGIONS), DIRECTEDNESS from edge asymmetry. On Bach BWV 267 (avg degree 1.9, modularity 0.5) the modularity classes cleanly recover G/C major, A minor, D minor 'without any a priori knowledge of the harmonic structure'. THE KEY MOVE (p.6): the static graph discards time, so they reinterpret the temporal network AS A TIME SERIES of a non-stationary signal and run CHANGE-POINT DETECTION to find key regions automatically -- explicitly without Krumhansl-Schmuckler templates and without corpus-trained ML. Tooling open-source Python (MUSICNTWRK, music21). Generative tail: min-length path via route optimisation as an abstraction of harmonic sequence | ANSWERS ALL THREE OF KIM'S ASKS AT ONCE (target / conditioner / new timeseries) AND THE INPUT ALREADY EXISTS IN MIR: the expanded whole-track set already carries a `chords` field (MASTER §2), which is exactly what this construction consumes. Three derivable fields with NO new extractor: TONAL-REGION ID over time (modularity class of the current chord; change-points give boundaries), CENTRICITY (degree of the sounding chord in that track's own network = 'how close to tonal home are we right now', continuous per-frame), and HARMONIC MOBILITY (region-change rate / path length per bar). Conditioners Kim would ask for by ear -- 'stay in one key vs modulate', 'return home', 'harmonically restless vs static' -- NONE of which our current axes (onset density, brightness, bass weight, rms bands) can express; a genuinely new dimension, not a re-parameterisation. CHAINS ONTO C's CHROMA RESULT rather than competing: z0 -> chroma -> chords -> score network -> tonal region, where the first link is already measured (Tier-2 demeaned air 0.918). CORPUS CAVEAT that may be the most useful part: their demos are common-practice tonal (Bach, Beethoven); goa sits on one chord for 8 bars, so expect near-DEGENERATE graphs -- but the degeneracy IS a measurement, and 'how much harmonic movement' is a dimension our genre is unusual along, i.e. an interesting knob rather than a broken one. RISKS: their chords come from symbolic scores via music21, ours from noisy audio estimation, and errors compound INTO the graph (a wrong chord = a spurious node+edge) rather than averaging out; community detection on small noisy graphs is unstable. NEXT STEP: build score networks for a few goa tracks straight from the existing chords field and eyeball the modularity structure -- a day, no GPU, and it settles the degenerate-or-not question before anyone designs a head. **BOUND (Mukherji, the Rules-and-Representations row): edges = SUCCESSIVE chords = a surface/adjacency model, so a modularity class is a GROUPING, not a PARSE -- tonal-region/centricity/mobility stay computable but do NOT capture harmonic SYNTAX; the hierarchical middle term (Rohrmeier 2011 PCFG) is absent from the folder, and 2201.02715's HSMM is what would make it affordable.**  >>> DOWNGRADED FOR OUR CORPUS 2026-08-12 (W, after C's Rohrmeier ladder): Mukherji shows this is a SURFACE/adjacency model giving a GROUPING not a PARSE, and C's Rohrmeier 2011/2020 read finds goa's harmonic tree NEAR-DEGENERATE -- grammar route 'over-powered for goa', confirming Kim's decomposition that goa's controllable variety lives in MELODY/FX/TIMBRE not harmony. Construction still sound and the three features still trivially computable from the existing chords field, but for OUR corpus expect LOW-VARIANCE and weakly informative, NOT a headline axis. Genuine route for harmonically-rich (tonal/jazz) material, where the full ladder is now read (this -> Rohrmeier -> 2201.02715). Keep the one-afternoon degeneracy check only because a null is now the EXPECTED result and costs almost nothing to confirm. Downgrade, not retraction: the paper did not change, our reason to want it did. <<<|
| [Representing Sounds as Neural Amplitude Fields / Fourier-ASR (2601.06406)](<arxiv-2601.06406 - Representing Sounds as Neural Amplitude Fields.md>) **deep-read (W 08-12, pp.1-6 detail)** — reading-sweep | Li et al. (Tongji): implicit neural representation for AUDIO -- fit f(t): time-coordinate -> amplitude ('Neural Amplitude Field', the audio NeRF), so a signal is stored as weights and samplable at any rate. First benchmark of the design space: 3 positional encodings x 16 activations = 48 configs on speech+music. MOST ACTIVATIONS FAIL OUTRIGHT on audio -- only strongly-nonlinear (Gaussian) or PERIODIC (Sine) work; ReLU+Identity scores SNR -2.55 dB avg, worse than useless. Positional encoding indispensable (+11.02 dB Gaussian, +18.96 dB Sine). Best coordinate-MLP = Sine+NeFF (avg SNR 32.66). Their fix, Fourier-ASR: a Fourier-KAN whose learnable EDGE FUNCTIONS are Fourier series rather than B-splines, justified by an explicit LOCAL PERIODICITY ASSUMPTION + the Fourier series theorem -- periodicity lives in the BASIS, so no positional encoding and no activation choice, which removes the hyperparameter fragility. Two specifics: INVERTED FREQUENCY PYRAMID (per-layer max-frequency thresholds DECREASE with depth; 3-layer Omega=[64,5,3] beats [8,8,8]) and a derived Fourier-coefficient init a,b ~ N(0, 1/(Omega_l*d_in)) | SCOPE FIRST: representation, NOT generation -- a net overfitted to one clip for compression/super-resolution, no conditioning, nothing competing with SAME or the DiT. TWO THINGS EARN THE ROW. (1) It CONVERGES WITH SW1PerS (1307.6188) from the same batch on the same premise: SW1PerS makes local recurrence the thing it MEASURES, Fourier-ASR makes local periodicity the thing it BUILDS INTO THE BASIS -- two unrelated papers, 2013 and 2026, both saying periodicity in audio must be STRUCTURAL rather than learned, and this one supplies the empirical backing (representations without it don't underperform, they FAIL). (2) Together with HiPPO (2008.07669, also this batch) it is the second principled answer to a problem we actually have: whole_track_target_source.resample_axis0 INTERPOLATES control timeseries to arbitrary n_frames; an implicit representation would let a control curve be EVALUATED at any t instead. HiPPO answers via optimal polynomial projection, this via periodic-basis coordinate nets -- and the benchmark says the basis choice is the difference between working and not. Third, weaker: the inverted frequency pyramid (HF capacity at the input layer, flat allocation is wrong) is hypothesis-shaped for the layer-site thread (C's site_L8_15/L13_15, TADA's 16-23) but different architecture and different task -- ANALOGY, NOT TRANSFER. Caveats: 250-270K nets fitted per clip, GTZAN/VCTK, reconstruction metrics only, KAN literature young enough that Fourier-KAN-beats-MLP is their benchmark not a settled fact |
| [HiPPO: Recurrent Memory with Optimal Polynomial Projections (2008.07669)](<arxiv-2008.07669 - HiPPO - Recurrent Memory with Optimal Polynomial Projections.md>) **deep-read (W 08-12, pp.1-2 detail)** — NeurIPS 2020, Gu/Dao/Ermon/Rudra/Re. ⚠ ARRIVED MIS-TITLED as 'Persistence-based summaries (verify title on read)'; PDF renamed | NOT a topology paper -- it is the polynomial-projection memory framework S4 and Mamba are built on. Poses MEMORY as ONLINE FUNCTION APPROXIMATION: store a signal's history as optimal coefficients against an orthogonal-polynomial basis, weighted by a MEASURE saying how much each past instant matters; the projection operator then has a closed-form ODE / linear recurrence so coefficients update incrementally. Recovers the Legendre Memory Unit exactly (sliding-window measure) and explains LSTM/GRU gating as the low-order extreme. HiPPO-LegS (scaled Legendre, whole-history measure) claims INVARIANCE TO INPUT TIMESCALE -- no hyperparameter or prior on sequence length -- plus bounded gradients and cheaper updates; stress-tested on a trajectory task with UNSEEN TIMESCALES and missing data, beating RNN + neural-ODE baselines by 25-40%, and permuted-MNIST 98.3% (2020 SoTA) | TIMESCALE INVARIANCE IS A NAMED SOLUTION TO A PROBLEM WE BRUTE-FORCE. Our length regime is hand-pinned: MASTER §5 mandates crop lengths in multiples of 256 frames, models trained at one T behave differently at another, and the whole length-variant eval exists to compare native vs the 20s grid. We fix T everywhere and render separate ladders; this addresses T at the level of the REPRESENTATION rather than the schedule. Also a principled version of something we already do crudely: whole_track_target_source.py slices [start,end] from the 100Hz timeseries and resample_axis0's it to the target n_frames -- HiPPO is that same compress-arbitrary-length-into-fixed-size operation but with an optimality criterion and an online update rule, which is the variable-offset-crop case the whole-track npz exists to serve. And it grounds a line we already reference: STAR-VAE (2606.23064) uses a CNN+Mamba bottleneck; Mamba <- S4 <- this. CAVEATS: 2020, RNN-era, permuted-MNIST and trajectory classification -- NO generative modelling, no audio, no diffusion; we are not building an RNN and this is not 'swap in Mamba'. Transferable object is the REPRESENTATION (bounded-memory optimal projection of a continuous history under a timescale-free measure), not the architecture. Probably C's lane (state-space theory) rather than mine -- flagged for reassignment |
| [Guiding a Diffusion Model with a Bad Version of Itself / autoguidance (2406.02507)](<arxiv-2406.02507 - Guiding a Diffusion Model with a Bad Version of Itself.md>) **deep-read (F 08-12, subagent, full PDF)** — reading-sweep, Karras/NVIDIA NeurIPS'24 | Guide the good denoiser D₁ with a *deliberately worse copy of itself* D₀ (smaller and/or under-trained on the SAME task/cond/data), not the unconditional model: `D_w = w·D₁+(1−w)·D₀`. No task-discrepancy → the correction is "clean" and concentrates on high-prob regions WITHOUT dropping distribution branches (FID-as-coverage: ImageNet-512 2.56→1.34; unconditional 11.67→3.86, where CFG can't even apply). LOAD-BEARING LAW: the two degradations must be the SAME KIND — mismatched (dropout vs input-noise, or quantized post-hoc) gives ZERO benefit, best FID at w=1. | **COMPETING ARM to interval-CFG (exp #26), not an add-on** — authors found autoguidance+interval jointly NOT beneficial → either/or. Cleanest published case that guidance's quality gain decouples from its diversity/recall loss — exactly our CFG mode-drop concern. RF port is mechanical (`v_w=v_full+(w−1)(v_full−v_weak)`), parameterization-agnostic. Cheapest test (lightweight-first): reuse an early/under-trained SA3 fine-tune ckpt as the weak guide, A/B vs CFG at matched quality, check recall holds — NO new training if a weak ckpt exists. CAVEAT that bites us harder than their from-scratch setup: the weak guide must come from OUR OWN fine-tuning run (base-model ckpt vs our controllable fine-tune = the distribution mismatch that kills it). All evidence EDM/VE/images — zero audio/RF/music. |
| [Elucidating the Design Space of Diffusion-Based Generative Models / EDM (2206.00364)](<arxiv-2206.00364 - Elucidating the Design Space of Diffusion-Based Generative Models.md>) **deep-read (F 08-12, subagent, full PDF)** — reading-sweep, Karras/NVIDIA NeurIPS'22 | Decomposes diffusion into 4 orthogonal axes (sampling / preconditioning / training / architecture), each swappable. Reference map of the σ-schedule + Heun sampler + c-precond + log-normal-σ training space. | **The σ↔t dictionary that makes the interval-CFG σ-vs-t port mechanical, not a guess.** Cast RF into EDM's VE form: scale s(t)=1−t, **σ(t)=t/(1−t)** and inversely **t=σ/(1+σ)** — monotone but strongly nonlinear (σ=1↔t=0.5; σ=80↔t≈0.988). A CFG band in EDM-σ maps to RF-t by t=σ/(1+σ); applying an EDM σ-interval to RF t-values WITHOUT this transform silently misplaces the band — that IS the caveat we've been carrying. DO NOT transfer: ρ=7/σ_min/σ_max grid, c_noise=¼ln σ, σ_data=0.5, churn params (all VE-σ artifacts). DOES transfer: design-axes framing (interval-CFG = sampling-axis knob decoupled from training), preconditioning principle (RF v-pred already an instance), logit-normal-over-t = RF port of EDM's log-normal-over-σ (SD3 already does this). |
| [eDiff-I: Ensemble of Expert Denoisers (2211.01324)](<arxiv-2211.01324 - eDiff-I - Text-to-Image Diffusion Models with an Ensemble of Expert Denoisers.md>) **deep-read (F 08-12, subagent, full PDF)** — reading-sweep, NVIDIA'22 | The canonical "different noise levels do different jobs" paper: text drives coarse structure at high σ, is "almost entirely ignored" at low σ (prompt-switch: swap caption in last 7% → no visible change; first 40% → totally different). Method = ensemble of σ-interval expert denoisers (final 3: high/middle/low-noise), adds TRAINING params but not inference cost. | **METHOD OUT OF BUDGET — be blunt:** N experts trained/branched on ~1B pairs across a cascade is the opposite corner from our one 1.4B RF fine-tune. The DIAGNOSIS transfers: independent, well-instrumented corroboration that conditioning is noise-level-dependent (the premise under interval-CFG). Their "last 7% = text does nothing" supports applying CFG only in a middle band, not uniformly. Honest mismatch: eDiff-I is two-regime monotone-in-σ, ours is three-regime (middle band) — both land on "single CFG scale across all timesteps is the wrong default." Affordable analog = σ-dependent inference-time knobs on ONE net (interval-CFG already is this; their paint-with-words uses exactly a per-σ schedule on one shared model). Diagnostic we CAN afford: prompt-switch / attn-vs-σ ablation to localize where the drone lives, no retrain. Don't port their numeric σ boundaries to RF-t (use EDM 2206.00364's t=σ/(1+σ)). |
| [VeRA: Vector-based Random Matrix Adaptation (2310.11454)](<arxiv-2310.11454 - VeRA - Vector-based Random Matrix Adaptation.md>) **deep-read (W 08-12, pp.1-6 detail)** — ICLR 2024, reading-sweep | Kopiczko/Blankevoort/Asano: replace LoRA's per-layer trainable BA with ONE pair of FROZEN RANDOM matrices SHARED ACROSS ALL LAYERS, and train only two per-layer DIAGONAL scaling vectors (h = W0x + Lambda_b B Lambda_d A x). Param count becomes L*(d_model + r) -- ADDITIVE in rank, vs LoRA's 2*L*d_model*r multiplicative -- so the gap widens with rank and model size (GPT-3 r16: LoRA 75.5M/288MB vs VeRA 2.8M/10.5MB). Frozen matrices regenerate from an RNG SEED so a stored adapter is b + d + seed. b zero-init (dW=0 at step 0, as LoRA's B), d init to a constant (0.1) = a new hyperparameter. On par with LoRA on GLUE at ~10x fewer trainable params; BEATS LoRA on E2E at 3-4x fewer (GPT2-M: LoRA 0.35M BLEU 68.9 vs VeRA 0.098M BLEU 70.1); Llama 7B/13B instruction-tune with 1.6M/2.4M vs LoRA-r64's 159.9M/250.3M. Merges into W0 -- no inference latency | THE HEADLINE BENEFIT IS NOT OUR BOTTLENECK: VeRA exists to store millions of per-user adapters (their 275TB framing); we train a handful and have TB of drive. What is interesting is a side effect they barely dwell on -- VeRA is STRUCTURALLY DAMPED, its whole hypothesis class being 'rescale rows/cols of a fixed random basis', far more constrained than LoRA let alone DoRA or full-FT. Our recurring failure is the opposite of underfitting (Kim: 'earlier eps sound like they are making too large updates, patchwork sound'; 'our LR is massively too large, we should use EMA or increase batch size'; and the full-FT drone IS an output-scale runaway). So read it as a DAMPING PRIOR, not a compression trick -- that reframing is the only reason it belongs in our stack. REAL CAUTION: shared A,B means every layer adapts inside the SAME random subspace, differing only by gain -- in direct tension with the layer-site thread (C's morph sweep site_L8_15 / site_L13_15 arms; TADA-2602.11910's 16-23 injection site). If different DiT blocks need different update DIRECTIONS rather than different gains on one shared direction, VeRA's constraint is exactly wrong. Testable, not fatal -- test before adopting. Domain gap stated plainly: all results NLP + ViT classification, ZERO generative audio / diffusion / flow; our adapters sit on a rectified-flow DiT over audio latents and we have already seen a method transfer badly across that gap (genre meter-in-gradient, MASTER §4). Cheap to try: freeze A,B, share across layers, train two diagonals -- slots into the existing dora-rank sweep as one more arm. Predicted signature if the damping reading is right: FLATTER degradation across epochs on the boards where Kim hears collapse by ep2-ep8, not a better peak |
| [Evaluating Disentangled Representations for Controllable Music Generation (2602.10058)](<arxiv-2602.10058 - Evaluating Disentangled Representations for Controllable Music Generation.md>) **deep-read (W 08-12, full PDF, 5pp)** — reading-sweep, ICASSP-format, UPF Barcelona | Ibanez-Martinez/Serra/Rocamora et al. probe 3 unsupervised structure-timbre models (SS-VQ-VAE, TS-DSAE, AFTER) on FOUR axes instead of one: informativeness (probe acc), EQUIVARIANCE (does an input transform produce a PREDICTABLE embedding change -- P: predict transform params, R: predict transformed embedding), INVARIANCE (stability under irrelevant perturbation), DISENTANGLEMENT (delta = |Acc(probe on z_t + z_s) - Acc(probe on z_t)|; large delta = the other embedding carries residual info). Findings: intended vs actual semantics diverge SYSTEMATICALLY; leakage is ASYMMETRIC and architecture-specific (SS-VQ-VAE's timbre embedding carries structure, AFTER's structure embedding carries timbre -- so 'is it disentangled' has no single answer, only per-direction ones); TEMPO IS ENCODED IN THE TIMBRE EMBEDDING OF ALL THREE MODELS; informativeness TRADES OFF against equivariance (bigger embedding = more info, less transform-consistent); adversarial losses + augmentations move separation, NOT capture; high reconstruction coexists with note-level probe failure (info present but non-linearly encoded). Their conclusion: disentangled reps are NECESSARY BUT NOT SUFFICIENT -- predictable control also needs the DECODER to preserve separation at signal level; they name output-level + perceptual-listening evaluation as the missing future work | THE PROTOCOL TRANSFERS, THE NUMBERS DO NOT (Slakh2100 synthetic, drums+mixes EXCLUDED = fatal for our full-mix drum-dominant corpus; shallow linear probes). We currently evaluate control heads on ONE of their four axes -- informativeness (onset_eval correlation, LatCH gain ladders) plus a disintegration floor -- and have NO equivariance or invariance measure. Equivariance is the exact formalisation of what Kim's gain ladders test by ear: not 'did it move' but 'did it move PREDICTABLY'; our 08-11 rarity finding (only evr1x monotonic across prompt bands, base+newstack mid>rare) is an equivariance failure we described in prose for want of the name. The tempo-in-timbre result is a cheap warning at the attribute-branches plan: 3 unrelated architectures all leak a structural property into the timbral embedding, and the concatenation-delta test would tell us whether our per-attribute controls are independent BEFORE we spend GPU-months finding out they are not. Hold beside C's chroma readout as a scope caution, not a criticism: readable-from-z0 (Tier-1/2) is not yet steerable-at-output, and this paper names the axis that gap lives on -- C's own framing (conditioner TARGET) is already correct. Their stated missing piece is our unusual asset: Kim's per-cell listening verdicts are now filed verbatim in run_meta across 12+ runs, so the four axes could be correlated against his ear -- the direct method for the 08-11 open question of which metric tracks him. Equivariance needs pitch-shift + time-stretch, which mir already generates (pitch_shift_scores, stretch_scores, bungee binding) -- probe only, no new augmentation machinery. Untested |
| [Sliding Windows and Persistence / SW1PerS (1307.6188)](<arxiv-1307.6188 - Sliding Windows and Persistence.md>) **deep-read (W 08-12, full PDF)** — reading-sweep, PDF in Prospective Unchecked/ | Perea & Harer: time-delay embed a scalar signal, score periodicity as the ROUNDNESS of the resulting point cloud via 1-D persistent homology. Exact result (pp.3-4): for cos(Lt) the embedding is an ellipse whose roundness peaks iff L(M+1)tau = 0 mod pi, i.e. when WINDOW SIZE ~ PERIOD -- roundness is a resonance test against the signal's own period. Deliberately pattern-AGNOSTIC: the authors attack prior methods as "based on finding cosine-like behavior, a rather limited definition of periodicity". Stability d_B <= 2 d_H (p.7); convergence in N and in sampling (Thms 6.6/6.7). Quantitative payoff Thm 6.8: mp >= sqrt(3)*max_n r-tilde_n - delta*kappa_N -- max persistence is bounded below by the largest normalized harmonic, PENALIZED by a roughness term kappa_N proportional to ||f'||. TWO GOTCHAS: (1) diagrams from different window sizes / embedding dims are NOT comparable raw -- centering+normalizing is load-bearing, the authors say outright the un-normalized object "is changing" and need not converge (p.18); (2) Thm 6.8 needs field char p > N, so default F_2 implementations silently violate it on multi-harmonic signals | NOT a beat tracker -- madmom is not the cosine-strawman it critiques; do not touch rhythm/beat_grid. Value is a STRUCTURE screen orthogonal to our spectral ones: every current disintegration proxy (flatness, hf_ratio, zcr, CE) is spectral-shape and twice shown foolable (noise drone moves the meter; CE ranked Kim's two favourites 1st and 8th of 8), while Kim's own failure language is recurrence-shaped ("constant spectral pad-like signature", "the beat has disappeared"). Thm 6.8's roughness penalty means a noise drone cannot score high by being loud or bright. Sweeping window size gives a PERIODICITY SPECTRUM that measures BOTH failure directions with one instrument: deficit at beat windows = rhythm broke down; EXCESS at phrase windows = Kim's a2a complaint "the whole song plays just one melody", which we have never been able to quantify. Input already exists (whole-track timeseries, 100 Hz, 46 fields, 4461 tracks) -- scoring only, no new extraction. Cost: Rips is superlinear, so per-CLIP on a landmarked cloud, budget like Audiobox not like zcr. Complementary to C's morphological/contour thread: contour asks WHAT SHAPE, this asks DOES IT RETURN -- a drone can have a well-defined contour and never recur. Untested on our data; first experiment is cheap and labelled: score the clip families Kim has already graded and see if it separates them |
| [The Structure of Morphological Space — Kant & Polansky (Perspectives of New Music)](<deep-research/MORPHOLOGICAL_SPACE_DESIGN_NOTE.md>) **theory ref; C design-note deep-read 08-12, all `[paper]` claims PDF-verified** *(music theory, no arXiv id)* | Contour theory: **morph** = any parameter-over-time; **contour** = its shape as a transposition/scale-invariant equivalence class; dimension collapses to **L−1** (basis = zeroed-morph / transposition-invariant coordinate); **shape = angle, magnitude = rank** (OCD↔angle, OCM↔magnitude); **n-ary rank reduction = a resolution continuum**; contour-distance = angle = **COSINE = differentiable**; **CC** (all-pairwise) = memoryful now-vs-past; embedding = the hierarchy. | **Formalizes the whole chroma / melody-movement / invariance thread** — names the invariance our raw-chroma cosine kept tripping on (monotone gain/EQ/compression = a genre-generic offset). The **differentiability** remark is the ML door: contour-cosine is a drop-in **control-head target/loss** — our `cos12` is a crude instance; C's `control/sa3_control/contour_loss.py` soft-rank prototype (8/8 TDD, monotone-transform→loss≈0) implements the invariant version. **n-ary resolution = our raw↔demeaned↔whitened knob.** **Primary answer to the movement-encoding Gemini brief Q2/Q3/Q4.** Full analysis + our experiments (E1 kick-contamination PARTIALLY confirmed +67% rel; training-target-circularity confound) in the linked design note. |
| [Applying Guidance in a Limited Interval (2404.07724)](<arxiv-2404.07724 - Applying Guidance in a Limited Interval Improves Sample and Distribution Quality in Diffusion Models.md>) **deep-read (subagent 08-12)** — Kynkäänniemi/Karras, NVIDIA, NeurIPS'24 | CFG helps only in a MIDDLE noise band — apply it there, off at high/low noise → better FID + **recall/diversity** (EDM2-XXL 1.81→1.40 FID; DiT-XL 3.04→2.40). 1-D tuning recipe (fix low bound, sweep high, then low). | **Source of our interval-CFG A/B (#26 / gap-audit win #2).** Recipe ports cleanly; ⚠️ **σ-vs-t porting caveat** — their band is EDM sigma, ours must be RE-DERIVED in RF timestep t (their 0.19/1.61 etc. are meaningless in our units; anchor to fraction-of-trajectory, re-derive empirically). Win shows as **FAD/recall**, not per-clip sharpness → our pending FAD is load-bearing. |
| [StoryScope: idiosyncrasies in AI fiction](<StoryScope - Investigating idiosyncrasies in AI fiction.md>) **deep-read (subagent 08-12)** *(no arXiv id; preprint, github.com/jenna-russell/storyscope)* | Content/narrative features (NOT style) distinguish AI-vs-human fiction at **93.2% macro-F1** (~97% of narrative+style); human stories **rarer** (0.71 vs 0.49 mean rarity percentile); detection survives surface rewrites. | **Template for our rarity-lite / content-invariant idiosyncrasy scoring** — score the *choices* not the surface, dodge the style→(our production/mastering) confound; rarity-percentile-in-content-space validated as a distinctiveness metric; edit-robustness = a design goal. **Cross-domain method-borrow only** (text, not audio); our MERT **gen-vs-gen** mechanism + the production-confound finding stay ours. |
| [TADA: Timestep-Aware Data Augmentation](<TADA - Timestep-Aware Data Augmentation for Diffusion Models.md>) **deep-read (subagent 08-12)** *(OpenReview U6Mb3CRuj8; ⚠️ DISTINCT from the audio-steering TADA! 2602.11910)* | Augmentation-induced distribution shift comes ONLY from a middle ("sensitive") timestep band — apply strong aug at rough/fine, weak in the middle (parabola in log-SNR). Beats h-flip, ≈ AR without a conditioning module; benefit scales with model size. | **The *when* of augmentation ports** (log-SNR is RF-defined; x̂₀ recoverable from velocity). Keep our bungee/avp-aug operators (image-only paper). Caveats: it's an **online** (training-loop) change not our offline pipeline; find OUR sensitive band with an audio metric (don't reuse image thresholds); **TEST the "fine-band aug is safe" claim on audio first** — cheap x̂₀ diagnostic, lightweight-first, before any online rewrite. |
| [Can Muon Fine-tune Adam-Pretrained Models? (2605.10468)](<arxiv-2605.10468 - Can Muon Fine-tune Adam-Pretrained Models.md>) **deep-read (C 08-19, full PDF incl. appendices); ICML'26** — PDF in papers/ root | Qu/Huang/Horvath (MBZUAI): OPTIMIZER MISMATCH — fine-tuning an Adam-pretrained model with Muon (or vice versa) is measurably worse than the matched optimizer, symmetric (NanoChat 561M pretrained both ways: rel-PPL Full-Adam 0.710 vs Full-Muon 0.719 on Adam-pretrained). Mechanism = distinct implicit biases: SignGD→min max-norm, Muon→min spectral-norm solution (toy theorems); Muon-trained weights keep higher stable rank/SVD entropy. Mismatch = hypersensitivity to UPDATE STRENGTH (LR curve shifts up+left), 5.6–7.4× larger cosine displacement under full FT, MORE forgetting despite worse FT (Llama-2-7B). **LoRA mitigates**: gap −78 % (Adam-pretrained), LoRA-Muon ≥ LoRA-Adam on GLUE/NLG/CLIP (LoRA-Muon-PE best overall); worst-case mismatch inflation ≤ r / √r (App D). Rank study: where mismatch is pronounced LoRA-Muon degrades at r ≥ 128 and forgets more at r ≥ 256. Adam-tuned LoRA variants (rsLoRA/PiSSA/LoRA-One) do NOT transfer; DoRA-Muon untested. Their Muon: Moonlight scaling, Nesterov 0.95, NO wd (LLM), **cosine + 3 % warmup everywhere**, LR swept per optimizer. F.2: LoRA A and B are spectrally flat under Muon (stable rank ~7/8) — measured per factor, never B·A | **SA3's base is AdamW-shaped ⇒ our full-FT + Fusion runs ARE the mismatch case**: paper-grounded frame for the late collapse / drone / "grad-clip & AdaGC did nothing" (Muon's magnitude is LR-set). Prescription: matched AdamW (decayed) for full-FT, or adapt. **Our healthy LoRA/DoRA r128 + Fusion family = the mitigated case ✓**, with two cautions: r128 sits at the paper's degradation edge (our r256 arm's straight-line HIGH-EFF fits), and **DoRA-rows + Fusion is an untested variant combo** — Adam-tuned variants didn't transfer, so run DoRA-vs-LoRA under Fusion. **Never constant LR** — supports the 08-19 damping work (`--fusion-decay cosine`, warmup ≥ 3 % which also fixes the zero-init-B 6× first step). F.2 = C's per-factor NS5-flattening mechanism; OUR step data adds the product view: B·A is rank-1-dominant at r16 under both optimizers (flat factors, spiky product). **Stays ours:** the small-batch regime (their batches 32–256 carry signal) — gradient SNR = 1/w, walk-vs-drift, Muon as low-rank signal amplifier, SF/NorMuon/DoRA/DiT all outside the paper. Follow-ups: DoRA-vs-LoRA under Fusion; rank ladder × {AdamW, Fusion} with per-optimizer LR sweep + trajectory sketch; full-FT AdamW-cosine as the matched control we never ran |
| [WaveFreqAnchor — training-free face restoration (2608.06717)](<arxiv-2608.06717 - WaveFreqAnchor (training-free face restoration).md>) **deep-read (C 08-19, full)** — PDF in papers/ root | Du et al.: frozen FFHQ DDPM prior + DPS, no training. SDEdit init at t_s; normalised gradient ḡ=∇L/max(‖∇L‖,1); ASWG = pixel anchor in a downsampled anchor space + L_wave = ‖φ(u_t)−φ(u_y)‖ with φ a FIXED damped anisotropic wave operator (Laplacian init, FFT-domain, knobs τ/α/v_x,v_y; stop late); MWFI = keep predicted LL-subband amplitude, REPLACE its Fourier phase with the observation's, re-noise, keep HF subbands (middle window only); SHE = bounded masked HF boost + E1/E3 frequency-ratio router (real-world only). Synthetic SR: best LPIPS/ID/LMD at 4–8×, loses PSNR to SubDAPS++; REAL-WORLD wins everything while DPS-style baselines collapse (NIQE 11.34) → strength = unknown-operator regime. Ablation: +ASWG ID .553→.574, +MWFI .585; anisotropic φ best balance | **Kim: "face reconstruction is a bit like a2a" — yes, a2a is their REAL-WORLD branch, stronger (no degradation operator at all). Mine three things:** (1) ASWG = our readout-space guidance already built (latch_guided TFG, chroma-guided sampler) + two stabilisers (soft-clamped gradient; stop-late from OUR R²(t)) + per-band reliability weights from Tier-2 (air yes, bass no); (2) **NEW: the wave operator on the SAME chroma planes** — three (128,T) planes (never one (384,T)), pitch axis circular (FFT-right), time padded; 10.667 bins/semitone × 92.9 ms frames ⇒ slope 1 bin/frame ≈ 1 st/s, so v_pitch/v_time selects glide rate: pads near-horizontal, goa 16th arps ~37 st/s near-vertical — the static-bass/free-melody discrimination from a fixed filter; band-invariant orientation (harmonics move with the fundamental) ⇒ one setting + cross-band agreement as a melody detector; transposition-invariant energy = "what shape of motion" next to TIV's "what harmony". **Run it on WHITENED chroma (the chroma-trap)**; composes with contour_loss; (3) MWFI → TEMPORAL-phase anchoring of the latent trajectory (keep the reference's timing/groove = the noise-invariant rhythm emergent; ⚠️ NOT waveform STFT phase; soft / reduced-coordinate / low-temporal-frequency to stay on-manifold; a sampler callback). Skip SHE. RF port mechanical; base many-step = what we run. Order: SDEdit survival curves (temporal phase vs TIV) → offline gesture-separation test on reference chromagrams → then wire |
| [Forward-Evolution Error Analysis and Adaptive Design for Matrix-Valued Diffusion Models (2608.15103)](<arxiv-2608.15103 - Forward-Evolution Error Analysis and Adaptive Design for Matrix-Valued Diffusion Models.md>) **deep-read (C 08-19; §1,5,6,7 full)** — PDF in papers/ root | Pang/Shen/Zhang (Tsinghua/NUS), math.ST: theory companion to 2602.19512 for VP-OU with matrix schedules. Score-freezing d/ε² vs posterior-mean-freezing k·log k/ε² (matches, doesn't beat, known orders). Design rules: Cor 5.2 optimal fixed-spectrum schedule shares eigenvectors with the marginal covariance and pairs LARGER rate with LARGER variance (low-variance directions noised slower); Prop 5.3 assign the k largest cumulative rates to the data subspace; Prop 5.5 asymptotically optimal grid = equidistribute √(local growth), estimable from forward-noised pilots (5.23), "for a pretrained model … only the grid-design step applies". Synthetic 100-d Gaussian mixture, N=64: uniform 68.20 → adaptive 57.36; forward rotation 57.36 vs reverse 60.66. VP-OU only, no code, no learned predictor | **Three routes, one answer:** resolved for SAME (melody in-manifold, low-variance) the discretization-optimal schedule noises melody SLOWER ⇔ melody leads in reverse = SFD's Δt = our R²(t) curve (inference, not theorem: it minimises discretization error, not learnability). Our geometry ROTATES (rhythm at high noise, harmony at low) → a rotating schedule is the eventual object. **Usable NOW (Kim's correction 08-19: we train AND render on `medium-base` exclusively, 16–50 steps — the APT few-step `medium` is the one we never use):** the adaptive grid applies to EVERY render we make; at N=16–32 the discretization term is large (their curve: ~200 at 16 vs ~50 at 64 steps) so the measured-grid gain should be ≥ their 7–16 %, plausibly more at our step counts = better clips at the same steps or fewer steps for the same clips, board-wide. RF analogue = curvature-equidistributed steps (cf. Align Your Steps): pilot-noise a batch through base(+LoRA), estimate local growth along the path, √-quantile grid into `build_schedule`; A/B at 16/24/32 steps, PQ + ears. Cheap, local, this week. Sharpens what the model knows — does not put melody there. Pair with 2602.19512 (criteria here, implementation there) when a melody-schedule finetune is committed; P_melody then used twice (schedule + #59 loss) |
| [Variational Trajectory Optimization of Anisotropic Diffusion Schedules (2602.19512)](<arxiv-2602.19512 - Variational Trajectory Optimization of Anisotropic Diffusion Schedules.md>) **deep-read (C 08-19)** — PDF in papers/ root | Liu/Li/Cheng (Duke): scalar noise level → LEARNED matrix-valued monotone PSD trajectory M_t(θ)=Σ g_j(t)P_j over orthogonal projectors (DCT low/high, class-cond PCA); trajectory-level score-matching loss with matrix weight (Girsanov surrogate; optimal net = true score for any schedule); schedule gradient via x-directional derivatives only (3 backward passes); anisotropic Heun in M_t^{1/2} increments. Fine-tuned from pretrained EDM: best-FID gains small (CIFAR 1.829→1.803, AFHQ 2.042→2.010, FFHQ 2.374→2.242 via learned ISOTROPIC g, ImageNet-64 2.276→2.238) but LOW-NFE gains large (nfe 9: CIFAR 35.5→3.6, AFHQ 28→4.5). Lemma A.2: isotropic case = per-noise-level loss weighting | **The learned-clock generalisation of SFD's hand-set Δt**: P_melody (chroma readout row space / 15-d melody-selective subspace) + P_rest → melody denoised on its own schedule; genre-conditional bases ↔ class-cond PCA. Cheap half first: learning g(t) = learning a t-weighting = our R²(t)-gated melody term (FFHQ's best was exactly the isotropic learned g). Few-step is where anisotropy pays and SA3 samples in 8 steps (APT) → also an inference-quality lever. Port cost real: score-based VE → RF needs a per-subspace time warp + re-derived weight/Heun; after #59+v3, x0-target, R²(t) gating, SFD |
| [Semantics Lead the Way — Semantic-First Diffusion, SFD (2512.04926)](<arxiv-2512.04926 - Semantics Lead the Way (SFD).md>) **deep-read (C 08-19, main text); CVPR'26** — PDF in papers/ root | Pan et al. (XJTU/MSRA/ByteDance): COMPOSITE latent [semantic s (16-ch, learned SemVAE over DINOv2 features) ; texture z (32-ch SD-VAE)] denoised ASYNCHRONOUSLY — semantics ahead by Δt (t_z = t_s − Δt; Δt=0.3 optimal, Δt=1 sequential is worse), DiT predicts both velocities (β=2 on the semantic term) + REPA on the noisy semantic states; 3-phase inference; only z decoded. ImageNet-256 FID 1.06/1.04; ~100× faster convergence than DiT-XL/2 (33× vs LightningDiT). Ablation 8.17→7.08 (REPA)→5.24 (SemVAE)→3.03 (semantic-first); PCA semantics 4.06 vs SemVAE 3.03 | **"Give melody its own channel and clock."** SAME already carries semantics as LINEAR readouts (384-d chroma; C's 15-d melody-selective subspace) → the semantic latent is nearly free (their Table 4 says learn a small compressor rather than project); SA3's 257-ch zero-init `local_add_cond` inlet + native prepend/global cond can carry s_ts and the second timestep; needs a v̂_s output head + two-timestep noising in the wrapper. Dissolves the melody-wall mechanism (786× anisotropy, melody in the suppressed eigendirections, v-target under-recovery 8.3×) instead of re-weighting it; generalises chroma-guided/Head-B/prepend-cond from CONDITION-on-melody to GENERATE-melody-first. Images/class-cond only; base ckpt (post-trained are APT-distilled). Rank: after #59 subspace loss (built, A/B pending), x0-target (E1a built), R²(t)-gated melody weight (cheap, new) — the structural next lever |
| [Why Low-Precision Transformer Training Fails — Flash Attention (2510.04212)](<arxiv-2510.04212 - Why Low-Precision Transformer Training Fails - An Analysis on Flash Attention.md>) **deep-read (W 08-04, full PDF); ICLR'26** — PDF in papers/ root | Qiu & Yao: bf16 flash-attention pretraining catastrophically LOSS-EXPLODES (nanoGPT #303/#524/#554, 2y unresolved). Causal chain: backward's δ=rowsum(dO∘O) from forward-stored bf16 O → rank-1 gradient-error components (PK)ᵀX structurally SIMILAR across tokens+steps (shared direction R) × positively-biased δ-coefficients → coherent weight-error integration → spectral-norm blowup in specific heads. Bit cause: tied row-maxima pin P̄=1, same-signed V adds overflow significand, sticky-bit round-up (survives fp32 accumulation — bias enters at the bf16 FINAL ROUNDING of the fp32 accumulator). Fix = dynamic-max softmax (Alg 3, forward-only, β∈[2,8], conditional on repeated maxima; fixed offset FAILS — own bias). Leading indicator: multiple-maxima frequency climbs pre-explosion (Fig 13). W's 2 corrections to the report-level row: bias site = P̄V product NOT softmax rescaling; "low-rank collapse" = the ERROR direction, attention doesn't collapse → positive check is multiple-maxima freq + per-layer spectral norms (our telemetry has the latter), not attention effective-rank | **Literature anchor for the melody-precision consult's update-precision arm** — mechanistically demonstrates biased-RTN-on-structured-data accumulating via optimizer steps (the consult's relocated mechanism, different site). PREDICTS C's report-#2 null (fp16-FA melody-cosine 0.99999): single-pass inference has no accumulation loop, ~0.015/op eaten by 700× headroom + Remark 1 precondition (P̄=1 + sign-coherent V = attention-sink phenomenon) absent in a flow DiT forward → attention axis settled null, live axis = master-weight+update precision. Remark 1 sharpens residual-write analysis: systematic bias needs SIGN-COHERENT accumulands — look where DiT activations are sign-coherent, testable on captures. Watch-item: long LUMI full-ft late loss spike → this diagnosis; checks cheap (multiple-maxima freq, spectral norms). Keep F's separation: Qiu = acute mode, melody-drowning = sub-clinical, distinct. Companion to pull: 2510.04202 (spectral alignment early-detection) |
| Spectral Alignment as Predictor of Loss Explosion (2510.04202) *(verified real, WebFetch abstract; same group as 2510.04212; deep-read pending)* | Qiu/Wu/Tan/Wang/Yao — COMPANION to the flash-attn paper above: a metric tracking distributional alignment between layer INPUTS and weight-matrix SINGULAR VECTORS predicts training divergence EARLIER than conventional metrics (loss/grad-norm) | **The early-warning TOOL for the #68 fp16 watch** — instrument this (+ the flash-attn paper's multiple-maxima-freq indicator + our existing per-layer spectral-norm telemetry) as a cheap pre-explosion canary before/ during any fp16-attention big-FT. Same acute-mode scope as 2510.04212; not the sub-clinical melody axis |
| [CoFrGeNet — continued-fraction FFN/attention replacements (2601.21766)](<arxiv-2601.21766 - CoFrGeNet — Continued Fraction Architectures for Language Generation.md>) **read (W 08-03)** — PDF in reviewed-low-relevance/ | IBM: transformer FFNs (and weakly, attention) replaced by ensembles of learned continued fractions (rational functions, d≤7); **continuants closed-form gradient** = 1 division per ladder fwd+bwd instead of d (~9× speedup, custom autograd.Function); **dyadic depth-unfreezing schedule** is load-bearing (PTB 29.9 vs 33.7); -F variant beats GPT2-xl at 2/3 params, competitive w/ Llama-3.2B | **Off-lane, not actionable** — we fine-tune SA3, never pretrain backbones; FFN-class swap = retrain by another name. Heads (LatCH/cc_probe) are the only drop-in scale but they're signal-bound, not capacity-bound → no authority gain expected. KEEP: continuants gradient trick (any future division/rational layer) + dyadic unfreezing (stabilizer for hard-to-train nonlinear layers) |
| [Stable Audio 2 / Long-form Music Generation (2404.10301)](<arxiv-2404.10301 - Long-form Music Generation with Latent Diffusion.md>) **deep-read** | Lineage step SA1→SA3: 1.1B DiT (v-obj, cross-attn/prepend conditioning only — no AdaLN yet) on a 21.5Hz/64-ch VAE generates full 4m45s tracks; pretrain 3m10s → finetune 4m45s at ~18% extra compute with NO metric regression; structure emerges from context length, not semantic tokens; 2m gens WORSE than 4m45s (data distribution at that length) | Superseded architecturally by SA3, but direct prior art for #50/#54 long-context fine-tuning (extension-by-finetune is cheap+safe in this exact lineage); the 2m-worse finding warns against reading short eval cells as model quality (native-cells lane exists for this); low-latent-rate design law = published case for SAME's 10.77Hz. Zach Evans = Discord-notes Zach |
| [Stable Audio 1 (2402.04825)](<arxiv-2402.04825 - Fast Timing-Conditioned Latent Audio Diffusion.md>) | Lineage origin (ICML'24): 907M U-Net LDM on 64-ch/43Hz DAC-style VAE, from-scratch CLAP, and THE canonical seconds_start/seconds_total timing conditioning (per-second learned embeds concat'd into cross-attn); releases stable-audio-metrics (stereo FD_openl3, windowed KL_passt, feature-fusion CLAP_score) | Ancestor — all architecture superseded by SA3; keep as the timing-conditioning citation (incl. measured slightly-short bias + 40-60s data-sparsity error bump — useful priors for SA3 length debugging) and for benchmark-comparable long-form stereo metrics; prompt-recipe DNA (key:value \| plain, shuffled) explains SA-family prompt response |
| [StemGen (2312.08723)](<arxiv-2312.08723 - StemGen - A Music Generation Model That Listens.md>) | ByteDance ICASSP'24: masked-LM (250M) over Encodec RVQ tokens generates a target stem conditioned on a context submix; multi-source CFG (independent per-source guidance scales + per-source dropout) and MIRDD (MIR-descriptor KL penalizing musical misalignment with context) | Wrong paradigm (discrete tokens + masked-LM, fails both hard filters) — but multi-source CFG ports to sa3_control (check: do we drop conditions independently per source? if not, cheap change unlocking per-source guidance) and MIRDD-style descriptor-KL is an eval-page candidate via our Essentia stack |
| [SAE music steering (2505.18186)](<arxiv-2505.18186 - Discovering and Steering Interpretable Concepts in Large Generative Music Models.md>) **deep-read 07-11, indexed late** | Top-k SAEs on MusicGen residual streams (5 depths) + Gemini/Essentia/CLAP auto-labeling; steering by decoder-direction addition peaks at 75% depth (35.1%), collapses near-final (15.3%); quality cost unmeasured | Consonant neighbor to TADA {12,13} + our L8-15 (AR-token model, coarse grid, later peak; late-layer collapse matches our ARC/output-skin reading); SAE lane bolts onto existing LatCH activation dumps; AxBench-style diff-in-means directions from our labeled MIR features = the cheaper first experiment. ⚠️ the 07-21 re-download of its PDF was mislabeled (contained 2607.17624; quarantined in reviewed-low-relevance/) — the papers/ root PDF (4.75MB, 07-11) is the real one |
| [SwiftAudio (2606.31259)](<arxiv-2606.31259 - SwiftAudio - Data-Efficient Caption-Only Distillation for One-Step Text-to-Audio Diffusion-based Generation.md>) | Caption-only VSD distillation of a DDPM mel-spectrogram TTA teacher to one-step, no paired audio, plus an L1 temporal-TV latent regularizer | DDPM+mel, not RF+SAME — no drop-in; SAO already ships APT-distilled few-step checkpoints so this solves no live gap. TV-on-z0_hat regularizer is a cheap idea worth a footnote for any future distillation/consistency work |
| [STAR-VAE (2606.23064)](<arxiv-2606.23064 - STAR-VAE - Structured Topology-Aware Regularization for Audio Reconstruction and Generation.md>) | Channel-wise Gamma-Growth KL power-law forces low-index VAE channels to store structure, high-index channels to store texture; CNN+Mamba bottleneck, beats SAO's own VAE on matched-rate reconstruction | Autoencoder/tokenizer-layer paper — out of scope while SAME stays frozen. Parallel (KL-based) route to a goal SAME already hits via soft-norm + linear-decodability losses, not a checked box. Worth checking against if SAME is ever retrained; flags a "Reconstruction Drift" SSM-under-flat-KL failure mode |
| [Hybrid DiT instruction editing via RF (2606.20101)](<arxiv-2606.20101 - Hybrid Diffusion Transformer for Instruction-Guided Audio Editing via Rectified Flow.md>) | Coarse-to-fine joint/cross-attention mel-domain DiT for add/remove/replace instruction editing, RF-trained, small (78.6M) and fast | Mel+vocoder, not SAME latent — model itself doesn't port; RF objective and source-latent channel-concat conditioning are things SAO already has analogs of (Longform, LatCH). Only actionable piece: the procedural mix-two-clips synthetic add/remove/replace data recipe, architecture-agnostic |
| [Diffusion Warm Initialization (2606.18968)](<arxiv-2606.18968 - Audio-to-Audio via Diffusion Warm Initialization.md>) | DAFx26 empirical study locating the SDEdit skip-fraction sweet spot via a pitch-Jaccard-distance + FAD metric sweep, on Stable Audio Open | Mechanism is exactly Longform's already-shipped SDEdit re-anchor — no new capability. The JD+FAD tuning-harness pattern is a possible way to formalize re-anchor timestep selection if ever revisited |
| [AudioWeave unified gen+edit (2606.16435)](<arxiv-2606.16435 - Unified Audio Generation and Editing via Joint Condition Modeling and Progressive Training.md>) | Single DiT does TTA + 6 instruction-editing tasks by concatenating reference-audio latents as context tokens with a factorized (global/local) RoPE, staged training curriculum | Mel+vocoder small from-scratch backbone — doesn't port. Two portable patterns: concat-not-channel-expand reference conditioning w/ factorized RoPE, and a concrete data point that naive joint gen+edit training hurts both tasks vs. a staged curriculum |
| [DirectAudioEdit (2606.07356)](<arxiv-2606.07356 - DirectAudioEdit - Inversion-Free Text-Guided Audio Editing via Diffusion Prediction Contrast.md>) | Inversion-free DDPM audio editing via shared-noise re-noising + one-step reverse-dynamics contrast (not raw prediction contrast), dynamic increasing target CFG | DDPM/mel sibling of FlowEdit; its own justification (needed because diffusion SDE paths are curved) argues FlowEdit's simpler raw-velocity contrast is already the right tool for SA3's straight RF paths. Confirms FlowEdit as the lineage to keep developing, not a new method |
| [Entropy as a Structural Prior / Eisbach log-barrier (2606.07207)](<arxiv-2606.07207 - Entropy as a Structural Prior - How a Log-Barrier on DiT Belief Space Drives Musical Diversity and Development.md>) | Parameter-free per-sample training-loss weight from a DiT output's own temporal-energy entropy, claimed to fight mode collapse in DoRA fine-tunes of SA3 Medium | Training-loss curriculum, outside the control-methods sourcebook's scope. Evidentiary base is weak (single seed/prompt, qualitative only, validating ablations deferred, mostly-"Anonymous" citation list) — speculative lead worth a cheap internal rerun, not a technique to adopt on this paper's evidence |
| [UAT unified audio-text diffusion (2606.04939)](<arxiv-2606.04939 - UAT - Unified Audio-Text Diffusion for Audio Generation, Editing, and Captioning.md>) | Dual-stream DiT makes the text stream an actively co-updated peer of the audio stream, so one backbone does gen + SDEdit editing + masked-discrete-text-diffusion captioning | Cosine/EDM schedule not RF (needs re-derivation), backbone is AudioX/Stable Audio Open (weaker prior by its own ablation). Editing = our shipped SDEdit; guidance = plain CFG. Co-updated conditioning stream is outside SAO's current control-method mandate (no captioning thread) |
| [SegTune song generation (2606.02638)](<arxiv-2606.02638 - SegTune - Structured and Fine-Grained Control for Song Generation.md>) | Song DiT (RF, Stable-Audio-lineage 1D VAE) adds hierarchical global+segment natural-language text conditioning, channel-concatenated per time-window, plus an LLM lyric-duration predictor | Lyrics/vocal task outside SA3's scope, but the segment-broadcast text-conditioning pattern is a third design option alongside LatCH's trained probes and FusionCC's frozen-meter consistency; channel-concat-beats-linear-mix fusion finding is reusable |
| [Score-Control hallucination reduction (2606.00377)](<arxiv-2606.00377 - Score-Control for Hallucination Reduction in Diffusion Models.md>) | DDPM/image-LDM theory+method: penalizing score-Jacobian norm (via I-DDPM variance head) shrinks off-manifold "hallucination" probability mass | Pixel/DDPM-only, no tractable RF analog of the Jacobian trick exists yet. Conceptual cross-reference for the book's "honest frontier" chapter (score-Lipschitz-as-control-knob framing of SA3's untrained crispness-late regime), not a shippable method |
| [UNISON unified sound gen/edit via deep LLM fusion (2605.31530)](<arxiv-2605.31530 - UNISON - A Unified Sound Generation and Editing Framework via Deep LLM Fusion.md>) | MM-DiT unifies T2A + bilingual TTS/cloning + speech-in-scene editing via depth-matched per-layer Qwen2.5-Omni hidden-state injection into DiT blocks | Mel-spectrogram continuous VAE — fails hard filter 2. Depth-matched multi-layer text conditioning (vs. our single-final-layer T5Gemma cross-attn) is a plausible, cheap future ablation; editing-as-channel-concat reconfirms rather than extends Longform's approach. Bulk of results are TTS/speech, off SA3's turf |
| [DEMON streaming music diffusion (2605.28657)](<arxiv-2605.28657 - DEMON - Diffusion Engine for Musical Orchestrated Noise.md>) | StreamDiffusion-style ring-buffer + windowed VAE decode turns ACE-Step 1.5 (DiT+flow-matching, Oobleck VAE) into a live-playable instrument at 12+ gens/sec | Systems/infrastructure paper, not a control method. Concrete existence proof that per-frame live denoising-strength curves are buildable and fast on a fast few-step RF-DiT; candidate delivery vehicle (ring-buffer/per-slot scheduling/windowed decode) IF SAO ever builds a real-time SA3 instrument |
| [Diffusion Domain Expansion / DDE (2605.23275)](<arxiv-2605.23275 - Diffusion Domain Expansion - Learning to Coordinate Pre-Trained Diffusion Models.md>) | Small trained ViT coordinator (RoPE) reconciles overlapping patches' predicted-clean outputs to extend a frozen pretrained diffusion model beyond its trained length/conditioning-count | DDPM/EDM + raw-waveform UNet1d, not RF+SAME latent — no drop-in. Competes conceptually with Longform's training-free SDEdit+crossfade: a trained z0_hat-coordinator beats naive overlap-averaging on FAD in its own (much weaker) setting — worth a future ablation note, not urgent |
| [Live Music Diffusion Models (2605.22717)](<arxiv-2605.22717 - Live Music Diffusion Models - Efficient Fine-Tuning and Post-Training of Interactive Diffusion Music Generators.md>) | Routing+attention-mask KV-cache trick turns an offline block-outpainting RF music model into a low-latency streaming generator; ARC-Forcing fixes AR rollout drift via RL-free adversarial rollout training | Passes both hard filters. KV-cache routing mask is a candidate blueprint for a lower-latency/streaming SA3 continuation variant (should compose with differential attention, unverified); ARC-Forcing's rollout-level adversarial objective is a mechanism FusionCC's single-block consistency loss doesn't have — speculative future tool if Longform long-track degradation is ever observed |
| [Stable Audio 3 (2605.17991)](<arxiv-2605.17991 - Stable Audio 3.md>) **deep-read** | OUR model's tech report: 3-pathway conditioning (cross-attn / AdaLN / local-additive), 64 memory embeddings, t-sampling logit-normal truncated@0.075 then RESCALED to full support (near-clean = trained-but-thin, NOT untrained — corrected 07-07), length-dependent timestep shift, TrackType prompt prefixes recommended, Muon+AdamW split | Re-read 2026-07-06 vs current knowledge: crispness-late mechanism found (t<0.075 untrained); Axis-2 noise bands must use SHIFTED t′; TrackType prefix was missing from ALL our captions/evals (now in caption_tools/train_lora); layer-14 discriminator tap = mid-stack semantics hint |
| [SAME autoencoder (2605.18613)](<arxiv-2605.18613 - SAME - A Semantically-Aligned Music Autoencoder.md>) **deep-read** | OUR latent space: 256-d/4096×/10.76Hz, soft-norm (not VAE), semantics TRAINED IN (flow-matching alignment loss + linear chroma/ILD regressors + contrastive text alignment), decoder noise-robust by design | Explains why latent slerp/LatCH probes work (linear semantics + robust decode are by construction); caution vs TADA transfer: SAO's acoustics-only VAE ≠ SAME, so SA3's semantic bottleneck may sit elsewhere — localize, don't transplant {12,13} |
| [TADA! activation steering (2602.11910)](<arxiv-2602.11910 - TADA! Tuning Audio Diffusion Models through Activation Steering.md>) **deep-read** | Semantic bottleneck: cross-attn layers **{12,13}/24 in Stable Audio Open** causally control ~all musical concepts (activation patching); CAA/SAE steering at ONLY those layers = SOTA concept modulation (+46–49% CAA; benchmarked on ACE-STEP, not SAO — FINN's scope catch), while localizing WEIGHT-space sliders backfires (−21% AUC) | Closest-to-SA3 localization result; gives us (1) the patching protocol + a predicted 2-layer bottleneck to verify on SA3, (2) a training-free CAA steering lane, (3) hard evidence for the relevance-routed-DoRA caveat: route rank by importance, don't confine adapters to the bottleneck, (4) an alignment-preservation AUC eval worth adopting. Our LatCH numeric lanes, noise-band routing, era axis, self-attn/MLP maps remain ours |
| [ControlNet++ (2404.07987)](<arxiv-2404.07987 - ControlNet++.md>) **deep-read** | Cycle-consistency for controllable diffusion: noise training images, 1-step denoised estimate → frozen meter → t-gated consistency loss (t_thre 200–400/1000), adapter-only | **PRIOR ART for FusionCC's mechanism** (we reinvented it, incl. the t-gate). Their window scales with property globality (edges 200 < depth 400). Our boundary condition (blind-vs-redundant) + genre negative remain unpublished |
| [InnerControl (2507.02321)](<arxiv-2507.02321 - InnerControl.md>) **deep-read** | All-step alignment via t-conditioned probes on intermediate features — because clean-z0 meters fail at high noise (RMSE↓ but FID↑ artifacts) | Our LatCH heads already ARE t-conditioned probes — the upgrade path for all-step onset supervision exists in-house. Adds a 3rd failure mode (meter *unreliable*) to our taxonomy (meter *redundant*, probe-*hacked*) |
| Ctrl-U (unread, not fetched) | Uncertainty-aware reward weighting — down-weight unreliable meter feedback | Named by both papers above; relevant if probe reliability ever becomes the binding constraint |
| [Backbone trajectory-PCA (2602.23696)](arxiv-2602.23696.md) | 60–80% of optimizer *displacement* in low-dim subspace, *uncentered* PCA | Different, weaker statement than our centered 88%-variance + PC2-turnover early-stop signal (claim 5: "not documented") |
| [Cautious/modded-nanogpt (2510.12402)](<arxiv-2510.12402 - Cautious Weight Decay.md>) | Speedrun cautious-optimizer work — weight-decay-only caution | Bounds claim 2: our keep≈0.53 + 1/√keep + mask×orthogonalization degradation mechanism unpublished anywhere |
| [UltraViCo — breaking video extrapolation limits (2511.20123)](<arxiv-2511.20123 - UltraViCo — Breaking Extrapolation Limits in Video Diffusion Transformers.md>) **deep-read (subagent)** ❌ loop-collapse: TESTED-NULL on SA3 | Video length extrapolation: root cause = ATTENTION DISPERSION; repetition special-case proven to come from **harmonic RoPE frequencies** -> periodic attention at alignment positions. Fix (training-free): down-scale out-of-window attention logits (alpha), stronger at harmonic positions (beta). Diagnostic: looping model had ONE freq = 79.6% of attention amplitude vs 31.6% non-looping | **Testable loop-collapse mechanism + diagnostic for SA3** (RF-vs-diffusion irrelevant; it is attention/RoPE geometry). Cheap probe: measure whether one temporal-RoPE frequency dominates SA3 attention amplitude = predictor of looping. Fix = attention-concentration (2-tier alpha/beta kills the loop without flattening musical form), additive to any windowed scheme. Pairs with LoL (below) = 2 independent papers, same RoPE-periodicity cause. Caveat: 3D M-RoPE vs our 1D — redo harmonic analysis on SA3 freqs; visual metrics do not port. Ours: SA3 measurement + audio dispersion metric + buzz gate **SA3 TESTED-NULL (C #60, 2026-07-30):** harmonic analysis + dominance diagnostic run on Kim-labeled loopy (nl≥.55) vs non-loopy (nl35-42) renders (same track/ckpt) → dominance **0.419 loopy vs 0.395 non-loopy = NO discrimination** (papers: 0.796/0.316, a clean split). The scope insight beats the number: **SA3's loop attractor is STATE-dependent** (emerges at nl≥.55 within ONE model), whereas UltraViCo/LoL treat looping as a fixed MODEL property — so the transplant fails at the **FRAMING level, not just empirically**. UltraViCo logit-downscale is NOT the SA3 loop fix; fixes stay behavioral (breathing #35, interval-CFG). Probe: `eval/rope_dominance_probe.py`, results in `sa3_lora_runs/rope_dominance_probe/`. |
| [LoL — hour-long video via RoPE jitter (2601.16914)](<arxiv-2601.16914 - LoL — Longer than Longer - Scaling Video Generation to Hour.md>) **deep-read (subagent)** ❌ loop-collapse: TESTED-NULL on SA3 | Hour-long AR video: "sink-collapse" (reverts to initial frames) traced to **RoPE phase re-alignment** at long horizons + inter-head attention homogenization; fix = **multi-head RoPE jitter** (per-head theta perturbation, sigma=0.8) breaks cross-head phase sync. Collapse at FIXED indices, prompt-independent | **Independently corroborates UltraViCo: RoPE periodicity -> collapse.** Two training-free fixes to try on SA3: per-head RoPE jitter (LoL) + attention-concentration (UltraViCo). Diagnostic: LoL phase-coherence kernel C(Delta) — test if SA3 loop points land on RoPE phase maxima. Head-diversity = early-warning metric. **Honest limit:** fixes local phase maxima, NOT long-term memory (our prompt-arc/structure-recall gap survives). Video-AR streaming/KV machinery does not port (SA3 non-AR RF) — only the RoPE diagnosis+jitter. Ours: SA3 phase measurement, structure memory, buzz gate **SA3 context (C, task #60):** our LoL rope-jitter is ALREADY NULL for WINDOWED longform (positions reset 0..W per window -> aliasing precondition never arises, per the nulled verdicts row) — do NOT re-run that. The LIVE testbed was the WITHIN-window loop attractor (`docs/a2a-loop-attractor.md`, Kim nl55+ "loops a phrase for minutes"), a single forward where RoPE geometry IS live. **RESOLVED NULL there too (C #60, 2026-07-30):** the dominance diagnostic did NOT discriminate loopy vs non-loopy (0.419 vs 0.395; see UltraViCo row) — deeper reason: SA3's loop attractor is **STATE-dependent** (nl≥.55 within one model), but the RoPE-periodicity framing is a fixed-MODEL property, so it mismatches at the framing level. So BOTH regimes are now null: windowed (aliasing precondition absent) AND within-window (state- not model-determined). RoPE-jitter and logit-downscale are both OFF the SA3 loop-fix table; fixes stay behavioral (breathing #35, interval-CFG). |
| [STAS — steering video DiTs via massive activations (2603.17825)](<arxiv-2603.17825 - STAS — Steering Video Diffusion Transformers with Massive Activations.md>) **deep-read (subagent)** | Massive Activations (outlier channels >50x mean, fixed dims, text-invariant) are the steerable control points: only true ~59x MA dims steer, non-MA/weak-MA do nothing or hurt. Steer at anchor + chunk-boundary tokens, early steps, via polarity-preserving max-anchor (not scaling). Training-free | **Corroborates targeting OUTLIER channels for our LatCH/activation steering** — peak-to-mean ratio = diagnostic for which dims are causal. {12,13} vs their layer-invariant-MA + block-15: test whether our band coincides with SA3 MA peaks (if MAs layer-invariant in audio, our single-band is a contrast to explain). Positional targeting (chunk seams / onset-aligned latents) = coherence steering site. max-sign polarity-preserving + no-broadband-overdrive = our buzz-gate regime. Early-step-only = RF scheduling prior. Caveat: aggregate gains small, weight is in ablations |
| [DC-SAE — dual-contrastive SAE, features of musical interpretation](dc-sae-dual-contrastive-musical-interpretation.md) **deep-read (subagent)** (no arXiv id — workshop) | Two-branch TopK SAE on MusicGen L24: splits **work-identity (z_i) vs performance-variation (z_j)** via same-work-different-recording pivots (positive for z_i, hard-negative for z_j). z_j survives channel augmentation where MERT/CLAP collapse; emergent performer structure w/o labels; rank-1 steering wins 70% vs random (small). MIT group, successor to 2505.18186 | **Interpretation-axis successor to our indexed 2505.18186** (same MIT group): flat dict -> explicit work-vs-interpretation split. Portable core = dual-branch + same-work-pivot hard-negative recipe (content-vs-interpretation split for an SA3 SAE). Injection site does NOT port (MusicGen AR L24 residual vs SA3 RF DiT block). "Steer interpretation, preserve the piece" = a named feature. Steering weak (~70%) = calibration warning (need multi-feature/larger-alpha). Consistent w/ AxBench (SAEs weak for raw steering; DC-SAE value = interpretable disentanglement, not control). Was the false-fabrication case (real, confirmed via Kim PDF) |
| [Low-Resource Guidance / origin of "LatCH" (2603.04366)](<arxiv-2603.04366 - Low-Resource Guidance for Controllable Latent Audio Diffusion.md>) **deep-read (subagent)** ⚠️terminology | Stability/SAO (Zach Evans et al.): **LatCH = a guidance READOUT head** (predict control feature -> distance-gradient, skips VAE-decoder backprop) + Selective-TFG (guide first 20% steps); LatCH-B trains on generated reverse-trajectory latents. 1D low-freq controls (RMS/beats) work, sparse high-dim (pitch/chroma) FAIL. DDIM/v-param, 21.5Hz latent | **CORRECTED 2026-08-27 (C, Kim asked "aren't we doing the same thing?" — we are).** The earlier warning here read *"here it is GUIDANCE, not our training-time conditioning adapter: reconcile the term before porting"* and was WRONG — it conflated our TWO control families. Verified in code: `models/latch.py` `class LatCH` is a readout (`out_proj = Linear(dim, out_channels)`, `forward(x, t)` — predicts a feature from the latent, t-conditioned), and `inference/latch_guided.py` does `grad = autograd.grad(criterion(head(z0 + gamma*eps, t), target), z0); z0 -= mu_t * grad` — gradient guidance in latent space, no decoder backprop, with rho/mu/gamma/n_iter and start_pct/end_pct. **That IS the paper's LatCH + selective TFG, feature for feature.** The training-time conditioning family is the SEPARATE one (FiLM density, `mir_ctrl` pianoroll, `melody_contour` riffers — installed into the model and trained). So the paper's findings PORT DIRECTLY to our LatCH heads; nothing needs reconciling first. 1D-works/high-dim-fails constrains adapter targets (favor onset/RMS, avoid pitch — worse at our 10.77Hz; predicts our Head-A pitch ceiling). LatCH-B train-on-generated-latents = transferable discipline. TFG math is DDIM -> needs RF re-derivation. Cheap heads => feasible per-genre (goa) control under data limits |
| [NA-RFM — general steering of unconditional diffusion (2602.11395)](<arxiv-2602.11395 - General and Efficient Steering of (Unconditional) Diffusion Models — NA-RFM.md>) **deep-read (subagent)** | Training-free steering of pretrained UNCONDITIONAL diffusion: RFM direction (AGOP eigenspace) + noise alignment, added to a block activation; **explicitly REJECTS difference-of-means** (6.2% vs RFM 30.5%, DoM collapses diversity); CFG-like amplification s; ports to DiT (SiT middle blocks) | **Difference-of-means is beatable** -> RFM = upgrade candidate for our DoM mood-steering (esp. the moods that would not steer). (Tension w/ AxBench-DoM-best is fine: RFM is a stronger LEARNED direction, not a fixed vector.) CFG-amplification s-sweep = the over-steer regime our disintegration gate bounds. **Activation-norm-proportional edit (H+w||H||V) = principled anti-buzz scaling** worth adopting. Steering-without-text = our uncaptioned mood/timbre mode. Ports to MM-DiT |
| [SteeringDiffusion — bottlenecked activation control (2605.01653)](<arxiv-2605.01653 - SteeringDiffusion — A Bottlenecked Activation Control Interface.md>) **deep-read (subagent)** | S-BEC: frozen backbone + single low-dim shared code v -> FiLM/AdaGN affine on activations, timestep-gated, zero-init; **k~8-16 suffices** (bottleneck IS the mechanism); strictly monotonic single-scalar control; beats LoRA (which style-collapses) + ControlNet (720x fewer params). GroupNorm U-Net (not DiT) | Three independent convergences with us: **(1) bottleneck-as-mechanism confirms our LatCH low-dim thesis** (control = interface design, not param count); **(2) mid-up injection band ~ our {12,13}**; **(3) FiLM affine + zero-init ~ Head-B forward-conditioning** (reuse the zero-init-for-base-equivalence trick). Their monotonic-single-scalar surface = a rigor bar to test on our control. Caveat: GroupNorm U-Net -> inject via SA3 AdaLN path instead; RF timestep-gate re-derivation |
| [SimDPS — similarity-guided long-gap music inpainting (2509.16342)](<arxiv-2509.16342 - Similarity-Guided Diffusion for Long-Gap Music Inpainting — SimDPS.md>) **deep-read (subagent)** | Aalto: DPS + retrieved gap-length reference as a 2nd auxiliary likelihood pulling the under-constrained null-space; boundary-continuity refinement + cross-fade; waveform-domain (CQTdiff 44.1kHz, NOT latent). Beats plain DPS + beats pure retrieval when match is Fair/Poor | Failure diagnosis = our loop-collapse (weak null-space constraint over long spans -> smooth collapse). **Retrieval-as-conditioning FROM THE SAME TRACK earlier sections** = motivic-consistency lever against loop-collapse (repetition as signal not hazard). Boundary-continuity refinement = concrete recipe for our transition/join seams. Confidence-gated conditioning strength (omega_x tilde). Caveat: waveform splice does NOT port to 10.77Hz latent - conceptual guidance ports, splice mechanics need latent re-derivation |
| [SaFa — latent-swap joint diffusion for long-form (2502.05130)](<arxiv-2502.05130 - Latent Swap Joint Diffusion — SaFa.md>) **deep-read (subagent)** | Training-free length extrapolation: merge OVERLAPPING windows via binary latent SWAP (not MultiDiffusion averaging) -> removes seam/aliasing; self-loop swap (bidirectional overlap) + reference-guided swap (early unidirectional, consistency without repetition); **MM-DiT-validated (SD3.5), no attention/RoPE manipulation**; 2-20x speedup, low overlap 0.2. DDPM/2D-mel | Directly the seam mechanism SA3 lacks (we reset-clock + generate windows INDEPENDENTLY). Port = run adjacent windows concurrently w/ shared per-step denoise + overlap swap. **Reference-guided swap = consistency-without-repetition knob (r_guide) for loop-collapse**; MM-DiT-validated + touches no positional handling (attractive, since ours drives loop-collapse). **Caveat (their own untested cases): 2D-mel+DDPM; our 1D-SAME+RF unverified** - check if RF straight-path trajectories keep the adjacent-window similarity the swap needs |
| [DiffRhythm2 — block flow matching (2510.22950)](<arxiv-2510.22950 - DiffRhythm 2 — Efficient High-Fidelity Song Generation via Block Flow Matching.md>) **deep-read (subagent)** | Semi-AR song gen: **block flow matching** — NAR flow-matching within blocks, AR attention-mask BETWEEN blocks (clean block i attends clean 1..i-1 + own noisy). Shares SA2-VAE encoder, pushed to 5 Hz for 210s; EOP-frame variable length; block REPA (MuQ SSL) coherence loss; block-KV cache | **Most graftable anti-loop-collapse mechanism**: injects causal dependency into RF via ONLY an attention-mask + training change — no change to SA3 flow objective or SAME latent. Shares our VAE lineage; their 5Hz-vs-our-10.77Hz is direct rate/length/fidelity evidence (+ caution: low rate caps fidelity). Block-REPA portable as aux coherence loss; EOP = clean variable-length termination. Ours: SAME@10.77Hz, instrumental, buzz gate |
| [SongBloom — interleaved AR-sketch + diffusion (2506.07634)](<arxiv-2506.07634 - SongBloom — Coherent Song Generation via Interleaved AR Sketching + Diffusion Refinement.md>) **deep-read (subagent)** | Full-song gen (150s, beats Suno on structure): interleaved AR **sketch** (LLaMA over MuQ SSL tokens @25Hz) + NAR **diffusion refinement** (stable-audio-vae + RF DiT), hidden-vector bridge AR->diffusion; ablation: drop sketch -> PER 9->109 | Refinement half = stable-audio-vae + RF-DiT (close to us); the AR-sketch+bridge grafts onto SAME. **Direct evidence a learned coarse sketch is the anti-loop-collapse mechanism** our prompt-arc patches. Frame-rate matching load-bearing (10.77Hz => longer patch; larger patches favored). Caution (their own): needs an AR module bolted onto pure-diffusion SA3; SSL sketch = fragile part. Ours: pure-diffusion, instrumental, buzz gate |
| [InfiniteAudio — training-free infinite length (2506.03020)](<arxiv-2506.03020 - InfiniteAudio — Infinite-Length Audio Generation with Consistency.md>) **deep-read (subagent)** | Training-free inference wrapper (AudioLDM/VoiceLDM): **FIFO** rolling window, each frame at a different diffusion timestep on a diagonal schedule (pop denoised front, push noise back) = constant memory, no seam; buffer-zone closes train/infer gap; self-attn maps pick step budget | FIFO diagonal-noise buffer = **seamless alternative to our reset-the-clock-per-window** scheme (the seam is where loop-collapse lives). Training-free, testable on SA3. Frictions: DDPM/mel -> RF-timestep remap needed; mel not latent. Buffer-zone = reset-artifact fix; their attention-map diagnostic should be run on SA3 to test the over-attends-fixed-region loop-collapse hypothesis. Ours: RF/SAME, buzz gate, conditioning arc |
| [UniSteer — text-guided flow in activation space (2605.30076)](<arxiv-2605.30076 - UniSteer — Text-Guided Flow Matching in Activation Space for Versatile LLM Steering.md>) **deep-read (subagent)** | LLM steering: trains ONE conditional velocity field over frozen LLM activations, NL-condition via cross-attn (DiT-style), flow-inversion edit at inference; beats CAA (diff-of-means) on composed/multi-constraint; doubles as classifier by reconstruction-energy | Same RF/flow-matching math as SA3, trained on activations. **Sits in the Head-B tension**: a separately-TRAINED steering module acting at INFERENCE (middle path) — informs but does not validate forward-conditioning; argues compositional control without retraining the generator (testable counter-premise). **Beats diff-of-means on composed moods** (our mood-vector interference failure). Reconstruction-energy classifier = LatCH-head-as-scorer template. LLM-domain, long-form unevaluated. Ours: audio, buzz gate, Head-B-in-generator |
| [TC-LoRA — timestep-conditioned LoRA hypernetwork (2510.09561)](<arxiv-2510.09561 - TC-LoRA — Temporally Modulated Conditional LoRA for Adaptive Diffusion Control.md>) **deep-read (subagent)** | A hypernetwork generates LoRA B,A **per denoising step + condition** (W'=W+B(i,t,y)A(i,t,y)) instead of static adapters -> coarse-early/fine-late stage-specialized control; beats ControlNet on depth-cond @3.6x fewer trainable params; image domain | Live design Q for our adapter stack: static vs **flow-time-generated** DoRA/onset/LatCH/Head-B weights. Thesis (weight-space > activation-space FiLM) + coarse->fine maps onto RF trajectories (global form early, timbre late). Param-efficiency: one hypernet replaces many static adapters. **Caution aimed at us:** per-step weight-swap threatens temporal consistency (their unsolved limitation) = our loop-collapse risk — pilot against the disintegration/continuity gate first. Ours: audio/SAME, buzz gate |
| [AxBench — simple baselines outperform SAEs (2501.17148)](<arxiv-2501.17148 - AxBench — Steering LLMs - Even Simple Baselines Outperform Sparse Autoencoders.md>) **deep-read, ALREADY FOLLOWED** | Steering+detection benchmark (Gemma-2): prompting > finetuning > all representation methods; **SAEs not competitive**; difference-of-means (DiffMean) is the best fixed-vector; their ReFT-r1 (rank-1 finetune) is Pareto-optimal; bigger steering factor monotonically harms capability | **The paper our 07-12 mood-steering acted on** — skipped SAE training, used diff-of-means (→19× dark on closed-loop meter). Replicated on SA3: detection≠steering (our "relaxing" best-separability-but-won't-steer), big-vector→buzz (= our disintegration gate), SAEs-underperform (bounds the SAE-steering lane: SAE-music 2505.18186, DC-SAE 2605.31295-adjacent). ReFT-r1 = candidate vs our readout heads. Ours: SA3 audio domain + the buzz gate (they have no noise screen) + Head-B forward-CONDITIONING (distinct from all their inference-time readout steering) |
| [SHIFT — steering flow transformers (2604.09213)](<arxiv-2604.09213 - SHIFT — Steering Hidden Intermediates in Flow Transformers.md>) **deep-read, closest-to-SA3** | Inference-time activation steering for MM-DiT (FLUX/SD3.5 = SA3 family): concept erasure + style/object shift, no retrain. Steers pooled text-embed + post-shared-attention text tokens; vector = contrastive-pair DiffMean or SVM normal; **one TIME-INVARIANT vector works across all timesteps** (concepts = stable global DiT directions); adaptive strength via SVM concept-presence signal | **Closest steering method to SA3** (MM-DiT, rectified-flow) — ports more directly than AxBench LLM steering; gives concrete SA3 hook points. **Independently confirms C's temporally-stable-direction finding** on this architecture class. Its concept-presence strength-gate COMPLEMENTS our disintegration/buzz gate (orthogonal axes). Concept-erasure = an untried removal-steering probe. Cross-distillation transfer predicts our APT-distilled SA3 ckpts share vectors (testable). Ours: audio domain + buzz gate + Head-B forward-conditioning (distinct from all their readout steering) |
| [Latent disentanglement via activation steering (2605.31295)](<arxiv-2605.31295 - Latent Space Disentanglement via Activation Steering for Symbolic Music.md>) **CONFIRMED on SA3** | Symbolic-music: to steer concept A without dragging entangled concept B, **Gram-Schmidt-project A orthogonal to B** (per σ,layer) before adding it | **CONFIRMED strong-form on SA3 audio (C, task #58, 2026-07-30) — stronger than the paper:** naive sum of two anti-correlated dirs (dark+onset, cos −0.3, shared layer) DISINTEGRATES; GS-orthogonalizing one strips ~5% norm + 100% of the buzz → gate-clean DUAL steering, both axes move (dark 79% solo-authority, onsets 0.7→2.0/s). Poison lives in the SHARED subspace; GS removes the disintegrating component, not just interference. **Ports directly to multi-concept sa3_control/LatCH — orthogonalize before summing.** Ours: the disintegration-gate that made the win legible + the SA3 (σ,layer) placement. *(abstract confirmation-based; full PDF deep-read pending)* |
| OTAD — optimal-transport anomaly detection w/ learned Riemannian metric (2605.05554) *(cite-verified by C's 2026-08-01 pass; no abstract file yet)* | Replaces a Euclidean cost with a **learned Riemannian ground metric + OT** for detecting off-manifold samples; fixes Euclidean-FAD's invariance-set blindness (artifacts orthogonal to the top PCs sit at near-zero distance) + rank-1 dilution | **The PRINCIPLED version of our DSP disintegration gate** — the gate catches that exact artifact class (off-manifold buzz orthogonal to the signal) via hand-built features; OTAD does it with a learned metric. Candidate eval-stack addition; needs PDF read for compute cost before adopting. High relevance |
| MAPCA — whitening methods as implicit metric choices (2604.14249) *(cite-verified by C's pass; no abstract file yet)* | W-MSE / Barlow-Twins / VICReg are implicit metric choices (Σ⁻¹ / Σ / diagonal); output-whitening pressure forces isotropy | **E1c design LAW (locked, C 2026-08-01):** whitening→isotropy would EXPAND our latent's giant degenerate shells and destroy the functionally-necessary 1/f tail, so E1c = **capped noise-floor compensation ONLY** (w_i≈(λ_i+1)/λ_i clipped), NEVER spectrum flattening; VICReg-style variance-PRESERVING penalties are the safe family. Directly constrains the x0/E1 arm's regularizer |
| Dimension-Corrected Hitting Times (Zongmin Liu, Stanford; `arXiv:submit/7812416`, 2026-07-10) *(submission-stage preprint — zero footprint, Kim-fetched + C-verified real 2026-08-01; no public id/md yet)* | Spectral hitting-time scaling with a dimension correction: τ_HT ~ C·Δ₁^-γ·d^ρ (R² .683); 'first-step spike-bulk gap alone insufficient'; right-censored AFT model. Tail-onset diagnostics (eq 6): Hill estimator + KS power-law score + tail mass + effective rank | **Concrete recipe for the HTSR anti-grokking monitor** (row above) we're folding into checkpoint-trajectory-stats for the winning +40ep over-training watch — its eq-6 tail-onset battery is the measurement kit. Spectral-geometry adjacency to our α≈1.12+GOE+shells model-space characterization. Example of the (b) submission-stage no-footprint pattern (see preamble) |
| Martin & Mahoney HTSR / weightwatcher (heavy-tail self-regularization) *(real, C-verified; body-of-work, no single id)* | Per-layer heavy-tail exponent α of weight-ESDs as a **training-health readout**: α≈2 ideal, α≥5 underfit, rank-collapse = anti-grokking. CPU-cheap (weightwatcher-style) | **Cheap monitor to fold into checkpoint-trajectory-stats** (lightweight-first). Priority target = the winning +40ep continuation (over-training is exactly its risk — α-drift would catch it). Complements our PCA-trajectory/backbone read (2602.23696) with a per-layer weight-spectrum health signal |
| MTop-Div — multiscale topological gen-vs-real divergence (2106.04024) **NeurIPS'21** *(cite-verified by C's pass; no abstract file yet)* | Non-parametric TOPOLOGICAL divergence between generated + real distributions; no Gaussian assumption | Candidate BOARD metric **where FAD/Gaussian assumptions fail** (our latent is non-Gaussian: GOE-with-shells). Lower priority than OTAD — TDA compute cost + subsample sensitivity. Bank as an eval-page candidate |
| Auxiliary-Loss-Free load balancing (2408.15664, DeepSeek) *(cite-verified by C's 2026-08-01 pass; no md yet)* | MoE expert load-balancing via a per-expert **bias term OUTSIDE the gradient** (nudged by observed load), NOT an auxiliary balance LOSS — the aux loss injects interference gradients that hurt quality | **Design LAW for any MoE we build (E2 / state-dependent tier-2): do NOT add a load-balance loss** — use the bias-update trick. Rhymes with our own genre-consistency-aux-is-harmful finding (interference gradients, same family) |
| Diff-MoE — timestep-conditioned expert routing (ICML'25) *(cite-verified real by C; exact arXiv id TBD)* | Diffusion MoE where experts are routed by **diffusion timestep** = "two clocks" on the noise axis (structure-experts early, texture-experts late) | Real tier-2 direction that **dovetails the x0/timestep (E1) work** — a state-dependent weight indexed by the RF/noise clock; the natural MoE form for our RF-DiT |
| State-dependent / input-indexed weight vocabularies — cluster *(all cite-verified 11/11, C's 2026-08-01 pass; detail in `papers/deep-research/2026-08-01-state-dependent-weights-triage.md`)* | Concrete forms of Kim's "weight vocabulary indexed by input / reactive weights": **UniMoE-Audio (2510.13344)** + **MoE-Adapter (2601.02967)** audio-MoE w/ gradient-conflict segregation; **PEER / PKM / UltraMemV2** million-single-neuron experts / product-key memory; **SMDIM (2507.20128)** Mamba-SSM+diffusion two-clocks; **HyperSound / FewSound** hypernet→INR "generators not samples"; **OscillaTTS (2606.25424)+SnakeBeta** periodic activation | Tier-2 state-dependent-model directions for the reality-structured plan. **CAVEATS (C):** DeMa (2601.05527) is MULTIVARIATE TIME-SERIES, NOT audio (arch accurate, domain misattributed — never cite as audio); SMDIM 'slow/fast-clock' + UniVerse-1 'triggers' are report GLOSSES, not the papers' terms; FewSound lightly conflated w/ its KAN sister (2503.02585) |
| [JLT — clean-latent (x0) prediction in latent-diffusion DiTs (2605.27102)](<arxiv-2605.27102 - JLT — Clean-Latent (x0) Prediction in Latent Diffusion Transformers.md>) **cite-verified + SAME eigen-measured; PDF deep-read pending** | Predicting the CLEAN latent (x0) instead of velocity beats v-target on image DiTs (FID-50K 2.50 vs 6.56; 130M DiT / frozen FLUX.2 VAE / ImageNet-256). Claimed mechanism: velocity regression amplifies low-variance *ambient* eigendirections, starving informative ones. Caveats: eps analytic (untested); x-vs-v ONLY; one scale/dataset/VAE (authors self-flag no cross-tokenizer val); **untested in distillation/few-step where v historically wins** (Salimans-Ho) | **Mechanism CONFIRMED applicable to SAME by our eigen-measurement (C, 2026-07-31):** SAME per-dim variance MISLEADS (flat, 5.4×) but the covariance EIGEN-SPECTRUM is **786× anisotropic** — 188/256 eigendirections below the velocity target's unit noise floor (bottom λ 0.05), and **the melody subspace sits in the suppressed region**. So v-target under-weights exactly where melody lives → x0-target = a measurement-motivated **3rd independent attack on the melody wall** (after subspace-loss + Head-B). Awaiting Kim. Ours: the SAME eigen-measurement + melody-subspace localization + the per-dim-misleads catch |
| [Closing the Loop — PID feedback steering (2606.18790)](<arxiv-2606.18790 - Closing the Loop - PID Feedback Control for Interpretable Activation Steering in Symbolic Music.md>) *(abstract-level; PDF deep-read pending)* | **Same group as 2605.31295.** Symbolic music: diff-in-means Pitch/Duration dirs (Linear-Representation-Hypothesis validated) + **Gram-Schmidt Dual Steering** to decouple entangled attributes + a titular **closed-loop PID controller on the steering coefficient** that holds an attribute constant across long AR gen (~5% lower FMD vs static, per our citation audit) | The GS-dual-steering half is the mechanism **task-#58 already CONFIRMED strong-form on SA3** (see 2605.31295) — external corroboration in a symbolic domain. New lead = the **PID/closed-loop coefficient hold** as a candidate for keeping a steered attribute from **washing out across SA3 windowed long-form** (attribute-hold, NOT structure-memory). Compare: 2605.31295 (same GS), AxBench 2501.17148 (the diff-in-means baseline), our LatCH heads. Risk aimed at us: per-step coefficient swings on RF continuous latents are exactly what the buzz gate watches. Ours: audio domain, buzz gate, RF-stability of the loop |
| [Rectified-CFG++ for flow models (2510.07631)](<arxiv-2510.07631 - Rectified-CFG++ for Flow-Based Models.md>) **NeurIPS 2025** *(abstract-level; PDF deep-read pending)* | Naive CFG on **rectified-flow** models causes "severe off-manifold drift"; fix = **predictor–corrector** guidance (conditional update anchored to the transport path, then a weighted conditional/unconditional correction), claimed to keep trajectories in a bounded tubular neighbourhood of the manifold across guidance strengths. Image (Flux/SD3/3.5/Lumina) | **SA3 IS rectified-flow**, so on-target: the off-manifold-drift-under-CFG it fixes is the *same disease* as our **disintegration/buzz gate** (the grunge from over-driving steering/CFG = leaving the manifold). Attacks it at the **sampler level** where our gate attacks it at the **loss level** — likely complementary. The deep-research brief's prescribed **on-manifold anchor for strong activation steering under high CFG**; testable on SA3 as a sampler mod, no retrain. Open Q: does the corrector-toward-unconditional blunt our conditional-branch-only steer? Ours: audio manifold, buzz gate, the steering-under-RCFG++ test |
| [Residualized Temporal SAEs (2605.27813)](<arxiv-2605.27813 - Residualized Temporal Sparse Autoencoders for Interpreting Diffusion Models.md>) *(abstract-level; PDF deep-read pending)* | SAEs over diffusion activation **trajectories** not single timesteps: fit linear predictors between neighbouring steps, train the SAE on the **residual** (structure beyond linearly-predictable dynamics); each latent reads as a feature trajectory over denoising time. Image (SD1.5) | Trajectory-aware cousin of our **t-conditioned probe** view (LatCH `t_injection`, InnerControl 2507.02321) — models dynamics *between* steps rather than conditioning on t; a candidate **SAE-lane refinement** on SA3 trajectory dumps (bolts onto LatCH dumps, cf. 2505.18186). Most-cited-by-us: **the only citable precedent for CONDITIONAL-CFG-branch-only steering** — our Phase-3 convention (mt_dark 19×), now **self-backed by our own result** rather than the literature *(that branch-only detail is from the paper body per the citation audit, NOT the fetched abstract)*. Ours: the branch-only convention (self-backed), RF/SAME, the buzz gate |
| [MIDI-RAE-JEPA / STORMBIRD — hierarchical self-supervised MIDI representations (2608.04378)](<arxiv-2608.04378 - MIDI-RAE-JEPA — Helping Music Co-Creation Agents Listen Well.md>) **BLOG-PAGE-LEVEL ONLY — arXiv abstract/PDF not fetched, code not released** | Swin-MAE on piano-roll IMAGES (symbolic, not audio), 6-level hierarchy, JEPA-style objectives (LeJEPA attraction+SIGReg, factorization, cross-level masked prediction, **no reconstruction loss**). Chord root 90% linearly decodable w/ light aux loss; phrase-boundary signal rises monotonically toward the COARSEST level (F1@32=0.303 at L0 vs 0.098 pixel baseline); pitch/time equivariance peaks exactly where the factorization loss acts. Self-reported failure modes: 2 variants show chroma-R² collapse (−77 to −713); EMOPIA probes barely beat chance | Domain gap: symbolic MIDI-as-image, nothing ports as code even once released. Relevant as DESIGN, not artifact — phrase-structure hierarchy EMERGING from training with no hand-built tree is adjacent to (not identical to) our metrical-tree-PE hypothesis (position GIVEN as a tree vs LEARNED to emerge) — corroborating or competing, unclear without the PDF. Flagged for C; not a graduated arm, not on the two-week critical path |
| TAC-GAN (1907.02690) *(no abstract file yet)* | AC-GAN outputs confined by the frozen auxiliary classifier's decision boundaries | The HARM mechanism of our boundary condition, formalized — but with no redundancy conditioning. Cite in any boundary writeup |
| Du et al. gradient-cosine gating (1812.02224) *(no abstract file yet)* | Gate aux losses by grad cosine ≥ 0 (MTL task-gradient conflict) | **Their predictor MISCLASSIFIES our genre case as harmless** — the gate can't see manifold-confinement harm (not gradient conflict). Frame as "a gap their predictor doesn't cover," not head-on refutation (W's precision 07-03) |
| Physics-aware aux losses (2606.12651) *(no abstract file yet)* | States the redundancy condition near-verbatim (discriminative GNN setting) | Finds redundant aux **neutral**, not harmful — our generative-manifold degradation goes beyond it |
| Bjerva 2017 (ACL W17-0225) *(no abstract file yet)* | Information-theoretic prediction of MTL aux-task benefit | Cousin of the helps-half (help vs no-help only, representation-sharing MTL, no frozen probe) |
| [Mousse: Rectifying the Geometry of Muon with Curvature-Aware Preconditioning (2603.09697)](<arxiv-2603.09697 - Mousse - Rectifying the Geometry of Muon with Curvature-Aware Preconditioning.md>) **deep-read (C 09-22, Algorithm 1 + §3.2 verified against the PDF)** — Shanghai AI Lab/SJTU/Fudan, official code github.com/Anti-Entrophic/Mousse, cloned to ~/Projects/Mousse | Muon's Newton-Schulz forces every singular value to 1, an ISOTROPIC trust region; real curvature is heavy-tailed, so it over-drives high-curvature directions and under-steps flat ones. Mousse whitens with Shampoo's Kronecker factors FIRST, runs NS in the whitened frame, then unwhitens: dW = -L^(-1/4) msign(L^(-1/4) G R^(-1/4)) R^(-1/4). ~12% fewer steps than Muon at ~3% wall-clock, GPT-2 160M-800M on 20B FineWeb, beating AdamW/Muon/SOAP across a peak-LR grid at every scale. | DIRECTLY ADOPTED — it is what our `--modular-whitening` path is trying to be. THREE things our code gets wrong and this paper names: (1) the **gamma renormalization** (save ||Mbar||_F after NS, restore after unwhitening) is ABSENT from ours and is the likeliest reason naive whitening blows up; (2) **Trace Normalization** (L*dim/Tr(L)) replaced in ours by bias correction; (3) **Spectral Tempering** (Lambda^-alpha, alpha tunable) hardcoded to 0.25 in ours. The paper names (2) and (3) as its two critical stability techniques. One thing ours gets RIGHT and looks wrong: the unwhitening reuses the SAME negative powers, not their inverses — confirmed at Algorithm 1 line 9 and the p5 closed form. Beats SOAP FOR US on memory by its own argument (p6): SOAP needs a second-moment state v in the rotated frame, Mousse needs none — and our ckpts are already 3.1 GiB on a 16 GB card. CAVEAT THAT IS OURS, NOT THE PAPER'S: it assumes full weight matrices; on LoRA/DoRA factors the wide side is 12288, and a single 12288^2 eigh measured **7.23 s** on our card (173 s/step across 24 such tensors). Restrict the preconditioner to the rank-128 side: 27.9 GiB -> 28.6 MiB. See also 2602.02016 DASH (batching 458 128x128 eighs = **52.8x** faster than looping) and 2509.03378 KL-Shampoo (coupled two-sided estimator, so it does NOT apply under one-sided bottleneck). |
| [Post-Grokking Collapse at the Representation–Readout Interface in Muon-Trained Transformers (2608.07436)](<arxiv-2608.07436 - Post-Grokking Collapse at the Representation–Readout Interface in Muon-Trained Transformers.md>) *(abstract + key passages, C 09-22)* | Muon (hidden matrices; AdamW on embed/head) reaches grokking FASTER on (a+b) mod 113, then LOSES it: every one of 9 configs groks and then falls below threshold. Once train loss is solved the gradient is ~1e-6, and Muon's applied step has elasticity −0.03 on gradient magnitude (AdamW +1.5), so it keeps taking full steps along the representation/readout ambiguity (identified only up to an invertible map). Freezing either side prevents it; ablating Muon's normalisation is not a substitute. | **HIGH, and it reverses a research-agent summary** that read it as "Muon/LMOs accelerate grokking". What transfers: (1) LoRA has exactly this symmetry, B·A = (BX)(X⁻¹A), and we run Muon-family updates on A and B separately; → MEASURED 09-22 with `eval/lora_gauge_drift.py` on audition_160ep_2026-09-22-b (DoRA r128, ns5+normuon+sf; `checkpoint-stats/audition_160ep_20260922b_gauge.json`): our ‖B‖ velocity is NOT inflated — rel. velocity of B and of B·A agree to 3 digits at every interval, ‖A‖ stays ~99 (A carries only ~7% of step energy, so we are close to the paper's "freeze one side" already). But the gauge share of step energy RISES monotonically 4.8%→6.6%→8.4%→10.5%→12.4% (chance ~2%) as the loss flattens — the paper's mechanism, small and growing — and is concentrated in the conditioning embedders (to_cond_embed / to_global_embed / postprocess_conv / to_local_embed, 40–70% gauge). Watch it on longer runs; the remedy if it grows is freezing or down-weighting A. (2) Evidence FOR magnitude-sensitive steps near convergence (SoftMuon, decaying LR), not for "never kill a plateau". |
| [ScheduleFree+ — Scaling Learning-Rate-Free & Schedule-Free Learning to LLMs (2605.19095)](<arxiv-2605.19095 - ScheduleFree+ - Scaling Learning-Rate-Free & Schedule-Free Learning to Large Language Models.md>) *(title-level, C 09-22)* | The paper behind the official `schedule_free/.../adamc_schedulefree_plus_paper.py` we ported: Schedule-Free averaging + Polyak-style LR-free step + AdamC-style lr²-scaled decay. | **DIRECTLY ADOPTED** — it is our current audition recipe's `sf` component (audition_160ep_2026-09-22-b: c_k ACTIVE, velocity 36 u/1k). Needs a real deep-read: our port was from CODE; the paper's claims on warmup length (c_warmup) are what we tuned by hand. |
| [HyperTransfer: Equivalence between Base Optimizer and Hyperball (2609.07017)](<arxiv-2609.07017 - HyperTransfer - Understanding the Equivalence between Base Optimizer and Hyperball.md>) *(abstract-level, C 09-22)* | For scale-invariant nets, a Hyperball (fixed-norm, direction-only) optimizer is DYNAMICALLY EQUIVALENT to an ordinary one; the difference is only the induced effective (angular) LR schedule. Constructs either from the other; warns matched nominal LR-decay ratios give different effective ratios. | **Deflates the Hyperball/SCG stage of modular_opt**: not a new geometry, an LR schedule — not worth its own audition arm. And LoRA B is NOT scale-invariant (zero init; its norm growth IS the learning), so a fixed-norm B is ill-posed. The fair-comparison warning is our "5e-6 illusion" in general form: compare effective step sizes, never nominal LRs. |
| [MALT: Lightweight Curvature-Aware Muon via Diagonal Preconditioning (2608.05088)](<arxiv-2608.05088 - MALT - Lightweight Curvature-Aware Muon via Diagonal Preconditioning.md>) *(abstract-level, C 09-22)* | Two-sided DIAGONAL preconditioners around Newton-Schulz (whiten → NS → map back), norm grafting for magnitude; MALTER adds adaptive step rescaling. Beats Muon on GPT-2 S/M/L at ~Muon memory and wall-clock. | **The whitening we can afford.** Full Kronecker factors were measured non-viable for our 12288-wide LoRA tensors (7.23 s eigh; 95% mass needs rank 512–2048 → 2.3 GiB). Diagonal = O(m+n). Caveat from our own covariance probe: energy is low-RANK (eff. rank 90–228/8192), which a diagonal cannot represent — so expect less than Mousse, but it is the cheapest whitening arm. |
| [Dion3: Full-stack orthogonal updates (2608.11612)](<arxiv-2608.11612 - Dion3 - Full-stack orthogonal updates.md>) + [Turbo-Muon (AOL pre-conditioning)](<Turbo-Muon - Almost-Orthogonal Pre-Conditioning for Fast Muon Updates.md>) *(abstract-level, C 09-22)* | Dion3: Gram Newton-Schulz (iterate on the small r×r Gram), symmetric GEMM kernels, fractional updates — Muon step 26×→4× AdamW on a SHARDED 7B/4×GH200. Turbo-Muon: AOL rescaling starts NS closer to orthogonal, drop 1 of 5 iterations, drop-in. | Speed only. The 26× is a distributed-sharding cost we don't pay. Gram-NS is genuinely apt for our 12288×128 LoRA shapes (iterate on 128×128). **Measure the optimizer's share of step time before building either.** |
| [Grokfast (2405.20233)](<arxiv-2405.20233 - Grokfast - Accelerated Grokking by Amplifying Slow Gradients.md>) *(abstract-level, C 09-22)* | Amplify the slow (EMA) gradient component, G + λ·EMA(G), to accelerate grokking. | LOW. Under Newton-Schulz the λ magnitude is normalised away; what's left is a heavier-momentum direction mix, which our momentum β already spans. Grokking is an algorithmic-task phenomenon; our 300-track LoRA is judged by ear, not by a delayed validation jump. |
| [PsiLogic: Chaos-Aware Active Cancellation for Adam (2607.16268)](<arxiv-2607.16268 - PsiLogic - Chaos-Aware Active Cancellation for Adam with a Fair Cross-Domain Benchmark.md>) *(abstract only, C 09-24)* | Adam + a damping term gated by a dual-EMA "chaos detector": strong while gradients are unstable, fading as training stabilises. Diffusion arena tied with Adam/AdamW. | Our "adaptive damping for Adam" candidate; would NOT have caught the goa3 magnitude drift (stable phase). |
| [Softsign / SoftSignum / SoftMuon (2605.31371)](<arxiv-2605.31371 - Softsign - Smooth Sign in Your Optimizer For Better Parameter Heterogeneity Handling.md>) *(abstract only, C 09-24)* | Temperature-smoothed sign: sign-like steps for large gradients, SGD-like for small; quantile temperature schedule; matrix version SoftMuon. | Nearest fix to our sign-group scalars, but the step is still absolute, so consistent-gradient small scalars still cross zero. We built the multiplicative magnitude step instead. |
| [LoRA-TSD: Tangent-Space Spectral Descent for LoRA (2609.02734)](<arxiv-2609.02734 - LoRA-TSD - Tangent-Space Spectral Descent for LoRA via Muon-Style Updates.md>) *(paper title only; official CODE read in full, C 09-22/23)* | Muon-style spectral steps on the tangent space of the rank-r manifold, sized on B·A; optional A/B norm balancing. | DoRA-compatible, zero-B-safe on our adapters, but 8.5 s/step unbatched on ROCm (QR latency). Gated on EXPERIMENTS A14. |
| Muown: Row-Norm Control for Muon (2605.10797) *(arXiv abstract page only, no PDF here; via the Gemini DoRA-magnitude report, C 09-24)* | Treats the row-magnitude vector as its own optimizer variable under ℓ∞ geometry, Muon unchanged on the direction; row magnitudes drive Muon's norm drift. | **The principled version of what our DoRA magnitudes need**, and the closest published analogue of the modular stack. Deep-read candidate. |
| LionVote: Per-Layer LR Adaptation for Lion (2607.09266) *(arXiv abstract only, no PDF)* | Lion's effective scale is 2.6–2.8× too high for attention/MLP and ~2× too high for normalization params (ViT-Tiny/CIFAR-100); per-tensor compound LR level. | Evidence that sign steps miscalibrate 1-D params, the goa3 NaN's mechanism class. |
| M+Adam: Low-Precision Training via Additive-Multiplicative Optimization (2607.10611) *(arXiv abstract only, no PDF)* | Additive steps for sign changes and small magnitudes, multiplicative for large; builds on Madam (Bernstein et al., NeurIPS 2020). | Precedent for our multiplicative magnitude step. Its caveat (a multiplicative step can't cross zero) doesn't apply: DoRA magnitudes are row norms. |
| DoRAN: Stabilizing DoRA via Noise Injection and Auxiliary Networks (2510.04331) *(arXiv abstract only, no PDF)* | Learnable term in DoRA's normalisation DENOMINATOR against instability when ‖W0+BA‖ is small; auxiliary networks generate the low-rank factors. | About the direction norm, NOT the magnitude (the Gemini report conflated them). Relevant to our tiny-row conditioning layers; fallback if multiplicative magnitudes still NaN. |
| *Brief* `Prospective Unchecked/papers_review_synthesis.md` (external agent, 09-20, 41 docs) | Taxonomy + relevance map of the prospective folder. | Useful as a MAP; its relevance verdicts are **unverified** — it attributes audio effects ("acoustic distortion", "clicks", "harsh timbres") to LLM/vision papers that test no audio. Papers added after it (the rows above — PDFs moved to `papers/` 09-22, plus QSD 2609.07597, 2608.04607, Control Allocation 2608.02991) are not in it. |

**Open verification threads** *(update rows when they land)*:
- ~~Counterexample hunt on the blind-vs-redundant boundary~~ **LANDED 2026-07-03 16:12**:
  no prior statement of the biconditional as a predictive law; both halves exist
  separately (rows above). Verdict: downgrade to **"novel predictive boundary
  condition, first explicitly stated and cleanly tested — demonstrated in diffusion
  control."** Full resolution: docs/research-brief-2026-07-03-novelty-check.md
  (RESOLUTION §, claim 1). Abstract files for the four new rows: FINN's archive pass.
- Full novelty-verdict context: AGENT_DIALOGUE.md 2026-07-03 11:03–11:09 (CONTINUITY's
  7/7 citation verification, zero fabrications, two Gemini soft spots caught).

## StoryScope: Investigating idiosyncrasies in AI fiction (Russell et al., UMD/GDM, arXiv:2604.03136)
- Pipeline: LLM converts stories → structured narrative templates (10 NarraBench dims) →
  cross-source comparative analysis → LLM-proposed discriminative features (304) → XGBoost+SHAP
  → 30 core + 75 fingerprint features. Narrative structure alone: 93.2 F1 human-vs-AI.
- **Rarity as originality proxy**: statistical rarity percentile in the induced feature space —
  humans 0.71 vs AI 0.49; all 5 LLMs collapse into one shared narrative region; per-model
  fingerprints enable 6-way attribution.
- Style-editing robustness (LAMP rewrites): detection drops only 1.6 pts — the signature lives
  in STRUCTURAL decisions, not surface. (Music analog: fixing timbre won't fix generic melody.)
- **Transfer for us (Kim 2026-07-08)**: featurizer needs to be CONSISTENT, not a perfect
  transcriber → no MIDI needed. Melody-discourse features from pitch-salience on separated
  stems; Music Flamingo = our structured-template featurizer; rarity-lite = kNN percentile over
  MIR structure features, matched-length windows. Plan in task #33; rarity score doubles as a
  best-of-K anti-mode-collapse selection-steering signal.

## Underfit memo — CFG dynamics during LoRA training (Kim 2026-07-09, important)
- **CFG optimum MOVES with training stage.** Early ('getting it'): high CFG (~7) demos light up
  first — style on a coherent prompt. Past the elbow: CFG=7 OVER-COOKS (artifacted/over-saturated),
  and LOW CFG (~1) becomes cleanest. If CFG=1 good & CFG=7 bad => LoRA has internalised the style,
  no longer needs prompt-classifier guidance.
- **Conditional→unconditional crossover.** Later, even EMPTY-prompt demos sound like the style =
  the model ABSORBED the dataset (a diagnostic for memorisation).
- **Don't fear a memorised checkpoint.** Overfitting only hurts if chasing creative variation. A
  memorised ckpt is still useful: weaken LoRA strength at inference, AND a2a/style-transfer hits
  HARDER with a memorised model (strong style signal pulls input decisively into the training dist).
- **IMPLICATION FOR OUR AVP WORK:** we rendered EVERYTHING at fixed cfg ~5. Our late-epoch
  'thin/over-sharp/hissy/breathy' renders may be CFG=7-style over-cooking at a stage that wants
  low CFG. Late ckpts should be re-rendered at cfg 1-2. And the 'overtrained' late ckpts may be
  EXCELLENT for a2a even where they're bad for txt2audio. (ARC-distilled demos lag base RF by a few
  k steps but end cleaner.)

## Longform-continuation starred-ID verification (THE-FINN, 2026-07-15, for W's validation-experiment-plan §6)
All 6 checked against the archive FIRST (none pre-existing), then fetched — all REAL. Only
SRMC + DPP-window gate near-term code (E1/E2); rest are pool per W's ask.
**Overlap note:** G independently verified the same 6 (DM 01:56, before W separately asked me
04:02 — a real coordination gap, not a design intent) as part of a broader plan-review; G's read
adds one nuance I missed from the abstracts alone — DPP and SAGD are both TRAINING-time
mechanisms in their source papers (DPP-GRPO reward at RL-training time; SAGD's anisotropic
noise is a training-time forward-process design), so neither is an inference-time precedent
verbatim — folded into both entries below. G also flagged a 7th starred id in §6 I wasn't asked
about and didn't fetch: **Music Boomerang, arXiv:2507.04864 — unverified, needs a pass.**
- **SRMC — Score-Repellent Monte Carlo** (Hu, Chen, Kim, Choi, Han, Eun; arXiv:2604.22948).
  Running average of past score evaluations tilts the target via exp(−α·θᵀs); normalization-free
  drop-in wrapper for any base sampler; joint CLT + O(1/α) variance reduction; O(d) constant
  memory. Claims in the tangential-refinements doc match well. Caveat already logged there:
  the exactness theorem assumes a *fixed stationary target under ergodic time-averaging* — a
  finite-horizon generative rollout breaks that premise; construction transfers, the theorem
  doesn't verbatim (CONTINUITY's open sub-question).
- **DPP-window** — actually **"Diverse Video Generation with Determinantal Point
  Process-Guided Policy Optimization"** (Kazimi, Dunlop, Yanardag; arXiv:2511.20647). Real,
  and the core mechanism (DPP-based diversity reward penalizing redundant generations, "DPP-GRPO")
  is confirmed — but the abstract does NOT confirm the specific windowed/log-det-Gram-matrix
  formulation the refinements doc attributes to it. **G's sharper catch: it's DPP+GRPO at
  TRAINING time (a reward during RL fine-tuning), not an inference-time log-det hinge** — the
  DPP-for-diversity idea transfers, but the paper is not itself a precedent for the inference-time
  arm; soften the citation accordingly, keep the arm on its own merits.
- **Explicit-Critic Guidance** (Liang, Zhang, Yang; arXiv:2605.27736). Strong match: the
  diffusion model serves as its own timestep-conditioned value function, trained via
  trajectory-level PPO, aligning to non-differentiable objectives — exactly the derivative-free
  fit the refinements doc wants for a hard-to-differentiate recurrence statistic.
- **SemanticAudio** (Dai, Zhang, He, Li, Li, Wu, Guo, Kong; arXiv:2601.21402). Real, matches
  "RF + continuous-latent" — two-stage flow matching (semantic planner + acoustic synthesizer),
  audio generation/editing in a continuous semantic latent space, training-free text-guided
  editing via velocity-field differences.
- **SAGD — Spectrally Anisotropic Gaussian Diffusion**, real title: "Learning What Matters:
  Steering Diffusion via Spectrally Anisotropic Forward Noise" (Scimeca, Jiralerspong,
  Earnshaw, Hartford, Bengio; arXiv:2510.09660). Matches well — frequency-diagonal forward
  covariance, band-pass/power-law weighting, score relation derived. **Caveat: validated on
  vision datasets only**, and per G's read, designs the forward (noising) process at TRAINING
  time — using it at sampling-time-only, on a frozen model never trained with anisotropic
  noise, is the ported idea, not what the paper demonstrates. No audio precedent either, same
  transfer-risk pattern as the rest of this
  literature.
- **FMRG — Flow Map Reward Guidance**, real title: "How to Guide Your Flow: Few-Step Alignment
  via Flow Map Reward Guidance" (Huang, Lin, Shah, Nair, Boffi; arXiv:2604.27147, code
  confirmed at github.com/jrrhuang/fmrg). Training-free, deterministic-optimal-control framing,
  strong results at ~3 NFEs — core framing matches; the specific "exact lookahead to the clean
  endpoint via a distilled flow map" detail isn't confirmable from the abstract alone.

## Training-stability / optimizers — the full-FT drone-fix cluster

*C deep-read all 12 from the actual PDFs (papers/ root), 2026-08-11 — NOT Gemini summaries (which over-weighted Hyperball/CMuon and MISSED the routing fix + the adamw-first framing). Full per-paper analysis + Zach-Evans (SA author) ground-truth + the locked recommendation: `papers/CONTINUITY-drone-optimizer-synthesis-2026-08-11.md`. These paper verdicts + our recipe recommendation are **STABLE (run-independent)**; the empirical "which fix wins on Kim's music" is **PENDING the re-scoped AVP sweeps + Kim's ear** (placeholder below).*

**The convergence — FIVE independent sources say the same thing:** the **output projection + AdaLN modulation** are where the full-FT magnitude runs away (the drone), and they need **bounded/normal treatment, NOT unconstrained Muon orthogonalization** — from (1) SA3's own recipe (Muon on QKV/FFN, AdamW elsewhere), (2) rotational-equilibrium/Hyperball (scale-invariance holds for hidden layers, not the output proj), (3) MaP-DiT (AdaLN scale is the non-magnitude-preserving culprit), (4) EDM2 (control weight+activation magnitude at the output), (5) the output-std penalty / SFWN.

- **[Fantastic Optimizers I (2509.02046)](<CONTINUITY-drone-optimizer-synthesis-2026-08-11.md>)** Wen/Hall/Ma/Liang, Stanford — 10 optimizers × 4 scales, HPs tuned separately, eval-at-end. Matrix optimizers (Muon/Soap/Kron) do beat scalar, but the margin over a **fairly-tuned** AdamW is **1.4×@0.1B → 1.1×@1.2B** (prior "2×" claims inflated by under-tuned AdamW + intermediate-ckpt comparison). **FOR US:** at 1.4B Muon's premium is ~1.1× AND Muon causes our runaway → a fairly-tuned AdamW full-FT is the pragmatic front-runner (test `adamw_fair` with its OWN lr ~2e-4, not 8e-5); the Muon-repair stack must earn its ~10%.
- **[Rotational Equilibrium (2305.17212)](<CONTINUITY-drone-optimizer-synthesis-2026-08-11.md>)** Kosson/Jaggi, EPFL — with weight decay, weight-norm + angular update converge to a steady state; for scale-INVARIANT (normalized) weights wd is an effective-LR knob, too-weak wd → collapsed effective LR. **FOR US — the key nuance the Gemini report blurred:** our runaway lives in the OUTPUT proj, which is NOT scale-invariant → weight-norm growth is DIRECTLY output-magnitude growth (the drone), not just an effective-LR shift. So an OUTPUT-targeted fix is conceptually cleaner than global wd. Conceptual ancestor of Hyperball.
- **[WD > µP for LR Transfer (2510.19093)](<CONTINUITY-drone-optimizer-synthesis-2026-08-11.md>)** Kosson et al., Amazon+EPFL, ICLR26 — weight decay (not µP) is what stabilizes cross-width dynamics → enables LR transfer; µP ≈ implicit warmup. **FOR US:** the small-T512-A/B's winning lr/wd transfers small→large via decoupled wd (which FusionOpt already applies) — good news for the small→big plan.
- **[Muon-SW / Scale Weight Decay (2607.23777)](<CONTINUITY-drone-optimizer-synthesis-2026-08-11.md>)** Apte, JPMorgan — scale wd by η/η_max (Robbins-Monro) so Muon's weight norm settles constant instead of shrinking; 30% faster. **FOR US — schedule-free CAVEAT is REAL:** FusionOpt is Schedule-Free (η≡η_max) → the η/η_max factor ≡ 1 → **the scheduling trick is MOOT for us.** What transfers: the steady-state result (right constant wd → Muon norm settles). Our runaway = under-decay → fix is purely the wd MAGNITUDE (the A/B pins it). Confirms the wd 0.1 direction.
- **[CMuon (2608.02502)](<CONTINUITY-drone-optimizer-synthesis-2026-08-11.md>)** Chen/Sun/Yuan, PKU/Westlake — Muon on DiTs plateaus late because DiTs FUSE functionally-distinct weights (AdaLN α/γ/β + QKV) into one tensor; orthogonalizing the fused tensor couples subspaces. Fix: chunk before NS5. **FOR US:** we already chunk QKV (`--fusion-split-qkv`); the missing piece is chunking **AdaLN scale/shift/gate**. Retrofit-safe (optimizer-internal). Addresses CONVERGENCE/quality, ORTHOGONAL to the runaway — a low-risk quality ADD if we stay on Muon. (The "2×" is vs a Part-I-under-tuned AdamW → read as "removes the plateau", not literal 2×.)
- **[Hyperball (2606.16899)](<CONTINUITY-drone-optimizer-synthesis-2026-08-11.md>)** Wen/Lyu/Ma/Liang, Stanford (Part-I sequel) — wrapper fixing ‖W‖_F and ‖update‖_F to constants; **R = ‖W_0‖_F = norm at run start**, W ← R·Normalize(W_updated). **FOR US — retrofit-CLEAN (corrects the earlier hedge, positively):** for a fine-tune W_0 = the pretrained matrix → step 0 leaves W unchanged → NO loss spike, and it then BOUNDS the output-proj norm at the pretrained level = a structural drone cure, plus the LR-transfer we need. Subtlety: designed for scale-invariant layers, but since our target latent ~unit-variance, NOT adapting output scale is exactly right. **Strong paper-grounded candidate** (Round 2).
- **[AdaGC (2502.11034)](<CONTINUITY-drone-optimizer-synthesis-2026-08-11.md>)** PaddlePaddle — per-tensor gradient clip relative to a tensor-wise EMA of historical clipped norms; device-local (good for AMD DDP), Muon-compatible, kills loss spikes. **FOR US:** the clip to adopt — but it targets transient SPIKES, our drone is sustained DRIFT → a robustness ADD, not the cure (keep a good clip regardless; EDM2/NFNets agree clipping stays even with mag-preservation). **UPDATE 2026-09-26 (C): now the leading candidate** — the shampoo run's failure mode IS transient per-tensor spikes (5, all conditioning-layer LoRA B, up to 42× median). Rule: `g ← g·min(1, λ·γ_{t−1}/‖g‖)`, γ = EMA (β=0.99) of the CLIPPED norms, global-clip warm-up; paper default λ_rel = 1.04 (continuous trimming, not rare-event). Never implemented here. Triage: `deep-research/2026-09-26-gradient-noise-spike-handling-triage.md`.
- **[Non-Euclidean GNS for sign & spectral descent (2602.03001)](<arxiv-2602.03001 - Adaptive Batch Sizes Using Non-Euclidean Gradient Noise Scales for Stochastic Sign and Spectral Descent.md>)** **deep-read (C 09-26); ICML'26** Naganuma et al., Mila/Meta — the right noise measure for Muon-type steps is the NUCLEAR-norm GNS `‖C_row^½‖²_S1/‖∇L‖²_S1` (dual of the step's spectral norm), not Euclidean/per-element SNR; used for adaptive BATCH SIZE (up to 66% fewer steps, 160M Llama). **FOR US:** formalises why our SNR gate measured the wrong thing; momentum is explicitly OPEN in the paper, so no licence to gate steps/momentum on it; a 2-half-batch estimate is weak → occasional diagnostic only.
- **[DP-aware AdaLN-Zero (2602.22610)](<arxiv-2602.22610 - DP-aware AdaLN-Zero - Taming Conditioning-Induced Heavy-Tailed Gradients in Differentially Private Diffusion.md>)** **deep-read (C 09-26)** Huang et al. — AdaLN conditioning paths give rare heavy-tailed per-example gradients EVEN WITHOUT DP (cond p99 ~3.5× cut by bounding vs 1.2× elsewhere); fix = project c + `M·tanh(x/M)` on modulation. Small (8-layer, d=256) time-series model from scratch. **FOR US:** independent support for our 5/5 conditioning-layer spikes (2026-09-25) and the 08-11 AdaLN convergence above; the fix alters a pretrained model's forward → last resort; copy its cond-vs-other gradient-quantile diagnostic.
- **[Isotropic Curvature Model: Is Gradient Orthogonalization Optimal? (2511.00674)](<arxiv-2511.00674 - Isotropic Curvature Model for Understanding Deep Learning Optimization - Is Gradient Orthogonalization Optimal.md>)** **deep-read (C 09-26)** Su, UPenn, theory — under isotropic curvature the optimal matrix update keeps G's singular vectors and ordering but only HOMOGENIZES its spectrum (small σ stay small); full orthogonalisation is optimal only at a sharp curvature transition, which GPT-2 measurements do not show; Newton–Schulz polynomials are generally non-monotone. **FOR US:** our cubic5 lifts σ = 0.01 to ~1.02 (~100×) and is non-monotone (1.0 → 0.77, max 1.30), i.e. it amplifies the weakest, most noise-dominated directions. Candidate lever needing no noise estimate: a monotone partial map (σ^p, p ≈ 0.3–0.5), exact via the 128×128 Gram on rank-128 factors. Hypothesis; test vs cubic5.
- **[LionMuon (2605.19811)](<arxiv-2605.19811 - LionMuon - Alternating Spectral and Sign Descent for Efficient Training.md>)** **deep-read (C 09-26)** Bolatov et al., MBZUAI/BRAIn — alternate Muon and sign steps (period P) over one Lion-style DUAL-EMA buffer (direction 0.9·M + 0.1·G, buffer β2 = 0.99); P = 2 beats Muon by 0.01–0.04 nats at 124M–720M LLM pretraining; theory under heavy-tailed noise. **FOR US:** the dual-EMA is a cheap spike dampener — with our single β1 = 0.9 a 42× spike dominates the step direction ~15 steps, with 0.9/0.99 only the arrival step. Complements AdaGC. Alternation itself: low priority (needs a ×100 lr ratio, sign steps on LoRA/DoRA are risky for us).
- **[Gradient Orthogonalization as Non-Euclidean Trust-Region (2503.12645)](<arxiv-2503.12645 - Understanding Gradient Orthogonalization for Deep Learning via Non-Euclidean Trust-Region Optimization.md>)** **deep-read (C 09-26)** Kovalev, Yandex, theory only — Muon = trust region in the spectral norm; momentum weight α and step η should both shrink as gradient noise grows; averaging must precede orthogonalisation (why Muon beats Orthogonal-SGDM); weight decay = shifted trust region. **FOR US:** supports a slower buffer (dual-EMA β2 0.99) and an explicit lower lr for our noisy DiT; spike defence belongs on the raw gradient before momentum. Surfaced a 2nd spike pathway: Shampoo's accumulators take the RAW gradient (G Gᵀ ⇒ a 42× spike enters ~1764×), so a clip must precede both momentum and preconditioner.
- **Cautious Optimizers ("one line of code"; Liang et al. 2024, arXiv id from memory 2411.16085 — NOT verified, no PDF here)** — mask update coordinates whose sign disagrees with the current gradient, rescale survivors by 1/keep. **OUR RESULT (C, 2026-08; MASTER §5):** in FusionOpt (`apply_cautious`) on the DoRA r128 arm `sa3-goa-dora-47s-r128-fusion-caut`, keep ≈ 0.53 (per-element gradients are noise-dominated, so the mask is near-random), and the 1/keep rescale inflated the update NORM by 1/√keep ≈ +37% effective spectral lr → NaN between ep 2→3 while the identical no-caution arm trained clean; fixed to a norm-preserving rescale. Also incompatible with orthogonalised steps (masking an orthogonal matrix destroys its structure) and blind to momentum contamination. **DO NOT re-enable with spectral/Muon-family steps.** Its 108 quarantined NaN cells (2026-09-08) are genuine model failures. See also the Cautious Weight Decay row (2510.12402).
- **[OptMuon (2606.08783)](<arxiv-2606.08783 - OptMuon - Closed-Loop Orthogonalized Momentum Methods for Stochastic Optimization with Zero-Noise Optimality.md>)** **deep-read (C 09-26)** Yuan — theory only (no experiments; assumes bounded gradients); AdaGrad-Norm scalar on a polar-factor step, running max stops the COEFFICIENT collapsing after a spike. **FOR US:** low relevance — does not protect momentum, and its step scales with ‖M‖ so a spike enlarges it.
- **[σReparam (2303.06296)](<CONTINUITY-drone-optimizer-synthesis-2026-08-11.md>)** Zhai et al., Apple 2023 — reparam each linear as γ·W/σ(W) to bound Lipschitz / fight attention-entropy collapse; retrofit-safe (γ init = pretrained σ(W)). **FOR US:** bounds SPECTRAL norm (largest SV) — good for attention-entropy instability, but NOT our output-SCALE (Frobenius/total-energy) runaway → a safeguard arm, not the primary cure.
- **[EDM2 (2312.02696)](<arxiv-2312.02696 - EDM2 Analyzing and Improving the Training Dynamics of Diffusion Models.md>)** *(re-read in detail C 08-19 after Zach flagged it — own note now; the §3 POST-HOC EMA half was under-read: two power-function EMAs snapshotted during training reconstruct ANY EMA length after the fact (O(1/n⁴)); optimal EMA length depends on config, training time AND cfg strength (no-CFG ~13 % vs cfg1.4 ~2 %), and per-tensor optima disagree in unbalanced nets = our soups/PQ-weighting done principled; Config E: normalised weights + constant LR = no convergence → explicit inverse-sqrt decay = the 08-19 damping finding.)* Karras et al., NVIDIA — NAMES OUR PATHOLOGY: "uncontrolled magnitude changes in activations AND weights over training"; redesign layers to preserve magnitude (forced weight-norm + MP-SiLU/MP-sum) + post-hoc EMA. **FOR US:** our drone, diagnosed + cured (from scratch); the Frobenius/magnitude frame is the right one for output-scale. Retrofit piece = forced-weight-norm on the output proj (learnable scalar init = pretrained norm). from-scratch MP-SiLU/MP-sum → SKIP for a fine-tune.
- **[MaP-DiT (2505.19122)](<CONTINUITY-drone-optimizer-synthesis-2026-08-11.md>)** ETH 2025 — CLOSEST TO US: extends EDM2 mag-preservation to a DiT; the unit-magnitude noise target "implicitly defines a magnitude constraint on outputs that most DiTs don't enforce" = our exact problem. **AdaLN scale is the sticking point** (inherently magnitude-non-preserving); their fix = Rotation Modulation (NOT retrofittable — changes conditioning). **FOR US:** third independent pointer at "AdaLN + output proj need special handling"; the retrofit-compatible form is force_scalar / bounding AdaLN, not the rotation swap.
- **[JLT: Clean-Latent Prediction (2605.27102)](<arxiv-2605.27102 - JLT — Clean-Latent (x0) Prediction in Latent Diffusion Transformers.md>)** Fu et al. — predicting the CLEAN latent (x0) beats velocity/noise (which carry ambient off-manifold components). **FOR US:** the velocity target's isotropic component is the runaway's OBJECTIVE-side driver; the retrofit form = an x0-space loss weight (σ²-weighted v-loss ≡ x0-MSE) = our dormant **#59 x0equiv** thread → JLT validates reviving it as the objective-side complement (Round 2).
- **[Decoder-Only LLMs for T2I (2506.08210)](<CONTINUITY-drone-optimizer-synthesis-2026-08-11.md>)** Wang/Karras et al., NVIDIA — OUT OF SCOPE for the drone (text encoders for T2I). **Banked lead (not drone):** layer-normalized averaging across ALL LLM layers beats last-layer embeddings for prompt alignment → if SA3's T5-Gemma conditioning uses last-layer embeddings, a layer-averaged variant could improve PROMPT ADHERENCE. Distinct thread; flag, don't chase.

**⚠️ Zach-Evans (SA author) ground-truth CORRECTIONS** (relayed by Kim 2026-08-11): (1) the SA3 optimizer split is **DIMENSIONALITY-based (2D→Muon, 1D→AdamW)** — the output proj is 2D → was on MUON in SA3, so **`force_scalar_output` is DEMOTED** from "matches SA3's recipe" to a weaker test-worthy arm (canonical-Muon-excludes-output/embeddings hypothesis), NOT the headline. (2) **SA3-base is overwhelmingly AdamW-shaped** (Muon was a LATE, BRIEF finish on variable-length) → our full-FT runs sustained Muon from step 0 on AdamW-shaped weights → the runaway; AdamW full-FT is the faithful continuation. (3) **methodological warning:** the runaway-bounding signal (std ~1 vs inf) is unambiguous, but FINE quality/creativity rankings are seed-sensitive at n=1/arm → **two-stage: sweep → replicate top 1-2 on fresh seeds** before crowning a winner; Kim's ears are the arbiter. (4) **Muon "seemed MORE CREATIVE" to Zach** + smaller optimizer state (VRAM) → the sweep's real question shifts from "which bounds the runaway" (all fixed arms will) to **"does fixed-Muon sound more creative than well-tuned AdamW on Kim's music?"**

**LOCKED RECOMMENDATION (all 12 read, paper-grounded):** ROUND 1 (cheap, test the priors) — (a) `adamw_fair` [may make the rest moot], (b) `force_scalar_output` [test-worthy per Zach], both vs `wd0.1`+`anchor` controls. ROUND 2 (if detail left on the table) — (c) Hyperball wrapper, (d) x0-space loss weighting (revive #59), (e) SFWN forced-weight-norm on the output proj. ORTHOGONAL ADDS (any time) — CMuon (chunk AdaLN/QKV), AdaGC (clip), σReparam (attention-entropy safeguard). SKIP for a fine-tune — MaP-DiT rotation-modulation, nGPT, MP-SiLU/MP-sum, NFNets WS. **Gate on QUALITY (Kim's ears + disintegration DSP), not just the z0-std number** — every fix must bound the runaway AND keep fine detail ≥ the wd baseline.

**EMPIRICAL VERDICT — [PENDING]:** which fix actually wins on Kim's music, from the re-scoped AVP sweeps (20971444 regsweep + 20971445 surgical, correctly-pinned) + Kim's audition. C hands the per-arm result once those + the z0-std tables land; fill this line then. *(Gotcha noted 2026-08-11: an AVP sweep stall that looked drone-related was a **bf16-mixed fused-fallback THROUGHPUT collapse** — the plain adamw arm hung identically → NOT the drone-fix code; fixed by dropping to fp32. Don't re-chase the drone-fix for a throughput hang.)*

---

## Reading-sweep cluster — Symbolic music-theory papers as CONTROL CANDIDATES (G's 14, re-verdicted 2026-08-12)

**CRITERION CORRECTION (Kim direct, 2026-08-12, relayed by W).** The original "off-lane because it doesn't process continuous-latent AUDIO" verdict was the WRONG QUESTION for this category. Kim, verbatim: *"the symbolic music papers are exactly what interests us now, when we are planning musical controls, IE we need prediction targets, conditioners, possible new time series."* A symbolic construct does not need to run on audio to be useful — **mir extracts the audio side; the paper supplies the definition.** Verdicts re-judged (papers NOT re-read) against W's three-part test: (1) a quantity computable from a rendered clip, even via mir's existing extractors? (2) something Kim would ask for by ear — "more X"? (3) time series at some rate, or only a whole-piece scalar? Yes to (1)+(2) ⇒ candidate **CONTROL DIMENSION / PREDICTION-HEAD TARGET / new whole-track-timeseries field**.

**Verdict authority:** reconciled to **G's reader-grounded re-verdict** (G read the papers; F re-judged the index and deferred to the reader where they diverged). G's sharpest addition: for the PH papers the **definitional layer** (height function, transition matrix, interval-vector) is the candidate, while the **PH / bottleneck-distance layer on top stays off-lane**. Net: **5 of 12 candidates, 7 genuinely off-lane** (+ Why-Topology off-lane, + Stemphonic on a separate axis).

**CANDIDATE control-dimension sources (5):**
- `2204.11139` **Intervallic Transition Graphs via PH** — the **12×12 intervallic transition matrix + "dispersion" stat** = a harmonic-exploration / predictability measure, windowable. **Strongest + cheapest-concrete; ties to the melody-movement / contour thread (C's `contour_loss`).** CAVEAT (G): only the *definitional* idea transfers — the composer ranking Haydn<Mozart<Beethoven is unvalidated (N=5–6/group, no sig test); the PH layer on top is not the useful part.
- `1602.00739` **Towards a Topological Fingerprint of Music** — the **duration-weighted pitch-class HEIGHT FUNCTION ≈ chroma summed over a window** — essentially the exact bridge to C's melody thread (matches Kim's earlier spot-check). Cheap+concrete. The PH / bottleneck-distance layer stays off-lane.
- `2204.09744` **A Framework for Topological Music Analysis (TMA)** — mappings **II/III (interval-vector)** + **IV (binary pitch-class {0,1}^12)** are real prediction-target candidates independent of the PH layer; "more chromatic / consonant" maps onto interval-vector composition, windowable.
- `2505.08752` **Configurations, Tessellations & Tone Networks** — **graph-distance-from-tonal-center** ("polar/subpolar" position) = a harmonic-tension / distance-from-home measure, naturally per-chord/time-resolved. **Most speculative:** needs a chord-estimation front end that doesn't exist here yet; the paper's own validation is narrative (5 excerpts), not quantitative.
- `2502.15849` **Synthesizing Composite Hierarchical Structure** — **structural-coherence-to-corpus-centroid** score (ρ_s=0.82). Real but **highest implementation cost** ("someday not soon"): needs 4 discrete extractors (segmentation / motif / key-chord / contour) on polyphonic audio, none exist here, and their own ablation shows the full hierarchy is load-bearing (1-level score sign-flips to −0.44).

**GENUINELY OFF-LANE even under the corrected criterion (7 — G checked each, didn't default):**
- `2506.13595` PH of Music Network, 3 Distances — the PH/inclusion-proof layer adds nothing beyond what `2204.11139` offers more directly.
- `1307.1201` Topology of Musical Data — theory-grounding only (pitch-circle/torus); grounds chroma but defines **no new extractable quantity**.
- `2604.19960` Tonnetz Theory / Combinatorial Geometry — pure configuration classification.
- `2208.04974` Mathematical Foundations of Complex Tonality — tuning-ratio number theory, **not a controllable time-varying quantity** (F had wrongly guessed "consonance-via-Sethares" from the title; G's read: it isn't that).
- `2012.04517` A Geometric Framework for Pitch Estimation — a WEAKER, redundant pitch-tracking method, not a new quantity; our HPCP/MERT already do better.
- `2001.00001` Quantum GestART — no concrete quantity ever defined; pseudocode only.
- `2607.05007` Quantum-Inspired Harmonic Decision — chord-density is generic borrowed vocabulary this paper doesn't define or contribute; a symbolic-generation method, not an extractable target.

**Also off-lane:** `10.1080/17459737.2020.1799563` **Why Topology** (the 14th; 12-TET voice-leading theory). Voice-leading distance is a *defined* quantity, but it needs multi-voice pitch tracking (hard on dense mixes) and G did not promote it — hold as theory reference, revisit only if a voice-separation front end appears.

**Separate axis (unchanged):** `2602.09891` **Stemphonic** — RF-DiT-over-audio-VAE, same architecture family as SA3; solves parallel multi-stem GENERATION (a capability, not a control dimension). Architecture-family neighbour to watch.

**Meta (W):** SECOND time this sweep that METADATA, not content, misled us — a wrong filename hid HiPPO, then a wrong relevance criterion hid a whole category. Pairs with the session's "print statements are not evidence": the triage QUESTION has to be right, or a sound fact filed under it still yields a wrong verdict. **Which of the 5 become mir fields / prediction heads is a C+Kim design call — flagged, not chased. The 2 cheap+concrete ones (`2204.11139`, `1602.00739`) are the near-term ones; live topology-over-audio threads remain those two + SW1PerS `1307.6188`.**

---

## Sweep synthesis — C's 5, cross-paper (2026-08-12)

C's 5 reads converge on three things we are ALREADY building, and one structural through-line:
1. **Movement-as-feature** has two rigorous, differentiable, low-d-only encodings — **Polansky contour** (ordinal, monotone-invariant) + **path signatures** (analytic, reparam-invariant, `signatory`), with **SigDiffusions** as the generator to make movement skeletons to condition on.
2. **Consonance/harmony** has two reconcilable theories — **Tymoczko** even-division/orbifold-center (pitch-only, cheap, chroma-circle-differentiable) + **Sethares** spectral-roughness (timbre-aware, **Essentia-computable today**) → a fused harmonic-tension control/eval is the obvious build.
3. **Guidance at high gain** is a live SOTA paradigm (**Anatomica**) = our LatCH family-A, and it validates the 1000× gain Zach's team never tried.

**Through-line:** every framework has a **RESOLUTION DIAL with a sweet spot** (n-ary / raw–demean–whiten / sig-depth / orbifold-quotient / Q-and-β) — 5 independent sightings ⇒ structural, not incidental. **Actionable-today ranking:** dissonance/roughness timeseries (Essentia, free) > path-signature movement feature (`signatory`) > PH structure control (heavy, 2nd-wave). Full synthesis went to Kim.

---

## Sweep synthesis — W's 11, cross-paper (2026-08-12)

Emerged only from reading W's eleven together; no single row states it. **Filed here because it is the compiler's job to keep a cross-cut that would otherwise live only in the channel.**

**(1) FOUR papers bottom out in the SAME missing piece — and a fifth supplies it.** Three methods are each blocked on one prerequisite step:
- `2505.10004` (topology-driven repetitions) needs a **scalar surrogate v(t)** "capturing relative position within the current cycle";
- **Yust–Popoff meter networks** need an **articulation set per part** (and per Kim's generalisation, any descriptor stream can be a part — but only once you can say *when* it articulates);
- `2405.04796` (featured-PH) needs a **discretisation**, since its graph nodes are distinct VALUES;
- `2201.02715` (low-rank structured inference) **ANSWERS it**: an **HSMM over continuous emissions** yields discrete states, durations, and boundaries in one model, made affordable at real state-space sizes.

**Shape:** three analyses blocked on one step + a fourth paper supplying it. That single piece of work — **derive the right discrete/scalar stream from our continuous 46-field timeseries** — gates ALL THREE analyses plus Kim's harmonic-rhythm idea. It is **CPU-only, no GPU, no training**, and `spectral_flux_ts` is already a novelty curve to start from. **W ranks it above any individual head/control we could build this week**, because it is the common prerequisite rather than one more parallel experiment.

**(2) Strongest single control-target find (corrected criterion):** `2006.01033` **dynamical score networks** — tonal regions from graph modularity, with mir's existing **CHORDS** field as its input, yielding **tonal-region-id / centricity / harmonic-mobility** timeseries and controls none of our current axes express (stay-in-key vs modulate, return home, harmonically restless vs static). **BOUND (Mukherji, W 08-12):** 2006.01033's edges are SUCCESSIVE chords = a surface/adjacency model -> a GROUPING, not a PARSE. The find STANDS (features computable, Bach demo recovers real regions) but does NOT capture harmonic SYNTAX; the hierarchical middle term (Rohrmeier 2011 PCFG) is missing from the folder.

**(3) Meta:** THREE of W's eleven were nearly lost to METADATA not content — `2008.07669` arrived mis-titled and is actually **HiPPO** (S4/Mamba ancestor); `1708.09359` nearly skipped as redundant, actually supplies the **witness complex** that makes the other topology methods affordable at corpus scale; and the whole symbolic cluster was dismissed by a wrong criterion. **"In a reading sweep the filenames and the categories are the least reliable part of the corpus."** (Third sighting of the metadata-not-content trap this sweep.)
